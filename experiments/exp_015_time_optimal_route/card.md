# exp_015 実験カード: 時間モデルによる経路選択（最短「歩数」→ 最短「時間」）

- 起票: 2026-08-13 学生A（評価・古典ベースライン担当）
- 実験番号の割り当て: 教授（裁定 **2026-08-13-R23 / R29**）
- 位置づけ: **古典高速化トラックのレバー ①**（`research_notes/p5_classical_speedup_design.md`）
- 起票時点: **時間モデルの再回帰は済ませたが、方策側は 1 行も実装していない**
- **土台は exp_014 の L0-c+E1**（受理済み）。対照も同じ

## 0. 1 実験 1 変更の宣言

**変えるのは「既知地図の上でどの経路を引くか」だけ**である。

| | 内容 |
|---|---|
| 対照 | **L0-c+E1**（exp_014。最短「**歩数**」の経路を引く） |
| 処理 | **L0-c+E1+TR**（最短「**時間**」の経路を引く） |

**探索戦略・制御ゲイン・速度プロファイル・軌道生成・ロボットパラメータは変えない。**

### ⚠️ 本実験は exp_016（斜め走行）の配管工事でもある

経路選択を**状態空間（区画 × 進行方向）上の Dijkstra** で行う形にする。
**exp_016 では、この Dijkstra のコストモデルを差し替えるだけで斜め経路が引ける。**
したがって**実装は「L0-c 専用」ではなく「コストモデルを差し替えられる」形にすること**
（教授指示・裁定 R29）。

## 1. 時間モデルの再回帰（**実施済み**・設計帯）

**現行係数は評価帯の走行由来なので使えない**（そのまま使うと評価帯の情報が設計へ漏れる。
教授指示）。**設計帯 `competition/mazes/design_v4`（seed 41000 以降）で再回帰した。**

- 出所: `outputs/exp_014_design_check/l0c_e1/runs_detail.json`（L0-c+E1・設計帯 20 面）
- 対象: **最短走行のみ 80 本**（探索走行は速度制限が違うので混ぜない）
- 形: $t = a \cdot (\text{移動区画数}) + b \cdot (\text{旋回回数})$、切片なし

| | $a$ [s/区画] | $b$ [s/折れ] | $b/a$ | 残差 RMS |
|---|---|---|---|---|
| **L0-c（本実験で使う）** | **0.1554** | **0.1358** | **0.8741** | **0.211 s** |
| 参考: 切片ありの当てはめ | 0.1556 | 0.1358 | 0.8732 | 0.211 s（切片 −0.0095 s） |
| 現行（評価帯・**超信地旋回**由来） | 0.7216 | 0.8667 | 1.2010 | 0.167 s |

**当てはまりは良い**（相対残差の中央値 **1.03%**）。切片はほぼ 0 なので切片なしを採る。

### ⚠️ P5 の主張「再回帰は精度のためではない」は**誤りだった**（本実験で判明）

P5 §4-3 は `turn_cost` を 0.6〜2.0 で振って「結果は係数にほとんど依存しない」と書いた。
**この検証は循環していた。**

> **経路の選択に使った係数と同じ係数で、その選択を採点していた。**
> `turn_cost`=1.201 が選んだ経路を、1.201 を生んだ係数で測れば、当然よく見える。

**L0-c の実測係数で採点し直すと、`turn_cost` を取り違えた選択は 2 面で厳密に遅い**:

| 面 | 歩数最短 | `tc`=0.874 の選択 | `tc`=1.201 の選択 | L0-c 係数での損 |
|---|---|---|---|---|
| maze_41038 | 48 歩 26 折 | **48 歩 26 折** | 50 歩 24 折 | **+0.0391 s** |
| maze_41155 | 57 歩 30 折 | **57 歩 30 折** | 61 歩 26 折 | **+0.0782 s** |

**機構**: スラロームは旋回で停止しないので**旋回が相対的に安い**（0.874 < 1.201）。
超信地旋回の交換レートを使うと**旋回回避を過大評価し、余分な区画を買ってしまう。**

**損の絶対値は小さい**（0.04〜0.08 s）が、**向きは一意である**。
**「係数を間違えても結果は変わらない」は成り立たない。**

## 2. 期待効果（**設計帯 20 面で実測**・L0-c の実測係数で採点）

| | 値 |
|---|---|
| **発動した面**（歩数最短 ≠ 時間最短） | **4 / 20**（maze_41020, 41037, 41234, 42134） |
| 発動面の短縮率 中央値 | **3.12%**（最大 4.05%） |
| **全 20 面の短縮率 中央値** | **0.00%** |

**L0-c の (d) 中央値 15.07 s に当てはめると、発動面で 0.4〜0.6 s 程度。**

## 3. 設定

- 帯: **凍結帯 `competition/mazes/eval`（v4・20 面）**。対照 = exp_014 の L0-c+E1（同一帯）
- **開発・調整・係数の回帰は設計帯のみ。凍結帯は最終の 1 回だけ**
- 出力: `outputs/exp_015_time_optimal_route/l0c_e1_tr/`
- 集計: `experiments/exp_015_time_optimal_route/aggregate.py`。
  **指標の定義は `importlib` で exp_013 の集計器を読み込んで再利用する**（古典系列の標準・裁定 R23）
- **係数はハードコードしない。**回帰結果を JSON に保存し、方策はそれを読む（§9）

## 4. 予測の事前登録（実装前に記入・**変更しない**）

| # | 予測 | 向き | 根拠 |
|---|---|---|---|
| **T1** | **(d) が改善する面は 20 面中 2〜7 面**、残りは完全に不変 | ↑（一部） | 設計帯で 4/20。評価帯は同じ受理条件なので同程度 |
| **T2** | **(d) の対応差の中央値は 0.00 s**（動いても ±0.05 s 以内） | **→** | 発動面が少数なので中央値に出ない（`note_016`） |
| **T3** | **改善面の短縮率は 2〜5%**（絶対値で 0.2〜0.9 s） | ↑ | 設計帯で中央値 3.12%・最大 4.05%。L0-c の (d) は 10〜22 s |
| **T4** | **(d) が悪化する面は出ない** | → | 時間モデル上、時間最短は歩数最短以下。**悪化が出たらモデルが実走と乖離している** |
| **T5** | **(e') が悪化する面が出る** | ↓（一部） | **時間最短は歩数最短より長いことがある**（maze_42134 は 46 歩 → 50 歩）。**(e') は区画数の比なので定義上悪化する。これは欠陥ではなく、指標が経路長しか見ていないため** |
| **T6** | **(a)(b)(c) は不変** | → | 経路の選び方だけが変わる。到達可能性・走行構成は変わらない |

### 4-1. 事前に置く「外れたらこう読む」

- **T4 が外れた（悪化面が出た）** → **時間モデルが実走を説明できていない。**
  まず**その面の実測タイムと予測タイムの差**を出す。残差 RMS 0.211 s を超えていれば
  モデルの限界、超えていなければ実装の誤り
- **T1 が外れて発動 0** → 方策が既知地図の楽観／悲観の扱いで設計帯と違う経路を引いている。
  **`_flood_fill` の未知壁の扱いを Dijkstra 側と揃えているかを確認する**
- **T5 が外れた（(e') が悪化しない）** → 時間最短が歩数最短と一致した面ばかり。
  **T1 の発動面数と突き合わせる**
- **(d) の改善が T3 の幅を大きく超えた** → 経路以外が変わっている。**1 実験 1 変更の違反を疑う**

## 5. 報告に必ず含める

- **対照との比較は §9-15 準拠**（面ごとに対応をとった差の分布・改善/悪化/不変の面数）
- **二値指標の対応比較は McNemar**
- **(e') が悪化した面は必ず報告する**（T5。**改善だけを報告しない**）
- **発動面数**（発動 0 なら効果の測定になっていない）
- **効いた／効かなかった理由**（ユーザ裁定）
- **「参照線が動いたか」を明記する**（裁定 R23）。動いた場合は
  **准教授の独立確認が入る**（裁定 R29）

## 6. 結果

（実行後に記入）

## 7. 判定

（実行後に記入）
