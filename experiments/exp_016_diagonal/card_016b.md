# exp_016 実験カード: 斜め走行 — **段階 016-B（1 本の斜め区間を走る。本命の関門）**

- 起票: 2026-08-14 学生A（評価・古典ベースライン担当）
- 段階分けの承認: 教授（裁定 **2026-08-13-R23**）。016-A の検収は 2026-08-14
- 起票時点: **実装は 1 行もしていない。016-A の理想値は出ているが、走行は 1 回もしていない**
- **本カードは段階 016-B のみを対象とする**

## 0. この段階で決めること

> **斜め走路の余裕 15.154 mm の中に、実際に機体を収められるか。**

**収まらなければ、機体を細くする（ロボット仕様の変更＝ MODEL_VERIFICATION_PLAN の
改訂と検証スイート再実行）か、斜めを諦めるかの二択になる**（P5 §5-3）。
**この分岐を早く引くために段階を切った。**

## 1. 主判定（主張 A）と副次測定（問い B）を**排他的に分離する**（§9-14・教授裁定）

**016-B の走行データから 2 つの量を取るが、判定は完全に分ける。**

| | 主判定（主張 A） | 副次測定（問い B） |
|---|---|---|
| 問い | **斜め区間で機体は走路に収まるか** | **45° 旋回の費用はいくらか** |
| 量 | 横偏差 $e_y$ と方位偏差 $\psi$ の**対** | 遷移 1 回あたりの余分な時間 $b_{45}$ [s] |
| 判定 | §3 の条件系 | §4（**幅つきの予測**） |
| **依存** | **$b_{45}$ の値は A の判定に一切影響しない**（A は幾何だけで判定する） | — |

> **B の測定値がどう出ても A の合否は動かない。**
> A は「$e_y \le e_{y,\max}(\psi)$ を全時刻で満たすか」という幾何の判定であり、
> **時間の量を一切参照しない。**

## 2. 量の定義（§9-17: **どの量を・どちら向きに・どの規約で測るか**）

### 2-1. 横偏差 $e_y$

**機体の回転中心**（左右車輪ヒンジの中点。016-A で `privileged_pose()` の基準点と
**0.000000 mm で一致**することを確認済み）から、**斜め走路の中心線**
（隣り合う柱の中点を結ぶ 45° 線）への**垂直距離** [mm]。

### 2-2. 方位偏差 $\psi$

機体の方位と、**走行中の斜め走路の方向**（45° 線）との差 [deg]。符号は問わず $|\psi|$ を使う。

### 2-3. 関門 $e_{y,\max}(\psi)$

$$e_{y,\max}(\psi) = 55.154 - \left[\frac{L}{2}|\sin\psi| + \frac{W}{2}|\cos\psi|\right]
\quad (L = 100.0,\ W = 80.0\ \text{mm})$$

$\psi$ = 0° で **15.154 mm**、**1° につき 0.867 mm 減る**（016-A カード §1-1）。

> **これは機体を矩形とみなした保守的な上界である。**准教授の $h(\theta)$ 表
> `TABLE_001`（`e1ab4ea`）で置き換えれば余裕を回収できる（$\psi$=45° で 63.64 → 56.57 mm）。
> **本カードの主判定は矩形（安全側）で行い、$h$ 表での再判定を副次的に併記する。**
> **「45° の値で代表させない」**（真値の最大は $\psi$=60° の 58.30 mm）。

### 2-4. $b_{45}$（副次）

**直進 → 斜めの遷移 1 回**にかかる**余分な時間** [s]:

$$b_{45} = t_\text{実測}(\text{遷移を含む区間}) - t_\text{基準}(\text{同じ距離を方位変化なしで走った場合})$$

**基準の作り方を先に決めておく**: 同じ走行の中の**方位が変わらない区間**から
1 m あたりの所要時間を求め、遷移区間の距離に掛ける。**遷移区間の切り出しは
「方位が変わり始めた時刻」から「方位が 45° に達して定常になった時刻」までとする。**

## 3. 主判定 A の条件系（**排他的**・§9-14）

| 判定 | 条件 |
|---|---|
| **A-成立** | 斜め区間の**全時刻**で $e_y \le e_{y,\max}(\psi)$。すなわち余裕 $m = e_{y,\max}(\psi) - e_y$ の**最小値が正** |
| **A-不成立** | $m$ が**どこかの時刻で 0 以下**（＝矩形の上界では壁・柱と干渉しうる） |
| **判定不能** | **斜め区間に入れなかった**（軌道生成が破綻した・その場旋回に落ちた・衝突で走行が終了した等）。**この場合は P1'〜P3' を判定しない** |

**A-不成立のときは $h(\theta)$ 表で再判定する**（矩形は上界なので、真の凸包では
成立する可能性がある）。**再判定の結果は主判定を上書きせず、併記する。**

## 4. 予測の事前登録（実装前に記入・**変更しない**）

### 4-1. P5 §5-4 の P1〜P3 → **P1'〜P3'**（量を名指しした形。教授裁定 2026-08-13）

**P5 本文は書き換えない。**下は**言い換えの対応表**であり、**向きと意図は不変**である。

| P5 の原文 | 本カードの登録 | 何を変えたか |
|---|---|---|
| **P1**「横偏差の最大が直進時より大きくなる」 | **P1'**: **同じ走行の中で**、斜め区間の $e_y$ の**最大値**が、直進区間の $e_y$ の**最大値**より大きい | 「横偏差」を §2-1 の量として名指しし、**比較の対象（同一走行内の直進区間）**を明示 |
| **P2**「13.75 mm を超える」 | **P2'**: 斜め区間の**どこかの時刻で $e_y > e_{y,\max}(\psi)$** となる（＝ A-不成立） | 閾値を **13.75 mm（AABB 由来）から $e_{y,\max}(\psi)$（$\psi$=0 で 15.154 mm）へ訂正**し、**$\psi$ 依存であることを明示**。**「失敗すると予測する」という意図は不変** |
| **P3**「速度を下げれば収まる」 | **P3'**: 斜め区間の指令速度を下げると $e_y$ の最大値が**単調に減り**、ある速度で A-成立になる | 「収まる」を A-成立として名指しし、**単調性**を明示（測る量は速度ごとの $e_y$ 最大値） |

### 4-2. 追加の予測

| # | 予測 | 量と許容差（§9-16） |
|---|---|---|
| **P4'** | **方位偏差 $\psi$ の最大は斜め区間で 5° を超える** | 量 = §2-2。許容差 ±0.5°（記録の分解能） |
| **P5'** | **余裕 $m$ の最小値は、$e_y$ 単独で見た余裕（15.154 − $e_y$）より小さい** | **$\psi$ を無視すると余裕を過大評価する**、を反証可能にする。**両方を必ず報告する** |
| **B1**（副次） | **$b_{45}$ は $b/4$ 〜 $b$ の幅に入る**（**0.034 〜 0.136 s**。現在の仮定 $b/2$ = 0.068 s を挟む） | 量 = §2-4。許容差は幅そのもの。**A の判定には影響しない** |

### 4-3. 事前に置く「外れたらこう読む」

- **A-成立（P2' が外れ）** → **良い驚きである。**段階分けが過剰だったのではなく、
  **余裕の再計算（13.75 → 15.154 mm）と、直進用ゲインが斜めでも足りたこと**の両方を確認する。
  **016-C（速度の探索）へ進む**
- **A-不成立（P2' 的中）** → **P3' の検証へ進む**（速度を下げて単調に減るか）。
  **減らなければ制御の作り直しが要る**（P5 §5-4 の P3 の趣旨）
- **判定不能（斜めに入れなかった）** → **軌道生成の問題であって関門の判定ではない。**
  **P1'〜P3' を判定しない**（登録した条文どおり）。原因を切り分けてから再測する
- **P4' が外れた（$\psi$ が 5° 以下）** → 方位が想定よりよく揃っている。
  **$e_y$ 単独の判定でよかったことになる**が、**P5' の報告は省略しない**
- **B1 が外れた（$b_{45}$ が幅の外）** → **016-A の短縮率の見積もりが変わる。**
  **016-A のカードは書き換えず、016-B の結果の節に「016-A の値をこう更新すべき」と書く**
  （教授裁定: 理想値の遡及書き換えをしない）

## 4-4. 設計判断: 直進 ↔ 斜めの接続は**円弧半径 $R$ = 60 mm**（教授承認 2026-08-14）

**走行前に幾何だけで決めた**（`transition_geometry.py`。016-A の経路の方位変化を
半径 $R$ の円弧で接続し直し、機体矩形を掃いて壁・柱との最小距離を出した）。

| $R$ [mm] | 最小余裕 | 干渉 | **その場回頭に落ちた数** |
|---|---|---|---|
| 45 | 15.154 mm | 0 | **0** |
| **60（採用）** | **15.154 mm** | **0** | **0** |
| 75 | 15.154 mm | 0 | **48〜70** |
| 90 | 15.154 mm | 0 | 48〜70 |

（設計帯 4〜6 面・全 406〜636 の方位変化。接線長は 90° で $R$、45° で $0.414R$。
斜め 1 歩は 127.3 mm・半歩は 90 mm なので、接線に使える余地は 63.6 / 45 mm）

**選定の根拠**:

1. **余裕は $R$ に依存しない**（全候補で 15.154 mm）。**最悪姿勢はどの $R$ でも
   yaw = ±45°/±135°、すなわち斜め走路を柱の間を抜ける瞬間**である
   → **律速は接続ではなく斜め区間そのもの**
2. **$R$ = 60 mm は、その場回頭への転落が 0 件である最大の半径。**
   $R$ ≥ 75 mm では斜め ↔ 斜めの 90° 接続で接線長が区間長を超え、48〜70 件が
   その場回頭に落ちる（**斜め化の目的そのものを損なう**）
3. **曲率が緩いほどコーナー速度が上がる**（同じ横加速度上限なら
   $v = \sqrt{a_\text{lat} R}$ で、$R$=60 は $R$=45 の約 1.15 倍）
4. $R$=45 mm の唯一の利点（接線長が短く隣の区間に余裕が残る）は、
   **$R$=60 で転落 0 件である以上、実利が無い**

> **余裕は $R$ に依存しないので、この選択で失うものは無い。**

**測定の適用範囲**: 掃引の距離は機体の外接箱 +20 mm で打ち切っている
（計算量のため）。**20 mm を超える余裕は `inf` になるので、値が大きい側は
信用しない。**判定に使うのは 15 mm 前後の領域なので実害は無い。

## 5. 設定

- 帯: **設計帯 `competition/mazes/design_v4` のみ**（1 面）。**凍結帯は使わない**。
  **スモークテストも設計帯で行う**（裁定 R40）
- 面の選び方: **016-A の結果から、斜め区間が最も長く取れる面**を選ぶ
  （`outputs/exp_016_diagonal/016a/results.json` から決める。**恣意的に選ばない**）
- 走行: **1 本の斜め区間だけ**（直線 → 斜め → 直線）。**全面の統合は 016-D**
- 記録: **制御周期ごとに $(t, x, y, \text{yaw}, e_y, \psi, v)$ を残す**
  （**横偏差だけでなく方位偏差も残す** — 教授指示）
- 出力: `outputs/exp_016_diagonal/016b/`。**面ごと・時刻ごとに全量**保存し、
  `git rev` と入力の版を含める（独立再計算のため）

## 6. 報告に必ず含める

- **主判定 A の結果**（$m$ の最小値・その時刻の $e_y$ と $\psi$）と、**$h(\theta)$ 表での再判定**
- **$e_y$ 単独の余裕と、$\psi$ を考慮した余裕の両方**（P5'）
- **P1'〜P3' の判定**と、**P5 原文との対応表**（§4-1 をそのまま再掲）
- **$b_{45}$ の測定値**（副次。**A の判定と混ぜない**）と、それが 016-A の短縮率に与える影響
  （**016-A のカードは書き換えない**）
- **効いた／効かなかった理由**（ユーザ裁定）

## 7. 結果

（実行後に記入）

## 8. 判定

（実行後に記入）
