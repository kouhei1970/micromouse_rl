# exp_016 実験カード: **016-cal の切り替え（安全率 0.70 → 0.75）と、基準の再導出**

- 起票: **2026-08-14** 学生A（評価・古典ベースライン担当）
- 裁定: 教授（**2026-08-14**。**016-cal〔旋回安全率の校正実験〕の (A) を採用 = 既定を 0.75 へ**）
- 起票時点: **確保済みの評価用 20 迷路（変更・日常使用禁止）での測定は 1 回も行っていない**
- **実施は教授の GO を待つ**（**「最終 1 回」級の測定を含むため**）

## 0. なぜやるか — **これは研究計画書の条項の執行である**

`docs/RESEARCH_PLAN.md` §5 の M5 の行に、**既に条項がある**:

> **限界: …基準値は安全率 0.7（未校正・`docs/L0PLUS_DESIGN.md` §8）の上に乗る —
> 安全率を校正し直した場合は M5 基準を再導出する**

**M5 の確定基準表の注記にも同じ条項がある**:

> **安全率 0.7 を校正し直した場合はこの表ごと再導出する**

**016-cal で校正が完了し、0.75 が確定した**（`card_016cal.md` §8）。
**したがって本カードは新しい提案ではなく、既にある条項の執行である。**

## 1. 🔴 **動かす対象は 2 つあり、性質が違う**（**混同しないこと**）

| # | 量 | いまの値 | 何で測ったか | 性質 |
|---|---|---|---|---|
| **A** | **M5 gate の基準**（確定基準表の迷路ごと 20 値・中央値 15.07 s） | **15.07 s** | **素の L0-c**（E1・TR・F0・F0-b なし）・安全率 0.7 | **合格の基準線。動かすと合否が変わる** |
| **B** | **参照線**（「動く参照線」） | **15.06 s** | **L0-c+E1（+TR）**・安全率 0.7 | **古典高速化トラックの現在地。gate は動かさない** |

**研究計画書 §5 は「古典高速化トラックの成果は gate を動かさず『動く参照線』として併記する」
と定めている**（2026-08-13 ユーザ裁定）。
**しかし安全率は素の L0-c にも効くパラメータ**なので、
**校正は A と B の両方を動かす。**

> ### 🔴 **確認を仰ぐ点**
> **A（gate の基準）の再導出は「素の L0-c を安全率 0.75 で測り直す」で正しいか。**
> **私はそう読んだ**（確定基準表の出所が `outputs/exp_013_band_v4_reeval/l0c/runs_detail.json`
> ＝ 素の L0-c であるため）。
> **もし「その時点の最強古典」で置き換えるなら、gate の性質が変わる**ので、
> **私からは決めない。**

## 2. 新しい既定の実装（**既存ファイルは変更しない**）

**`competition/baseline_slalom_e1t_tr_f0b_cal.py` を新設した**
（`SlalomE1TTRF0bCalPolicy`。**安全率だけが親と違う**）。

- **`SlalomPolicy.__init__` の既定値 `safety_factor=0.7` は変更しない** —
  変えると `baseline_slalom.py` が固定の基準でなくなり、**過去の全記録を
  ビット単位で再現できなくなる**（`maze_gen_v2.py`・`K_TARGETS` を固定したのと同じ理由）
- **`SlalomE1TTRF0bPolicy`（安全率 0.7）も変更しない** —
  **切り替え前の基準スナップショットを再現するために残す**
  （`card_016f0b.md` §8 の (γ) はその方策で測ってある）

## 3. 実施の手続き（`card_016f0b.md` §8 と同じ 5 条件）

1. **切り替え時点のコミットハッシュをカードへ記録**
2. **新既定での測定を取り直して確定させる**（下記 §4）
3. **参照線と M5 の確定基準表の扱いを明記**（**今回は「動かす」ので、
   §1 の A・B のどちらをどう動かしたかを 1 行ずつ書く**）
4. **切り替え前後の対応差を 1 度だけ両系列で出す**
5. **完了したらハッシュ報告**

## 4. 測定の計画（**実施は教授の GO 待ち**）

| # | 測定 | 迷路 | 方策 | 目的 |
|---|---|---|---|---|
| **M-1** | **M5 gate 基準の再導出** | **確保済みの評価用 20 迷路（変更・日常使用禁止）** | **素の L0-c**・安全率 **0.75** | §1 の A |
| **M-2** | **参照線の再導出** | 同上 | **`SlalomE1TTRF0bCalPolicy`**（新既定） | §1 の B |
| M-3 | 切り替え前後の対応差 | 同上 | 既存の記録と突き合わせ | 条件 4 |

- **M-1 と M-2 はどちらも確保済みの評価用迷路での測定**であり、
  **「最終 1 回」級**である（`docs/RESEARCH_PLAN.md` §9-7）。
  **`common/seed_bands.py` の安全弁を `purpose='gate'` ＋ `reason` つきで通す**
- **M-3 の「前」は既存の記録を使う**（新たに測り直さない）:
  A の前 = 確定基準表（`outputs/exp_013_band_v4_reeval/l0c/runs_detail.json`・
  SHA-256 `353f5049…d05101`）／B の前 = exp_014 以降の記録

### 4-1. 🔴 **M-2′ を追加する — 3 つの変更を 2 段に分解する**（2026-08-14 裁定・**投入より前に登録**）

**問題**: **参照線 15.06 s は exp_018 の L0-c+E1T+TR（安全率 0.7・F0 も F0-b も無し）**で
測った値であり、**F0・F0-b の効果は確保済みの評価用 20 迷路では一度も測っていない**
（`card_016f0b.md` §8 のとおり、(γ) は**測定用の 20 迷路での代理測定**だった）。
**したがって M-2 と参照線の差には 3 つの変更が同時に入る。**

**対処**: **M-2′（F0＋F0-b・安全率 0.70）を確保済みの評価用 20 迷路で測り、2 段に分解する。**

| 測定 | 方策 | 安全率 |
|---|---|---|
| 参照線（既存の記録） | L0-c+E1T+TR（F0・F0-b なし） | 0.70 |
| **M-2′**（追加） | **`SlalomE1TTRF0bPolicy`**（F0＋F0-b） | **0.70** |
| M-2 | `SlalomE1TTRF0bCalPolicy`（F0＋F0-b） | **0.75** |

### 🔴 **どの差を何に帰属するか**（**この読み方を投入より前に固定する**）

| 差 | **帰属する原因** | 期待する向き |
|---|---|---|
| **参照線 → M-2′** | **F0＋F0-b（速度ループと参照の読み出しの是正）だけ** | **遅くなる**（行き過ぎの追い風が消えるため。測定用の 20 迷路では **+0.30 %**） |
| **M-2′ → M-2** | **安全率の校正だけ**（0.70 → 0.75） | **速くなる**（測定用の 20 迷路では **−2.51 %**） |
| 参照線 → M-2 | **3 つの合計** | 上の 2 つの和に近いはず |

> **⚠️ この分解は「他の要因が無い」ことを仮定している。**
> **各差は 1 変更ずつになっているが、迷路が変わる（測定用 → 確保済みの評価用 20 迷路）ので、
> 効果の大きさは設計帯の値と一致しなくてよい。**
> **一致することを予測しているのではなく、向きが一致することを予測している。**
>
> **副産物**: **参照線 → M-2′ の差は、`card_016f0b.md` §8 の代理測定（+0.30 %）が
> 確保済みの評価用迷路でも成り立つかの後づけの検定になる。**

## 5. 予測の事前登録（**測定より前に記入・変更しない**）

> **⚠️ 作法 10 に従い、各予測で「上／範囲の中／下」の 3 方向すべてに読みを与える。**
> **根拠は 016-cal の実測**（校正用の 60 迷路で 0.70 → 0.75 の対応差が
> **中央値 −2.51 %・60 迷路すべて改善**）**と、測定用の 20 迷路での同じ測定**である。

| # | 予測 | 量と許容差 | 外れの向き（**全域**） |
|---|---|---|---|
| **S1** | **M-1 の (d) が速くなる**（対応差の中央値が負） | 量 = 迷路ごとに対応をとった最速タイムの差の中央値。**予測 −1.5 〜 −4.0 %** | **正**（遅くなった）= **素の L0-c では安全率が効かない**（機構の見直しが要る）／**−1.5 % より小さい改善** = 校正の効きが弱い／**−4.0 % より大きい改善** = 想定外に効いた |
| **S2** | **M-1 の完走率 (a) は 100 % のまま** | 量 = (a)。許容差 0 | **100 % 未満** = **確保済みの評価用迷路で崩れた**。**校正用の 60 迷路と測定用の 20 迷路の代表性の仮定が破れた証拠**（§3-6）。**その場合は切り替えを中止して報告する** |
| **S3** | **M-1 の (b)(c)(e′) は変わらない** | 量 = 迷路ごとの一致数。**予測 20/20 一致** | **不一致あり** = 安全率が経路そのものを変えている（**速度計画は経路を変えないはずなので、機構の見直し**） |
| **S4** | **M-2 の (d) も同じ向きに速くなる** | 量 = 同上。**予測 −1.5 〜 −4.0 %** | S1 と同じ読み。**S1 と S4 の符号が食い違ったら、E1T・TR・F0・F0-b のどれかが安全率と干渉している** |
| **S5** | **横偏差の最悪値が公称の余裕（50 mm）の内側** | 量 = 迷路ごとの $\lvert e_y\rvert$ 最大の最大値。**予測 ≤ 40 mm** | **40〜50 mm** = 内側だが余裕が薄い（**記録して次段階の入力にする**）／**50 mm 超** = **公称を超えた。切り替えを中止して報告する** |
| **S6** | **参照線 → M-2′ は遅くなる**（F0＋F0-b の代償） | 量 = 迷路ごとに対応をとった最速タイムの差の中央値。**予測 0 〜 +1.5 %**（測定用の 20 迷路では +0.30 %） | **負**（速くなった）= **代理測定と符号が逆**。**設計帯の代理測定の妥当性を疑う根拠になる**／**+1.5 % 超** = 代償が確保済みの評価用迷路では大きい（**参照線の解釈に影響するので報告する**） |

### 5-1. **外れたらこう読む**

- **S2 が外れ（確保済みの評価用迷路で完走しない）** → **最も重い。**
  **校正の帯の代表性の仮定（§3-6）が破れたことになる。**
  **切り替えを中止し、校正の帯の作り方から見直す**
- **S1 と S4 の符号が食い違う** → **安全率と、F0・F0-b・E1T・TR のどれかが干渉している。**
  **どの組み合わせで符号が変わるかを切り分ける**
- **S5 が 50 mm 超** → **公称の余裕を超えた。**
  **完走率が 100 % でも採用しない**（**たまたま当たらなかっただけの可能性がある**）

## 6. 結果 — 2026-08-14（教授 GO ののち実施）

### 6-0. **道具の無害性**（測定より前に確認）

| 検査 | 結果 |
|---|---|
| 合言葉なしで確保済みの評価用迷路を渡す | **拒否された**（`SeedBandViolation`）。**投入より前に実測** |
| `--policy` / `--gate-reason` の追加の無害性 | 追加前後で測定用の 20 迷路・安全率 0.75 を**全フィールド比較して差分 0 件** |

**⚠️ 最初の投入は失敗した**（シェルスクリプト経由で渡した理由の文字列が文字コードで壊れた）。
**走行は 0 本だったので確保済みの評価用迷路は消費していない。**直接投入し直して解消。

### 6-1. **3 つの測定**（すべて確保済みの評価用 20 迷路）

| 測定 | 方策 | 安全率 | 完走 | **最速 中央値** | $e_y$最大 中央値 | $e_y$最大 最悪 |
|---|---|---|---|---|---|---|
| 確定基準表（既存） | 素の L0-c | 0.70 | 20/20 | **15.075 s** | — | — |
| **M-1** | 素の L0-c | **0.75** | **20/20** | **14.690 s** | 24.78 mm | 27.49 mm |
| 参照線（既存・exp_018） | L0-c+E1T+TR | 0.70 | 20/20 | **15.065 s** | — | — |
| **M-2′** | ＋F0＋F0-b | 0.70 | **20/20** | **15.135 s** | 23.83 mm | 26.48 mm |
| **M-2** | ＋F0＋F0-b | **0.75** | **20/20** | **14.760 s** | 28.81 mm | **31.75 mm** |

**失敗様式は 0 件**（衝突・転倒・立ちすくみのいずれも無し）。

### 6-2. 🔴 **2 段の分解 — 和がほぼ厳密に合った**（§4-1 で登録した読み方）

| 差 | 帰属する原因 | **実測**（同じ迷路どうしの対応差の中央値） | 改善/悪化 |
|---|---|---|---|
| **参照線 → M-2′** | **F0＋F0-b だけ** | **+0.30 %**（+0.045 s） | 2 / 18 |
| **M-2′ → M-2** | **安全率の校正だけ** | **−2.54 %**（−0.375 s） | **20 / 0** |
| 参照線 → M-2 | 3 つの合計 | **−2.21 %**（−0.340 s） | **20 / 0** |

> **和の照合: +0.30 % + (−2.54 %) = −2.23 % に対し、実測の合計は −2.21 %。**
> **差は 0.02 ポイントである。**
> **2 つの変更はほぼ加法的に効いており、帰属の分解が成り立っている。**

### 6-3. 🔴 **設計帯の代理測定が、確保済みの評価用迷路でも成り立った**

**`card_016f0b.md` §8 で「(γ) は測定用の 20 迷路での代理測定であって参照線の更新ではない」
と断ったうえで記録した値は +0.30 %（中央値）だった。**

**今回、確保済みの評価用 20 迷路で同じ量を測ると +0.30 %。**

> **代理測定の妥当性が、後づけの検定で確かめられた。**
> **⚠️ ただし 1 例の一致である。**「代理測定は常に妥当」とは言えない。

## 7. 判定

記入: 2026-08-14 学生A。**予測 S1〜S6 は 1 文字も変更していない。**

### 7-1. 事前登録した予測の判定 — **的中 5 / 判定不能 1**

| # | 予測 | 実測 | 判定 |
|---|---|---|---|
| **S1** | M-1 の (d) が **−1.5 〜 −4.0 %** | **−2.66 %**（**20/20 迷路すべて改善**） | **的中** |
| **S2** | M-1 の完走率が 100 % のまま | **20/20 = 100 %** | **的中** |
| **S3** | M-1 の (b)(c)(e′) が変わらない | **(a)(b)(c) は 20/20 一致。(e′) は照合できない**（下記） | **一部のみ的中／(e′) は判定不能** |
| **S4** | M-2 の (d) も同じ向きに速くなる（−1.5 〜 −4.0 %） | **参照線基準 −2.21 %／M-2′ 基準 −2.54 %**（どちらも範囲内） | **的中** |
| **S5** | 横偏差の最悪値が **≤ 40 mm** | **31.75 mm**（M-2） | **的中** |
| **S6** | 参照線 → M-2′ が **0 〜 +1.5 %** 遅くなる | **+0.30 %** | **的中** |

### 7-2. ⚠️ **S3 は完全には判定できなかった — 私の測定設計の穴**

**(e′)（経路効率の指標）は照合できない。**
**本カードのランナー `run_016cal.py` が `n_cells`（計時窓で通過した区画の数）を
記録していない**ためである。

- **(a) ゴール到達・(b) 最短走行の成立・(c) 最短走行が有効 は 20/20 迷路で一致**
  （(c) は記録した `fast_time` と `explore_time` から復元して照合した）
- **(e′) は未確認。**「変わっていない」とは言えない

> **⚠️ 「照合できなかった」を「変わらなかった」と書かない**（作法）。
> **速度計画は経路を変えないので (e′) も不変のはず**だが、**それは推論であって実測ではない。**
>
> ### 🔴 **申し送り（次のセッションへ・教授承認 2026-08-14）**
> **(e′) の照合を `exp_013/run_arm.py` 系で行う**（同スクリプトは `n_cells` を記録する）。
> **確保済みの評価用 20 迷路 × 素の L0-c・安全率 0.75 を 1 回**。
> **安全弁は `purpose='gate'` ＋ 理由つきで通す。**
> **本カードの結論（切り替えの成否）はこれに依存しない** —
> **(e′) が変わっていたら、それは「速度計画が経路を変えた」ことの発見であり、
> 別の問題として扱う。**

### 7-3. **切り替えの結論**

| 量 | 前 | **後** | 差 |
|---|---|---|---|
| **M5 gate の基準**（確定基準表の中央値） | 15.075 s | **14.690 s** | **−2.66 %**（20/20 迷路改善） |
| **参照線**（動く参照線） | 15.065 s | **14.760 s** | **−2.21 %**（20/20 迷路改善） |

**S2（完走率 100 % のまま）と S5（横偏差 ≤ 40 mm）がどちらも満たされたので、
§5-1 の「切り替えを中止する」条件には当たらない。**

### 7-4. **判定に効かないが記録すべきこと**

- **横偏差は増えている**（M-2′ の最悪 26.48 mm → M-2 の最悪 **31.75 mm**）。
  **公称の余裕 50 mm の内側だが、余裕は 6 割ほど使っている**
- **F0＋F0-b は確保済みの評価用迷路でも代償だった**（+0.30 %）。
  **ただし物理の外を走るのをやめた代償**であり、**016-cal の校正がその 8 倍を取り返した**

## 7-5. 🚀 **切り替えの記録**（§3 の 5 条件の履行）

### 条件 ① **切り替え時点**

| 項目 | 値 |
|---|---|
| 新既定の方策 | **`competition/baseline_slalom_e1t_tr_f0b_cal.py`**（`e78ff5c`） |
| 測定と判定 | **`541111e`** |
| **切り替えの宣言** | **`11f8317`**（本節を新設したコミット。**ここが切り替え時点**） |

### 条件 ② **新既定での測定**（SHA-256 つき）

| 測定 | 場所 | SHA-256（先頭 16 桁） |
|---|---|---|
| **M-1**（M5 gate 基準の再導出） | `outputs/exp_016_diagonal/016cal_switch/m1_plain_l0c_sf0.75.json` | `6a03f34b5038e111` |
| **M-2**（参照線の再導出） | 同 `m2_newdefault_sf0.75.json` | `7058226eb6905482` |
| **M-2′**（分解の中間点） | 同 `m2p_f0b_sf0.70.json` | `20e4046fb099da27` |
| 改修の無害性検査 | 同 `regress_sf0.75_design.json` | `5684a556d47866f7` |

**切り替え前の記録も残す**: 確定基準表 = `outputs/exp_013_band_v4_reeval/l0c/runs_detail.json`
（SHA-256 `353f5049…d05101`・**版管理下**）／参照線 =
`outputs/exp_018_time_confirmed_exploration/l0c_e1t_tr/runs_detail.json`。

### 条件 ③ **参照線と M5 の確定基準表の扱い**（**今回は両方とも動かす**）

- **M5 gate の基準**: **15.075 s → 14.690 s**（素の L0-c・安全率 0.75）。
  **迷路ごとの 20 値ごと差し替える**（`docs/RESEARCH_PLAN.md` §5 の条項の執行）。
  **確定基準表そのものの改訂は教授の作業**（`docs/` は教授のみ編集）
- **参照線**: **15.065 s → 14.760 s**（新既定 = F0＋F0-b＋校正 0.75）

### 条件 ④ **切り替え前後の対応差**（**1 度だけ・両系列**）

§6-2 の表がそれである（**F0＋F0-b だけ +0.30 % ／ 校正だけ −2.54 % ／ 合計 −2.21 %**）。

### 条件 ⑤ ハッシュ報告 — 教授へ送信する

## 8. 持ち回る限定（**最終報告まで外さない**）

1. 本トラックの数値は**真の自己位置を知っている前提**（`privileged_pose()`）
2. **壁も真の壁配列を読んでいる**（センサを壁判定に使っていない）
3. **参照線 15.06 s と過去記録は「行き過ぎ込みの速度ループ」の値**
   （F0＋F0-b の是正で設計帯の対応差は +0.30 %・最悪 +0.75 %）

> ### 🔴 **2026-08-14: 限定 3 の読み分けが発効した**
>
> **これ以降、限定 3 は次のように読む**:
>
> | 記録 | 速度ループ | 安全率 | 参照線 |
> |---|---|---|---|
> | **切り替え前**（exp_013〜018・016-F0/F0-b/F の測定） | **行き過ぎ込み** | **未校正 0.70** | 15.06 s |
> | **切り替え後**（本カード以降） | **是正済み**（F0＋F0-b） | **校正済み 0.75** | **14.76 s** |
>
> **境目は §7-5 条件 ① の切り替えの宣言のコミットである。**
> **過去記録と新記録を並べるときは、必ずこの表を添えること。**
> **量は §6-2 の分解のとおり**（**F0＋F0-b が +0.30 %・校正が −2.54 %**）。
