# exp_016 実験カード: 斜め走行 — **段階 016-F（追従制御の刷新 第 1 弾）**

- 起票: 2026-08-14 学生A（評価・古典ベースライン担当）
- 位置づけ: **P6 レバー①(a)(b) の実装**（`research_notes/p6_diagonal_success_design.md` §2-1）
- 起票時点: **実装は 1 行もしていない**
- ⚠️ **「制御を変えない」規律はユーザ裁定で全面解除された**（2026-08-14）。
  **本実験は制御そのものを変える初のケースである**

## 0. この段階で決めること

> **前方注視と方位のダンピングを入れると、$v_\text{斜め}$ の上限はどこまで上がるか。**

**016-C で確定した上限は 0.45 m/s。目標 1（対照 15.06 s に並ぶ）には 0.72〜0.75 m/s が要る**
（P6 §1-2）。**本段階はその第 1 歩である。**

## 1. 1 実験 1 変更の宣言と、**基準スナップショット**

| | 内容 |
|---|---|
| 対照 | **現行の制御**（`baseline_slalom.py` の `_do_drive_control`）。**版として固定する** |
| 処理 | **(a) 前方注視つき 2 自由度化 ＋ (b) ジャイロによるレートダンピング** |

**制御を変えるので「対照の方策ファイルを変更しない」だけでは帰属が守れない。**
**基準スナップショット**（改良前の方策を版として固定し、**面ごと対応比較**する）で守る
（教授裁定 2026-08-14）。**既存ファイルは変更せず、新規ファイルで継承する作法は維持する。**

### ⚠️ (a) と (b) を**同時に**入れる理由

**F1（前方注視が無い）と F2（$e_\psi$ のダンピングが無い）は独立の欠陥**であり、
**どちらか一方だけでは効果が出ない可能性が高い**（前方注視で $\kappa$ の遅れを消しても、
$\psi$ が振動すれば意味がない。逆も同じ）。
**ただし「どちらがどれだけ効いたか」は分けて測る**（§4 の E3・E4）。

## 2. 実装の設計

### 2-1. 内側ループの時定数 $\tau_w$ の同定（**先にやる**）

$\omega_\text{cmd} \to \omega_\text{実測}$ の応答を測り、一次遅れ $1/(\tau_w s + 1)$ として
$\tau_w$ を同定する。**前方注視の距離はこの $\tau_w$ から決める**ので、**推定ではなく実測する**。

**手順**: 直線区間で $\omega_\text{cmd}$ にステップを与え、ジャイロ $\omega_z$ の応答から
時定数を読む。**設計帯の 1 面で行う。**

### 2-2. (a) 前方注視つき 2 自由度化

現行: $\omega_\text{ref} = \kappa(s_\text{cursor})\, v_\text{ref} + \ldots$

改良: **$\tau_w$ ぶん先の曲率**を使う。

$$\omega_\text{ff} = \kappa\big(s_\text{cursor} + v_\text{ref}\,\tau_\text{la}\big)\, v_\text{ref}$$

$\tau_\text{la}$（前方注視時間）は **$\tau_w$ を初期値**とし、$\{0,\ 0.5\tau_w,\ \tau_w,\ 1.5\tau_w,\ 2\tau_w\}$ で振る。

### 2-3. (b) ジャイロによるレートダンピング

$$\omega_\text{ref} \mathrel{+}= -\,k_r\big(\omega_z - \omega_\text{ff}\big)$$

$\omega_z$ は観測ベクトルにある（**現行の経路追従則は使っていない**）。
$k_r$ は $\{0,\ 0.1,\ 0.2,\ 0.4\}$ で振る。

**$\omega_\text{ff}$ を基準にする**ことで、**定常旋回では効かず、遅れ・振動にだけ効く**
（定常偏差を作らない）。

## 3. 設定

- 帯: **設計帯 `competition/mazes/design_v4` のみ。凍結帯は使わない**
  （**スモークテストも設計帯**・裁定 R40）
- 判定の harness は **016-C の `run_016c.py` をそのまま使う**（面と区間の選定・
  速度の梯子・$e_y$/$\psi$ の計装は同一）。**比較の土俵を変えない**
- **速度の梯子は 016-C と同一に凍結**: $\{0.30, 0.40, 0.45, 0.50, 0.55, 0.60, 0.70\}$ m/s
- **主判定は「20 面すべて A-成立かつ衝突 0 となる最大の速度」**（016-C §1-3 と同一）
- 出力: `outputs/exp_016_diagonal/016f/`。**版込み JSON・生成スクリプトはコミット**

## 4. 予測の事前登録（実装前に記入・**変更しない**・§9-16／§9-17 準拠）

| # | 予測 | 量と許容差 |
|---|---|---|
| **E1** | **$\tau_w$ は 0.01〜0.10 s に入る** | 量 = $\omega_\text{cmd}$ ステップに対する $\omega_z$ の一次遅れ時定数。許容差 ±20%（読み取りの幅）。根拠: 制御周期 10 ms・車輪 PI の帯域 |
| **E2** | **$v_\text{斜め}^{\max}$ が 0.45 → 0.55 以上**（梯子の 2 段以上の改善） | 量 = §3 の主判定。許容差は梯子の刻み 0.05 m/s |
| **E3** | **同じ $v$ で $\psi$ の最大が減る**: $v$=0.60 で **11.51° → 8.0° 以下** | 量 = 斜め標本の $\psi$ 最大。許容差 ±0.5°。**(b) の直接の効果** |
| **E4** | **同じ $v$ で $e_y$ の最大が減る**: $v$=0.60 で **15.75 → 12.0 mm 以下** | 量 = 斜め標本の $e_y$ 最大。許容差 ±0.3 mm。**(a) の直接の効果** |
| **E5** | **衝突は 0 面** | 016-C は 140 走行で 0 回。許容差 0 |
| **E6** | **直進区間の追従が悪化しない**: 直進標本の $e_y$ 最大が対照の **+1.0 mm 以内** | **ゲイン変更が直進を壊していないことの確認**。許容差 ±0.3 mm |

### 4-1. 事前に置く「外れたらこう読む」

- **E2 が外れた（0.50 止まり）** → **(a)(b) では F1・F2 の寄与が足りない。**
  **E3・E4 のどちらが動いていないかで、残る欠陥を特定する**
  （$\psi$ が減らない → (b) の実装かゲインの問題／$e_y$ が減らない → F3 の
  「横偏差ゲインが速度で減衰」が支配的）→ **P6 のレバー①(c)(d) へ進む根拠になる**
- **E3・E4 のどちらか一方だけが動いた** → **F1 と F2 の寄与を分離できたことになる。**
  **どちらが支配的かを報告に明記する**（P6 §2-1 では「実測で切り分けていない」と
  限界に挙げていた点が、ここで解ける）
- **E1 が外れて $\tau_w$ が 0.10 s より大きい** → 内側ループが想定より遅い。
  **前方注視だけでは足りず、内側ループ自体の設計（車輪 PI のゲイン）が対象になる**
- **E6 が外れた（直進が悪化）** → **斜め用のゲインが直進を壊している。**
  **ゲインスケジューリング（P6 レバー①(c)）が必要**という結論になる
- **E5 が外れた（衝突）** → **矩形上界の関門の内側でも当たった**ということ。
  **幾何の仮定（$h(\theta)$・回転中心）から見直す**

---

## 4-A. 【2026-08-14 追記】**起票から対照が変わった**（本文は書き換えない）

**本カードは 2026-08-14 09:39 に起票された**が、その後に **016-F0 と 016-F0-b** が入り、
**教授裁定 5-2 により古典トラックの既定が切り替わった**（切り替え時点 = **`b0eb5c4`**・
記録は `card_016f0b.md` §8）。**§1 の「対照 = 現行の制御」は起票時点の意味であり、
いまの対照は新既定である。**

| | 起票時（2026-08-14 09:39） | **いま（`b0eb5c4` 以降）** |
|---|---|---|
| 対照の方策 | `baseline_slalom.py` の素の制御 | **F0 ＋ F0-b 系**（$k_\text{acc,ff}$=1.0・`ref_interp`=True） |
| 判定の harness | `run_016c.py` | **`run_016f0_ladder.py --k-acc-ff 1.0 --ref-interp`**（**中身は 016-C のまま**） |
| $v_\text{斜め}^{\max}$ の出発点 | 0.45 | **0.45**（F0 で 0.40 へ下がり、F0-b で戻った） |

**梯子・面の選定・$e_y$/$\psi$ の計装・主判定の定義は 016-C のまま**である
（**比較の土俵は変わっていない**）。

### 4-A-1. 🔴 **E3・E4 の注記の出所を復元した**（記載を信用せず生データから）

**E3 の「11.51°」と E4 の「15.75 mm」は、20 面の代表値ではなかった。**

**復元の結果**（`outputs/exp_016_diagonal/016b/run.json`・`git_rev` = `6c6d6ae`）:

```
/results[0]/psi_max_deg      = 11.512856013484654   → E3 の 11.51°
/results[0]/e_y_max_diag_m   = 0.015754433398815257 → E4 の 15.75 mm
```

**どちらも 016-B の harness・maze_41038 の 1 面・v=0.60 の値**である。
**§3 が定める判定の harness は 016-C（20 面）なので、母集団が違う。**

**20 面での実測**（$e_y$ 最大・$\psi$ 最大の**面ごとの値の中央値**）:

| 版 | v=0.45 $e_y$ / $\psi$ | v=0.60 $e_y$ / $\psi$ | 0.60 の A-成立 |
|---|---|---|---|
| 起票時（016-C・F0 前） | 8.99 mm / 8.67° | **16.33 mm / 13.26°** | 0/20 |
| 中間（F0 のみ） | 7.98 mm / 8.75° | 16.26 mm / 12.58° | 5/20 |
| **いまの対照（F0+F0-b）** | **8.09 mm / 8.87°** | **14.75 mm / 12.50°** | 2/20 |

> **⚠️ 予測 E3・E4 は変更しない。**
> **判定の閾値（$\psi \leq 8.0°$・$e_y \leq 12.0$ mm）は絶対値なので、そのまま判定できる。**
> **変わったのは「→」の左側（出発点）だけである。**
> **ただし「量 = 斜め標本の ψ 最大」がどの母集団の代表値かを条文が書いていない**
> （1 面か 20 面か、中央値か最大か）。**これは作法 9 と同じ型なので、
> 教授の裁定を仰ぐ**（§4-A-4）。

### 4-A-2. **016-F0-b からの申し送り 3 件**（教授指示・本段階の入力）

1. **終端の残差は残っている**（超過 終端込み +0.12 対 終端抜き +0.074）。
   **「停止という要求そのもの」なので追従の是正では消せない**（裁定 5-1 で判定量から外し、
   停止性能として別名で記録する方針）。**016-F でも同じ扱いを踏襲する**
2. **梯子に強い非単調性がある**（0.50 成立・0.55 不成立・0.70 成立の面が複数）。**機構は未解明。**
   **単調性を仮定した読み方をしないこと**
3. **maze_41018 が 0.50 → 0.45 へ下がった機構は未確認**

### 4-A-3. 🔴 **最初の検証対象 = maze_41036 の横偏差の破れ**（教授の指示）

**F0 で唯一 A-不成立へ落ちた面**であり、**破れ方が横偏差だった**
（余裕 +0.939 → **−2.214 mm**・$e_y$ 最大 10.108 → **12.825 mm**・**衝突なし**）。
**円弧の物理ではなく操舵の問題**なので、**016-F がまさに効くはずの面である。**
F0-b で 0.45 へ戻ったが、**余裕の薄さ自体は残っている**（新既定の 0.45 で $e_y$ 最大 9.78 mm）。

**この面を「(a)(b) が効いたかどうか」の最初の検証対象にする。**

### 4-A-4. **副次測定 3 件を追加登録する**（准教授 REVIEW_009・未反映だったもの）

**予測 E1〜E6 は変更しない。**以下は**新規の予測として追加**する
（**作法 10 に従い、各予測で「上／帯の中／下」を明示的に数えた**）。

| # | 予測 | 量と許容差 | 外れの向き（**全域**） |
|---|---|---|---|
| **E7** | **$e_y$ の $v$ 依存は $v^2$ 比例に近い**（＝ F1 = 曲率 FF の遅れが支配的） | 量 = **斜め直線標本のみ**で $\log e_{y,\max}$ を $\log v$ へ回帰した冪 $n$。**予測 $n$ = 1.5〜2.5** | **$n$ < 1.5** = $v$ 依存が弱く **F2（ダンピング欠如）が支配的**／**$n$ > 2.5** = $v^2$ より急で、**第 3 の機構**（飽和・滑り）を疑う |
| **E8** | **円弧出口からの距離に対して $e_y$ が減衰する**（＝持ち越し仮説が真） | 量 = 円弧出口からの弧長 $\ell$ に対する $e_y$ の回帰の傾き。**予測: 有意に負** | **傾きが 0 近傍** = 持ち越しではなく**斜め直線そのもので発生**している／**正** = 出口から離れるほど悪化（**別の機構**。想定外） |
| **E9** | **飽和は起きない**（$\omega$ 指令 12 rad/s・電圧 3.0 V） | 量 = 飽和したティックの割合。**予測 0.000** | **> 0** = **線形の議論が無効になる**。**この場合 E3・E4 の読みを保留する**（F0/F0-b では全区分 0.000 だったが、**操舵ゲインを上げるので改めて測る**） |

**E7〜E9 は診断であり、主判定（$v_\text{斜め}^{\max}$）には使わない。**

### 4-A-5. 🔴 **裁定を仰ぐ事項**

**E3・E4 の「量」の母集団を確定していただきたい。**条文は「斜め標本の $\psi$ 最大 /
$e_y$ 最大」としか書いておらず、**面の集合と代表値の取り方が未指定**である。

| 案 | 量 | いまの対照の値（v=0.60） | 備考 |
|---|---|---|---|
| **(A)** 20 面の**面ごとの値の中央値** | 中央値 | $\psi$ **12.50°** / $e_y$ **14.75 mm** | §3 の harness と整合。**私の推奨** |
| (B) 20 面の**最大**（最悪の面） | 最大 | $\psi$ 14.81° / $e_y$ 17.61 mm | 保守的だが 1 面律速（作法 8） |
| (C) 注記どおり **maze_41038 の 1 面** | 単一面 | 起票時 11.51° / 15.75 mm | **判定の harness と食い違う** |

**推奨は (A)**。理由: **§3 が判定の harness を 016-C（20 面）と定めており、
1 面の値で判定すると土俵が変わる**。**(B) は作法 8 の問題（1 面律速）を持ち込む**。
**閾値（8.0°・12.0 mm）は変更しない。**

## 5. 報告に必ず含める

- **$\tau_w$ の同定値**と、前方注視 $\tau_\text{la}$・$k_r$ の探索結果
- **$v_\text{斜め}^{\max}$**（主判定）と、**面ごとの合格速度の分布**
- **E3・E4 の分離**（$\psi$ と $e_y$ のどちらがどれだけ動いたか ＝ **F1 と F2 の寄与**）
- **直進区間への影響**（E6。**斜めだけを見て直進を壊さない**）
- **効いた／効かなかった理由**（ユーザ裁定）
- **目標 1（0.72〜0.75 m/s）までの残り**を明示する

## 6. 結果

（実行後に記入）

## 7. 判定

（実行後に記入）
