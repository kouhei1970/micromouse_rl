# exp_016 実験カード: **段階 016-F0（速度ループの是正）**

- 起票: **2026-08-14** 学生A（評価・古典ベースライン担当）
- 裁定: 教授（**2026-08-14**。**F0 = (A) 速度ループの是正**。(B) は F0-b として登録）
- 起票時点: **実装は 1 行もしていない**（原因の実測だけ済ませた）
- 順序: **F0 →（残れば F0-b）→ F（操舵ループ）→ cal → G → H → I → K**

> **【2026-08-14 追記・日付の是正】** 起票時に日付を **2026-08-15** と書いていたが誤りで、
> **正しくは 2026-08-14** である（教授の指摘）。根拠は git の記録:
> 本カードの起票コミット `8bc3ae1` が **2026-08-14 09:59:31 +0900**、
> 原因特定の記録 `d116846` が同 **09:56:56**、016-F の起票 `e86f6b8` が同 **09:39**。
> **同日 09:39 の `card_016f.md` は「起票: 2026-08-14」と正しく書けている**ので、
> 20 分の間に書き手の「今日」がずれたことになる。**時系列の記録が主目的のカードなので、
> 本文を書き換えるのではなく、この追記で是正する**（§0 の思想と同じ）。
> **§0 本文の「（2026-08-14）」= 当初 F0 の裁定日は、git から独立に確定できなかった**
> （裁定は SendMessage で行われ版管理に残らない）。**未検証のまま残す。**

## 0. ⚠️ 起票時の見立ては外れていた（**時系列で残す**）

**当初 F0 は「円弧進入前の減速の是正」として裁定された**（2026-08-14）。
**その見立ては、実装前の実測で外れていることが分かった。**

| 区間 | 参照 $v$ | 指令 | **実速度** | **実 − 参照** |
|---|---|---|---|---|
| 直進 | 0.600 | 0.600 | **0.635** | **+0.139** |
| **円弧** | **0.497**（= $\sqrt{a_\text{lat}R}$。**正しい**） | 0.497 | **0.604** | **+0.107** |
| 斜め | 0.600 | 0.600 | 0.615 | +0.042 |

（設計帯 maze_41038・指令 0.60。毎ティックで `path.speed[cursor]`・`_v_setpoint`・
`privileged_velocity()` を記録）

> **【2026-08-14 追記・列の統計量が書かれていなかった件】**（教授の裁定 (b)）
>
> **上の表は、どの列がどの統計量なのかを書いていない。**そのため同じ行で
> **0.635 − 0.600 = +0.035** と **「実 − 参照」= +0.139** が並ぶ（円弧だけは
> 0.604 − 0.497 = +0.107 で一致し、斜めは 0.615 − 0.600 = +0.015 ≠ +0.042）。
>
> **復元の試み（結果: 生データは復元できなかった）。**探した先は
> ①`outputs/exp_016_diagonal/`（016a・016b・016c・016f のみ。F0 の診断出力は無い）
> ②リポジトリ内の生成スクリプト（存在しない）
> ③全セッションの scratchpad と背景タスクの出力（2026-08-14 09:00〜11:00 の範囲を走査。該当無し）
> ④起票コミット `8bc3ae1` と `d116846` の差分（**表そのものだけで、生データも生成コードも無い**）。
> **当時の測定は使い捨てで、記録が残っていない。**
>
> **列の算術から立つ唯一の整合的な読み**（**これは推論であって復元ではない**）:
> **参照・指令・実速度の 3 列は各ティックの中央値**、**「実 − 参照」列は
> 「各ティックの差」の中央値**。中央値は線形でないので、
> **区分の中で参照が変動すると「中央値の差」≠「差の中央値」**になる。
> 直進と斜めは参照が加減速で変動し、**円弧だけは参照が一定（0.497）なので両者が一致する** —
> 3 行の食い違いの向きがこの読みと過不足なく合う。
> （これは `note_015` の「同じ名前で別の量」と同じ族である）
>
> **この読みは D0 で検定する**（**実行前に登録する予測**。§4 の G1〜G5 とは別の、復元のための検定）:
>
> | # | 予測 | 許容差 |
> |---|---|---|
> | **R1** | maze_41038・v指令 0.60 を同一条件で再実行すると、**直進標本の「差の中央値」が +0.139 を再現する** | ±0.010 m/s |
> | **R2** | 同条件で**直進標本の実速度の中央値が 0.635 を再現する** | ±0.010 m/s |
> | **R3** | 同条件で**円弧標本は「差の中央値」= 「中央値の差」**（＝参照が一定） | ±0.002 m/s |
>
> **R1〜R3 が揃って的中したら、上の読みを列定義として確定して追記する。
> 外れたら「統計量は不明のまま」と記録し、D0 の実測値で置き換える。**
>
> **⚠️ 判定量の定義は条文どおり変えない**（教授裁定 (b)①）: **G1 = 中央値・G2 = 最大値**。
> ただし **G1 の量は「定常区間に限った差の中央値」であり、+0.139（直進の全標本）とは別の量**である。
> **G1 の現状値は測られたことがない。**D0 で初めて測る。
>
> ### 【2026-08-14 追記・R1〜R3 の判定】**R1・R2 は外れ。R3 は的中。予測は変更していない。**
>
> **D0 で maze_41038・指令 0.60 を再実行した結果**（`run_016f0_diag.py`）:
>
> | # | 予測 | 実測 | 判定 |
> |---|---|---|---|
> | R1 | 直進の**差の中央値**が +0.139 | **−0.0002**（016-B 型）／**−0.0411**（016-C 型） | **外れ** |
> | R2 | 直進の**実速度の中央値**が 0.635 | **0.5996**（016-B 型）／**0.6846**（016-C 型） | **外れ** |
> | R3 | 円弧は「差の中央値」=「中央値の差」 | **+0.0531 = +0.0531**（参照の標準偏差 5.6e-17） | **的中** |
>
> **外れて分かったことが 2 つある**（外れの中身は 2 種類で、区別が要る）:
>
> **(1) 診断の harness が 016-C ではなかった。** 016-C は**直進を $v_\text{cap}$、斜め・円弧を
> $v_\text{斜め}$ と分けて**上限を掛ける（§3 の 016-B からの差分）ので、
> **直進の参照は 0.80 前後になり 0.600 にならない**。§0 の表は直進・斜めとも参照 0.600 なので、
> **経路全体に 1 つの上限を掛ける 016-B 型**で測られたことが分かる
> （`run_016f0_diag.py --single-cap` を新設して再現した。参照は 直進 0.6000・
> 円弧 0.4965・斜め 0.6000 で **3 列とも §0 の表と一致**）。
>
> **(2) 「実速度」列は中央値ではなく最大値である**（016-B 型で再現）:
>
> | 区分 | §0 の表 | 実測の**最大値** | 実測の中央値 |
> |---|---|---|---|
> | 直進 | 0.635 | **0.6350** | 0.5996 |
> | 斜め | 0.615 | **0.6155** | 0.6000 |
> | 円弧 | 0.604 | 0.6099 | 0.5496 |
>
> **直進は 4 桁一致、斜めも一致する。**（円弧は 0.006 ずれる — 未解明）
>
> **【不採用の仮説（記録のみ・追跡しない）】**（教授の参考観察・2026-08-14）
> 表内の関係として「実 − 参照」≈「実速度」− **0.4965**（＝円弧の参照）が
> **直進**（0.635 − 0.4965 = 0.1385 ≈ 0.139）と**円弧**（0.604 − 0.4965 = 0.1075 ≈ 0.107）で
> 近く一致する。**表を作るときに円弧の参照が他区分の行へ混入した集計の誤り**を示唆するが、
> **斜めが説明できない**（0.615 − 0.4965 = 0.1185 ≠ 0.042）ので決め手にならない。
> **不採用とし、これ以上追わない**（費用対効果。円弧の 0.006 ずれも同様に**未解明のまま**とする）。
>
> **「実 − 参照」列だけは、どの統計量でも復元できなかった。**試した形は
> 差の中央値／平均／p90／p95／最大／最大実速度 − 区分内の参照の最小値／
> 実速度が最大になった時刻での差 — **いずれも +0.139・+0.107・+0.042 を再現しない**。
> **統計量不明のまま記録する**（教授裁定 (b)② の「復元できなければ正直に書く」）。
>
> > **⚠️ ただし、この列が何であれ結論は変わらない。**
> > **D0 の実測では、直進の定常区間の超過は中央値 −0.0000 m/s（設計帯 20 面・n=450）である。**
> > **+0.139 が何の統計量であっても、それは定常の偏りではない。**（§6 に詳細）

> **速度計画（参照）も指令も正しい。行き過ぎているのは実速度だけである。**
> **直進でも +6% 超過している**ので、**円弧進入時の減速の問題ではない。**
> **「速度計画が自分の参照を守れていない」という私の当初の説明も不正確だった** —
> **計画は正しく、追従が守れていない。**

**この誤診は 016-D の交絡の誤診と同じ型である**（推論を実測で確かめずに設計へ持ち込んだ）。
**今回は実装前に測ったので、手戻りは診断だけで済んだ。**

## 1. 1 実験 1 変更と基準スナップショット

| | 内容 |
|---|---|
| 対照 | **現行の速度ループ**（`WheelPI` ＋ 逆モデル前置補償）。**版として固定する** |
| 処理 | **速度ループの是正**（行き過ぎを減らす） |

**速度ループは全方策が共有する最内ループである。**したがって
**基準スナップショット**（現行の版を固定して面ごと対応比較）を必ず取る（教授指示）。

**操舵ループ（016-F）とは別のループなので、混ぜない。**

## 2. ⚠️ 対照の脚注（**読み手向け**・教授指示）

> **現行の参照線 15.06 s と、exp_013〜018 の全記録は、
> 「行き過ぎ込みの速度ループ」で達成された値である。**
> **行き過ぎは対照にも +6% の追い風を与えていた。**
> **対照では無害だった**（円弧が $R$=90 mm で、行き過ぎ込みでも物理の内側だったため）。
> **F0 の後で比較するときは、この非対称を踏まえること。**
>
> **M5 の凍結表・参照線の記録自体は動かさない。**

## 3. 実装の候補（**実測で選ぶ。決め打ちしない**）

| # | 手 | 対象 |
|---|---|---|
| (i) | **車輪 PI のゲイン**（`kp_wheel`=0.05・`ki_wheel`=0.3）の見直し | 行き過ぎの主因が PI なら |
| (ii) | **逆モデル前置補償の係数**（`Ke_eff`・`inv_gain`・摩擦項）の見直し | 前置補償が過大なら定常で行き過ぎる |
| (iii) | **速度指令のレートリミット**（現在は上げ方向のみ $a_\max$） | 下げ方向を制限していないことが効いていれば |
| **(iv)** | **加速度（慣性）の前置補償**: $V \mathrel{+}= \text{inv\_gain} \cdot J_\text{eff} \cdot \dot{\omega}_\text{des}$ | **過渡の遅れが主因なら**（2026-08-14 追加。下記） |

> **【2026-08-14 追記・候補 (iv) の登録】**（教授の裁定 (a)。**D0 の実測より前に登録する**）
>
> **現行の逆モデル前置補償は静的である**（`WheelPI.step` の
> $V_\text{ff} = K_{e,\text{eff}}\omega_\text{ref} + \text{inv\_gain}\,(b\,\omega_\text{ref} + \tau_c \operatorname{sgn}\omega_\text{ref})$）。
> **定常負荷しか埋めていないので、加減速に要るトルクは PI が全部背負う。**
> $K_p$ = 0.05 V/(rad/s) は小さいので、**加減速の間だけ追従が遅れる**という機構が立つ。
> **H_lag が支持された場合の本命候補**として登録する。
>
> **$J_\text{eff}$ は新規に推定しない**（教授条件②）。**パラメータの基準文書から導出する**:
>
> $$J_\text{eff} = \underbrace{N^2 J_\text{rotor}}_{\texttt{RobotParams.armature}} + \underbrace{\tfrac{1}{2} m_\text{wheel} r^2}_{\text{車輪の慣性}} + \underbrace{\tfrac{1}{2} m_\text{total} r^2}_{\text{片輪が負う機体の並進慣性}}$$
>
> 出所: `mouse/params.py`（`armature` プロパティ = $N^2 J_\text{rotor}$、`mass_wheel`、`wheel_radius`）
> ／`docs/MODEL_VERIFICATION_PLAN.md` §4。
> **$m_\text{total}$ はソースの記載を合算せず、MuJoCo モデルから実行時に読む**
> （`model.body_mass` の機体サブツリー和）。**数値のハードコードはしない。**
>
> **⚠️ 第 3 項の $\tfrac{1}{2} m_\text{total} r^2$ は「左右輪が対称に前後加速を負う」という
> モデル上の仮定である**（旋回中は成り立たない）。**仮定であることを明示して使い、
> 効いたかどうかは G2 で判定する。**採否は D0 の層別結果で決める（教授条件③）。

**まず (ii) を疑う**: **直進の定常でも +6% 行き過ぎている**ので、
**過渡ではなく定常の偏り**である可能性が高い。**前置補償が要求より高い電圧を出していれば
定常で行き過ぎる。**（**ただしこれも仮説である。実測で確かめてから直す。**）

## 4. 予測の事前登録（実装前に記入・**変更しない**・§9-16／§9-17）

| # | 予測 | 量と許容差 |
|---|---|---|
| **G1** | **行き過ぎは定常の偏りが主**（過渡の行き過ぎではない） | 量 = 直進の定常区間での（実速度 − 参照）の中央値。**+0.10 m/s 以上なら定常が主**。許容差 ±0.01 |
| **G2** | **是正後、直進の（実 − 参照）が +0.03 m/s 以内**（現状 +0.139） | 量 = 同上の最大値。許容差 ±0.01 |
| **G3** | **是正後、円弧進入時の実測 $a_\text{lat}$ が 5.87 m/s² の内側**（現状 6.08） | 量 = 円弧標本の $v^2\kappa$ の最大。**成立条件**（教授指示） |
| **G4** | **$v_\text{斜め}^{\max}$ が 0.45 から動く**（上がる方に 1 段以上） | 016-C の梯子で再測定。許容差は刻み 0.05 |
| **G5** | **直進の走行タイムは悪化しない**（行き過ぎが消えるぶん遅くなるが 3% 以内） | **⚠️ 行き過ぎは追い風でもあった**ので、**是正すると遅くなる方向に働く**。量 = 対照の (d) との対応差 |

> ### 【2026-08-14 是正注記・G2 の「同上」が二義的だった】（教授裁定）
>
> **G2 の条文「量 = 同上の最大値」の「同上」が 2 通りに読める。**
> 「同上」は文法的には G1 の量（＝**直進の定常区間での**（実速度 − 参照））を指すが、
> 注記の「現状 +0.139」は**直進の全標本**を指しているように読める。
> **D0 で両方の現状基準値を測ったところ、まったく違う値になった**:
>
> | 読み | G2 の量 | **現状（D0 実測）** | +0.03 以内か |
> |---|---|---|---|
> | **(A)** 条文どおり = **定常区間**の最大 | 定常標本の max | **+0.0007 〜 +0.0253** | **既に満たしている** |
> | **(B)** 注記どおり = **直進の全標本**の最大 | 全直進標本の max | **+0.2160 〜 +0.2267** | 満たしていない |
>
> **裁定（2026-08-14・教授）: (B) を主判定とし、(A) は参考併記する。**
> 根拠 ①**(A) では是正しなくても合格する** — 「発火しえない反証条件は反証条件ではない」
> （R50 の教訓）に当たる ②注記「現状 +0.139」は復元不能だが全標本系の値であり、
> 起票時の意図は全標本側にあった ③**判定対象（是正後の値）は未測定**なので、
> **この裁定は事前登録として成立する**。
>
> **⚠️ この裁定は、上表のとおり両方の現状基準値が開示された時点で下されている。**
> **G2 の条文（+0.03 以内・許容差 ±0.01）は変更していない。**

### 4-1. 外れたらこう読む

- **G1 が外れ（定常でない＝過渡が主）** → (i) か (iii) が対象。**(ii) を直しても効かない**
- **G3 が外れ（$a_\text{lat}$ が依然 5.87 超）** → **速度ループだけでは物理の外を消せない。
  F0-b（計画側の余裕）へ進む根拠になる**
- **G5 が外れ（3% 超の悪化）** → **行き過ぎは想像以上に大きな追い風だった。**
  **参照線の解釈に影響する**ので、教授へ報告して扱いを仰ぐ
- **G4 が外れ（梯子が動かない）** → 円弧の物理は改善したが**斜め直線の関門は別の要因**。
  **016-F（操舵ループ）の役割がより大きいことになる**

## 5. 設定

- **設計帯のみ**（スモークテストも設計帯・裁定 R40）
- 判定の harness は **016-C の `run_016c.py`**（梯子・面の選定・計装を変えない）
- **各段の実測 $a_\text{lat}$ の表**を出す（起票どおり）
- 出力: `outputs/exp_016_diagonal/016f0/`。**版込み JSON・生成スクリプトはコミット**

### 5-1. 基準スナップショットの取り方（2026-08-14 追記・教授承認済みの設計）

**速度ループは全方策が共有する最内ループ**なので、是正前の版を固定してから測る（§1）。

| # | 対象 | 内容 |
|---|---|---|
| **(α)** | 016-C の梯子 | 設計帯 20 面 × 梯子 7 速度。`run_016c.py` を**引数も含めて変えずに**再実行 |
| **(β)** | D0 の診断計装 | `run_016f0_diag.py`（**測るだけ。制御には触れない**） |
| **(γ)** | 走行タイム | 設計帯 20 面 × **L0-c+E1T+TR**（exp_018 の現行最良系列） |

- 全 JSON に `git_rev` を埋める。出力は `outputs/exp_016_diagonal/016f0/baseline/`
- **(α) は既存の `outputs/exp_016_diagonal/016c/run.json`（`git_rev` = `d610660`）と
  全フィールド照合する。**一致すれば「016-C 以降、速度ループに効く変更が無かった」ことの
  機械的な確認になる（exp_013 の determinism 照合と同じ形）
- **是正は既存ファイルを変更せず、新規 `competition/velocity_loop.py` の
  `VelocityLoopMixin` で `_wheel_targets_to_voltage` だけ差し替える**
  （`control_2dof.py` と同じ形）。**既定パラメータで現行と 1 ビットも変わらないことを
  単体テストで示す** — これが基準スナップショットを守る実装上の担保

> **⚠️ (γ) は退行確認のための代理測定であって、参照線の更新ではない**（教授裁定 (c)）。
> **参照線 15.06 s と M5 の凍結表は動かさない**（§2 の脚注と整合）。
> 凍結帯は使えないので設計帯で代理を取る、という位置づけである。

### 5-2. 持ち回る限定（**最終報告まで外さない**）

1. 本トラックの数値は**真の自己位置を知っている前提**（`privileged_pose()`）
2. **壁も真の壁配列を読んでいる**（センサを壁判定に使っていない）
3. **参照線 15.06 s と過去記録は「行き過ぎ込みの速度ループ」の値**（§2）

## 6. 結果

### 6-0. D0（診断・**実装 0 行**）— 2026-08-14

実行: `run_016f0_diag.py`（設計帯 20 面 × 梯子 7 速度 × 2 harness）。
出力 `outputs/exp_016_diagonal/016f0/baseline/d0_diag{,_cap1}.json`。

**監査 A**（$r(\omega_{L,\text{des}}+\omega_{R,\text{des}})/2 - v_\text{cmd} \equiv 0$）
= **1.11e-16**（016-C 型）／**5.55e-17**（016-B 型）。指令側の運動学に誤りは無い。

#### (1) 仮説の判別 — **H_ff 偽・H_kin 偽・H_lag 支持**

**016-B 型・設計帯 20 面**（単位 m/s。med = 中央値）:

| v指令 | 直進/加速 超過 | **直進/定常 超過** | 直進/減速 超過 | 直進/定常 層K |
|---|---|---|---|---|
| 0.30 | −0.0896 | **−0.0007** | +0.0219 | −0.0000 |
| 0.40 | −0.0803 | **−0.0014** | +0.0285 | −0.0000 |
| 0.45 | −0.0780 | **−0.0015** | +0.0322 | −0.0000 |
| 0.50 | −0.0753 | **−0.0020** | +0.0088 | −0.0001 |
| 0.55 | −0.0667 | **−0.0014** | +0.0150 | −0.0001 |
| 0.60 | −0.0646 | **−0.0000** | +0.0276 | −0.0001 |

- **H_ff は偽**: 定常の層W は **−0.0020 〜 −0.0000**。前置補償は定常で過大ではない。
  **これは車輪 PI に積分があるので当然でもある**（直進の前置補償電圧は
  $9.9\times10^{-3}\cdot 44.4 + 108.08\,(2\times10^{-6}\cdot 44.4 + 9.1\times10^{-4}) \approx 0.55$ V で、
  上限 3.0 V から遠く、**飽和率は全区分 0.000** なので積分が定常誤差を潰しきる）
- **H_kin は偽**: 層K は定常で $\leq 0.001$、全区分でも $\leq 0.013$
- **H_lag が支持**: 超過は**加減速の間だけ**現れ、**定常では消える**。
  **層W が超過の 85〜94% を担う**（例: 直進/減速 v=0.30 で超過 +0.1074 に対し層W +0.1034）

#### (2) **G1 は外れ**（§4-1 の条文が発動する）

**G1 の量**（直進の定常区間での（実速度 − 参照）の中央値）:

| v指令 | 0.30 | 0.40 | 0.45 | 0.50 | 0.55 | 0.60 | 0.70 |
|---|---|---|---|---|---|---|---|
| 面ごとの中央値 | −0.0007 | −0.0014 | −0.0015 | −0.0020 | −0.0014 | −0.0000 | +0.0039 |

**判定条件は「+0.10 m/s 以上なら定常が主」（許容差 ±0.01）で、全段が桁違いに下回る。**
**最も外れた 1 面でも +0.0278。→ G1 は外れ。**

> **定常標本では $v_\text{参照} = v_\text{指令}$ が厳密に成り立つことを確認した**
> （1197 個の統計量を照合し **|差| の最大 = 0.000e+00**）。
> したがって G1 の量（実 − **参照**）と本表の超過（実 − **指令**）は定常では同一であり、
> **判定量の取り違えは起きていない。**

**§4-1 により、対象は (i)/(iii)＋承認済み (iv) である。(ii) を直しても効かない。**

#### (3) 副次測定「飽和フラグ」（016-F の 3 件のうち 1 件を先取り）

**電圧の飽和率は、全速度・全区分・両 harness で 0.000。**
**線形の議論は有効**（飽和していれば線形の議論は無効になるところだった）。

#### (4) 基準スナップショット: 円弧の実測 $a_\text{lat} = v_\text{act}^2|\kappa|$（**G3 の基準値**）

**016-C 型**（判定の harness）・設計帯 20 面・実測上限 **5.87 m/s²**:

| v指令 | 0.30 | 0.40 | **0.45** | **0.50** | 0.55 | 0.60 | 0.70 |
|---|---|---|---|---|---|---|---|
| 面ごとの最大の中央値 | 3.282 | 5.287 | **5.684** | **6.400** | 6.400 | 6.400 | 6.400 |
| 5.87 を超えた面 | 0/20 | 0/20 | **0/20** | **20/20** | 20/20 | 20/20 | 20/20 |

> **梯子が 0.45 で止まる境目が、そのまま $a_\text{lat}$ が 5.87 を割る境目と一致する。**
> **参照の要求は $v_\text{参照}^2/R = 0.4965^2/0.06 = 4.11$ m/s² で物理の内側**であり、
> **6.400 は行き過ぎ（減速の遅れ）だけで作られている。**
> **F0 が行き過ぎを消せば $a_\text{lat}$ は 4.11 に近づき、5.87 の内側に入る**見込みである
> （**これは見込みであって測定ではない。G3 で判定する**）。

#### (5) 016-C の harness では**直進に定常標本が 1 つも無い**（付随して分かったこと）

016-C は**直進を $v_\text{cap}$（約 0.80 m/s）に委ねる**ので、
**短い直進では参照に届かず、加速か減速のどちらかしか起きない**
（全速度で 直進/定常 の n = 0）。
**016-C の `v_straight_med_mps` は「直進の定常速度」ではなく「届かなかった過渡の中央値」である。**
**016-C の判定（A-成立/不成立）には影響しない**（判定は余裕 $m$ と衝突だけを見る）が、
**この列を速度として引用しないこと。**

## 7. 判定

（実行後に記入）
