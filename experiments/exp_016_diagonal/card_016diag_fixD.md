# exp_016 実験カード: **是正 (D) — 斜めの分岐が落とした「速度計画の防御」を戻す**

- 起票: **2026-08-15** 学生A（評価・古典ベースライン担当）
- 起票の指示: 教授裁定 2026-08-15（`card_016g.md` §9-0 の訂正を受けて）
- **起票時点: 実装は 1 行もしていない**
- 位置づけ: **016-D の欠陥 4 枚目の是正**。**根の是正**
- 順序: (B) ✅ → (A) ✅ → **(D)〔本カード・単独で判定〕** → **(C) を載せて P1〜P5 の測り直し**

---

## 0. 直すもの — **先人の防御が、再実装で落ちていた**

### 0-1. 親（共有部分）には防御が 3 つある（`baseline_slalom.py:1205-1241`）

| # | 防御 | 中身 |
|---|---|---|
| **①** | **未知の先への上限** | `end_reason == "continue"`（末尾区画の出口の壁が未観測）なら**上限を `v_uncertain` に固定** |
| **②** | **終端速度** | `v_end = 0.0 if stop_at_end else min(v_ceil_base, v_uncertain)`。**終端の手前 `cell_size/2` から `v_end` に落とす** |
| **③** | **落としきれないなら全体を落とす** | `d_brake = (v_ceil_base² − v_end²)/(2a_max)` を計算し、**`s_term < d_brake` なら経路全体を保守的な上限に固定** |

**同じ箇所のコメントに、先人の実測が残っている**:

> **「これを怠ると失敗率が 12 % → 64 % に悪化した」**
> **「区画 1 つぶんの既知区間で全開加速 → 即全開制動を繰り返す鋸歯状の速度指令になり、
> 経路追従が破綻する（実測: 横偏差が 45 mm まで拡大し探索走行で壁に衝突）」**

### 0-2. 🔴 **斜めの分岐は 3 つとも落としている**（`baseline_slalom_diag.py:170-173`）

```python
v_arr = np.where(kinds == "straight", self.v_cap, self.v_diag)
v_arr = np.minimum(v_arr, self.v_cap)
path.speed = build_speed_profile(path.s, path.curvature, v_arr, ...)
```

**`v_uncertain` も `v_end` も `d_brake` も無い。**

### 0-3. **警告されていた症状が、そのまま再現していた**

**`card_016diag_switch.md` の原因究明で観測した衝突 3 件は、
先人のコメントが警告していた症状そのものである**:

| 先人のコメントの警告 | 今回の実測 |
|---|---|
| **全開加速 → 即全開制動の鋸歯** | **短い経路で計画 $v_\text{cap}$ = 2.895 → 拘束が現れて $a_\max$ 以上で制動** |
| **横偏差の拡大** | **直進中に 30 ティックで 147.6 mm 横へ流れた**（maze_41020 run3） |
| **壁に衝突** | **計時走行の中で 3 件** |

### 0-4. **実測での裏づけ — 対照では 1 ティックも起きない**

| 条件 | 196 点未満の経路にいたティック | **うち計画速度が $v_\text{cap}$** |
|---|---|---|
| **対照（斜めなし）** | 104430（26.8 %） | **0（0.00 %）** |
| 是正 (A) 後（斜めあり） | 104445（25.8 %） | **68（0.02 %）** |

**短い経路そのものは対照にも同じだけある。違うのは防御の有無だけである。**

---

## 1. 是正の設計

### 1-1. 🔴 **親へ委譲できるかを先に確かめた — できない**（教授指示）

**教授の指針**: 「**防御 3 点を書き写すのではなく、可能なら親の速度計画の該当処理を呼ぶ形に**
（写しはまた同じ事故を生む）。**呼べない構造なら、その理由と写した防御の一覧を条文に**」。

**確かめた結果、呼べない**:

- **防御 3 点は `SlalomPolicy._replan` の【中にインラインで】書かれている**
  （`baseline_slalom.py:1205-1241`）。**独立した関数・メソッドになっていない**
- **`build_speed_profile`（`baseline_slalom.py:494`）は上限の配列を受け取るだけで、
  防御を含まない**
- **切り出すには親を書き換えるしかないが、`baseline_slalom.py` は
  基準のスナップショットとして固定されている**（作法 1・過去の全記録の再現性）

> **⇒ 写す。ただし「何を写したか」を条文と実装の両方に一覧で残し、
> 単体検査で「写した防御が実際に効いていること」を確かめる。**

### 1-2. 写す防御の一覧（**これがすべて。これ以外は写さない**）

| # | 防御 | 斜めの分岐での適用 |
|---|---|---|
| **①** | 未知の先への上限 `v_uncertain` | **経路が打ち切られている（`truncated`）とき、直進区間の上限を `v_uncertain` にする** |
| **②** | 終端速度 `v_end` | **`stop_at_end` なら 0、そうでなければ `min(上限, v_uncertain)`。終端の手前 `cell_size/2` から適用** |
| **③** | `d_brake` の検査 | **落としきる距離が無ければ、経路全体を保守的な上限に固定** |

**⚠️ 斜め区間・円弧区間の上限（$v_\text{斜め}$ = 0.45）は変えない。**
**防御は「直進区間の上限」と「終端」に効く。**

### 1-3. **実装の形 — 混ぜ込みにして切り分けられるようにする**

**教授指示「(D) 単独で判定 → その後 (C) を載せる」に合わせ、混ぜ込みで作る**:

```
TerminalSpeedGuardMixin（本カードが作る）
  ├ SlalomDiagCalFixDPolicy  = Mixin + 是正 (A)(B) 版      … **(D) 単独の判定用**
  └ SlalomDiagCalFixCDPolicy = Mixin + 是正 (C) 版         … **最終形 (A)+(B)+(C)+(D)**
```

**作法 1 に従い、既存の方策ファイルは 1 行も変更しない。**

---

## 2. 予測の事前登録（**実装より前に記入・変更しない**）

> **各予測に ① 量の定義／② 測定条件（対照の選び方を含む）／③ 閾値 を書く。**
> 🔴 **「その介入が効くのはどの走行・どの区間・どの時点か」を先に言葉にする**:
> **本カードの介入が効くのは「経路が打ち切られている間の直進区間と、経路の終端」である。**
> **したがって量はそこで測る。**

### D1 — 🔴 **計時走行の中の衝突が消える**（**主判定**・機構理解の最終検証）

| | 内容 |
|---|---|
| **① 量** | **計時走行の結末が `collision` になった走行の数**（20 迷路の合計） |
| **② 条件** | **調整用迷路（seed 41000〜）の 20 迷路**・**(C) は無効**（`keep_horizon=False`）・**対照は是正 (A) 後**（= `diag_fixab_v2`。**3 件**） |
| **③ 閾値** | **0 件** |

**外れの向き（全域）**:
- **1〜2 件に減ったが残る** → **防御 3 点では足りない場所がある。**残った件の経路長・計画速度・
  終端速度を出して**どの防御が効いていないか**を名指しする
- **3 件のまま** → **防御は原因ではなかった。**機構の理解が誤り。**原因究明へ戻る**
- **増える** → **是正が別の害を作った。**帰属を確かめる

### D2 — **「短い経路 × 高速の計画」が消える**（**直接の量**）

| | 内容 |
|---|---|
| **① 量** | **196 点未満の経路にいたティックのうち、計画速度が $v_\text{cap}$ = 2.895 だったティックの数** |
| **② 条件** | D1 と同じ。**対照は是正 (A) 後（68 ティック）と、斜めなしの対照（0 ティック）の両方** |
| **③ 閾値** | **0 ティック**（**斜めなしの対照と同じ姿になること**） |

**外れの向き**: **1 ティックでも残る** → **防御 ① か ③ が効いていない経路がある。**
**その経路の `truncated` と `stop_at_end` を出す。**

### D3 — **無害性**

| | 内容 |
|---|---|
| **① 量** | 走行ごとの記録と**制御周期ごとの左右のモータ電圧の列** |
| **② 条件** | **判定に使う 20 迷路の【外】**（校正用の 60 迷路）で 3 迷路。**混ぜ込みを無効にすると是正 (A) 後と一致すること** |
| **③ 閾値** | **全項目・全ティックでビット単位の一致** |

### D4 — **完走率は落ちない**

| | 内容 |
|---|---|
| **① 量** | **(a) ゴール到達**が成立した迷路数 |
| **② 条件** | D1 と同じ |
| **③ 閾値** | **20 / 20 迷路** |

### 2-1. **走行タイムは判定に使わない**

> **本カードも修理である。**
> **速さの判定は `card_016diag_switch.md` の P1〜P5 で、(C) を載せた最終形で行う。**
> **⚠️ ただし (D) は「速く走らせない」是正なので、走行タイムは悪化する見込みである。**
> **その量は §5 に参考として記録する**（判定には使わない）。

---

## 3. 実施の段取り

| # | 手順 | 備考 |
|---|---|---|
| 1 | 実装（§1-3 の混ぜ込み） | **新規ファイル。既存の方策は 1 行も変更しない** |
| 2 | **単体検査** | 🔴 **写した防御 3 点が実際に効いていることを、生成された速度計画の配列で確かめる**（写しがずれても気づけるように） |
| 3 | **無害性 D3** | 判定集合の外・ビット単位 |
| 4 | **空振り検査**（新規則） | 判定集合の外・**「短い経路 × 高速が起きる条件」で**（教授指示。回収が起きる迷路 = maze_51032） |
| 5 | **D1・D2・D4 の測定** | 調整用迷路 20 件・**(C) は無効** |
| 6 | 報告 → 検収 → **(C) を有効化して P1〜P5 の測り直し** | **各変更の寄与を切り分けて記録する**（教授指示） |

## 4. 教訓（**起票時点で登録する**）

1. 🔴 **防御つきの機能を再実装すると、防御ごと落ちる。**
   **再実装せず親に委譲する。委譲できないときは、
   「落とさなかったこと」を防御の一覧で示す**（教授裁定 2026-08-15・**016-D の欠陥 4 枚目**）。
   **是正 (B) で採った「親に任せる」設計が正解だったことの事後証明である。**
2. 🔴 **先人の実測コメントは防御の仕様書である。**
   **`baseline_slalom.py:1205-1241` のコメントは「12 % → 64 %」「鋸歯」「横偏差 45 mm」
   「壁に衝突」と、防御を外したときに何が起きるかを実測で書き残していた。**
   **その症状が今回そのまま再現した。**
   **コードを再実装するときは、まず周辺のコメントを防御の仕様として読むこと。**

## 5. 結果（2026-08-15）

### 5-1. 実装と検査

| 手順 | 結果 |
|---|---|
| 実装（`competition/terminal_speed_guard.py`・`baseline_slalom_diag_cal_fixd.py`） | **完了**（**既存の方策は 1 行も変更していない**） |
| **単体検査（写した防御が効いていること）** | **6 件・全 PASS** |
| **D3 無害性**（判定集合の外・校正用 3 迷路） | **走行ごとの記録・電圧の列ともビット単位で一致**（相違 0 件） |
| **空振り検査**（**短い経路 × 高速が起きる条件**・maze_51032） | **相違 13 件・電圧も食い違う。防御 ① が 3 回作動** |

**単体検査の中身**（**生成された速度計画の配列そのもので確かめた**）:
- **防御 ①**: 打ち切られた 0.19 m の経路（**衝突時と同じ長さ**）で上限が `v_uncertain` 以下
- **防御 ③**: 0.30 m（$d_\text{brake}$ ≈ 0.998 m より短い）の経路で**全体が固定**
- **長い経路（3.0 m）では上限が下がらない**（**効きすぎていないこと**）
- **斜めの上限 0.45 は引き上げられない**

## 6. 判定

記入: 2026-08-15 学生A。**予測 D1〜D4 は 1 文字も変更していない。**

| # | 予測 | 実測 | **判定** |
|---|---|---|---|
| **D1**（**主判定**） | 計時走行の中の衝突が 0 件 | **3 件 → 0 件** | **的中** |
| **D2** | 短い経路 × $v_\text{cap}$ のティックが 0 | **68 → 0**（**斜めなしの対照と同じ姿**） | **的中** |
| **D3** | 無効化すると (A)(B) 版とビット一致 | **相違 0 件**（3 迷路） | **的中** |
| **D4** | 完走率 20/20 | **20/20** | **的中** |

### 6-1. 🚀 **主判定 D1 — 衝突が消えた**

| | 対照（是正 (A)(B) 後） | **是正 (D) 後** |
|---|---|---|
| **計時走行の中の衝突** | **3 件** | **0 件** |
| 走行と走行の間の事故 | 0 件 | **0 件** |

> **機構理解の最終検証が通った。**
> **「先人の防御を戻せば衝突は消える」という予測が、そのまま実測で確かめられた。**

### 6-2. **D2 — 「短い経路 × 高速の計画」が対照と同じ姿になった**

| 条件 | 196 点未満の経路にいたティック | **うち計画速度が $v_\text{cap}$** |
|---|---|---|
| **斜めなしの対照** | 104430（26.8 %） | **0** |
| 是正 (A)(B) 後 | 104445（25.8 %） | **68** |
| **是正 (D) 後** | 104736（25.3 %） | **0** |

**短い経路そのものは 3 条件とも同じだけある**（25〜27 %）。
**違っていたのは防御の有無だけであり、それを戻したら対照と同じ姿になった。**

### 6-3. ⚠️ **私の見立てが外れた — 走行タイムは悪化しなかった**

**§2-1 に「(D) は『速く走らせない』是正なので走行タイムは悪化する見込み」と書いたが、
悪化しなかった。**

| | 対照（斜めなし）との対応差の中央値 |
|---|---|
| 是正 (A)(B) 後 | **+18.6 %** |
| **是正 (D) 後** | **+18.6 %**（**変わらず**） |

**機構**: **防御が効いたのは 68 ティック（全体の 0.02 %）だけ**であり、
**そのティックを含む走行は、どの迷路でも最速走行ではなかった。**
**⇒ 「安全側に倒すと必ず遅くなる」は成り立たなかった。**

> **⚠️ これは「判定に使わない」と先に宣言していた量なので、判定には影響しない。**
> **ただし見立てが外れたことは記録する**（作法）。

### 6-4. **結論（1 行）**

> **是正 (D) は目的を達した** — **先人の防御 3 点を戻したところ、
> 計時走行の中の衝突が 3 件 → 0 件になり、
> 「短い経路 × 高速の計画」も斜めなしの対照と同じ 0 ティックになった。**
> **しかも走行タイムの代償は無かった。**
