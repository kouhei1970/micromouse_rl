# 事前登録: 016-H1e（第三容疑 — 是正距離の速度依存）

登録: 2026-08-18 教授セッション。**結果を見る前に確定した条文である。**
対象カード: `experiments/exp_016_diagonal/card_016h1e.md`
規約: `docs/RESEARCH_PLAN.md` §12-9 (c)（①本文書 ②錨の独立再計算 ③否定対照 の 3 点セット）

**本文書の条文は、測定後に一字も変えない。**変えたくなったら、変えずに判定し、変更は次のカードで行う。
**⚠️ 2026-08-18・凍結群の本測定より前に 1 度改訂した**（副判定量 W2 と §1 の 2 行のみ。主判定量は不変）。**経緯は §7。**

## 1. 錨（独立に再計算する量）

判定に使う対照値は、**カードや過去の報告から引き写さない。**一次記録から自分で出し直す。

| 錨 | 一次記録 | 出し方 |
|---|---|---|
| $E_{\rm in}^{\rm 健常}(v)$（円弧入口の $\lvert e_y\rvert$ 中央値・6 水準） | `outputs/exp_016_diagonal/016h1/d1d2_sf0.75_v{0.45..0.70}.json` の `rows[].entry_exit[][0]` | 20 面の全円弧を連結し中央値。**面ごとの内訳も併記する** |
| 「上乗せの 62%」 | 同上（`entry_exit[][0]` と `[][1]`） | 第 2 段の定義 $r=(E_{\rm in}(0.70)-E_{\rm in}(0.45))/(E_{\rm out}(0.70)-E_{\rm out}(0.45))$ を独立実装して再計算 |
| 模型の $\tau(v),L(v),a$ | なし（理論値） | $k_\psi{=}12.0,k_y{=}10.0,v_\epsilon{=}0.15$ を `competition/baseline_slalom.py:649` から**目視で書き写し**、コードを呼ばずに計算 |
| 実測速度 $\bar v$ | 各走行の per-tick 記録 | 🔴 **進入直線区間の `v_act` の中央値**（円弧の `arc_v_med` ではない・指令値でもない）。W1 の回帰の横軸はこれを使う |

**照合の規則**: 再計算値とカード記載値の差が絶対値 $5\times10^{-4}$ 以内なら一致（🟢）、それ以外は不一致として表に出す。
**不一致が 1 件でもあれば、判定に進む前に原因を書く。**

**⚠️ 件数・中央値は少数の面に支配されうる**（exp_023 の実例: 対照 12 件が `[0,0,0,2,5,5]`）。
$E_{\rm in}$ は**必ず面ごとの内訳を併記**し、連結中央値だけで「増えた／減った」と書かない。

## 2. 検算の層

| 層 | 中身 |
|---|---|
| L1 出所 | 使った JSON のパスと SHA-256、`git_rev` を判定文書に記す |
| L2 錨 | §1 の 4 つを独立再計算し照合表を出す |
| L3 決定性 | 健常群 1 水準（$v=0.60$）を再走行し、保存済み `d1d2_sf0.75_v0.6.json` の `rows` と **bit 一致**を確認 |
| L4 判別力 | 🔴 **W2**（下記 §4）。分母を凍結しない設定で健常と bit 一致しなければ、凍結は分母以外も変えている |
| L5 否定対照 | 凍結群そのもの。**変わらなければ、この経路は使われていない**（K3） |
| L6 母集団 | 除外した進入直線（$\Lambda<0.10$ m）の件数と率を、群ごと・水準ごとに報告 |

## 3. 判定の規則（先に固定する）

### 3-1. 主判定量 $q$ の定義

$$q = \frac{E_{\rm in}^{\rm 凍結}(0.70) - E_{\rm in}^{\rm 凍結}(0.45)}{E_{\rm in}^{\rm 健常}(0.70) - E_{\rm in}^{\rm 健常}(0.45)}$$

- $E_{\rm in}(v)$ = 当該水準の全円弧（20 面連結）の入口 $\lvert e_y\rvert$ の**中央値**[mm]。第 2 段と同一の定義。
- **分母は §1 で独立再計算した健常群の値を使う**（カードの記載値ではない）。
- 分母が $0.20$ mm 未満なら $q$ は定義しない → **K5**（下記 3-4）。

### 3-2. 判定の分割（実数直線を切れ目なく覆う）

| 判定 | 範囲 | 帰結 |
|---|---|---|
| **K1** | $q < 0.25$ | 的中。是正の設計へ進む |
| **K2** | $0.25 \le q < 0.60$ | 部分的。残差の機構の同定を続ける |
| **K3** | $0.60 \le q \le 1.15$ | 外れ。この経路は使われていない → 第四の容疑へ |
| **K4** | $q > 1.15$ | 想定外（凍結で悪化）。模型を捨てて設計へ戻る |

$(-\infty,0.25)\cup[0.25,0.60)\cup[0.60,1.15]\cup(1.15,\infty) = \mathbb{R}$。**穴なし・重なりなし。**

### 3-3. 同値の扱い（境界がちょうど閾値のとき）

- $q = 0.25$ ちょうど → **K2**（K1 は $0.25$ を含まない）
- $q = 0.60$ ちょうど → **K3**（K2 は $0.60$ を含まない）
- $q = 1.15$ ちょうど → **K3**（K3 は上端を含む。K4 は厳密に超過）
- **$q < 0$ は K1 に含まれる。**意味は「凍結すると速度依存が反転した」であり、消失より強い当たりである。
  判定は K1 だが、**判定文にはこの向きを明記する**（「消えた」と「反転した」を同じ語で書かない）。

### 3-4. 主判定量が定義できない場合

$E_{\rm in}^{\rm 健常}(0.70) - E_{\rm in}^{\rm 健常}(0.45) < 0.20$ mm のとき → **K5「上乗せが再現しない」**。
この場合は第 2 段の結果自体が再現していないことになるので、**判定へ進まず、まず再現性を調べる。**

### 3-5. 丸め

$q$ は**小数第 3 位で四捨五入**して判定する（例: $0.2496 \to 0.250 \to$ K2）。
$E_{\rm in}$ は mm 単位で小数第 3 位まで記録する。**丸める前の値も判定文書に残す。**

## 4. 副判定量（判定を左右しない・記述として登録する）

**W1（模型の量的な当否）**: 健常群の実測減衰長 $L(v)$ の 6 点回帰の傾き $a$ [s]。模型値 **1.110 s**、切片 1.807×10⁻¹ m。
$L$ の推定は進入直線ごとに $L_{\rm seg} = \Lambda_{\rm seg} / \ln(\lvert e_y\rvert_{\rm start}/\lvert e_y\rvert_{\rm end})$
（$\Lambda$ は直線の弧長）で出し、水準ごとに中央値を取る。
**$\lvert a - 1.110\rvert \le 0.33$（模型の ±30%）なら「模型は量まで当たっている」と記す。**外れても主判定は動かさない。

**W2（構造的同一性の検査・空振り防止）** 🔴 **2026-08-18 差し替え。理由と経緯は §7**:
凍結クラスを**分母を凍結しない設定**（$D_0$ を渡さず $v+v_\epsilon$ のまま）で走らせると、
健常と**軌跡・電圧が bit 一致する**こと。設計帯の先頭 2 面で確認する。
**これが破れたら、凍結クラスは分母以外も変えているので、主判定を出してはならない。**
（旧 W2「低速端で両群が一致すること」は前提が誤っていたため破棄した。§7 参照。）

さらに**発火と空振りの両方を実測してから使う**（§12-9 (c)）:
`tests/test_016h1e_frozen_denominator.py` で、同一の $(e_y, e_\psi, v)$ に対し
(a) $v + v_\epsilon = D_0$ となる $v$ では健常と凍結の $\omega_{\rm ref}$ が**一致する**（空振り側）、
(b) $v = 0.70$ では**一致しない**（発火側）、の 2 つを検査する。**片側だけの検査は認めない。**

## 5. 私（教授セッション）の事前予測 — **判定には使わない**

後知恵を防ぐために書く。$q \approx 0.3\!\sim\!0.5$（**K2** に落ちる）と予測する。
理由: 分母の凍結は「是正距離の伸び」を消すが、**円弧出口の初期条件（$e_\psi$ の大きさ）自体が速度とともに増える分**は
残るはずで、これは §5-4 の限界に書いたとおり模型の外にある。
**この予測が当たったかどうかは判定文書に書くが、判定条文には一切影響させない。**

## 6. 本測定で確かめないこと（範囲外）

- 円弧の中の挙動（016-H1 の射程。K4 判定で既に「実装しない」と決着済み）
- 検証帯・評価帯での再現（章の完了確認の工程で行う）
- 是正案そのものの効果（$k_y \propto v$ とするか分母を定数にするかの選択は、K1/K2 が出てから別カードで測る）
- 実機との対応（実機は本模型の理想の 3.0 倍速く、完成基準の分母には使えない）

## 7. 改訂の記録（2026-08-18・**凍結群の本測定より前**）

**主判定量 $q$ と判定の分割 K1〜K4・同値の扱い・丸めは一字も変えていない。**変えたのは副判定量 W2 と §1 の 2 行である。

### 7-1. なぜ変えたか

旧 W2 は「$D_0=0.60$ は $v=0.45$ 水準の公称値そのものだから、低速端では両群がほぼ一致するはず」を前提にしていた。
**この前提は誤りである。**実測で次を確認した。

- **進入直線では速度が水準 $v_{\rm diag}$ ではない。**方策は `kinds == "straight"` の区間に速度上限 `1e9` を与えており、
  直線では加減速の制約だけで決まる別の速度で走る（$v_{\rm diag}$ が縛るのは円弧である）。
- 円弧の実測は指令 0.45 に対し 0.4509 m/s（$D_0$ との差 0.2%）だが、**是正が起きるのは直線側**であり、そこは一致しない。

したがって「低速端で一致する」は構成上の必然ではなく、これを判定の門にすると**正しい実装を誤って弾く**。
新 W2 は「分母を凍結しなければ健常と bit 一致する」という**厳密で構成上必然の同一性**に置き換えた。旧 W2 より強い。

### 7-2. $D_0$ の値が主判定量に効かないことの確認

模型上、$D_0$ を変えても $L(v)$ の傾きはほぼ 0 のままである（$D_0=0.60$ で $-0.097$ s、$D_0=0.75$ で $-0.084$ s）。
変わるのは水準そのもの（オフセット）だけであり、**$q$ は水準間の差の比なのでオフセットは分子・分母で相殺する。**
ゆえに $D_0$ の選び方は主判定を動かさない。

### 7-3. 改訂の時点で分かっていたこと（**開示**）

実装の検分の過程で、subagent が設計帯 20 面・**$v=0.45$ の 1 水準だけ**を両群で走らせた。
その結果 $E_{\rm in}^{\rm 健常}(0.45) = 0.671$ mm、$E_{\rm in}^{\rm 凍結}(0.45) = 0.777$ mm を私は既に見ている。
**$v=0.70$ の凍結群は未測定であり、$q$ は計算できない状態で本改訂を行った。**
本改訂は上の 2 つの数値ではなく §7-1 の構造的な理由に基づく。低速端の差（15.9%）は
**判定に使わず、判定文書に記述として残す。**
