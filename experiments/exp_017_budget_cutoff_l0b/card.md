# exp_017 実験カード: 予算つき打ち切り（E2）を L0-b へ

- 起票: 2026-08-13 学生A（評価・古典ベースライン担当）
- 実験番号の割り当て: 教授（裁定 **2026-08-13-R29**。着手承認は **R36 の便**）
- 位置づけ: **exp_014 が壊した (b) の修復**。古典高速化トラック（レバー ③ の続き）
- 起票時点: **実装は 1 行もしていない**（カード → 実装 → 投入の順を守る）
- 土台は **exp_014 の L0-b+E1**（受理済み）。対照も同じ

## 0. 1 実験 1 変更の宣言

**変えるのは「追加探索をいつ打ち切るか」だけ**である。

| | 内容 |
|---|---|
| 対照 | **L0-b+E1**（exp_014。**最短経路が確定するまで無条件に**追加探索を続ける） |
| 処理 | **L0-b+E2**（**残り持ち時間で「帰路 + 最速走行」が入らなくなる前に**打ち切る） |

**探索の目標の決め方・経路決定・制御ゲイン・速度プロファイル・軌道生成・
ロボットパラメータは変えない。**打ち切りの判断が入るだけである。

## 1. 動機 — exp_014 は (e') を直したが、(b) を 1 面壊した

exp_014 で E1 を L0-b へ統合した結果:

- **良くなった**: (e') 1.034 → 1.000、超過区画 0 の面 8/20 → 19/19（悪化 0 面）
- **壊れた**: **maze_7837 で (b) を失った**（$D$=97。帯で最も長い面）。
  E1 が帰路を **+60.4 s** 延ばし、走行 2 の開始が 274.4 → **334.8 s** へずれ、
  **必要 85.9 s に対し残り 85.2 s。0.6 s 足りずに timeout**（exp_014 カード §6-5）

**E1 は「確定するまで探索を続ける」無条件の設計なので、持ち時間と正面から競合する。**
この費用は**計時窓の外（走行の隙間）に落ちるため走行タイムには現れず、持ち時間が
尽きた瞬間に「走行の消失」として全損で現れる**（`note_017`）。

**E2 は、まさにこれを見て打ち切る設計である。**exp_013 で E2 は L0-a の (b) を
80% → 90%（16/20 → 18/20）にした実績がある（決定的だったのは maze_1018・maze_1103 で、
run2 が timeout → goal に変わった）。

## 2. E2 の機構（`competition/baseline_classical_e2.py` の既存設計をそのまま使う）

各計画時に次を評価し、成立したら `target_mode` を `verify` → `to_start` へ切り替える。

$$\text{残り持ち時間} < (D_\text{帰路} + D_\text{最速}) \cdot s_\text{区画} \cdot (1 + \text{余裕})$$

- $D_\text{帰路}$: 既知地図での「現在地 → スタート」の最短**距離**（区画数）
- $D_\text{最速}$: 既知地図での「スタート → ゴール」の最短**距離**（区画数）
- $s_\text{区画}$: **その回で実測した** 1 区画あたりの所要時間 [s/区画]
  （経過時間 ÷ 通過区画数。**ハードコードしない**）
- 余裕: 0.15（既定。`baseline_classical_e2.py` の `DEFAULT_MARGIN`）
- 距離は**悲観地図**（未知壁 = 壁）で測る。悲観で到達不能なら楽観へフォールバック

**E1 と E2 の差は打ち切りの有無だけ**であり、探索の目標の決め方（`explore_targets`）は
同じものを呼ぶ。

## 3. 実装の見込み

`competition/baseline_straightrun_e1.py`（対照）は**変更しない**。新規ファイル
`competition/baseline_straightrun_e2.py` で `StraightRunE1Policy` を継承し、
**`_target_cells` だけを上書き**する（`AdachiE2Policy` が `AdachiPolicy` に対して
やっているのと同じ形）。予算判定の本体（`_budget_exhausted` / `_seconds_per_cell` /
`_known_distance`）は `baseline_classical_e2.py` から**取り込んで再利用する**
（コピーしない。裁定 R23 の精神）。

## 4. 設定

- 帯: **凍結帯 `competition/mazes/eval`（v4・20 面）**。対照 = exp_014 の L0-b+E1（同一帯）
- **開発・調整は設計帯 `design_v4` のみ。凍結帯は最終の 1 回だけ**
- 出力: `outputs/exp_017_budget_cutoff_l0b/l0b_e2/`
- 集計: `experiments/exp_017_budget_cutoff_l0b/aggregate.py`。
  **exp_013 の指標定義と exp_015 の対応差・McNemar・持ち時間の見方を importlib で再利用**（R23）
- **参照線（M5 gate の基準 15.06 s）は L0-c のものであり、本実験は L0-b なので
  参照線は動かない見込み**。動いたら明記する

## 5. 予測の事前登録（実装前に記入・**変更しない**）

| # | 予測 | 向き | 根拠 |
|---|---|---|---|
| **V1** | **maze_7837 の (b) が回復する**（走行 2 が timeout → goal）。**走行 2 の開始が 334.8 s より早まり、310 s 以前になる** | ↑ | §5-1 の紙の推定。教授指示により**面を指定して登録する** |
| **V2** | **(b) が 19/20 → 20/20**。**新たに (b) を落とす面は 0** | ↑ | 打ち切りは持ち時間を**空ける**方向にしか働かない |
| **V3** | **E2 の発動面（打ち切りが起きた面）は 10 面以上** | — | L0-b+E1 は **20 面中 7 面で最終走行が 420.0 s の打ち切り**に達しており、持ち時間が全体に苦しい |
| **V4** | **(e') が悪化する面が出る**（**2〜8 面**）。ただし (e') の中央値は **1.010 以内**に留まる | ↓（一部） | 確定前に打ち切るので、E1 が保証していた「真の最短」が得られない面が出る。exp_013 の L0-b（E1 なし）は 1.034 |
| **V5** | **(d) は maze_7837 で大きく改善する（182.24 s → 100 s 未満）。他の面の対応差は ±1 s 以内で、悪化する面は 0〜2 面** | ↑（1 面） | 対照の maze_7837 は**探索走行しかゴールしていない**ので (d) = 182.24 s。走行 2 が成立すれば 90 s 前後になる |
| **V6** | **走行本数が増える面が 4 面以上** | ↑ | 打ち切りで浮いた時間に走行が入る |

### 5-1. V1 の根拠（**紙の上の推定。実測ではない**）

対照（exp_014 の L0-b+E1）の maze_7837 の軌跡から、E2 の打ち切り条件が
**いつ成立するか**を計算した。$s_\text{区画}$ は軌跡から実測どおりに求め、
$D$ は**真の壁**の悲観距離で代用した。

| 量 | 値 |
|---|---|
| $D_\text{最速}$（スタート → ゴール） | **97 区画** |
| 打ち切り成立時刻 $t_\text{cut}$ | **197.9 s**（$s$=0.970 s/区画・$D_\text{帰路}$=102・必要 222.1 s） |
| 帰路の見積 | 99.0 s → **走行 2 の開始 ≈ 296.9 s**（対照の実測は 334.8 s） |
| 走行 2 に使える時間 | **123.1 s**（必要は実測 85.86 s）→ **入る** |

**この推定は「打ち切りが遅い側」に寄せた下界である**: 既知地図の悲観距離は
真の壁の悲観距離**以上**なので、実際の $D_\text{帰路}$ はこれ以上になり、
条件はこれより**早く**成立する。したがって $t_\text{cut} \le 197.9$ s。

**含意（V1 の裏側）**: この面では E2 が**ほぼ即座に**追加探索を打ち切る。
**maze_7837 では E1 の (e') の利得は失われる。**(b) と (e') の交換である。

### 5-2. 事前に置く「外れたらこう読む」

- **V1 が外れた（maze_7837 が救えない）** → **打ち切りが遅すぎるか、見積りが甘い。**
  打ち切り時刻 `_cut_at` と、そのときの $s_\text{区画}$・$D_\text{帰路}$・$D_\text{最速}$ を
  記録して、**推定 197.9 s と比べる**。$s_\text{区画}$ が実際の最速走行の
  1 区画あたり時間（85.86/96 ≈ 0.89 s）より小さく出ていれば、**余裕 0.15 では足りない**
- **V2 が外れた（新たに (b) を落とす面が出た）** → **打ち切りが早すぎて地図が育たず、
  かえって遅い経路で走っている可能性。**その面の (e') と走行本数を確認する
- **V3 が外れて発動 0〜数面** → 予算式が成立していない。**$s_\text{区画}$ が
  小さく見積もられていないか**（探索走行の平均で割っているので、実際より速く見える）
- **V4 が外れて (e') がまったく悪化しない** → **打ち切りが「確定した後」にしか
  起きていない**＝E2 が実質的に何もしていない。V3 の発動面数と突き合わせる

## 6. 報告に必ず含める

- **対照との比較は §9-15 準拠**（面ごとに対応をとった差の分布・改善/悪化/不変の面数）
- **二値指標の対応比較は McNemar**
- **発動面数と打ち切り時刻**（`_cut_at`。発動 0 なら効果の測定になっていない）
- **持ち時間の使い方**（最終走行の終了時刻・走行の隙間）。
  **走行タイムだけを見て「費用は無かった」と書かないこと**（`note_017`）
- **(b) と (e') の交換**を必ず両方報告する（**片方だけを報告しない**）
- **効いた／効かなかった理由**（ユーザ裁定）
- **「参照線が動いたか」を明記する**（裁定 R23）

## 7. 結果

（実行後に記入）

## 8. 判定

（実行後に記入）
