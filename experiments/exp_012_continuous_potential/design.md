# exp_012: ポテンシャル Φ の連続化（M2-0 失敗の機構仮説の検証）

- 起草: 学生B 2026-08-11（任務 2026-08-11-N2）
- 状態: **裁定 R5 で承認済み。学習は未実行**（`docs/RESEARCH_PLAN.md` §9 に従い予測を先に登録する）
- 机上計算: `research_notes/scripts/check_m2_gamma_horizon.py` → `outputs/m2_gamma_horizon/returns.json`
- 実測: `research_notes/scripts/check_m2_step_economy.py` → `outputs/m2_step_economy/step_economy.json`
- **独立の盲検再計算を通した**（§8）。私の誤り 2 件が見つかり、本文はすべて是正後の値である

## 改訂履歴

| 日付 | 内容 |
|---|---|
| 2026-08-11 | 初版（任務 N2）。独立の盲検再計算で私の誤り 2 件を是正（§8） |
| **2026-08-12** | **裁定 R7（准教授の反証監査 N3-4）の 6 項目 ＋ 裁定 R8（§4 の開口部中点方式）を反映。**支持条件を「1 本以上 ≥0.30」→**「3 seed の中央値 ≥0.30」**へ差し替え、§5-1 第 1 行を予測から**撤回**、仮説 H を**報酬の時間分布**の話へ書き換え、§2-4 の γ 棄却を**期待値**の形へ、テスト (b) の閾値を式で定義、Φ の連続化の実装を**開口部中点方式**へ差し替え |
| **2026-08-13 (6)** | **裁定 R34（マスクを真の配置空間へ）・R37（計量の宣言）・R38（(L) の是正）を反映。**(1) 条件 C のマスクを**障害物表面ベース**へ是正（**物理モデルに実際に生成されている**壁・柱の geom から導出。柱 `post_3_3` を自動包含）＋**機体モデルの限界**（半径 $w_\text{lat}$ の円板・向き非依存・前端 0.0500 は含めない）を条文化 (2) 場の定義を「**配置空間上の 8 近傍格子測地**」と宣言し、対ユークリッド誤差の上界（+8.24%・角度一様平均 +5.5%）と FMM 対 8 近傍の実測（中央値 +2.2%）を併記 (3) **(L) の未達は「帯へ延長した値が双線形に混ざる」実装の欠陥と確定**し、**解く領域を 1 格子セルぶん膨張**させて是正（最大比 60.78 → **1.3107**・超過 126.8 → **0.659 mm**）。McShane 型拡張は実測で悪化したため棄却し記録 (4) ゴール 2×2 中心の柱 `post_3_3` を**既知の環境特性**として登録（是正は R11 バッチ） (5) C' の $\rho$ 配管を実装し、$\rho$ を面ごとの定数とした設計の妥当性を実測で裏づけ |
| **2026-08-13 (5)** | **裁定 2026-08-13-P7（准教授 AUDIT_010 指摘 1・AUDIT_011 §5-ter、裁定 R33）を反映。記述のみの是正でコード動作・判定値は不変。**(1) §4 (b) の「遡及合格は無い」節を現行の判定形（乗法項 ＋ 加法項 $\mu_\max$）で計算し直した — **D2 は (b) で落ちる（1.20 倍超過）／D1 は (b) では通り (L) が落とす。加法項の導入により D1 型の検出は (b) から (L) へ移った**。**旧記述は時系列として残す**。(2) 条件 C の**収縮の基準点を併記**（禁止帯 = 壁中心線から $\pm$0.0460 m ＝ 壁面から $w_\text{lat}$=0.0400 m。同一の帯の 2 通りの書き方であり二重計上ではない）。**改訂時点でも `logs/exp_012*`・`models/exp_012*`・`outputs/exp_012*` は不在・学習プロセス 0 件＝閲覧した評価点は 0 点**（裁定 R7-7 の開示義務） |
| **2026-08-13 (4)** | **裁定 R25 を反映。**(b) の定数の機体半幅を、車輪外面 0.0395 から**メッシュ頂点由来の真の最外側 $w_\text{lat}$ = 0.04000（コーナー片）**へ差し替え、**$L_E$ = 2.7972 → 2.7669**（3 度目の是正。いずれも検査を厳しくする向き）。テストでは $w_\text{lat}$ を**モデルから実行時導出**する。AABB と真値の用途の非対称、$L_E$ の役割（E の必要条件ではなく、階段版と正しい E を分ける天井）を明記。**M1 の『機体最外部は車輪外面 39.5 mm』が誤りだったことが判明**（別途報告） |
| **2026-08-13 (3)** | **裁定 R24 を反映（准教授の投入前指摘: 多重性と報酬量の交絡）。**総整形量を条件 E に揃えた**条件 C' を追加**（計 3 条件・9 本）、**H の検証的判定は条件 E のみ**と確定（C・C' は探索的。「どちらかが支持なら H 支持」を禁止）、§5-3 を 3 条件対応へ拡張、**(b) の定数を 3.2223 → 2.7972 へ是正**（壁厚の半分を落としていた誤り。**検査を厳しくする向きの是正**）し、最適化問題の形・無次元形・上限に到達する 2 点対・中点一致の交換条件を明記 |
| **2026-08-13 (2)** | **裁定 R21 を反映（ユーザ方針「考えられることは全てやってみて比較する」）。**(E) 対 (C) の二者択一をやめ、**2 条件を並立させて比較する**構成へ変更。(b) の定数を**導出値 3.2223** へ改訂（辺全体一致・機体到達域から導出）、**条件 C（自由空間の測地距離場）を新設**し専用の検証項目を登録、**E × C の 2×2 解釈表を投入前に事前登録**、ゴール 2×2 の扱いを明文化して (L) の対象に含める |
| **2026-08-13** | **裁定 R13・R18 を反映。**単体テスト (b) が落ちたことで**開口部中点方式の実装に 2 つの欠陥（D1・D2）が実測で見つかり**、§4 の Φ の定義を**区画ごとの明示式 ＋ 全降下隣接の min** へ差し替えた。あわせて **(b) の閾値の前提 $\lvert\nabla\Phi\rvert\le1$ が数学的に達成不能**であることが判明したため閾値を $\sqrt2$ 由来へ再導出し、(c) の条文を限定、(b-2) を全降下開口部へ拡張、(b-2) のゴール側除外を明文化、**刻み不変性検査 (L) を新設**。§7-6 を新定義で再導出、§7-8 の限界は維持 |

> **📌 定数を式で書くことの効用（2026-08-13 の記録）**: (b) の定数 3.2223 の誤り
> （壁厚の半分 $t_w/2$ = 6 mm の脱落）は、**裁定 R24-3-2 が「無次元形・パラメトリックな式で
> 書け」と要求したこと**によって初めて表に出た（要求の出所は准教授）。
> **数値で書くと誤りが隠れ、式で書くと誤りが現れる。**同じ誤りは 2 度、数値のまま
> （3.2223 として）報告と監査を通り抜けていた。**以後、判定に使う定数は必ず
> パラメトリックな式の形で書き、生の数値は式の値としてのみ示す。**

> **⚠️ 2026-08-13 の改訂も exp_012 の学習を投入する前に行った**（裁定 R7-7 の開示義務）。
> **改訂の時点で `logs/exp_012*`・`models/exp_012*`・`outputs/exp_012*` はいずれも不在**であり、
> 学習プロセスも 0 件（`git status` と `ps` で確認）。**閲覧した exp_012 の評価点は 0 点**である。
>
> **発見の経緯**: 2026-08-13 に単体テスト `tests/test_maze6_potential.py` を実装して
> 実行したところ、10 項目中 (b) だけが落ちた（他の 9 項目は PASS）。除外が許された 2 種
> （ゴール到達の最終ステップ・方向反転）に当たらない**閾値超過が 26 件**あり、内訳は
> **区画内 18 件（最大 $\lvert\Delta\Phi\rvert$ = 0.07212 m）・前進 8 件（同 0.09099 m）**。
> 原因は独立した 2 つの欠陥（下記 D1・D2）だった。
>
> **本改訂は「介入の追加」ではない**（裁定 R13 条件 7）。介入は「Φ の連続化」1 点のままで、
> 本改訂はその連続化を**実際に成立させる**ための是正である。

---

> **⚠️ この改訂は exp_012 の学習を投入する前に行った**（裁定 R7-7 の開示義務）。
> **exp_012 のログ・モデル・評価結果は本改訂の時点で 1 つも存在しない**
> （`logs/exp_012*`・`models/exp_012*`・`outputs/exp_012*` すべて不在、学習プロセスも 0 件。
> `git status` と `ps` で確認した）。したがって**閲覧した exp_012 の評価点は 0 点**である。
> 一方 **exp_010/011 のログと検証履歴（6 本 × 21 評価点）は閲覧済み**であり、
> §1・§2-2〜2-4 はその実測に基づく。**これらは本実験の対照であって結果ではない。**

---

## 0. 結論の要約

1. **第一容疑者 γ=0.995 は棄却する。**6x6 では $\gamma^T$ = 0.245〜0.687 で潰れていない。
   決定的なのは**学習ログの実測**で、**ゴール到達は実際に 80 回起きており**、
   その条件つき総収益は **+0.62**（衝突 −0.71）だった。**割引はゴールの価値を
   奪っていない。**方策が凍結を選んだのは**期待値**（挑戦 −0.707 対 凍結 −0.200）で
   合理的だったからで、そうさせているのは**成功確率 1.14%** であって割引率ではない。
2. **望ましい順序「ゴール > 探索 > 滞留 > 衝突」は 20 面すべてで崩れている。**
   ただし崩れ方は $k$ で違う:
   - **$k$=8.7e-3: 走り続けること（探索）が最下位（−0.802）。**凍結（−0.200）が 4 倍得
   - **$k$=0: 衝突が正の報酬（最大 +0.408）**で、20 面中 15 面で 2 位
3. **共通の欠陥は Φ が区画単位の階段関数であること。**ただし**1 歩あたりの平均は
   階段でも連続でも +0.00455 で厳密に一致する**（1 区画の整形総和は telescoping で
   どちらも γ·0.18）。**違うのは時間分布**で、M1 は 100% のステップに進捗報酬が入るが、
   M2-0 は 5.3% のステップに集中し**残り 94.7% には「動くな」の信号しか入らない**。
   **これが M1 → M2-0 で起きた報酬の唯一の構造的変化である。**
4. **提案: Φ を連続化する 1 点のみを変える。** $k$・γ・並列数は変えない。
5. **⚠️ 検証しているのは報酬の地形ではなく最適化の力学である**（裁定 R7-4）。
   平均が同じなので、机上計算では H を支持も反証もできない。
   **机上計算の役割は「H 以外の説明を消すこと」に限られる。**

---

## 1. 【任務 (1)】1 歩あたりの経済 — M2-0 の条件で作り直した

引き継ぎメモ冒頭 (2) の表は **M1 廊下の値**であり、M2-0 では**表の形が変わる**。
罰の大きさは実測値を使う（$\mathbb{E}\lVert a-\bar a\rVert^2$ は**走行する方策で 0.459**、
M1 の 0.483 とほぼ同じ。**凍結した方策の停止層では 1.086e-4**、つまり 4200 分の 1）。

### M2-0（Φ は**区画単位の階段関数**。実装どおり）

| 挙動 | 時間罰 | 滑らかさの罰 | 整形 | 訪問 | **合計** |
|---|---|---|---|---|---|
| **停止する（Φ=0 の区画）** | −0.0010 | −0.0000009 | 0.0000 | 0 | **−0.0010** |
| **区画内を走る（Φ=0）** | −0.0010 | **−0.0040** | **0.0000** | 0 | **−0.0050** |
| 停止する（Φ=D₀ の区画） | −0.0010 | −0.0000009 | −0.0086 | 0 | −0.0096 |
| 区画内を走る（Φ=D₀） | −0.0010 | −0.0040 | −0.0086 | 0 | −0.0135 |
| 境界を越える歩（正・未訪問） | −0.0010 | −0.0040 | **+0.1791** | +0.02 | **+0.1941** |
| 境界を越える歩（逆・未訪問） | −0.0010 | −0.0040 | **−0.1800** | +0.02 | **−0.1650** |

### 参考: M1 廊下（Φ は**連続**）

| 挙動 | 時間罰 | 滑らかさの罰 | 整形 | **合計** |
|---|---|---|---|---|
| 停止する | −0.0010 | −0.0000009 | 0.0000 | **−0.0010** |
| **正しい方向へ走る** | −0.0010 | −0.0040 | **+0.0096** | **+0.0046** |

### 🔴 この 2 つの表を「符号が変わった」と読んではいけない（裁定 R7-3 の是正）

**初版は「M2-0 では走ることが −0.0050 の純損、M1 では +0.0046 の得」と書いていた。
これは非対称な比較であり、予測から撤回する。**M2-0 の −0.0050 は**区画内のステップに
限った値**、M1 の +0.0046 は**全ステップの平均**である。同じ土俵に載せると:

$$\sum_{\text{1 区画}} r_\text{shape} = \gamma\sum\Delta\Phi - (1-\gamma)\sum\Phi
= \gamma\cdot 0.18 - (1-\gamma)\sum\Phi$$

$\sum\Delta\Phi$ は 1 区画あたり 0.18 で**階段でも連続でも同じ**（telescoping）。
したがって **1 歩あたりの平均は両者でほぼ一致する**:

（下表はすべて drift 項 $-(1-\gamma)\Phi$ を除いた値。$\Phi\approx0$ のスタート付近では
drift はゼロで、両者の drift の差は 1 区画あたり 0.005 × 0.09 = 0.00045/歩 しかない。
訪問報酬も除く。）

| | 区画内のステップ | 境界のステップ | **1 区画の平均** |
|---|---|---|---|
| **M2-0（階段 Φ）** | **−0.0050** | **+0.194** | **+0.00455** |
| **M1（連続 Φ）** | **+0.00455** | （境界という特別な歩が無い） | **+0.00455** |

**1 区画の平均は厳密に一致する**（$\gamma\cdot0.18/18.75 - (p+c) = 0.0095520 - 0.0050$）。

**差は平均ではなく、時間分布である。**M1 は **100% のステップ**に +0.0096 の進捗報酬が
一様に入る。M2-0 は **5.3% のステップ**（18.75 歩に 1 回）に +0.18 が集中し、
**残り 94.7% のステップには「動くな」と言う信号（−0.0050）しか入らない。**

**本実験が検証するのはこの分布の違いである**（§4 の仮説 H）。**平均の大小ではない。**

**M1 → M2-0 での報酬の差は次の 4 点だけ**（ロボット物理・制御周期・行動空間・
電圧クランプ・観測の正規化定数は完全に同一。観測は M1 の 15 次元 ＋ ゴール相対 2 = 17）:

| | M1 | M2-0 |
|---|---|---|
| **Φ** | **連続**（真の位置を経路の折れ線へ射影した残り経路長。`corridor_gen.py:274`） | **階段**（区画インデックスの BFS 距離。`maze6_env.py:186`） |
| 訪問報酬 | なし | +0.02 / 未訪問区画 |
| エピソード上限 | 372 歩/区画（コース長に比例） | 6000 歩 固定 |
| ゴール判定 | 中心距離 0.05 m | 中央 2x2 の区画所属 |

### 実測がこの表を裏づけている

検証帯 20 面 × 1 試行・deterministic・最終モデル。**報酬の項別分解は環境が返した
reward と絶対誤差 2.2e-16 以内で一致することを全ステップで assert した。**

| モデル | 停止層の割合 | 経路効率 | **ゴールへの前進** | 到達区画 | E‖a−ā‖² | 結果 |
|---|---|---|---|---|---|---|
| exp_010 seed1（k=8.7e-3） | **99.73%** | **0.034** | **0 区画**（最大 1） | 1 | **2.1e-11** | 衝突 8 / 時間切れ 12 |
| exp_010 seed2（k=8.7e-3） | 93.77% | 0.327 | 1 区画（最大 4） | 2.5 | 6.4e-12 | 衝突 14 / 時間切れ 6 |
| exp_011 seed1（k=0） | 6.13% | **1.690** | **2 区画**（−1〜5） | 4 | **0.459** | **衝突 20 / 20** |
| exp_011 seed2（k=0） | 7.46% | **3.061** | 1 区画（−1〜4） | 3.5 | 0.453 | 衝突 19 / 時間切れ 1 |

経路効率 = 実走距離 ÷ ($D_0$ × 0.18 m)。前進 = $d_\text{start}-d_\text{end}$ [区画]。
$D_0$ の中央値は **9.5 区画**。すべて中央値。

1. **k=8.7e-3 は「動かない」を選んだ。**停止層 99.7%、最短経路長の 3.4% しか走らず、
   前進 0 区画。$\mathbb{E}\lVert a-\bar a\rVert^2$ を **2.1e-11** まで落として
   **罰そのものを消している**。「行動の変化への罰は行動することへの罰であり、
   何もしないことは無料」（引き継ぎメモ (3)-3）が、M1 の停止方策（1e-5）より
   **6 桁深く**実現している。
2. **k=0 は「動くが進まない」を選んだ。**最短経路長の 1.69〜3.06 倍を走って
   前進は 1〜2 区画 / 9.5 区画。

### 途中で否定した推定を 1 件記録する

`reward_breakdown_mean.shaping` が負（−0.00077）であることから
**「方策は始点より遠いところで終わっている（$\Phi_T<0$）」と推定したが、実測で否定された**
（前進の中央値は **+2 区画**）。正しい説明は
$\sum_t(\gamma\Phi_t-\Phi_{t-1}) = \Phi_T-(1-\gamma)\sum_t\Phi_t$ で、
**長いエピソードでは第 2 項（オフセット版 Φ の drift）が前進を食う**こと。
**「平均 shaping が負」は後退の証拠ではない。以後この量を進退の指標に使わない。**

---

## 2. 【任務 (2)】γ=0.995 の検算 — 検証帯 20 面すべてについて

計算モデルは `taskB_return_ordering.md` と同一。ポテンシャル項は telescoping するので

$$G = \gamma^{T}\Phi_T - (p+c)\,S(T) + \gamma^{T-1}\cdot(\text{終端ボーナス/罰}) + V,
\qquad S(T)=\frac{1-\gamma^{T}}{1-\gamma}$$

$p$=0.001、$c=k\,\mathbb{E}\lVert a-\bar a\rVert^2$。**$c$ は挙動ごとに違う値を使う**
（走行 3.99e-3、凍結 9.4e-7）。訪問報酬は $i$ 番目の未訪問区画を $i\cdot$18.75 歩目とし
$\gamma^{i\cdot 18.75-1}$ で割り引く（スタート区画には付けない。既存
`check_m2_return_ordering.py` は $i$ を 0 始まりにしていたため 1.098 倍大きく出ていた）。

### 2-1. $\gamma^T$ は潰れていない

検証帯 seed 7000-7019（loop）の $D_0$ は **4〜15 区画**（中央値 9.5）＝ 最短 **75〜281 歩**。
γ=0.995 の実効地平 200 歩と同程度で、$\gamma^T$ = **0.245〜0.687**。
**准教授が 16x16 で見つけた $\gamma^T$=0.00115 のような潰れは 6x6 では起きていない。
「16x16 の破綻の縮小版」ではない。**

### 2-2. 総収益の順序（$k$=8.7e-3、実際に回した条件）

| $D_0$[区] | 面数 | ゴール | 探索 | 滞留 | 衝突*（$x^*$） | 余裕 | **実際の順序** |
|---|---|---|---|---|---|---|---|
| 4 | 2 | +0.935 | −0.802 | −0.200 | −0.546（0.54 m） | 1.136 | ゴール>滞留>衝突>**探索** |
| 5 | 1 | +0.893 | −0.802 | −0.200 | −0.445（0.72） | 1.093 | ゴール>滞留>衝突>**探索** |
| 6 | 2 | +0.844 | −0.802 | −0.200 | −0.364（0.90） | 1.044 | ゴール>滞留>衝突>**探索** |
| 7 | 1 | +0.790 | −0.802 | −0.200 | −0.301（1.08） | 0.990 | ゴール>滞留>衝突>**探索** |
| 8 | 2 | +0.733 | −0.802 | −0.200 | −0.252（1.26） | 0.933 | ゴール>滞留>衝突>**探索** |
| 9 | 2 | +0.673 | −0.802 | −0.200 | −0.216（1.44） | 0.873 | ゴール>滞留>衝突>**探索** |
| 10 | 3 | +0.612 | −0.802 | −0.200 | −0.190（1.62） | 0.802 | ゴール>**衝突**>滞留>**探索** |
| 11 | 1 | +0.550 | −0.802 | −0.200 | −0.175（1.80） | 0.724 | ゴール>**衝突**>滞留>**探索** |
| 13 | 3 | +0.425 | −0.802 | −0.200 | −0.166（2.17） | 0.591 | ゴール>**衝突**>滞留>**探索** |
| 14 | 1 | +0.364 | −0.802 | −0.200 | −0.165（2.16） | 0.529 | ゴール>**衝突**>滞留>**探索** |
| 15 | 2 | +0.304 | −0.802 | −0.200 | −0.164（2.16） | 0.469 | ゴール>**衝突**>滞留>**探索** |

- **ゴールは 20 面すべてで 1 位**、余裕 0.469〜1.136。**γ^T による潰れはない**
- **「探索（6000 歩走り続ける）」が 20 面すべてで最下位。**機構は明快で、
  **滑らかさの罰は動いたときだけ掛かる**ので、走り続けると
  $(p+c)/(1-\gamma)$ = 0.999 を払うのに対し、**凍結すれば $p/(1-\gamma)$ = 0.200 で済む**。
  **走ることに 4 倍の税**がかかっている
- 滞留 (−0.200) と衝突の順位は $D_0$ で入れ替わる（$D_0\le 9$ の 10 面で滞留が上、
  $D_0\ge 10$ の 10 面で衝突が上）

### 2-3. 総収益の順序（$k$=0、対照条件）

| $D_0$[区] | ゴール | 探索 | 滞留 | 衝突* | **実際の順序** |
|---|---|---|---|---|---|
| 4 | +1.186 | −0.004 | −0.200 | −0.332 | ゴール>探索>滞留>衝突 ✅ |
| 6 | +1.188 | −0.004 | −0.200 | −0.064 | ゴール>探索>**衝突**>滞留 |
| 9 | +1.129 | −0.004 | −0.200 | **+0.207** | ゴール>**衝突**>探索>滞留 |
| 13 | +0.989 | −0.004 | −0.200 | **+0.375** | ゴール>**衝突**>探索>滞留 |
| 15 | +0.908 | −0.004 | −0.200 | **+0.408** | ゴール>**衝突**>探索>滞留 |

**$k$=0 では衝突の総収益が 20 面中 15 面で正**（最大 +0.408）。
**「進めるところまで進んで壁に当たる」は報酬が積極的に払う戦略である。**
機構: $\gamma^{T-1}$ で割り引かれた衝突罰は −0.24〜−0.69 に減るのに、
**それまでに稼いだ $\gamma^T\Phi_T$ と訪問報酬は残る**。exp_011 の衝突 20/20 と整合する。

（ただし exp_011 の実際の方策は前進 2 区画で衝突しており、最適な衝突 +0.375 にも
達していない。**最適な衝突すら見つけられていない**ので、これだけでは説明が閉じない。）

### 2-4. 【決定的】学習ログ — ゴールは 80 回到達し、しかも最高収益だった

`logs/exp_010_m2_0_seed1/episode_seeds.jsonl` ほか（独立に再集計して一致を確認）。

| | exp_010 seed1（k=8.7e-3） | exp_011 seed1（k=0） |
|---|---|---|
| 学習エピソード数 | 7036 | 3293 |
| 内訳 | 衝突 6871 / **ゴール 80** / 時間切れ 85 | 衝突 3261 / **ゴール 18** / 時間切れ 14 |
| **最後の 500 エピソードのゴール** | **0 回** | **0 回** |
| 最後にゴールした学習ステップ | 1,228,283（**全体の 61%**） | 810,791（**41%**） |
| ゴール走行の経路効率 $\rho$ 中央値 | **1.46**（0.58〜12.97） | **2.19**（1.31〜35.62） |
| **ゴール走行の総収益 中央値** | **+0.60**（−0.97〜+1.30） | **+0.62**（−0.20〜+1.02） |
| 衝突走行の総収益 中央値 | −0.73 | −0.25 |
| 検証帯ゴール率（21 評価点） | **0.05 が 1 点、他 20 点は 0.00** | **21 点すべて 0.00** |

**これが γ 容疑への最も直接的な反証である。**

- **ゴールは到達されていた**（80 回 / 18 回）。「見つけられなかった」のではない
- **その総収益は +0.62 / +0.65 で、衝突（−0.71 / −0.21）より高かった。**
  **割引はゴールの価値を奪っていない**
- ゴール走行の経路効率は 1.46 / 2.19 で、**回り道が原因でもない**
- **にもかかわらず、学習の 41〜61% 時点でゴール到達が止まり、以後 0 回。**
  exp_010 では訪問区画の平均が 3.58 → **1.58**（最後の 500）へ**後退**した
- **検証帯（deterministic）では 126 評価点中 125 点が 0.00。**
  つまり**ゴール到達は探索ノイズの下でだけ起き、決定的方策には一度も定着しなかった**

#### ⚠️ ただし「ゴール +0.62 > 衝突 −0.71」は方策の選択を決めない（裁定 R7-5 の是正）

これらは**条件つき収益**（ゴールできた走行の収益）である。方策の選択を決めるのは
**期待収益**であり、そこには「試みても 98.9% は衝突する」という**成功確率**が入る。
学習ログの全エピソードについて $G$ を計算して平均すると:

| | 「走ろうと試みる」の期待収益 | 「凍結する」の収益 | 合理的な選択 |
|---|---|---|---|
| exp_010 seed1（k=8.7e-3） | **−0.707**（ゴール 1.14% / 衝突 97.65% / 時間切れ 1.21%） | **−0.200** | **凍結**（差 0.51） |
| exp_011 seed1（k=0） | **−0.319**（ゴール 0.55% / 衝突 99.03%） | −0.200 | 凍結（差 0.12。ほぼ互角） |

**k=8.7e-3 では凍結が期待値で 0.51 も有利である。方策は合理的に振る舞っていた。**
（k=0 では差が 0.12 しかなく、頻度 × 中央値で近似すると −0.209 対 −0.200 で
**ほぼ同値**。実際に観測された衝突 20/20 はこの互角さと整合する。）

**この是正で結論は変わらず、むしろ強まる。**期待値が凍結を選ばせている原因は
**成功確率が 1.14% しかないこと**であって**割引率ではない**。γ を上げても成功確率は
上がらない（§2-5 のとおり区画内の勾配はむしろ悪化する）。
**上げるべきは成功確率であり、それを妨げているものが仮説 H の対象である。**

**H（§4）が説明しようとしているのはここである**: 1.1% の高収益エピソードでは
連続行動の方策を 200 歩ぶん動かせない。一方**残り 98.9% の全ステップに掛かる
密な信号（時間罰＋滑らかさの罰、合計 −0.0050/歩）は「動くな」と言い続ける**。
**M1 では進捗報酬が 100% のステップに入って符号を +0.0046 に変えていた。**

### 2-5. γ を上げても届かない（補助的な根拠。条件依存なので弱め）

**(a) 区画内の $dG/dx$ は γ を上げると悪化する**（$k$=8.7e-3、$x$ は区画の中央）:

| $x$[m] | γ=0.995 | γ=0.997 | γ=0.998 | γ=0.999 |
|---|---|---|---|---|
| 0.09 | +0.003 | −0.201 | −0.306 | −0.412 |
| 0.81 | −0.244 | −0.336 | −0.390 | −0.451 |

**(b) 滞留 vs 即衝突の安全余裕が消える**: 滞留 − 即衝突 は
γ=0.995 で **+0.805** → 0.997 で +0.680 → 0.998 で +0.517 → **0.999 で +0.023**。
**「何もしない」の安全余裕がほぼ無くなる**（$p/(1-\gamma)$ が衝突罰 1.0 に迫るため）。

**(c) 経路効率の許容倍率 $\rho_\text{max}$**（最短の何倍まで回り道してゴールしても
滞留・衝突より得か）は $k$=8.7e-3 で中央値 **1.98**（γ=0.995）→ **3.14**（γ=0.999）。
踏破できる区画数の予算は 19 → 30 区画。一方**方向の情報を持たない方策の目安**
（区画グラフ上の一様ランダムウォークのゴール 2x2 への期待到達歩数を 20 面それぞれ
線形方程式で解いた値）は中央値 **225 区画**（125〜598）で、**不足は 11.9 倍 → 8.9 倍**
にしか縮まらない。
- **⚠️ この (c) は $k$ に強く依存する。** $k$=0 では $\rho_\text{max}$ が小さい面で
  探索の上限（6000 歩）に張り付き、予算が要求を上回る面もある。**$k$=0 でも
  学習は失敗しているので、予算の不足は失敗の必要条件ではない。**
  (c) は「γ を上げても足りない」ことの補助的な根拠にとどめ、主たる根拠は §2-4 とする。

**→ γ は原因ではなく、律速でもなく、上げると勾配と安全余裕が悪化する。γ は変更しない。**

### 2-6. 階段 Φ と連続 Φ で $dG/dx$ の符号が変わる

区画内では階段 Φ が動かないので $\Phi_T$ の項が $dG/dx$ に寄与しない。M1 は Φ が
連続だったのでこの項が寄与していた。

| $x$[m] | 階段 $k$=0 | 階段 $k$=8.7e-3 | **連続 $k$=8.7e-3** |
|---|---|---|---|
| 0.09 | +0.401 | **+0.003** | **+0.912** |
| 0.45 | +0.184 | **−0.146** | **+0.607** |
| 0.81 | +0.029 | **−0.244** | **+0.380** |
| 1.17 | −0.078 | **−0.304** | **+0.213** |
| 1.53 | −0.149 | **−0.337** | **+0.092** |

- **$k$=8.7e-3 は最初の 1 区画を除いて全区間で負。**exp_006 の $k$=1e-2 が M1 で示した
  破綻（「できるだけ手前で衝突するのが最適」）が、**M1 で合格した $k$ で起きている**
- **連続 Φ なら全区間で正。**差は $\gamma^T\!\left(1+x\ln\gamma/(v\Delta t)\right)$ で、
  階段化で失われたのはちょうどこの量である
- **ただし $k$=0 では階段でも $x<0.9$ m で正**であり、exp_011 の失速（前進 0.36 m）は
  勾配の**符号**では説明できない。**符号ではなく密度の問題**である（§2-4）

---

## 3. 【任務 (3)】γ を変える案は出さないが、地雷 1〜5 の再検算は実施した

| 地雷 | γ を上げるとどうなるか |
|---|---|
| 1（Φ=−d で滞留が 2 位） | **軽くなる**（(1−γ)·D₀ が 0.0081 → 0.0016）。現行はオフセット版なので元々踏んでいない |
| 2（オフセットのみで衝突がゴールより得） | 衝突罰 −1.0 がある限り γ を変えても ゴール > 衝突 は保たれる（差 0.403 → 0.914 で**むしろ広がる**）。**ただし滞留 vs 即衝突の余裕が 0.805 → 0.023 へ消える**（§2-5(b)） |
| 3（時間罰↑と Φ オフセットの等価性） | 等価性は γ に依らず成立。**実効的な罰の大きさが変わる**（下記） |
| 4（終端 Φ=0 の強制） | **γ に依らず禁止のまま。**オフセット版では最後の遷移で $F=\gamma\cdot 0-\Phi(s_{T-1})<0$、$\Phi=-d$ 版では $>0$。いずれも実装しない |
| 5（理論的にクリーンでない） | 構造は変わらないが相殺のバランスが動く。地雷 2 の余裕消失がその実体 |

### 地雷 3 の再検算で見つかった副産物（第二容疑者として登録する）

Φ = D₀ − d のオフセット版は、**ゴールに近づくほど毎歩 $(1-\gamma)\Phi$ の追加罰**になる。

| γ | (1−γ)·D₀ [/歩] | 進捗報酬 γ·ΔΦ [/歩] | **ゴール直前の純整形** |
|---|---|---|---|
| **0.995** | 0.0081 | 0.0096 | **0.0015（名目の 15%）** |
| 0.998 | 0.0032 | 0.0096 | 0.0063（66%） |

**γ=0.995 では、ゴール直前の 1 歩あたり進捗報酬が名目の 15% まで痩せる。**
Φ を連続化しても残る問題であり、**γ を上げれば軽くなる唯一の箇所**である。
ただし階段 Φ では進捗報酬が**そもそも 0** なので**二次の問題**。§7-2 に紐づける。

---

## 4. 【任務 (4)】機構仮説 — 1 つに絞る

> ### H（裁定 R7-4 に従い書き換え）: 検証対象は**報酬の 1 歩あたり平均ではなく、進捗報酬の時間分布**である。
>
> **1 区画あたりの整形報酬の総和は階段 Φ でも連続 Φ でも代数的に等しい（γ·0.18）**。違うのは分布だけで、M1（連続）は **100% のステップ**に +0.0096 が一様に入り、M2-0（階段）は **5.3% のステップ**に +0.18 が集中して**残り 94.7% には −0.0050（「動くな」）しか入らない**。
>
> **H: この時間分布の違いが、学習の力学（価値関数の関数近似・GAE(λ) による優位度の切り詰め・探索）を通じて、方策が「壁を避けて前進する」制御を獲得できるかを左右する。**平均が同じでも、突起状の報酬は (i) 価値関数が区画内で平坦になり優位度が雑音に埋もれ、(ii) GAE(λ=0.95) の実効窓 20 歩が 1 区画 18.75 歩とほぼ同じで突起の伝播が 1 区画ぶんに限られ、(iii) 区画内のどのステップも報酬が同じなので走り方の良否が区別されない。結果として成功確率が 1.14% に留まり、期待値では凍結（−0.200 対 −0.707）が合理的になる。

**⚠️ この H は報酬の地形（終点の順序・平均）についての主張ではなく、最適化の力学に
ついての主張である。**初版の §7-7 は「方策の分布・価値関数の推定誤差・PPO のクリップ・
GAE の切り詰めは含まない」と**免責していたが、裁定 R7-4 のとおりその当のものが主張の
本体になった。**したがって**机上計算だけでは H を支持も反証もできず、学習を回すことが
唯一の判別手段である**（初版はこの点を曖昧にしていた）。

**H が説明すること**:

| 観測 | H による説明 |
|---|---|
| k=8.7e-3 が停止層 99.7%・経路効率 0.034・前進 0 区画 | **区画内に限れば**走ると −0.0050/歩、停止なら −0.0010/歩。**区画内では動かない方が 5 倍得**（1 区画平均では +0.0046 なので、これは分布の話であって平均の話ではない）。期待値では 凍結 −0.200 対 挑戦 −0.707 |
| その方策の E‖a−ā‖² が 2.1e-11 | 凍結すれば罰そのものが消える。**罰から逃げる最適解が「動かない」** |
| k=0 が最短の 1.69〜3.06 倍走って前進 1〜2 区画 | 罰が無いと走行と停止が等価（どちらも −0.0010/歩）。**区画内はどう走っても報酬が同じ**なので、壁を避ける制御が精錬されない |
| ゴール到達が 80 回起きたのに定着しない | 高収益（+0.62）だが**全エピソードの 1.1%**。期待値では凍結が 0.51 有利になる |
| 訪問区画が 3.58 → 1.58 へ**後退**した | 密な負の信号が、疎な正の信号に長期的に勝つ |
| 検証帯 126 点中 125 点で 0.00 | 探索ノイズの下でだけゴールし、決定的方策には定着しない |
| M1 が同じ $k$・同じ γ で成功した | M1 は進捗報酬が **100% のステップ**に入り、密な信号の符号が **+0.0046** だった |

**H が説明しないこと（正直に記録する）**:

- **k=0 が最短の 1.7〜3.1 倍も走った理由。**走行と停止が等価なら、どちらでもよいはず。
  衝突が正の報酬（+0.21〜+0.41、§2-3）で説明はつくが、**その場合は最適な衝突
  （前進 12 区画）に近づくはずで、実測の前進 2 区画と合わない。未解明。**
- **M1 が成功し M2-0 が失敗した対比は、Φ の形以外の差でも説明できてしまう**
  （分岐の有無、観測 +2 次元、エピソード上限、訪問報酬）。**本実験が意味を持つのは
  Φ の形だけを変えて他を固定するからである。**

### 変更する 1 点: Φ を連続化する

**M1 が実際にやっていたこと**（`mouse/corridor_gen.py:274` の `remaining_path_length()`。
真の位置をセル中心を結んだ折れ線へ射影した連続量）を迷路へ持ってくる。**新しい仕掛けは無い。**

#### 🔴 旧案（2026-08-12 の開口部中点方式）は実測で破綻した

旧案は「折れ線 [$w_\text{in}$, center, $w_\text{out}$] へ真の位置を射影し、
`corridor_gen.remaining_path_length()` をそのまま再利用する」だった。
2026-08-13 の単体テストで**独立した 2 つの欠陥**が実測された。

**D1（区画内 18 件・最大 0.07212 m）— 折れ区画の内側・角の二等分線上に真の不連続がある。**
`remaining_path_length()` は**垂線距離が最小の線分**を選ぶ。折れ線が曲がる区画で機体が
内側を斜めに横切ると、角の二等分線をまたいだ瞬間に選ばれる線分が切り替わり、弧長が飛ぶ。

対立仮説（「勾配が急なだけ」なら刻みを細かくすれば $\lvert\Delta\Phi\rvert$ は比例して減る）
で判別した。面7004・区画 (5,5)・変位 22.2 mm の 1 歩を細分すると:

| 刻み | 1 刻みの長さ | 最大 $\lvert\Delta\Phi\rvert$ [m] |
|---|---|---|
| 1 分割 | 22.2334 mm | 0.068195 |
| 10 分割 | 2.2233 mm | 0.060942 |
| 100 分割 | 0.2223 mm | 0.059368 |
| 1000 分割 | 0.0222 mm | 0.059168 |
| 10000 分割 | 0.0022 mm | **0.059150 ← 減らない ＝ 真の不連続** |

跳びの大きさは **$\sqrt2\cdot\rho$**（$\rho$ = 区画中心から内側二等分線に沿った距離）で、
実測と **1.4e-9 の精度で一致**（$\rho$ = 0.01〜0.08 m の 7 点）。機体中心が**壁面**から
$w_\text{lat}$ = 0.0400 m 以上（＝**壁中心線**からは 0.0460 m 以上）離れる範囲
（＝実際に通れる位置）での**最大跳びは 0.12414 m ＝ 区画寸法の 0.69 倍**。
**速度に依存しない。**検証帯 20 面で曲がる構成は 1379 通り／曲がる区画 450 個。

**独立の盲検再計算が裏づけた**（結論を伏せて実装だけを渡した）: 刻み 30/120/480/1920 に対し
最大比 $\lvert\Delta\Phi\rvert/\lvert\Delta p\rvert$ ≈ 30/120/480/1919 と**刻み数に比例して発散**
（跳び約 0.18 m）。不連続は**区画内部を通る 1 次元の曲線**に沿って存在し折れ型に特有。
**直線型の区画では 3 水準とも比 1.0 ちょうど**。1468 構成の内訳は折れ 35.6% / 直線 20.8% /
後戻り 43.6%。

**D2（前進 8 件・最大 0.09099 m）— tie-break が選ばなかった降下開口部を通ると跳ぶ。**
降下隣接が同点で複数あるとき辞書順で 1 つを選ぶが、**機体が選ばれなかった方の開口部から
出て前進すると** Φ が別経路の残り距離へ切り替わる。

```
面7002 step35: 区画 (2,0)→(3,0)、d は 11→10 ＝ 前進
  (2,0) の降下隣接は (3,0) と (2,1) の 2 つ（同点）→ tie-break は (2,1) を選ぶ
  機体は (3,0) 側から出た → Φ が 0.36000 → 0.45099 へ +0.09099 跳ぶ
```

- **非ゴール区画 640 個中、降下隣接が 2 つ以上ある区画は 99 個（15.5%）**
- 選ばれなかった降下開口部を通過した瞬間の**最大跳びは 0.18000 m（＝ 1 区画）**
- **旧 (b-2) では検出できない。**旧 (b-2) は tie-break が選んだ $n$ との境界だけを見るので
  4.44e-16 m で PASS する。§7-6 は tie-break の跳びを**「後戻り＝方向反転」の話として
  登録していたが、前進でも起きる。**

**なお「線分ごとの残り弧長の min を採る」案（学生B が一度提案し裁定 R13 で承認された案 1）は
撤回した。**min は常に最も進んだ射影を採るので、**区画への進入点で $\ell$ が全長 0.18 m に
ならず 0.09 m になり、全区画で境界一致が 0.09 m 壊れる**（曲がる区画・直線区画のいずれでも
同じ。実測で確認）。

#### 🔴 $\lvert\nabla\Phi\rvert\le1$ は達成できない（下限は $\sqrt2$）

旧 (b) の閾値は「Φ は距離場なので $\lvert\nabla\Phi\rvert\le1$」を前提にしていたが、
**この前提は境界一致だけから否定される。**

境界一致より、区画へ入る点 $w_\text{in}$ と出る点 $w_\text{out}$ での Φ の差は
**厳密に $cs$ = 0.18 m** でなければならない。ところが曲がる区画では両点は隣り合う壁の
中点なので、**直線距離は $cs/\sqrt2$ = 0.127279 m しかない**。したがって

$$L \;\ge\; \frac{cs}{cs/\sqrt2} \;=\; \sqrt2 \;=\; 1.414214$$

**(a) を外しても回避できない**（境界一致だけから出る）。**旧閾値の係数 1.25 は、
正しい実装でも破られる値だった。**

#### 新しい定義（本改訂で採用する）

最近接点射影をやめ、**区画ごとの明示式**にする。$h = cs/2$、$C$ = 区画中心、
$a$・$b$ = $C$ から $w_\text{in}$・$w_\text{out}$ への単位ベクトル、
$s = (P-C)\cdot a$、$t = (P-C)\cdot b$ として、区画内の残り弧長 $\ell$ を

$$\ell(P)=\begin{cases}
\mathrm{clamp}(h-t,\;0,\;2h) & \text{直線}\;(a=-b)\\[2pt]
\mathrm{clamp}(s,\;0,\;h)\;+\;\bigl(h-\mathrm{clamp}(t,\,-h,\,h)\bigr) & \text{折れ}\;(a\perp b)\\[2pt]
\mathrm{clamp}(h-t,\;0,\;h) & w_\text{in}=w_\text{out}\;\text{（後戻り・reset 直後）}
\end{cases}$$

と定義し、**残り距離を全降下隣接についての最小値**で決める（tie-break を廃止する）:

$$\text{残り距離}(P)=\min_{n\,\in\,\text{降下隣接}}\Bigl[\;\ell_n(P)\;+\;d(n)\cdot cs\;+\;cs/2\;\Bigr],
\qquad \Phi = d_\text{start}\cdot cs-\text{残り距離}$$

$d(n)$ は全降下隣接で等しい（$=d(\text{cell})-1$）ので、min は $\ell_n$ に対してのみ効く。
$d(\text{cell})=0$（ゴール区画）のとき残り距離 = 0 は従来どおり。

**Lipschitz 定数は厳密に $\sqrt2$**: 折れの内側（両方の clamp が効かない領域）で
$\nabla\ell = a-b$、$a\perp b$ かつ単位なので $\lvert a-b\rvert=\sqrt2$。
$\sqrt2$-Lipschitz 関数の min は $\sqrt2$-Lipschitz なので、min を採っても定数は増えない。
**したがって上の下限 $\sqrt2$ に到達しており、この定義はこの制約の下で最良である。**

**D1・D2 はどちらも構成上消える**（明示式なので線分の選択が無い／全降下隣接の min なので
tie-break が無い）。**M1 と共有の `remaining_path_length()` は変更しない**（裁定 R13 条件 5。
M2 側に別実装を置く）。

#### 境界一致と C2 恒等式（階段版との比較の土俵の維持）

- **区画中心**: どの $n$ でも $\ell_n(C)=h$ なので 残り距離 $=h+d(n)\cdot cs+h=d(\text{cell})\cdot cs$
  → **階段版と一致**（テスト (a)）
- **降下開口部 $w(\text{cell},n_k)$**: cell 側では $\ell_{n_k}=0$ かつ他の $n_j$ は $\ell\ge0$ なので
  min $=d(n_k)\cdot cs+cs/2$。$n_k$ 側では進入点なので全ての $\ell$ が $2h=cs$ となり
  $cs+(d(n_k)-1)\cdot cs+cs/2=d(n_k)\cdot cs+cs/2$ → **一致**（テスト (b-2)）
- **C2 恒等式**: 境界での Φ の値が階段版と一致するので、**1 区画進むごとに Φ は正確に
  $cs$ = 0.18 増える**。よって**1 区画あたりの整形報酬の総和は階段版と代数的に等しい
  （$\gamma\cdot0.18$）。**§5-1 の「1 区画あたり整形の平均は +0.00455 で厳密に一致」は
  本改訂後も成立する。**変わるのは分布だけ**という H の主張の土俵は保たれる。

#### 試作による検証（裁定 R18-1: 事前登録前のスクラッチ試作は机上検算の実行可能な形態）

`scratchpad/proto_phi.py` で上の定義を試作し、検証帯 20 面で 4 項目を確認した
（**リポジトリには入れていない。実装は本改訂のコミット後に行う**）。

| 検査 | 結果 |
|---|---|
| (a) 区画中心で階段版と一致 | 2108 通り、**最大差 5.55e-16 m** |
| **全**降下開口部での境界一致 | **2402 通り**（tie-break 分 2032 → 全開口部 2402）、**最大差 4.44e-16 m** |
| 格子走査の最大 $\lvert\Delta\Phi\rvert/\lvert\Delta p\rvert$ | 刻み 6.0 / 1.5 / 0.375 mm で **1.414214 一定**＝**不連続なし・理論下限に到達** |
| (c) 横ずれ ±20 mm に不感 | 5412 通り、**最大差 0.000e+00 m** |

**⚠️ 試作の過程で学生B が犯した検証の誤り 2 件を記録する**（同型の再発防止）:
1. **Lipschitz の格子走査を軸方向だけで行い、最大比を 1.000000 と読み違えた。**
   折れ区画では**斜め方向**で最大になる。斜めを入れて $\sqrt2$ に修正
2. **(c) の絞り込みが漏れていた。**$c_{-1}$=None のとき折れ判定が素通りし、降下隣接が
   2 つある区画が混入して「最大差 20 mm」と出た。条件を締めて 0.000e+00

> ### ⚠️ 本節の主張の適用範囲（節の途中で読むのをやめる読者への前方参照）
>
> **本節の主張（Lipschitz 定数 $\sqrt2$・D1/D2 の消滅）は、区画の内部および
> 開口部の「中点」での境界一致に限られる。**大域的には**中点以外の辺上で
> 両側の Φ が食い違う**（最大 $\mu_\max$ = 0.0440 m）。
> **「条件 E は大域的に $\sqrt2$-Lipschitz」と読んではいけない。**
> 後段の「実装して全テストを回した結果」と §7-0 を必ず読むこと。

**理論上の弱まり**: $\Phi$ が $c_{-1}$ に依存することは**明示式でも変わらない**
（折れの $\ell$ が $s$ を通じて $w_\text{in}$ に依存するため）。§7-8 の限界は**維持する**
（裁定 R18-4）。

#### 🔴 実装して全テストを回した結果（2026-08-13）: 境界一致は**開口部の中点でしか成立しない**

D1・D2 は消えたが、**(b) が「前進」23 件で落ちた**（比 2.23〜2.39）。原因は上の定義とは
別で、**境界一致を開口部の中点でしか課していなかった**ことである。共有辺の上を横に
ずらすと、両側の Φ の差はずれ $\lambda$ に比例して増える:

| 中点からのずれ $\lambda$ [m] | Φ（$c_0$ 側） | Φ（$n$ 側） | 差 [m] |
|---|---|---|---|
| −0.050 | 0.81000 | 0.86000 | 0.05000 |
| 0.000 | 0.81000 | 0.81000 | **0.00000 ← 中点だけ一致** |
| +0.050 | 0.81000 | 0.76000 | 0.05000 |

**機体は中点を通らないので、前進のたびに最大 0.05 m の跳びが入る。**

**あわせて $\sqrt2$ という下限も誤りだった。**中点どうしの距離 0.127279 m で割って
$\sqrt2$ を出していたが、**辺全体での一致**を課すと、曲がる区画では
進入辺（$\ell=2h$ が必要）と退出辺（$\ell=0$ が必要）は**直交する 2 辺**であり、
**機体中心が通れる範囲どうしの最短距離は 0.064347 m しかない**
（各辺の中点から $\pm\mu_\max$。$\mu_\max$ の定義は下記）。
その間で $\ell$ は $2h=0.18$ m 変化しなければならないので

$$L \;\ge\; \frac{s}{\sqrt2\,(t_w/2+w_\text{lat})} \;=\; \mathbf{2.7669}$$

（導出は下の「#### 条件 E: (b) の判定定数」に無次元形で書く。**初出時に 3.2223 と
書いたのは壁厚の半分 $t_w/2$ = 6 mm を落とした誤りで、2026-08-13 に是正した。
是正は定数を小さくする＝検査を厳しくする向きである。**）

**要求 3 つ（(a) 中心 $=d\cdot cs$／辺全体での境界一致／小さい Lipschitz 定数）は
同時には満たせない。**裁定 R21 により、**二者択一をやめて 2 条件を並立させ比較する。**

---

### 条件 E と条件 C（裁定 R21。**両方を実施して比較する**）

| | **条件 E** | **条件 C** |
|---|---|---|
| Φ の定義 | 上の**区画ごとの明示式 ＋ 全降下隣接の min** | **自由空間の測地距離場**（reset 時に前計算） |
| (a) 中心 $=d\cdot cs$ | **要求する**（exp_010 との錨。R13 条件 2） | **要求しない**（裁定 R21。**錨の差そのものが比較軸**） |
| 判定形（(b)） | $\sqrt2\kappa\lvert\Delta p\rvert+\mu_\max$（$\mu_\max$=0.0440） | $\sqrt2\kappa\lvert\Delta p\rvert+$ 方式由来の項（**同形**） |
| $c_{-1}$ 依存 | **残る**（§7-8 維持） | **無い**（位置のみの関数） |
| 境界一致 | **開口部の中点のみ** | **全域で連続**（距離場そのもの） |

**この 2 条件の差は「Φ の連続化」の実現方法の差であり、介入の追加ではない**
（裁定 R13 条件 7 の趣旨を維持する）。$k$・$\gamma$・並列数・総ステップ・学習 seed は
**両条件で完全に同一**にする。

#### 条件 E: (b) の判定定数を導出値へ改める（裁定 R21-E）

$$\boxed{\;\lvert\Delta\Phi_t\rvert \;\le\; \sqrt2\cdot\kappa\cdot\lvert\Delta p_t\rvert
\;+\; \mu_\max,\qquad \kappa=1.10,\quad \mu_\max=\frac{s-t_w}{2}-w_\text{lat}=0.0440\ \text{m}\;}$$

##### 🔴 判定形の是正の経緯（時系列。**遡及合格ではないことの記録**。裁定 R31-1）

| 時点 | 判定形 | 実測 | 結果 |
|---|---|---|---|
| 〜2026-08-13 | $L_E\cdot\kappa\cdot\lvert\Delta p\rvert+\varepsilon$（**乗法のみ**）、$L_E$=2.7669・$\varepsilon$=1e-9 | 最大比 **4.385**（面7005・急停止・step57・$\lvert\Delta\Phi\rvert$=0.04084・$\lvert\Delta p\rvert$=0.00931） | **🔴 不合格**（許容 $L_E\kappa$=3.0436 を超過。6 件） |
| 2026-08-13（是正後） | $\sqrt2\cdot\kappa\cdot\lvert\Delta p\rvert+\mu_\max$（**乗法＋加法**） | 最大超過 **0.02635 m** | ✅ 合格（$\mu_\max$=0.0440 m 以内） |

**旧形では不合格だったという事実を消さない。**なお学生B は当初「実測最大比は 2.392」と
報告していたが、これは**失敗一覧（比で並んでいない）の先頭行を目視で読んだ誤り**であり、
真の最大は 4.385 である（全 10255 歩の走査で確認）。**「合否不変」という前提は
この時点で崩れていた。**

**是正が後出しの緩和でない理由**: §7-0 に**登録済み**の性質
「中点一致の破れ ≤ $\mu_\max$（**速度に依存しない加法的な跳び**）」を持つ設計に対し、
**乗法のみの閾値は $\lvert\Delta p\rvert\to0$ で比が必ず発散するので両立しない**。
つまり落ちたのは実装ではなく、**承認済みの設計に対して達成不能な判定形**だった。
これは $\lvert\nabla\Phi\rvert\le1$ が達成不能だった件と同じ範疇である。

**🔴 是正の順序**: **条件 E の正しい形を求めた結果として、条件 C と同形になった。**
D3-8（E と C の合格線は同形）を満たすために形を合わせたのではない。

**🔴 判定形を「乗法項のみ」から「乗法項 ＋ 加法項」へ是正した（裁定 R30）。**
中点一致の破れは**速度に依存しない加法的な跳び**なので、乗法項だけの形
（$L_E\cdot\kappa\cdot\lvert\Delta p\rvert$）では**低速時（$\lvert\Delta p\rvert\to0$）に
比が発散して原理的に通らない**。実測でも急停止台本の低速域で比 **4.385**
（$\lvert\Delta\Phi\rvert$=0.04084・$\lvert\Delta p\rvert$=0.00931）が出て落ちた。

- $\sqrt2$ = 区画内での Φ の Lipschitz 定数（導出値。折れの内側で $\nabla\ell=a-b$）
- **$\kappa$ = 1.10 は条件 C にもそのまま使う（裁定 R30 補足・D3-10）。**$\kappa$ の根拠は
  「**離散時刻で曲線軌道を弦で測る**ぶんの余裕」（1 歩の弧長／弦長 $(\theta/2)/\sin(\theta/2)$、
  物理的な最大 $\theta_\max=\omega_\max\Delta t$=1.136 rad で 1.0553、これに 4% の余裕）で
  あり、**Φ の作り方ではなく機体の運動と制御周期だけで決まる**。したがって条件 C
  （測地距離場）でも同じ根拠がそのまま成立する。**値だけ揃えて根拠を揃えない**のは
  D3-8 の裏返しになるので、ここに明記しておく
- $\mu_\max$ = 中点一致の破れの上界（導出値。§7-0 と同じ量）
- **実測の最大超過 $\lvert\Delta\Phi\rvert-\sqrt2\kappa\lvert\Delta p\rvert$ = 0.02635 m
  $\le\mu_\max$ = 0.0440 m** ＝ 合格
- **検出力は保たれる**: 階段版は跳び 0.18 m に対し閾値が最大でも
  $\sqrt2\cdot1.10\cdot0.0223+0.0440$ = 0.0787 m なので、**246 件すべてを検出**する

**🔴 $L_E$ = 2.7669 は判定に使わない**（記録として残す）。これは「**辺全体**での一致を
課したときに必要な Lipschitz 定数の下限」であり、本設計は辺全体一致を課さない
（§4 の交換条件）。**乗法項として判定に使おうとしたのが誤りだった。**

**🔴 この判定形は条件 C と同形である**（裁定 R30・D3-8）。E も C も
「**方式から導出した上界** ＋ **刻みに対する収束/不変性**」で判定し、
値だけが方式ごとに違う。**E に加法項を認めて C に認めない**という非対称を作ると、
E 対 C の差が「Φ の性質の差」なのか「基準の差」なのか分離できなくなる。

##### $L_E$ の導出（裁定 R24-3。**最適化問題の形・無次元形・到達する 2 点対を明示する**）

記号（生の数値を式に残さない）: $s$ = 区画寸法、$t_w$ = 壁厚（境界面を中心に厚み）、
$w_r$ = 機体最外半径（車輪外面）、$h=s/2$。

**機体中心が開口部の中点から横に外れられる量**は

$$\mu_\max=\frac{s-t_w}{2}-w_r
\qquad(\text{実装値: }\tfrac{0.18-0.012}{2}-0.0400=0.0440\ \text{m})$$

**🔴 相互検算のつもりだったものが独立でなかった件（記録）**: 本節は当初
「$\mu_\max$=0.0445 m は引き継ぎメモが独立に導いた**真の接触水準 44.5 mm** と一致する」
と書いていた。**この一致は独立の確認ではなかった。**両者とも
**同じ誤った入力（機体最外半径 = 車輪外面 0.0395 m）を使っていた**からである。
真の最外側 $w_\text{lat}$=0.0400 m で計算し直すと $\mu_\max$=0.0440 m ＝ 接触水準 44.0 mm
となり、両方が同じだけ動く。**共通の入力を持つ 2 つの計算の一致は、その入力の検証にならない。**

**$L_E$ が解く最適化問題**: 曲がる区画では、辺全体での一致を課すと
$\ell=2h$（進入辺の全域）・$\ell=0$（退出辺の全域）が要求される。よって Lipschitz 定数は

$$L_E=\frac{2h}{\displaystyle\min_{P\in E_\text{in},\,Q\in E_\text{out}}\lvert P-Q\rvert},
\qquad
\begin{aligned}
E_\text{in}&=\{\,C+h\,a+\mu\,b : \lvert\mu\rvert\le\mu_\max\,\}\\
E_\text{out}&=\{\,C+h\,b+\lambda\,a : \lvert\lambda\rvert\le\mu_\max\,\}
\end{aligned}$$

を下回れない。$\lvert P-Q\rvert^2=(h-\lambda)^2+(h-\mu)^2$ なので最小は
$\mu=\lambda=+\mu_\max$（**2 辺が共有する内側の角に、両方が到達できる限り寄った点対**）
で取られ、

$$\min\lvert P-Q\rvert=\sqrt2\,(h-\mu_\max)=\sqrt2\left(\frac{t_w}{2}+w_r\right)
\qquad(=0.064347\ \text{m})$$

$$\boxed{\;L_E=\frac{s}{\sqrt2\,\bigl(t_w/2+w_\text{lat}\bigr)}=\frac{1}{\sqrt2\,\hat w},
\qquad \hat w:=\frac{t_w/2+w_\text{lat}}{s}=0.25556\;}\;\Rightarrow\;L_E=2.7669$$

##### $w_\text{lat}$（機体の真の最外側半幅）の導出（裁定 R25-1・R25-2）

**AABB（軸並行境界箱）は使わない。**AABB は姿勢によって膨らむので幅を過大評価し、
**正しい実装を誤って落とす**。ここで要るのは「機体中心が実際に到達しうる集合」を
決める真の外形なので、mesh は全頂点、primitive はサポート関数で厳密に測る。

| 候補 | 値 [m] | 出所・性質 |
|---|---|---|
| 車輪外面 | 0.03950 | **M1 時代の記載。誤り**（最外側ではない）。`mouse/clearance.py` と `check_seed_robustness.py` にも波及していた（2026-08-13 是正） |
| AABB 規約（評価器と同じ） | 0.04141 | 准教授の測定。**保守側に膨らむ** |
| **$w_\text{lat}$（真値・本測定）** | **0.04000** | **コーナー片**（`mein_body2..5`、mesh `coner`）。**車輪ではない** |

**測定手続きの検証**: 同じ手続きで $+x$ を測ると **0.05000 m** となり、既に検証済みの
導出（geom pos 0.014 ＋ メッシュ頂点 0.036）と一致する。**AABB の 0.05051 は 0.51 mm
膨らんでいる**（准教授の +x 独立測定と整合）。**したがって $\pm y$ の 0.04000 も信頼できる。**

**🔴 用途の非対称（裁定 R25-3。混同すると逆向きの誤りになる）**:

| 用途 | 使う幅 | 理由 |
|---|---|---|
| 評価器の footprint（進入判定） | **AABB でよい**（0.04141） | **保守側**が許される。広めに見て「入らない」と判定するのは安全側 |
| **(b) の判定上限 $L_E$** | **真値 $w_\text{lat}$**（0.04000） | 「実際に到達しうる集合」上の最大勾配なので、**幅を過大評価すると $\mu_\max$ が小さくなり $L_E$ が大きくなって検査が甘くなる**／過小評価すると正しい実装を落とす |

**テストでは $w_\text{lat}$ をモデルから実行時に導出する**（`robot_lateral_half_width()`）。
0.0395 のハードコードは置き換えた。**機体の形状を変えれば定数が自動で追従する。**

- **到達する 2 点対**: 進入辺・退出辺のそれぞれで、**共有する内側の角へ最も寄れる点**
  （中点から $+\mu_\max$）。閉形式の候補と照合するときはこの点対で確かめること
- **🔴 是正を 2 度受けた定数である**。初出 3.2223（$t_w/2$ = 6 mm の脱落）→ 2.7972
  （$w_r$=0.0395 を使用）→ **2.7669**（真の最外側 $w_\text{lat}$=0.04000 を使用）。
  **いずれの是正も定数を小さくする＝検査を厳しくする向き**であり、条件 E の実測最大比
  2.392 はなお通る（許容 $L_E\kappa$ = **3.0436**）。階段版の比 8.08 以上も引き続き検出できる

##### 🔴 $L_E$ の役割（裁定 R25 追記・准教授 N-1c-5）

- **$L_E$ は「条件 E が満たすべき必要条件」ではない。**条件 E は**中点一致しか課さない**ので、
  辺全体一致から出る下限に縛られない。**実測 2.392 < $L_E$ はそれで説明がつく**
- **$L_E$ の役割は「階段版（比 ≥ 8.08）と正しい条件 E（実測 2.392）を分離する天井」である**
- **「E は $L_E$ を守っている」と読んではいけない。**E が守っているのは中点一致だけであり、
  $L_E$ は不合格を出すための天井にすぎない
- **准教授による独立再導出を監査項目とする**（裁定 R21-E-②・R24-3）

##### 🔴 中点一致に留めたことの交換条件（裁定 R24-3-3。**条文の穴を塞ぐ**）

**辺全体での一致を課すと下限が $L_E$ = 2.7669 まで跳ね上がる**（$\sqrt2$ の 2 倍近い）。
本設計は**開口部の中点でのみ一致させることを選び、辺全体での一致は課さない**。
**したがって、辺上の中点以外では両側の Φ は一致しない**（ずれ $\lambda$ に比例し、
機体が通れる範囲での最大は $\mu_\max$ = 0.0440 m ぶん）。
**「中点で一致する」とだけ書くのは不十分であり、この交換条件を必ず併記する。**
- **$\kappa$ は据え置き**（1.10。導出は §4 の旧記載どおり弧長／弦長 1.0553 に 4% の余裕）
- **🔴 遡及合格は無い**（裁定 R21-E-③）: 旧実装の D1 は**刻みを細かくすると上限なく発散する**
  ので**どんな有限の定数でも通らない**。D2 は跳び 0.18 m で比 8 以上。
  **階段版の比は実測 8.08 以上**（遷移の最大 $\lvert\Delta\Phi\rvert$ = 0.18000 m ÷
  $\lvert\Delta p\rvert$ の最大 0.02227 m）なので、**$L_E$=2.7669 でも (b) は階段版を検出できる**
- **🔴 真の不連続の検査は (L) が担う**（裁定 R21-E-⑤）。(L) は刻み不変性を見るので
  **定数の大きさに依存せず**、D1 型（発散するもの）を確実に捕らえる。
  **(b) の定数を上げても D1 型の検出力は落ちない**

#### 条件 C: 自由空間の測地距離場（裁定 R21-C）

reset 時に、**迷路の自由空間上でゴール 2×2 までの測地距離場**を前計算し、
$\Phi(P) = g(\text{start}) - g(P)$ とする（$g$ = 測地距離）。$\Phi_0=0$ は構成上成立する。

- 格子解像度 $h_g$ と近傍数は実装時に決め、**実測した Lipschitz 定数（(L) の最大比）を
  design.md へ追記する**。格子 Dijkstra は経路長を上に外すので $L_C = 1+\delta$
  （$\delta$ = 離散化誤差）になる。**$\delta$ は測って報告し、閾値はその実測から決める**
  （代表点ではなく全数走査で。§8 の自己チェック）
- **(a) は要求しない。**代わりに**区画中心の値と $d\cdot cs$ の差の分布を実測して記録する**
  （裁定 R21-C）。**この差そのものが「錨の違いが結果にどう効くか」の解釈材料になる**
- $c_{-1}$ に依存しないので**§7-8 の限界は条件 C では発火しない**。
  **報酬ポンプ検査も単純化される**（位置のみの関数なら閉路の整形総和は恒等的に 0）
- **🔴 場は「点の測地」ではなく「配置空間の測地」である**（裁定 R32。点の測地は
  実機の中心が辿れない経路の距離を返す代理量なので棄却した）。**収縮の基準点を必ず併記する**
  （裁定 2026-08-13-R33。准教授 AUDIT_011 §5-ter の推奨を両基準併記の形で採用）:
  > **禁止帯 = 壁中心線から $\pm$0.0460 m**（= 壁半厚 $t_w/2$ = 0.006 ＋ 機体最外側半幅
  > $w_\text{lat}$ = 0.0400）。**壁面からは $w_\text{lat}$ = 0.0400 m。**

  **同じ自由空間を 2 通りの基準点で書いたものであり、両者は同一の帯を定める。**
  実装のマスク（`mouse/maze6_env.py` `_geo_allowed_mask()`）は**壁を厚みゼロの線
  （区画境界線 = 壁中心線）として扱う**ので、判定に使うのは**中心線基準の 0.0460 m**
  （`_GEO_CLEARANCE`）であり、**壁半厚はここで一度だけ数えられている**（二重計上ではない）。
  独立の検算: $\mu_\max = h - 0.0460 = 0.09-0.0460 = 0.0440$ m が
  $(s-t_w)/2-w_\text{lat}$ = 0.0440 m と一致する。
  ⚠️ **基準点を書かずに数値だけを引き写すと、0.006 の出入りが「二重計上」に見える**
  （R33 の突き合わせで実際に起きた。visited_cells と同型の「基準点の混在」）。
  **0.0455 は誤り**（旧 $w_r$=0.0395 の伝播。裁定 R26 で否定済み）

##### 🔴 マスクの是正（裁定 2026-08-13-R34。**中心線ベースから障害物表面ベースへ**）

**准教授の指摘（AUDIT_011 続報）**: 上の中心線ベースのマスクは**配置空間になっていない**。
(i) **柱を一切描いていない**、(ii) **壁の端を丸めていない**。結果、許可点の **9.17%** が
物理モデルの壁・柱から $w_\text{lat}$=0.0400 未満の位置にある（最悪は柱の角で
**0.00 mm** ＝ 機体中心を置くと 40 mm 食い込む）。

**是正後の定義**:

> **障害物** = 物理モデルに**実際に生成される**壁（厚みあり）＋柱（`post_3_3` を含む現状のまま）。
> **許可** = **障害物表面からの距離 $\ge w_\text{lat}$ = 0.0400 m**（機体を半径 $w_\text{lat}$ の
> **円板**とみなす）。柱・壁端の丸めは表面距離で測れば自動的に従う。

- **障害物集合はモデル本体（生成される形状）から導出する**（独立の再記述から作らない。
  R34 の明示条件。実装は `mouse/mjcf.py` が生成した MuJoCo モデルの geom を読む）
- **R33 の両基準併記は生きている**が、**表面距離ベースでは「壁面から 0.0400 m」が
  一次の書き方**になる。**中心線基準の 0.0460 m は壁の直線部でのみ等価**であり、
  **壁の端・柱では等価にならない**（そこでは表面までの最短距離が斜めに測られるため）
- **機体モデルの限界（明記の義務。R34）**: 半径 $w_\text{lat}$ の円板は**向きに依存しない
  近似**である。実機の外形は向きによって張り出しが変わり、**前端は 0.0500 m** あるが、
  これは**含めない**。$(x,y,\theta)$ の 3 次元配置空間へ拡張すれば厳密になるが、
  **整形ポテンシャルの用途には過剰**（Φ は「ゴールへの遠さ」を滑らかに表す量であって
  衝突判定ではない）という判断でこの近似を採る。**この判断ごと条文として登録する。**

##### 🔴 場の定義は「配置空間上の **8 近傍格子測地**」である（裁定 2026-08-13-R37）

**真の測地距離の近似と称さない。**条件 C の $g$ は、**配置空間（R34 のマスク）の上の
8 近傍格子グラフでの最短路長そのもの**と定義する。真のユークリッド測地距離との差は
方法から導出できるので、**誤差として隠さず併記する**（裁定 R27-2）。

直線を格子の弦で近づけるときの伸び（metrication error）は、角度 $\theta$ の直線に対し

$$\frac{\text{8 近傍格子距離}}{\text{ユークリッド距離}}=\cos\theta+(\sqrt2-1)\sin\theta
\qquad(0\le\theta\le 45°)$$

- **最大 $\sqrt{4-2\sqrt2}$ = 1.082392（＝ +8.24%）**（$\theta$ = 22.5°）／
  **最小 1.000000**（$\theta$ = 0°, 45°）／**角度一様平均 1.054786（≈ +5.5%）**
- $g$ は**真の測地距離を上に外す**（下限ではなく上限側の偏り）
- **C と C' は同一の場を使うので、この偏りは C 対 C' の比較では相殺する。**
  $\rho$ も同じ場から導出される（$\rho = cs\cdot D_0/g(\text{start})$）ので**自己整合**である

**本迷路系での実測（准教授の測定 F。事前登録 `3f35e29` → 報告 `6841e6d`）**:
**1 次精度 FMM（等方的な解法。自身の異方性は最大 +1.29%・45° で最大）との差は
中央値 +2.2%**（5 面で −0.2〜+2.8%）。**上界 +8.24% よりはるかに小さく、
測地線の方位が 0° と 45° に集中していることを示唆する**。真の測地に対する 8 近傍の
超過は、この差に FMM 自身の異方性（0〜+1.29%）を足した **概ね +2〜+4%（約）**。

> 🔴 **重要な区別（作法 15 の適用）: 実測 +2.2% は「2 つの近似の誤差の差」であって
> 「8 近傍の誤差そのもの」ではない。**45° では 8 近傍が厳密で FMM の方が外すため、
> 5 面のうち 1 面では**差の符号が逆転している**。

**未確認 3 件（明示する）**: (i) 方位が 0°/45° に集中しているという読みは**差の小ささから
の逆推定**であって方位分布を直接測ったものではない (ii) FMM は**1 次精度**である
(iii) **5 面のみ**の測定である。

##### 🔴 帯の値を双線形に掴ませない（**解く領域の膨張**。2026-08-13。裁定 R38 の是正）

**距離場を解く領域は、機体が居られる集合を 1 格子セルぶん広げたもの**とする:

$$\text{解く領域の離隔} = w_\text{lat} - h_g\sqrt2 = 0.0400-0.00849=0.03151\ \text{m}$$

機体が居られる点 $P$（離隔 $\ge w_\text{lat}$）を囲む格子セルの 4 隅は
$\lvert P-\text{隅}\rvert\le h_g\sqrt2$ なので**必ず解けている側に入る**。
したがって**禁止帯の値が双線形に混ざることは構成上ない**（マスクの定義自体は変えない）。

- **効果（実測。走査 5 面）**: (L) の最大比が刻み 1.5 / 0.375 mm で
  **60.78 / 67.34 → 1.3107 / 1.3883**、超過が **126.8 / 35.2 mm → 0.659 / 0.206 mm**。
  **R28 の 2 本立てを両方満たす**（比は $\sqrt2$ 以下へ収束・超過は刻みに比例して縮む）
- **交換条件（🔴 2026-08-13 是正。准教授 AUDIT_014・裁定 R39 の続報）**:
  場は「機体より $h_g\sqrt2$ = **8.49 mm** 細い円板が通れる経路」まで数える。
  - **$h_g\sqrt2$ = 8.49 mm は「円板半径の減少量」の上界であって、$g$（経路長）の差の
    上界ではない。**細い円板は**角ごとに内側を通れる**ので、$g$ の差は**角の数だけ積み上がる**
  - **実測（准教授。20 面）**: $g_\text{spec}-g_\text{field}$ は**中央値 28.54 mm**
    （範囲 9.51–52.54 mm）で、**20/20 面で 8.49 mm を超過**（最大 6.2 倍）。
    $D_0$ が大きい面ほど大きい。**$1/\rho_\text{spec}$ 0.7344 対 $1/\rho_\text{field}$ 0.7203 の
    1.9% 差がこの偏りの正体**である
  - **向きの主張（$g$ を小さい側へ寄せる一方向）は 20/20 面で正しい**
  - **格子依存**: $h_g$ を 6 → 3 mm にすると差は平均比 **0.668** に縮む
    （**格子化由来であることは変わらないが、比例より遅い**）
  - **【時系列。上書きしない】旧条文**: 「$g$ を小さい側へ寄せる一方向の偏りで、
    上界は $h_g\sqrt2$（格子を細かくすれば比例して縮む）」。**上界の主張する量を
    取り違えていた**（円板半径の減少量と経路長の差を同一視した）。
    **この誤りは教授の承認込みで通った**（R39 で量の同定が確認されなかった）。
    教訓は `docs/RESEARCH_PLAN.md` **§9-17「上界・下界は量を名指しして主張する」**
    として規約化されている（`220ab63`）
- **🔴 棄却した案の記録**: 第 2 段の延長を全格子グラフ（壁を無視）で走らせる
  **McShane 型 1-Lipschitz 拡張**は、**実測で悪化した**（比 60.8 → **409.8**、
  超過 126.8 → **867.2 mm**）。**原因は場 $g$ が直線距離について 1-Lipschitz ではないこと**
  （壁を挟む 2 点は空間的に近くても測地的に遠い）。McShane の構成は台の上で
  1-Lipschitz な関数にしか使えない。**「1-Lipschitz 関数の min は 1-Lipschitz」を
  前提を確かめずに適用したのが誤りだった**（min の候補集合が $P$ に依存する罠と同型 ＝
  **前提の未確認**。§8 の自己チェックへ登録する）。
  **教授も承認時に同じ前提を検査していなかった**ことを、教授自身の裁定 R39-1 で記録している

##### 🔴 膨張は「方法誤差」であって「モデル誤差」ではない（分類の裁定。R39-2）

**なぜ膨張（8.49 mm）は許され、R34 が棄却した柱の不在は許されないのか**への答え:

| 種別 | 例 | $h_g \to 0$ で | 扱い |
|---|---|---|---|
| **格子化に由来する方法誤差** | **解く領域の膨張 $h_g\sqrt2$**、8 近傍の計量誤差、C-6 の実効離隔の量子化 | **消える**（比例して縮む） | **上界を導出して明記すれば可**（R27-2） |
| **固定的なモデル誤差** | **柱を描かない**・壁の端を丸めない（R34 以前のマスク） | **消えない**（どれだけ細かくしても柱は無いまま） | **不可**（是正が要る。R34） |

**この区別が判断の基準である。**「小さいから許す」ではなく「**格子を細かくすれば消える種類か**」で分ける。

**🔴 棄却した仮説の記録（2026-08-13。作法 14 の由来）**: 准教授の値との系統差 5.2% を
「8 近傍の計量誤差」と見立てたが、**棄却された**。実際の原因は**帯（seed 集合）の取り違え**で、
准教授が学習帯 1–20、私が検証帯 7000–7019 を測っていた。同一帯で再計算したところ
**中央値 0.6923・範囲 0.5012–0.7847 で 4 桁一致**した。

> **作法 14（准教授登録）: 比の一致は機構の証拠にならない。交絡を潰した差で判別せよ。**
> 「点の測地 1.0518 / 現行マスク 1.0520 とほぼ一定 ＝ 計量由来」という読みは、
> **同じ交絡（帯の取り違え）が両方の比に等しく効いていた**ために生じた偶然だった。

**ただし切り分けの手続き自体は有効だった**: 「マスク由来なら、マスクを使わない変種では
差が消えるはず」という反証条件を立てたこと、格子解像度を先に潰したこと（7.5 / 6.0 / 5.0 mm で
1/ρ 中央値 0.6968 / 0.6923 / 0.6938 ＝ 依存は 0.005 未満）は、いずれも独立に有効なまま残る。

#### 🔴 D3（条件 C の実装欠陥）— **調査中。本節の条件 C の数値は「D3 修正前」のもの**

**発見（2026-08-13）**: 測地距離場の値の取り出しに使っていた「下側包絡」
$g(P)=\min_{q}(\text{field}[q]+\lvert P-q\rvert)$ が、**壁の厚みをゼロとする実装の下で
壁越しの格子点を拾い、測地距離を過小評価する**。実装側で区画レベルの可視性判定を
追加したが、**それでも跳びが残った**。

**機構**（機体が到達しうる領域に限定して測定。壁を挟む点対は除外）:

| 刻み [mm] | 最大 $\lvert\Delta g\rvert/\lvert\Delta p\rvert$ | 最大超過 $\lvert\Delta g\rvert-\lvert\Delta p\rvert$ [m] |
|---|---|---|
| 6.000 | 1.1781 | 0.001069 |
| 1.500 | 2.3944 | 0.002135 |
| 0.375 | **7.6289**（発散） | 0.002536 |

**比が発散し超過が有界**なので、性質は Lipschitz ではなく**加法的な跳び**である。
原因は**下側包絡の候補集合が $P$ に依存すること**（窓が動くと min を与えていた候補が
抜ける／区画レベルの可視性判定が $P$ の区画とともに切り替わる）。

**🔴 罠として記録する: 「1-Lipschitz 関数の min だから 1-Lipschitz」は、
min を取る候補集合が $P$ に依存する場合は成立しない。**実装の docstring は
この論法で正当化していたが、上の実測が反例である（准教授の P-A と同型）。

**修正の見込み（実測済み・未実装）**: 候補集合を持たない**双線形補間**に置き換えると

| 刻み [mm] | 最大比 | 最大超過 [m] |
|---|---|---|
| 6.000 | 1.0000 | 0.000000 |
| 1.500 | 1.3107 | 0.000659 |
| 0.375 | **1.3883**（収束） | 0.000206 |

**比が $\sqrt2$ = 1.4142 以下へ収束し、超過は刻みとともに縮む**＝**真の不連続なし**。
$\sqrt2$ は双線形補間の理論上界（格子方向に 1-Lipschitz な場を補間するため）なので、
**実測当てはめではなく導出値**である。

**⚠️ 本節および §5・§7 に記載した条件 C の数値（1/ρ = 0.6378 等）は
すべて「D3 修正前」の実装で測ったものである**（裁定 R27-4）。**修正後に測り直す。**
准教授の順序予測 P-D（修正前 < 修正後の真値 ≤ 0.837）とは現時点で整合している。

#### 条件 C': 総整形量を条件 E に揃えた測地距離場（裁定 R24-1）

**動機（准教授の投入前指摘）**: 測地距離場は角を切るぶん**総整形量が条件 E より約 16%
小さい**（概算の中央値 0.837）。このまま C が悪い結果になっても、**「連続性のせいか、
報酬量が小さいせいか」を分離できない**（多重性と報酬量の交絡）。

**定義**: 条件 C の Φ を、**面ごとの定数**倍して総整形量を条件 E と揃える。

$$\Phi_{C'}(P)=\rho\cdot\Phi_C(P),
\qquad \rho=\frac{0.18\cdot D_0}{g(\text{start})}$$

$D_0$ = スタート区画の $d$（区画数）、$g(\text{start})$ = スタート位置の測地距離 [m]。
分子は条件 E での総整形量（＝ $\Phi$ の全変化量 $D_0\cdot cs$）、分母は条件 C でのそれ。
**$\rho$ は迷路ごと（エピソードごと）に決まる定数**であり、$\rho>1$ になる。

**切り分けの構造**:

| 比較 | 何が分かるか |
|---|---|
| **C 対 C'** | **報酬量の効果**（連続性・錨は同じで総整形量だけが違う） |
| **C' 対 E** | **連続性・錨の効果**（総整形量を揃えた上での差） |

#### 🔴 2 つの $1/\rho$ を名前で分ける（**命名の義務**。裁定 R39-3。R33 の適用）

膨張（上記）を入れたことで、**同じ量が 2 つの基準で違う値になる**。R33 と同じ事故
（基準点の混在）を防ぐため、**名前を分けて定義し、以後の全報告でどちらかを明記する**:

| 記号 | 場の定義 | 値（検証帯 20 面・区画中心・$h_g$=6.0 mm） | 用途 |
|---|---|---|---|
| $1/\rho_\text{spec}$ | **仕様マスクそのもの**（表面から $w_\text{lat}$。膨張なし） | **中央値 0.7344**（0.5965–0.8188） | **照合・検証用**（R34 の仕様に直接対応） |
| $1/\rho_\text{field}$ | **実行時の場**（解く領域を膨張させたもの） | **中央値 0.7203**（0.5755–0.8065） | **学習で実際に効く値**（$\rho$・`geo_inv_rho` の記録はこちら） |

C-1 も同様に対で持つ: **C-1$_\text{spec}$ = −0.36816 m ／ C-1$_\text{field}$ = −0.39499 m**（n=640）。

**🔴 条件 C' の $\rho$ は実行時の場から取る**（実装もそうなっている: `reset()` が
$\rho = cs\cdot D_0/g(\text{start})$ を、膨張ありの場で評価した $g(\text{start})$ から決める）。
**総整形量を条件 E に揃えるという C' の目的は、実際に効く場で揃えて初めて達成される**ため。

**記録の義務**: 条件 C・C' とも、**学習迷路ごとの $g(\text{start})/(0.18\cdot D_0)$
（＝ $1/\rho_\text{field}$）を記録して報告する**（裁定 R24-1）。分布が予想（中央値 0.837）と
食い違うなら、そのこと自体が結果の解釈に効く。実装は `info` に `geo_rho`・
`geo_inv_rho`・`geo_g_start_m` を毎ステップ載せる（`geodesic_potential=True` のときだけ）。

**🔴 $\rho$ を「面ごとの定数」とした設計の妥当性（実測。2026-08-13）**: reset の擾乱で
$g(\text{start})$ は面内で振れるが、**その振れは $1/\rho$ にして中央値 0.0059・最大 0.0141**
（検証帯 20 面 × 擾乱 reset 10 回）であり、**面と面の差（$1/\rho$ = 0.50〜0.78）より 1 桁小さい**。
**面ごとに 1 つの定数を割り当てる設計（R24-1）は実測に支持される**（エピソードごとに
$\rho$ が大きく揺れるなら、C' は「報酬量を揃えた条件」ではなくなっていた）。
測定と数表は `measurements_condC.md`・`measure_condC.py`。

#### 🔴 判定の構造（裁定 R24-2。**結果を見る前に確定する**）

- **仮説 H の検証的な判定は条件 E のみで行う。**R7 基準（3 seed の中央値 ≥ 0.30 で支持、
  3 seed すべて < 0.10 で棄却、打ち切りは棄却側）を条件 E にそのまま適用する。
  **理由**: E が最も純粋な H の検定である（錨を保存し、C2 恒等式を維持し、
  exp_010 からの変更が 1 点に収まる）
- **条件 C・C' は探索的条件**である。同じ数値基準で「機能したか」は述べるが、
  **H の支持／棄却の宣言には使わない**
- **🔴「どちらかが支持なら H 支持」は禁止。**条件を増やすと偽陽性が条件数倍に膨れる
  （多重性）。**支持の宣言に使ってよいのは条件 E の結果だけ**である

#### ゴール 2×2 区画の扱い（裁定 R21・准教授 AUDIT_006 の指摘）

ゴール 2×2 の 4 区画は**すべて $d=0$ で降下隣接を持たない**ため、「全降下隣接の min」は
そのままでは未定義になる。**実装は $d(\text{cell})=0$ で残り距離 $=0$ と短絡するので
降下隣接を参照せず、ゴール 2×2 の内部では $\Phi$ は一様**である
（実測: 4 区画とも $\Phi$ = 1.08000、区画内 9 点で最小＝最大）。

- **ゴール区画へ入る開口部での境界一致は成立しない**（remaining=0 の特別扱いのため）。
  これは (b) の条文 1（ゴール到達の最終ステップ）と (b-2) の除外として登録済みであり、
  **件数を報告する**（検証帯 20 面で (b-2) の除外 20 組）
- **(L) の走査対象にゴール 2×2 を含める**（裁定 R21）。一様なので最大比 0 になるはずで、
  **0 でなければ短絡の実装に欠陥がある**

##### 🔴 既知の環境特性: ゴール 2×2 の中心に柱 `post_3_3` が実際に立っている

**発見（2026-08-13。准教授の R33 突き合わせの副産物）**: `mouse/maze6_gen.py` 冒頭は
ゴール中央 2×2 を「**内壁も柱もない広場**」と書いているが、**物理モデルは
ゴール 2×2 の中心格子点に柱 `post_3_3`（12 mm 角）を実際に生成している**。
柱を落とす条件が `mouse/mjcf.py` で **16×16 の中央 (8,8) 専用**に書かれており、
6×6 には適用されないためである（`center_goal and width == 16 and height == 16`）。

- **裁定 2026-08-13-R34: いまは変更しない。**exp_012 が進行中であり、
  **柱は 3 条件（E / C / C'）すべてに同じように効くので交絡は生じない**
- **是正は R11 バッチ（exp_012 完走後）で影響評価つきで行う**として登録する
- **条件 C の配置空間マスクにはこの柱を障害物として含める**（R34 の「実際に生成される
  形状から導出する」に従う。文書の記述ではなくモデルの実体を正とする）
- ⚠️ **文書とモデルの食い違いはこれで 2 例目**（1 例目は機体最外側 39.5 mm）。
  **寸法・形状はモデルの実体から導出し、文書の記述を入力に使わない**

#### 開口部の検査が尽きていることの保証（准教授 AUDIT_006 §4-ter、`bfaaf7b`）

格子グラフは二部グラフなので、**開通した隣接区画の BFS 距離は必ず 1 だけ違う**。
評価帯 v4 の開いた辺 5481 本のうち降下（距離差 1）が 5401 本、等距離は 80 本で
**すべてゴール 2×2 の内部辺**（外部 0 本）。したがって
**「全降下開口部 ＋ ゴール 2×2 の別扱い」で開口部の検査は尽きており、漏れは無い。**

**実行前に必須の単体テスト（通らなければ学習は回さない。裁定 R5-1）**:
- (a) **区画中心で Φ が旧実装（階段版）と一致する**（検証帯 20 面 × 全区画。ゴール区画は除く）。
  **本改訂でも不変であること**が条件（裁定 R13 条件 2。exp_010 との比較の錨であり
  C2 恒等式の前提）
- (b) **跳びが無いこと。閾値は式で定義する**（裁定 R7-6・R18-2）。

  $$\lvert\Delta\Phi_t\rvert \;\le\; L\;\cdot\;\kappa\;\cdot\;\lvert\Delta p_t\rvert \;+\; \varepsilon,
  \qquad (L,\ \varepsilon)=\begin{cases}(\sqrt2,\ \mu_\max) & \text{条件 E}\\ (\sqrt2,\ \text{方式由来}) & \text{条件 C}\end{cases},
  \quad \kappa=1.10,\quad \varepsilon=10^{-9}\ \text{m}$$

  $\lvert\Delta p_t\rvert$ は**真の位置（`privileged_pose()`）から求めた機体中心の 1 ステップ
  変位**。**判定形は E と C で同形**（裁定 R30・D3-8）。乗法項は両条件とも $\sqrt2$（条件 C は
  距離場なので 1 ＋ 離散化誤差）。**$\sqrt2$ は中点だけで測った過小評価だったので使わない。**
  - **🔴 旧式（$v$ 基準）から変えた理由**: 旧式は車輪角速度から求めた前進速度
    $v=r(\omega_L+\omega_R)/2$ を使っていたが、$v$ は**惰行・制動時に機体中心の実変位を
    過小評価する**。実測（検証帯 20 面 × 台本 4 種・10311 歩）で
    比 $\lvert\Delta p\rvert/(\max(\lvert v_t\rvert,\lvert v_{t-1}\rvert)\cdot\Delta t)$ の最大は
    **2.414**（走行のみの 3 台本だと 1.288、急停止台本を足すと 2.414）。$v$ 基準を保つには
    余裕係数 2.5 が必要で、**検査が 2.4 倍甘くなる**。テストは真の位置を使えるので
    $v$ を経由する必要がない
  - **$\kappa$ の導出**（裁定 R18-2(iii): 旧 1.25 をそのまま流用しない）: Lipschitz 性は
    2 点間の**直線距離**についての性質なので、区画内に留まる歩では $\kappa=1$ で足りる。
    $\kappa>1$ が要るのは**区画をまたぐ歩**で、境界の交点 $q$ を経由して
    $\lvert\Delta\Phi\rvert\le\sqrt2(\lvert p_2-q\rvert+\lvert q-p_1\rvert)\le\sqrt2\cdot(\text{弧長})$
    と評価するため、$\kappa$ は**1 歩の弧長／弦長**を上から押さえればよい。
    旋回半径 $R=v/\omega$、$\theta=\omega\Delta t$ で 弧長／弦長 $=(\theta/2)/\sin(\theta/2)$。
    物理的な最大は両輪を逆向きに全電圧で回した場合の
    $\omega_\max=2v_\max/\text{tread}=2\times4.091/0.072=113.6$ rad/s、
    $\theta_\max=1.136$ rad で **弧長／弦長 = 1.0553**。**$\kappa=1.10$ はこれに 4% の余裕**
    （実際には全速旋回時は $v\approx0$ なのでさらに保守的）
  - **絶対上限**は $\lvert\Delta p\rvert$ を直接測るので不要になったが、
    **$\lvert\Delta p_t\rvert \le v_\max\cdot\Delta t = 0.0409$ m を別途照合**し、
    超えたら物理側の異常として報告する
  - **🔴 遡及合格は無い**（裁定 R18-2(ii)。**裁定 2026-08-13-P7(1) で記述を是正**。
    准教授 AUDIT_010 指摘 1）: **現行の (b)（乗法項 ＋ 加法項 $\mu_\max$）での判定は次のとおり。**
    - **D2 は (b) で落ちる**: $\lvert\Delta\Phi\rvert$=0.09099 に対し現行閾値
      $\sqrt2\times1.10\times0.02064+\mu_\max$=0.07611 ＝ **1.20 倍の超過**
    - **D1 は (b) では通る**: $\lvert\Delta\Phi\rvert$=0.06820 に対し現行閾値
      $\sqrt2\times1.10\times0.02223+\mu_\max$=0.07858。ただし D1 は**刻みを細かくすると
      上限なく発散する**ので、**(L)（刻み不変性）が落とす**
    - **🔴 加法項 $\mu_\max$ の導入（裁定 R30）により、D1 型の検出は (b) から (L) へ移った。**
      系として穴は開いていないが、**「(b) が D1 を落とす」と信じたまま将来 (L) を
      簡略化すると D1 型が素通りする**。**どのテストが何を担保しているか**を
      ここに固定しておく（准教授 AUDIT_010 指摘 1 の実害経路）
    - **【時系列。上書きしない】加法項の導入前の記載**（閾値を
      $\sqrt2\kappa\lvert\Delta p\rvert$ だけで計算していた時点のもの）:
      「**旧実装の FAIL は新閾値でも FAIL のまま**である。D1 は 0.06820 に対し新閾値
      $\sqrt2\times1.10\times0.02223$=0.03459、D2 は 0.09099 に対し
      $\sqrt2\times1.10\times0.02064$=0.03212。**比はそれぞれ 1.97 倍・2.83 倍の超過**
      （旧式基準では 3.07 倍・4.41 倍）。しかも D1 は刻みを細かくすると上限なく
      発散するので、**どんな有限の係数でも通らない。**」
      → **この記述は加法項を入れる前の閾値によるものであり、現行形では D1 について
      成立しない**（D2 の超過も 2.83 倍ではなく 1.20 倍）。**旧実装が旧判定形で
      落ちていた事実そのものは変わらない。**
  - 直進・90° 旋回・S 字・**急停止**の 4 種で全ステップを検査する。
    **同じ走行を階段版でも検査し、そちらでは 0.18 に近い跳びが実際に出ることを確認する**
    （＝この検査が連続化を検出できることの証拠。階段版で跳びが出なければ検査自体が無意味）
  - **🔴 判定対象からの除外（裁定 R12。准教授 AUDIT_005 が「正しい実装でも超える」と
    確定させた 2 種）**。**閾値ではなく対象を限定する**のが正しい是正である:
    1. **ゴール到達の最終ステップ**（$d$=0 で remaining=0 と特別扱いするため 0.09 m 跳ぶ）
    2. **方向反転ステップ**（$d$(新) = $d$(旧) + 1）
  - **除外の根拠**: 境界の一価性は全降下開口部で機械確認済み（2402 通り・最大差 4.4e-16 m）／
    ゴール到達はエピソードの終了なので閉路を作れない／**跳びは別々の状態間の値差であって
    報酬ポンプではない**
  - **除外したステップは黙って捨てず、種別ごとに件数と最大 |ΔΦ| を報告する。**
    **「区画内」と「前進」で閾値を超えたら、それは実装の欠陥として扱い、除外しない。**
  - **除外条件はコードに条文の形で書く**（将来テストの走行台本を足したときの再発防止）
- (L) **【新設・裁定 R18-4】刻み不変性検査。**区画内を格子で走査し、
  隣接格子点対（**軸方向 2 種と斜め 2 種の両方**）の
  $\lvert\Delta\Phi\rvert/\lvert\Delta p\rvert$ の最大値を求める。**刻みを 3 水準
  （6.0 / 1.5 / 0.375 mm）に変えても最大値が $L$ 近傍で一定であること**を検査する
  （条件 E は $\sqrt2$、条件 C は $1+\delta$。**ここは中点一致とは無関係に定義どおりの
  Lipschitz 定数が出る**ので、条件 E の (b) の 2.7669 とは別の値になる）。
  **ゴール 2×2 の 4 区画も走査対象に含める**（裁定 R21。Φ が一様なので最大比 0 が期待値で、
  0 でなければ $d=0$ の短絡の実装に欠陥がある）。
  真の不連続があれば最大値は刻み数に比例して発散するので、**D1 型の回帰検査になる。
  走行台本への依存が無い**のが (b) に対する利点。
  **⚠️ 軸方向だけを見ると折れ区画の最大を取り逃がす**（学生B が実際に取り逃がした）
- (b-3) **`step()` 経路での `_prev_cell` の更新タイミングの整合**（裁定 R12 の確認事項）。
  同一の行動列について (i) `step()` 経由、(ii) 同じ姿勢列への直接呼び出し（同じ
  `_prev_cell` 更新規則を並走させる）の 2 通りで Φ の系列を計算し、全ステップで
  一致すること（絶対誤差 1e-12 未満）を検査する。
  **本改訂の実装で再実行する**（裁定 R18-4）
- (b-2) **【必須・回帰防止】降下開口部での境界一致**（裁定 R8・R13 条件 3）。
  **tie-break が選んだ $n$ だけでなく、すべての降下隣接 $n$ について**、
  開口部の点で「$c_0$ の側から計算した Φ」と「$n$ の側から計算した Φ」が一致することを
  検査する（絶対誤差 1e-9 未満）。**tie-break 選択分だけでは D2 型を検出できないことが
  2026-08-13 に実証されたため拡張した。**
  - **降下方向が曲がる構成が 0 個なら検査が空振りなので、その旨を報告する**（面と区画の数を出す）
  - **🔴 除外（裁定 R18-3 により明文化）**: **$n$ がゴール区画である $c_0$ は対象外**とする。
    $d(n)=0$ では remaining=0 と特別扱いするため、条文 1 と同じ理由で境界は一致しない。
    **件数を必ず報告する**（検証帯 20 面で 20 個）。**除外は登録されたものだけ**とする規律を保つ
- (c) Φ が横方向のずれに不感（射影なので不変）。**🔴 対象を限定する（裁定 R18-3）**:
  **「降下隣接がちょうど 1 つ、かつ折れ線が一直線」の構成に限る。**
  - **理由**: 降下隣接が直角に 2 つある区画では、**横へずれることが本当にゴールへ近づく
    こと**なので Φ が動くのが正しい挙動である（全降下隣接の min を採るため）。
    折れ点付近もクランプで変わりうる。**飛ばした構成の数を報告する**
- (d) $\Phi_0=0$（reset 直後、**横 ±20 mm・方位 ±10° の擾乱あり**で）。
  **擾乱が実効的に効いていることを併せて報告する**（無擾乱の検査になっていないこと）
- (e) 予約 seed の読み飛ばしと、廊下側（`tests/test_corridor.py`）の全項目 PASS
- (f) **【学生B が追加】既定 `continuous_potential=False` で既存挙動が変わらないこと。**
  Φ が $x,y,c_{-1}$ を無視して階段版そのものであること・報酬が階段版から独立に組み立てた
  式と一致すること・同一条件の 2 回の走行が bit 単位で一致することを検査する

**既定は `continuous_potential=False`** とし、**既存挙動が bit 単位で変わらない**ことを
条件にする（exp_010/011 との対照が崩れないため）。

**条件 C 専用の検証項目（裁定 R21-C。条件 E の (a) の代わりに置く）**:
- (C-1) **区画中心の値と $d\cdot cs$ の差の分布を実測して記録する。**合否判定ではなく
  **記録**（この差が「錨の違い」そのものであり、2×2 解釈表で使う）。
  検証帯 20 面 × 全非ゴール区画で、平均・最大・分布を出す
- (C-2) **(L) を定数 $1+\delta$ で実施する。**$\delta$（離散化誤差）は**実測して
  design.md へ追記**し、閾値はその実測から決める（代表点で決めない。§8 の自己チェック）
- (C-3) **全辺での連続性。**条件 E で破れた「開口部の中点以外での境界一致」を、
  条件 C では**共有辺の全域**（機体が通れる範囲、中点から ±0.0505 m）で検査する。
  距離場なので成立するはずであり、**成立しなければ実装の欠陥**
- (C-4) $\Phi_0=0$（reset 直後、擾乱あり）
- (C-5) **ゴール 2×2 の内部**で $\Phi$ が最大値（$=g(\text{start})$）に達し、
  そこから減らないこと
- (C-6) **報酬ポンプ検査。**位置のみの関数なので閉路の整形総和は恒等的に 0 になるはず。
  **閉路を実際に列挙して総和 0 を機械確認する**（条件 E では拡張状態が要るぶん複雑だった）

---

## 5. 事前登録した予測と反証条件

### 5-1. 机上で確定している予測

| 量 | 階段 Φ（現状） | **連続 Φ（予測）** |
|---|---|---|
| ~~区画内を正方向に走る 1 歩あたり経済~~ | ~~−0.0050~~ | ~~+0.0046~~ **← 撤回（下記）** |
| **進捗報酬が入るステップの割合** | **5.3%**（18.75 歩に 1 回） | **100%** |
| **1 区画あたり整形の平均** | +0.00455 | +0.00455 ← **厳密に一致。ここは変わらない** |
| $dG/dx$（$x$=0.45 m、$k$=8.7e-3） | −0.146 | **+0.607** |
| $dG/dx$（$x$=1.53 m、$k$=8.7e-3） | −0.337 | **+0.092** |
| 終点の総収益の順序 | ゴール>滞留/衝突>探索 | **変わらない** |

**🔴 第 1 行を予測から撤回した（裁定 R7-3）。**「階段 −0.0050 → 連続 +0.0046」は
**階段側を区画内のステップに限り、連続側を全体平均で取った非対称比較**だった。
同じ土俵（1 区画平均）で比べると **+0.00455 対 +0.00455 で厳密に一致**しており、
**符号は変わらない**（§1 の是正を参照）。**変わるのは分布であって平均ではない。**
撤回しないままだと、学習が成功したときに「平均が正になったから成功した」という
**誤った機構の説明**が残ってしまう。

**終点の順序は変わらない。**本変更が直すのは**勾配の密度**であって終点の順序ではない。
「探索が最下位」「$D_0\ge10$ で衝突が 2 位」は残る。**これは H が正しくても残る欠陥**で、
§7 に条件つき限界として登録する。

### 5-2. 学習結果の予測（**観測前に登録する**）

**主指標: 検証帯 7000-7019（20 面 × 1 試行）の最終モデルのゴール率**

- **支持**: **3 seed の中央値 ≥ 0.30**
- **棄却**: **3 seed すべてで < 0.10**
- **中間**（機構は効いたが不十分）: 上記のどちらでもない場合
- **打ち切りが起きた seed は棄却側（ゴール率 < 0.10）として算入する**（裁定 R7-2）。
  打ち切りは「100 万歩まで改善なし」の判定なので、これを除外すると
  **失敗した試行を分母から落とすことになる**

**🔴 「1 本以上で ≥ 0.30」から差し替えた（裁定 R7-1）。**准教授の二項計算により、
**真値が 0.20（＝中間相当）でも max 統計量では 48% の確率で「支持」が出る**。
max での判定は `verification/AUDIT_001_m1_clearance.md` が指摘した失敗
（**生き残った 2 seed の範囲だけを報告した**）と同型である。中央値は 3 本のうち
2 本が閾値を超えることを要求するので、この誇大化が起きない。

**確信度: 中（0.55）。** Φ の連続化は M1 で低レベル運動の学習が成立した条件を復元するが、
M2-0 は**分岐でどちらへ曲がるかを観測から当てる**という M1 に無い課題を持つ。
**なお中央値へ差し替えたことで判定は厳しくなったが、確信度は改める必要が無い**
（0.55 は「機構が効くか」についての確信であって、max 統計量の誇大化を織り込んだ
数字ではなかった）。

**副指標（比較の相手は exp_010。同じ $k$・同じ seed）**

| 指標 | exp_010 の実測 | **exp_012 の予測** |
|---|---|---|
| 停止層（\|v\|<0.05 m/s）の割合 | 99.73% / 93.77% | **全 seed で < 20%** |
| **ゴールへの前進 中央値**（$D_0$=9.5） | **0 / 1 区画** | **≥ 4 区画** |
| 経路効率 中央値 | 0.034 / 0.327 | **0.8 〜 1.5** |
| 到達区画数 中央値 | 1 / 2.5 | **≥ 6** |
| 学習中のゴール到達回数 | 80 / 7036 = 1.1% | **≥ 5%** |
| 学習後半（最後の 500 エピソード）のゴール | **0 回** | **> 0 回**（後退が止まる） |

**衝突率は予測しない。**本変更は終点の順序を直さないので、走り始めれば衝突は増えうる。
**衝突が増えても前進が伸びれば H は支持される。**「衝突率が下がること」を成功条件に
混ぜると、**凍結（衝突しないが進まない）を成功と読み違える。**

**平均 shaping の符号は指標に使わない**（§1 末尾で否定した量）。

### 5-3. 【裁定 R21・R24】3 条件（E / C / C'）の解釈表（**投入前に登録する**）

**🔴 H の支持／棄却を宣言してよいのは条件 E の結果だけである**（裁定 R24-2）。
C・C' は探索的条件で、**E の結果の「理由」を切り分けるためにある**。

判定は **R7 基準を条件ごとに適用**する（3 seed の中央値 ≥ 0.30 で機能、
3 seed すべて < 0.10 で不成立、打ち切りは不成立側に算入）。

**第 1 段: H の判定（条件 E のみ）**

| 条件 E | 宣言 |
|---|---|
| 中央値 ≥ 0.30 | **H を支持する** |
| 3 seed すべて < 0.10 | **H を棄却する。**次の容疑者は**観測**（オドメトリ由来の方位だけでは $d$ の降下方向を予測できない）。**γ の変更には進まない**（§2 で棄却済み） |
| 中間 | **H は効いたが不十分。**次は §7 の限界 1（終点の順序を直さない）・限界 3（訪問報酬が 1 桁弱い） |

**第 2 段: 理由の切り分け（C・C' は探索的。H の宣言には使わない）**

| C 対 C' | C' 対 E | 読み方 |
|---|---|---|
| C' > C（**大きく**） | — | **報酬量が効いている。**測地距離場が角を切って総整形量が 16% 減ることが不利に働いた。→ 以後 C 系を使うなら $\rho$ 倍を入れる |
| C ≈ C' | C' ≈ E | **連続性も錨も報酬量も効いていない。**Φ の実現方法は結果を左右しない。→ 実装の簡単な E を採る |
| C ≈ C' | **C' > E（大きく）** | **連続性・錨が効いている。**E に残る中点一致の近似（辺上のずれで最大 0.0440 m ぶんの跳び。§7-0）が不利に働いた。→ **以後は C' を採る**。§7-0 の限界が実害だったと確定 |
| C ≈ C' | **E > C'（大きく）** | **錨（中心 $=d\cdot cs$）が効いている。**C 系の (C-1) で測った中心値のずれが原因の候補。→ M2-1・M3 は E で進め、ずれ分布を限界として登録 |

**⚠️ 「大きく」の基準も先に決める**: 3 seed の中央値の差が **0.15 以上**（＝支持閾値 0.30 の
半分）を「大きく」とする。それ未満は「差は見られない」と書き、**順位だけを根拠に
機構を語らない**（n=3 の中央値の差はばらつきに埋もれる）。

**⚠️ この表を後から書き換えないこと。**結果を見てから欄を書き換えると、
`verification/AUDIT_001` が指摘した後付けと同型になる。

**「副指標は動いたが主指標が動かない」場合の解釈**（先に決めておく）:
**H は支持されるが十分ではない。**次の容疑者は**観測**（オドメトリ由来のユークリッド
方位だけでは迷路距離 $d$ の降下方向を予測できない）へ移る。**γ の変更には進まない**
（§2 で棄却済み）。

---

## 6. 実験計画（§9-8・§9-10 準拠）

| 項目 | 値 | **なぜその値か**（§9-10） |
|---|---|---|
| 変更点 | **Φ の連続化のみ** | 1 実験 1 変更。他は exp_010 と完全に同一 |
| $k$ / $\alpha$ | **8.7e-3 / 0.5**（変えない） | exp_010 との差を 1 点に保つ。**加えて $k$=8.7e-3 では衝突の総収益が全 20 面で負**（$k$=0 では 15 面で正）なので、衝突解へ落ちにくい条件になっている |
| γ | **0.995**（変えない） | §2 で棄却 |
| 並列環境数 | 1 | exp_010 と同じ |
| 学習 seed | **1 / 2 / 3 の 3 本** | §9-8。設計変更の根拠にするので最低 2、3 が望ましい。exp_010/011 と同じ seed にして対応をとる |
| 総ステップ | **200 万** | exp_010/011 と同じにして比較可能にする |
| **打ち切りの基準** | 検証帯の定期評価（10 万歩ごと、計 20 点）で、**100 万歩までの 10 点すべてがゴール率 < 0.05 なら打ち切る** | 「頭打ちだった」で済ませないため。M1 は 25 万歩で 0.9 台に達し、exp_010/011 は 21 点中 20 点が 0.00 だった。100 万歩で 0.05 未満なら 200 万でも変わらない見込み |
| 実時間の見積り | 1 本 ≈ 28 分（M2 実測 1181 steps/s）＋定期評価。3 本並行で約 40 分 | `OMP_NUM_THREADS=2` を付ける |
| 評価帯 6000-6019 | **使わない** | gate 判定ではない（§9-7） |

**🔴 裁定 R21・R24 により 3 条件 × 3 seed = 計 9 本にする。**$k$・$\gamma$・並列数・
総ステップ・学習 seed は**3 条件で完全に同一**（変えるのは Φ の実現方法だけ）。
**H の判定は条件 E のみ**（§5-3 第 1 段）。C・C' は理由の切り分けに使う。

**成果物の分離（§9-1）**: 条件ごとに別パスにする。
- 条件 E: `logs/exp_012_condE_seed{1,2,3}/`、`models/exp_012_condE_seed{N}.zip`、
  `outputs/exp_012_condE_seed{N}/`
- 条件 C: `logs/exp_012_condC_seed{1,2,3}/`、…（同様）
- 条件 C': `logs/exp_012_condCp_seed{1,2,3}/`、…（同様）

**条件 C・C' の追加記録（裁定 R24-1）**: 学習迷路ごとに
$g(\text{start})/(0.18\cdot D_0)$（＝ $1/\rho$）を `episode_seeds.jsonl` へ書き出し、
**分布（中央値・最小・最大）を報告する**（准教授の概算は中央値 0.837）。

**実行順（裁定 R24-4）**: 9 本同時は重いので、**条件 E（検証的）の 3 本を先に投入し、
完走後に C・C' の 6 本**を回す。**監査の完了が投入の前提であることは変わらない。**
走行ごとの指標を全件保存する（§9-3）。**学習ログには `episode_seeds.jsonl` を必ず残す**
（§2-4 の分析はこれが無ければできなかった）。

---

### 🔴 6-1. 条件 E の実行記録と判定（2026-08-13。裁定 R38。**H は不支持**）

#### 実行

| 項目 | 値 |
|---|---|
| 投入 | **2026-08-13 21:43:53 JST**（3 seed 同時。`OMP_NUM_THREADS=2`） |
| 投入版 | `140ce5d`（作業ツリーの差分はモジュール docstring のみ。**現行差分全体でも条件 E の実行経路は不変**であることを教授が検証済み） |
| コマンド | `train.py --seed N --log-dir logs/exp_012_condE_seedN --model-out models/exp_012_condE_seedN.zip` |
| 実効速度 | 800〜880 steps/s（3 本同時。単独実行の実績 675 steps/s より速い） |

#### 🔴 打ち切り基準の発火と判定

**§6 の条文**（**投入の 46 時間前に凍結済み**）: 「検証帯の定期評価で、100 万歩までの
10 点すべてがゴール率 < 0.05 なら打ち切る」。

| seed | 100 万歩までの 10 点 | 発火 |
|---|---|---|
| 1 | 0.00 ×8、**0.05**（70 万歩）、**0.05**（90 万歩） | **しない**（0.05 は「< 0.05」を満たさない） |
| 2 | **0.00 × 10 点すべて** | 🔴 **発火** |
| 3 | **0.00 × 10 点すべて** | 🔴 **発火** |

**裁定 R38**: 条文どおり **seed2・seed3 は棄却側（< 0.10）に算入**する（§5 の R7-2）。
したがって **3 seed の中央値 ≥ 0.30 は seed1 の最終値に依らず達成不能**であり、
**仮説 H の検証的判定は「不支持」で確定**する。
**この確定は、最終評価の数値を誰も見ていない時点で宣言した**（結果を見てからの
再解釈の余地を残さないため）。

- **発火認定の時刻（AUDIT_013 指摘 2 の要件）**: 打ち切り基準はコードに実装されていない
  （1 実験 1 変更を守るため意図的にそうしている）ので機械的な発火時刻が無い。
  学生B が発火を認めたのは **2026-08-13 22:07–22:08 JST** に読んだ
  `logs/exp_012_condE_seed{2,3}/validation_history.json`（**1,000,000 ステップまでの
  10 点が入った版**）であり、**22:12 JST** に教授へ報告した。当時 seed1 は 9 点（90 万歩まで）
- **執行差分の記録**: 条文は「打ち切る」だが、**プロセスは完走させた**。理由は
  **経済的なもののみ**（残り数分・資源は投入済み）であり、**100 万歩以降のデータは
  すべて「判定外・参考記録」**として扱う

#### 🔴 境界事実（准教授 AUDIT_013 指摘 1。**不等号 1 つに判定が乗っていた**）

検証帯は **20 面**なので `goal_rate` の刻みは **1/20 = 0.05** である。したがって条文
「< 0.05」は実質「**20 面で 1 面もゴールしていない**」と同義であり、**閾値に内部が無い**
（0.05 未満に取りうる値は 0.00 だけ）。seed1 の 0.05 は丸めではなく**厳密な 1/20**。

> **条文が「≤ 0.05」だったら 3 seed すべてが打ち切られ、M2 で初めて決定的方策が
> ゴールに届いた事実は記録されなかった。**
> **これは緩和でも後出しでもない**（条文は投入の 46 時間前に凍結済み）。

**規約化**: この教訓は `docs/RESEARCH_PLAN.md` **§9-16「閾値は指標の刻みを確認してから書く」**
として制定された（`96ee192`。既存の凍結済み条文には遡及しない）。

#### 参考記録（**判定外**。100 万歩以降のデータ。3 本とも 200 万歩で完走: 22:31–22:41 JST）

| seed | 最終ゴール率 | 衝突率 | 時間切れ率 | 非ゼロのゴール率が出た点 | 実時間 |
|---|---|---|---|---|---|
| 1 | **0.00** | 0.75 | 0.25 | 70 万・90 万・**130 万**歩（各 0.05） | 3203 s |
| 2 | **0.00** | 0.80 | 0.20 | **200 万**歩（0.05） | 2881 s |
| 3 | **0.00** | 0.55 | 0.45 | なし | 3422 s |

- **200 万歩まで走らせても最終の決定的方策のゴール率は 3 本とも 0.00** であり、
  **打ち切り基準の判断（100 万歩で改善が無ければ 200 万でも変わらない）は結果として正しかった**
  （＝ 条文どおりの判定で失ったものは無い）
- **探索的所見（🔴 2026-08-13 是正。単位を明記する。准教授 AUDIT_016・§9-18 の適用第 2 号）**:
  検証帯の定期評価で**ゴール率が非ゼロになった点は 63 点中 4 点**（exp_010/011 は 126 点中 1 点）
  だが、**この数え方は評価点単位であり、63 点は 3 走行 × 21 スナップショットで独立ではない**。
  - **seed1 の 3 点はすべて同じ面（7017）**である ＝ 独立な 3 事象ではなく、
    **1 本の走行が 1 つの面に届く状態を 3 回目撃した**もの
  - **走行単位で数えると 2/3 対 1/6**（exp_012 E の 3 本中 2 本 対 exp_010/011 の 6 本中 1 本）で、
    **Fisher の正確検定 p = 0.2262 ＝ この n では有意でない**
  - **したがって「階段 Φ より起きやすくなった」とは言えない。**言えるのは
    「**exp_012 E でも非ゼロの目撃はあったが、定着はしていない**」までである
- 行動の振れ（`action_diff_rms`）: exp_012 E = **0.525 / 0.503 / 0.445**、
  exp_010（階段 Φ・同じ $k$）= 0.206 / 0.481 / 0.141、exp_011（$k$=0）= 3.97 / 2.92 / 1.81。
  **⚠️ 停止解の有無を語るにはこの量では足りない**（`check_m2_step_economy.py` の
  停止層・1 歩あたりの経済で測り直す必要がある。§1 の表を新条件で作り直すこと）
- **失敗の様式は衝突が主**（0.55–0.80）。exp_010 は時間切れが主だった面が多い
  （0.60/0.30/0.70）

---

### 🔴 6-2. 機構分析 — **1 歩あたりの経済は直った。走る方策になった。だが壁に当たる**（裁定 R41-①）

条件 E の 3 モデルを検証帯 20 面で決定的に再生し、報酬を項ごとに分解した
（`research_notes/scripts/check_m2_step_economy.py`。**毎ステップ env の reward と
再構成値の一致を assert**。最大絶対誤差 1.1e-16〜2.2e-16／3 モデル計 112,663 ステップ）。

#### 🔴 集計の単位に注意（この回に読み違えかけた）

**プールした全ステップでの層別割合は、長いエピソード（時間切れ 6000 歩）に支配される。**
seed1 は 31,176 歩のうち 30,000 歩が時間切れ 5 本のものなので、プール集計の「停止層 95.7%」は
**方策の性質ではなく、時間切れ面の長さを見ているだけ**である。
**方策が停止解かどうかは、走行ごとに測って outcome 別にまとめる**（以下の表）。

| モデル | Φ | $k$ | 停止層の平均 shaping | **走行層の平均 shaping** | **衝突走行の停止層** | 時間切れ走行の停止層 | 衝突/時間切れ |
|---|---|---|---|---|---|---|---|
| exp_010 seed1 | 階段 | 8.7e-3 | −8.60e-5 | 0.0（n=15 歩のみ） | **81.8%** | 100.0% | 8 / 12 |
| exp_010 seed2 | 階段 | 8.7e-3 | −2.53e-3 | +4.52e-3 | 1.1% | 97.6% | 14 / 6 |
| exp_011 seed1 | 階段 | **0** | −1.96e-3 | **−4.51e-4** | 6.0% | — | 20 / 0 |
| exp_011 seed2 | 階段 | **0** | −8.41e-4 | **−7.64e-4** | 5.8% | 9.6% | 19 / 1 |
| **exp_012 E seed1** | **連続** | 8.7e-3 | −7.78e-4 | **+8.81e-3** | **1.9%** | 98.7% | 15 / 5 |
| **exp_012 E seed2** | **連続** | 8.7e-3 | −2.24e-4 | **+3.49e-3** | **0.0%** | 96.9% | 16 / 4 |
| **exp_012 E seed3** | **連続** | 8.7e-3 | −4.45e-4 | **+2.07e-4** | **1.3%** | 95.8% | 11 / 9 |

（停止層 = 前進速度 < 0.05 m/s、走行層 = ≥ 0.4 m/s。停止層・走行層の割合は**走行ごとに測った値の
outcome 別中央値**。shaping の平均は層ごとのプール平均）

#### 読み取り（4 点）

1. **H が狙った「走ることが 1 歩あたり得になる」は実現した。**条件 E の走行層の shaping は
   **正**（+8.81e-3 / +3.49e-3 / +2.07e-4）で、時間罰 −1.0e-3 を seed1・seed2 では大きく上回る。
   **対照的に exp_011（$k$=0・階段 Φ）は走行層でも shaping が負**（−4.5e-4 / −7.6e-4）
   ＝ **走っても損**だった。§1 の表（M2-0 で走ることが停止の 5 倍損）は**条件 E では解消している**
2. **実際に走る方策になった。**衝突で終わる走行の停止層は **0.0〜1.9%**
   （exp_010 seed1 は衝突走行でも **81.8%** が停止）
3. **🔴 しかし凍結は消えていない。**時間切れで終わる走行では停止層が **95.8〜98.7%** ある。
   **面によって凍結する**（時間切れ 5 / 4 / 9 面）。「凍結が減った」であって「無くなった」ではない
4. **前進は中央値 2 区画で止まり、衝突で終わる。**衝突走行のエピソード長は中央値
   **52〜140 歩（2.6〜7.0 秒）**、経路効率の中央値は 0.257 / 0.652 / 0.435 で
   **最短経路長に達する前に壁に当たっている**

#### 🔴 機構の結論

**H の前提（報酬の時間分布 → 1 歩あたりの経済）は支持された。H の予測（ゴール率）は不支持だった。**
**失敗の様式が「凍結」から「衝突」へ移った**ことが、この 2 つを同時に満たす。
**次のボトルネックは報酬整形の時間分布ではなく、走行制御（壁を避けて走り続ける能力）である。**

**未確認**: (i) 停止層の閾値 0.05 m/s は既定のまま（感度は未測定） (ii) 20 面 × 1 試行 × 3 seed
(iii) 「衝突で終わる」の内訳（どの壁にどう当たるか）は未分解。

#### 「なぜ 7017 だけか」— 幾何では説明がつかなかった（裁定 R41 の追加指示）

seed1 の非ゼロは**すべて面 7017**、同じ $D_0$=4 の **7010 は一度もゴールしていない**。
両面の幾何を壁配列から比べた（`research_notes/scripts/check_m2_maze_7017_vs_7010.py`）:

| 量 | 7017 | 7010 |
|---|---|---|
| スタート区画 | (0, 5) | (0, 5) |
| $D_0$ | 4 | 4 |
| ゴール入口 | ('v', 2, 3) | ('v', 2, 3) |
| 開通辺の総数 | 41 | 41 |
| スタート区画の次数 | 1 | 1 |
| 初期方位 | 270°（唯一の出口の向き） | 0°（唯一の出口の向き） |
| **最短経路の曲がりの回数** | **3** | **2** |
| **最短経路の最長直進** | **1 区画** | **2 区画** |
| 経路上の分岐区画数（次数 ≥ 3） | 3 | 4 |

- **スタート・$D_0$・ゴール入口・込み具合・「出口を向いて始まる」ことまで一致**しており、
  **差は最短経路の曲がりの回数（3 対 2）と最長直進（1 対 2）だけ**である
- **その差は 7017 の方が難しい向き**であり、「7017 だけ届いた」ことの説明にならない
- **🔴 そもそもこの偏りは統計的に確立していない**（走行単位で 2/3 対 1/6・Fisher p = 0.2262）。
  **「なぜ 7017 だけか」は、まだ問いとして成立していない**可能性が高い。
  **本節は「幾何では説明がつかなかった」という否定的な結果として記録する**
- **⚠️ 届いた瞬間の重みは失われている**（後述 §9-19）。70 / 90 / 130 万歩で非ゼロだったが、
  チェックポイントは 40 / 80 / 120 / 160 万歩の 4 点しか無く、**その 4 点はいずれも
  検証帯でゴール率 0.00**（実測）。**したがって届いた方策の挙動は事後に再現できない**

---

## 7. 限界と、条件つき限界の紐づけ（§9-13）

0. **【裁定 R21・条件 E に固有】境界一致は開口部の中点でしか成立しない。**
   共有辺の上を中点から $\lambda$ だけ外れて通過すると、両側の Φ は $\lvert\lambda\rvert$ に
   比例して食い違う。**機体が通れる範囲での最大は $\mu_\max$ = 0.0440 m**
   （訪問報酬 0.02 の **2.2 倍**、境界の跳び 0.18 の **24%**）。実走行では**前進のたびにこの跳びが入る**
   （実測: 20 面 × 台本 4 種・前進 190 歩のうち 23 歩で判定定数を超過）。
   - **→ 発火時点: 条件 E の結果を解釈するとき。**E が棄却され C が支持されたら、
     **この跳びが原因だったと確定できる**（5-3 の解釈表）
   - **条件 C にはこの限界は無い**（距離場なので全域で連続）

1. **本変更は終点の順序を直さない。**「探索（走り続ける）が最下位（−0.802）」と
   「$D_0\ge10$ で衝突が 2 位」は残る。機構は**滑らかさの罰が動いたときだけ掛かる**ため
   走行に $(p+c)/(1-\gamma)$=0.999 対 凍結 0.200 の 4 倍の税がかかること。
   **→ 発火時点: exp_012 で停止層の割合が 20% を下回らなかったとき。**候補は
   「エピソード上限を短くして罰の飽和を抑える」または「滑らかさの罰を速度で正規化する」
   だが、**どちらも exp_004/005 の地雷 2・3 に触るので必ず再検算する。**
2. **γ=0.995 ではゴール直前の進捗報酬が名目の 15% まで痩せる**（§3）。
   **→ 発火時点: exp_012 で Φ を連続化した後、ゴール手前で失速する現象が出たとき。**
   そのとき初めて γ を 0.998 へ上げる価値が生じる。
3. **訪問報酬 0.02 は境界の跳び 0.18 の 11% しかない。**逆方向に未訪問区画へ入る歩は
   −0.165 で、訪問報酬では埋まらない。**探索を促す力は設計意図より 1 桁弱い。**
   **→ 発火時点: exp_012 で到達区画数が予測（≥6）に届かなかったとき。**
4. **$k$=0 で衝突の総収益が正（最大 +0.408）**であること自体が独立の欠陥である。
   **→ 発火時点: 将来 $k$ を下げる／外す実験をするとき。**衝突罰 −1.0 の深さを
   $\gamma^{T-1}$ の減衰込みで再設計する必要がある。
5. **ランダムウォークの到達歩数（§2-5(c)）は方策の挙動そのものではない。**
   PPO 方策の実際の到達歩数は未測定。かつこの指標は $k$ に強く依存する。
6. ~~**連続 Φ の実装は、降下隣接が複数ある区画でどれを選ぶかが恣意的である。**~~
   **🔴 2026-08-13 の改訂（裁定 R13・R18）で、tie-break そのものを廃止したため解消した。**
   以下に経緯を残す（「決定は引き継がれるが限界は引き継がれない」を防ぐため）。
   - **旧実装の限界（2026-08-12・准教授 AUDIT_005、裁定 R12）**: 座標順（辞書順最小）で
     降下隣接を 1 つ選んでいたため、**loop 迷路の 32% の配置が同点の降下隣接を複数持ち、
     後戻り配置 1448 通りのうち 208 件（14.4%）で 0.18 m の跳びが生じていた。**
   - **🔴 この登録は不完全だった（2026-08-13 に判明）。**「後戻り＝方向反転のときの話」として
     登録していたが、**前進でも起きる**（D2。§4 参照）。非ゴール区画 640 個中、降下隣接が
     2 つ以上ある区画は **99 個（15.5%）**で、選ばれなかった降下開口部を通過した瞬間の
     **最大跳びは 0.18000 m**。**旧 (b-2) は tie-break 選択分しか見ないので検出できなかった。**
   - **新定義での再導出（裁定 R13 条件 4）**: 全降下隣接についての min を採るため、
     **どの降下開口部を通っても境界の値が一致する**（**全降下開口部 2402 通り・
     最大差 4.44e-16 m** で機械確認済み）。**tie-break 起因の跳びは 0 件になり、
     「後戻り配置の 14.4% で 0.18 m」という旧数値は該当しなくなった。**
   - **したがって、旧登録が予告していた「H の効果が薄まる」交絡は解消した。**
     exp_012 が中間判定になった場合に「連続化が不十分だった」と「連続化しても足りない」を
     区別できない、という旧限界も**この経路では発火しない**（他の経路は残りうる）
9. **【裁定 R12】M2-1（`mode="full"`）に着手するとき → 跳びの発生率を full モードで
   再測定する。**行き止まりでは後戻りが必ず起きるので、loop モードで測った
   後戻り 14.4% の跳び（限界 6）は **full モードでは確実に増える**。
   M2-1 の設計に入る前に測り直すこと。
10. 本計算は**代表条件 1 本の決定論的な軌道**の机上計算であり、方策の分布・価値関数の
   推定誤差・PPO のクリップ・GAE($\lambda$) の切り詰めは含まない。
   **保証するのは「勾配が逆なら失敗する」という必要条件の側だけ**
   （`taskB_return_ordering.md` §7 と同じ限界）。
   - **🔴 裁定 R7-4 により、ここで免責していた当のものが仮説 H の主張の本体になった**
     （報酬の時間分布が価値関数の近似・GAE の切り詰め・探索を通じて学習を左右する）。
     **したがって机上計算だけでは H を支持も反証もできない。**学習を回すことが唯一の
     判別手段であり、**机上計算の役割は「H 以外の説明（γ・終点の順序・平均の符号）を
     消すこと」に限られる。**初版はこの線引きを曖昧にしていた。
8. **連続 Φ は直前の区画 $c_{-1}$ に依存するので、厳密には状態のみの関数ではない**（§4）。
   **🔴 2026-08-13 の改訂後も維持する（裁定 R18-4）。**区画ごとの明示式でも、折れ区画の
   $\ell$ は $s=(P-C)\cdot a$ を通じて $w_\text{in}$（＝ $c_{-1}$）に依存する。
   **「位置のみの関数になったので §7-8 は不要」と読んではいけない。**
   形式的には拡張状態（真の姿勢, 直前の区画）上のポテンシャルで、$c_{-1}$ は力学にも
   素の報酬にも影響しないため最適方策は変わらないと考えられるが、**Ng の定理の保証
   そのものではない。**境界上の値が両側で一致することは確認したので**報酬ポンプは無い**が、
   これは「ポンプが無い」の確認であって「最適方策が不変」の証明ではない。
   **→ 発火時点: exp_012 が支持され、この報酬で M2-1・M3 へ進むと決めるとき。**
   そのときは $c_{-1}$ に依存しない定式化（迷路の自由空間上の連続測地距離場を
   reset 時に前計算する等）へ置き換えるか、依存を残す判断の根拠を残す。

---

## 8. 検証の記録 — 独立の盲検再計算で見つかった私の誤り

> ### 🔴 自己チェック（裁定 R21。**検算のたびに必ず適用する**）
>
> **集合全体への主張は、上限・下限の導出か全数走査で確かめる。代表点では語らない。**
>
> 2026-08-13 に学生B が**同型の誤りを 3 件**犯したことによる。いずれも
> 「代表点だけを調べて集合全体を語った」ものである:
> 1. Lipschitz の格子走査を**軸方向だけ**で行い、最大比を 1.000000 と読み違えた
>    （折れ区画では斜め方向が最大。正しくは $\sqrt2$）
> 2. (c) の絞り込みが **$c_{-1}$=None** で漏れ、降下隣接が 2 つある区画が混入した
> 3. 境界一致を**開口部の中点だけ**で測り、必要な Lipschitz 定数の下限を $\sqrt2$ と
>    読み違えた（辺全体で課すと **2.7669**）。しかもその過程で 3.2223（壁厚の半分の脱落）→
>    2.7972（機体半幅に車輪外面を使用）と**二重に誤り**、確定値に至るまで 3 度是正した
>
> **3 件とも「代表点で PASS したから集合全体で成立する」と暗黙に仮定していた。**
>
> ### 🔴 極値の報告は機械化する（2026-08-13・裁定 R31-2）
>
> **報告する最大・最小は、必ず全数走査の argmax / argmin を
> コードで取得した値のみとする。一覧の目視抜粋から極値を語らない。**
>
> 同型の誤りは本日 4 件あり、**いずれも「出力の抜粋を目視で読んだ」**ものだった:
> 1. Lipschitz の走査を軸方向だけで行った（斜めを見ていない）
> 2. (c) の絞り込みが $c_{-1}$=None で漏れた
> 3. 境界一致を開口部の中点だけで測った
> 4. **(b) の失敗一覧（比で並んでいない）の先頭行を最大比として報告した**
>    （報告値 2.392 に対し真の最大は 4.385。**これにより「合否不変」という
>    前提が崩れていたことに、4 度の報告のあいだ気づかなかった**）
>
> **3・4 は自分の出力を読み違えたものである。**§8 の自己チェックは、
> **対象データだけでなく自分の報告にも適用する。**
>
> ### 🔴 検査すべきは「値」ではなく「列挙」のことがある（2026-08-13・裁定 R26）
>
> 機体半幅の誤り（車輪 39.5 mm を最外部と誤認）は**計算ミスではなく、
> 部位の列挙の漏れ**だった。**全 geom の ±y 張り出しを列挙した表**を作ると、
> 発生源が表の上で見える:
>
> | 順位 | geom | \|y\|max [m] |
> |---|---|---|
> | 1〜4 | `mein_body2..5`（コーナー片） | **0.04000 ← 見落としていた真の最外側** |
> | 5〜6 | 車輪 | 0.03950 ← **これを最外部と誤認した** |
> | 7〜10 | センサポッド | 0.03800 |
>
> **最大値 1 個だけを照合する手順では、同じ誤りを再現するだけだった。**
> **「値が合っているか」ではなく「数え落としが無いか」を検査する形にすること。**
> 未対応の型が現れたら黙って飛ばさず、**エラーにする**（飛ばすと列挙の漏れになる）。

## 8-1. 初版（2026-08-11）の盲検再計算で見つかった私の誤り 2 件

`docs/../CLAUDE.md` のレビュー原則に従い、**結論を渡さずに実装だけを読ませた
独立の再計算**を回した（解析スクリプトは読ませていない）。$D_0$ の 20 面・
階段 Φ の $dG/dx$ 全 9 点・総収益の式は**独立に一致**した。加えて**私の誤りが 2 件**
見つかったので、本文はすべて是正後の値である。

| # | 私の誤り | 正しい値 | 影響 |
|---|---|---|---|
| 1 | **滞留の総収益に「走行時」の滑らかさ罰を適用していた**（−0.999） | **−0.200**（凍結時の実測 E‖a−ā‖²=1.086e-4 を使う） | **結論が変わった。**誤った値では「衝突が 2 位」と読めたが、正しくは**「探索が最下位」で滞留が 2〜3 位**。$k$=8.7e-3 の凍結を説明するのはこちら |
| 2 | 最も得な衝突の探索範囲に $x=D_0\cdot0.18$（ゴール区画）を含めていた | 上限を $D_0\cdot0.18$ より真に小さくとる | $D_0$=4 で −0.445 → **−0.546**。小さい面ほど衝突を過大評価していた |

**誤り 1 は、引き継ぎメモ (3)-3「罰の大きさは挙動に依存する。比較表の各行で測り直すこと」
という前任者の警告を、私自身が破ったものである。**しかも同じ文書の冒頭で
「M1 の表を流用してはいけない」と書きながら、**滞留の行だけ走行時の値を使っていた。**
実測（凍結方策の E‖a−ā‖² = 2.1e-11）は既に手元にあった。

なお独立側にも 1 件の誤りがあった（連続 Φ の $dG/dx$ を $\gamma^T$ とし、
$x$ が $T$ を通じて $\gamma^T$ を動かす項を落としていた。0.9541 対 正しくは 0.9093 で
3〜5% 高い）。**符号と定性的結論は両者一致**しており、本文は私の値を採る
（数値微分で独立に検算済み）。

---

## 9. 付随して見つかった規律上の欠落（教授へ報告）

1. **exp_011（$k$=0、3 seed × 200 万歩）に実験カードのディレクトリが無い。**
   `experiments/exp_011_*` が存在せず、条件・予測・結果がどこにも残っていない
   （引き継ぎメモの表にだけある）。§9-1 は全実験にカードを要求している。
   **本任務の範囲外なので手を付けていない。**指示があれば事後復元する。
2. **`mouse/maze6_env.py` 冒頭の設計時検算が陳腐化している。**
   「36 区画ぶんの総和 0.72」と書くが、スタート区画は reset で訪問済みなので
   **最大 35 区画 = 0.70**。また記載の順序（最短ゴール +0.975 > … > 滞留 −0.200）は
   **滑らかさの罰（exp_006 以降に追加）を含まない前提**である。
   **私の担当ファイルなので、教授の承認後 exp_012 の実装と同時に是正する。**
