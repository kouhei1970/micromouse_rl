# exp_022: にせ履歴（帰無の原因の分離）

- 起票: 学生B 2026-08-15（教授発注。**投入は教授検収 → 准教授の投入前監査 → GO の後**）
- 一次資料: `experiments/exp_021_observation_history/judgment.md` §9（分離できていないこと）
- **比べる相手は 2 つある**: **対照群 = exp_019**（環境 v2 の基準実験・17 次元）／
  **参照群 = exp_021**（観測履歴の連結・153 次元）
- 用語: **M2** = マイルストーン 2（未知の 6×6 迷路で走破率 90%）／
  **exp_019** = 環境 v2 の基準実験／**exp_021** = 観測履歴の連結（M2 第 2 弾 A）／
  **PPO** = 近位方策最適化／**LSTM** = 長短期記憶（再帰型のニューラルネットワーク）

---

## 1. 問い

**exp_021 の帰無（短期文脈は走行制御を改善しない）の原因は、次のどちらか。**

| | 説明 | **本実験で分離できるか** |
|---|---|---|
| **(A) 情報の説** | **短期文脈そのものが走行制御に効かない** | **(B) とは分離できる。(C) とは分離できない** |
| **(B) 次元の説** | **入力次元が 17 → 153 になったことで最適化が難しくなり、(A) の効果を打ち消した**（第 1 層のパラメータが約 9 倍） | **✅ 分離できる**（本実験の目的） |
| **🔴 (C) 汚染の説** | **履歴が立て直し（リスポーン＝瞬間移動）を跨いでいて、渡された文脈が壊れていた** | **❌ (A) と分離できない**（§3-7） |

**exp_021 の介入は 2 つのことを同時に行っていた**（情報を与える／次元を増やす）。
**本実験は片方（次元）だけを行い、もう片方（情報）をゼロにする。**

### 1-0. 🔴 (C) 汚染の説の実測（**准教授 AUDIT_044 の発見・学生B が独立に再計算して一致**）

> **介入群（exp_021）では、履歴 128 歩がリスポーンを跨いだ歩が 29.0%**
> **（68,867 / 237,316 歩）だった。対照群（exp_019）の同じ量は 10.7%（25,655 / 240,000 歩）。**

- **方策が受け取る短期文脈の約 3 歩に 1 歩が「機体がもう居ない場所の観測」を含んでいた**
- **これはラッパの誤りではない**（`tests/test_obs_history.py` の T1〜T9 は全項目 PASS）。
  **1.28 秒の窓 × 瞬間移動のある環境という組み合わせの意味的な帰結**である
- **介入群の方が跨ぎが多いのは、介入群の衝突が 4.25 倍だからである**（**結果が原因を増やしている**）

### 1-1. なぜ exp_021 の条文で分離できなかったか（**起票の経緯・誤りの記録**）

**exp_021 カード §9-bis の「にせ履歴群」は、発火条件を「Q1 または Q2 が当たった場合」として
事前登録していた。**Q1・Q2 とも外れたので**条文どおり発火させない**。

> **🔴 教訓（exp_021 カード §11 の誤りの型 9・教授・准教授・学生B の 3 者の台帳）**:
> **全域被覆は「読み」だけでなく「追加検査の発火条件」にも要る。**
> **§5-1 の読みの表は Q1×Q2×Q3 の 8 通りを埋めたのに、
> 追加検査の側は「当たった場合」の 1 経路しか用意していなかった。**
> **悪化側にこそ分離の道具が要る。**

**本実験は、その道具を新しい問いとして立て直したものである。**
**exp_021 の判定は確定であり、本実験の結果で読み替えない。**

---

## 2. 変更範囲の宣言

### 2-1. 同一に保つもの（**exp_021 と同一**）

| 要素 | 状態 |
|---|---|
| **報酬**（項・係数・割引率 γ=0.995・訪問加点 0.02・時間罰 0.001・ゴール +1.0・衝突 −1.0） | **固定** |
| **物理モデル**（`assets/`・`mouse/mjcf.py`・ロボットのパラメータ） | **不変** |
| **環境 v2**（規約終端・衝突リスポーン・上限 2000 歩・行動の高周波成分への罰 k=0） | **不変** |
| **学習用迷路の与え方**（毎エピソード新規生成・seed 8000 以降・距離カリキュラムなし） | **不変** |
| **定期評価**（検証用の 20 迷路〔seed 7000〜7019〕× 1 試行・10 万歩ごと） | **不変** |
| **PPO のハイパーパラメータ**（学習率 3e-4・`n_steps` 2048・`batch_size` 256・`n_epochs` 10・`gae_lambda` 0.95・`ent_coef` 0・中間層 [128, 128]・活性化 tanh） | **不変** |
| **総ステップ 200 万／本・6 seed・並列環境数 1** | **不変** |
| **観測の次元 153**（17 × 9）・**方策のパラメータ数** | **exp_021 と完全に同一** |

### 2-2. 変えるもの（**1 点だけ**）

> **履歴の中身。**exp_021 は遅れの位置に**過去の観測**を入れたが、
> **本実験は同じ位置に【現在の観測の複製】を入れる。**
> **次元もパラメータ数も同一のまま、履歴の情報だけがゼロになる。**

```
exp_021 : [ o(t), o(t-1), o(t-2), o(t-4), …, o(t-128) ]   ← 情報あり
exp_022 : [ o(t), o(t),   o(t),   o(t),   …, o(t)     ]   ← 情報ゼロ・次元は同じ
```

### 2-3. 同一版基準 — **不活性の bit 一致**（exp_020・exp_021 と同じ形）

> **にせ履歴を無効にした新版を、exp_019 と同じ seed・同じ設定で走らせ、
> exp_019 の記録列と bit 一致することを確認する。**

**「不活性」の定義**: **`--obs-history` も `--obs-history-sham` も渡さなければ、
ラッパを構築しない**ので、**`make_env` の返す環境は現行と完全に同一のオブジェクト**である。

**🔴 これに加えて、本実験には固有の等価性検査がもう 1 つ要る**（§6-3）:

> **にせ履歴の出力ベクトルが、9 個のブロックすべてで現在の観測と bit 一致すること。**
> **これを確かめないと「情報がゼロである」という前提そのものが未検証になる。**

---

## 3. 事前登録した予測（**結果を見る前に書く**）

**主予測: にせ履歴群は対照群（17 次元）と同等である ＝ 次元の増加は無害であり、(A) 情報の説が正しい。**

### 3-1. 判定の型（**すべての量に共通・投入前に固定**）

**にせ履歴群の値が、2 つの錨（対照群 exp_019 と参照群 exp_021）のどちらに近いかで判定する。**

> **相対位置 r = ln(にせ履歴群 ÷ 対照群) ÷ ln(exp_021 群 ÷ 対照群)**
> **r < 0.5 → 対照群の側 → (A) 情報の説を支持**
> **r ≥ 0.5 → exp_021 群の側 → (B) 次元の説を支持**

- **r = 0.5 は 2 つの錨の幾何中央**である。**比で測る量なので対数の中点を採る**
  （**加法の中点は、比が 4 倍離れている量では対照側に寄りすぎる**）
- **恣意的な定数を置かない**ための形である。**錨の値は exp_019・exp_021 の確定した実測**であり、
  **本実験の結果を見てから決めることはできない**

### 3-2. 予測（**4 件。分離能の高い順**）

| # | 判定量 | 対照群（exp_019） | 参照群（exp_021） | **境界（r = 0.5）** | **予測** |
|---|---|---|---|---|---|
| **P1**（主） | **`respawn_per_1000`**（衝突の頻度） | **0.500** | **2.125**（4.25 倍） | **1.031** | **≤ 1.031 → (A)** |
| **P2** | **5 区画以上到達した走行の数**（/120 走行） | **3** | **26**（8.67 倍） | **8.8 件** | **≤ 8 件 → (A)** |
| **P3** | **rollout でゴールした走行の数**（/120 走行） | **0** | **4** | **比が定義できない**（§3-4） | **0 件 → (A)** |
| **P4** | `net_progress_per_1000`（正味の前進） | 1.500 | 1.250（0.833 倍） | 1.369 | **≥ 1.369 → (A)** |

**測定は `measure_driving.py`（exp_021 と同一のスクリプト）**で行う。
**最終方策 × 学習環境 v2 × 検証用の 20 迷路 × 各 1 エピソード（決定的）**、
**env は迷路ごとに作り直し、reset seed は `_trial_seed(0, maze_seed, 0)` に固定**する
（**exp_021 カード §4-1 と同一**）。**集約は迷路 20 本の中央値 → 6 seed の中央値**（P1・P4）。
**P2・P3 は 120 走行の件数**である（**中央値では裾が見えないため**）。

### 3-3. 🔴 外れの向きの区別（**exp_021 の教訓・教授裁定 2026-08-15**）

> **「閾値未達」と「逆向き」を分けて書く。**

**各判定量について、3 つに分けて報告する**:

| 読み | 意味 |
|---|---|
| **(A) 側** | にせ履歴群が対照群に近い ＝ **次元は無害** |
| **(B) 側** | にせ履歴群が exp_021 群に近い ＝ **次元が有害** |
| **🔴 どちらの錨よりも外** | **にせ履歴群が対照群より良い、または exp_021 群より悪い** ＝ **2 つの錨で挟む前提が崩れている**。**この場合は (A)(B) の判別をせず、そのまま報告する** |

**「どちらの錨よりも外」を先に書いておく理由**: **exp_021 では「外れ、しかも逆向き」と
「外れ、しかし別の側面では良くなった」を区別する用意が無かった。**
**同じ抜けを繰り返さない。**

### 3-4. 各判定量の分離能と限界（**投入前に明記**）

| # | 分離能 | 限界 |
|---|---|---|
| **P1** | **4.25 倍の隔たり**。**判定量の刻みは 0.125**（6 seed 中央値）なので、**境界 1.031 の近傍で取りうる値は 1.000 と 1.125** ＝ **境界に乗る値は無い** | **対照群には床がある**（対照 6 seed 中 2 本が 0.00）。**下振れ側の分解能が低い** |
| **P2** | **8.67 倍の隔たり**。**両側 Fisher の正確確率 p = 4.5×10⁻⁶** で、**exp_019 と exp_021 の差は明瞭**である | **「5 区画以上」の閾値 5 は、exp_019 の最大到達が 5 区画だったことに由来する**（**恣意的ではないが、exp_019 の実測に依存した定義**である） |
| **P3** | **弱い**。**4/120 対 0/120 の両側 Fisher の正確確率 p = 0.122** で、**exp_019 と exp_021 の差自体が有意でない** | **🔴 P3 単独では判別できない。**（**対照が 0 件なので比が定義できず、r も計算できない**）。**「0 件なら (A) 寄り・1 件以上なら (B) 寄り」という粗い読みにとどめ、P1・P2 と食い違ったときは P1・P2 を採る**と投入前に決めておく |
| **P4** | **最も弱い**。**隔たりは 0.833 倍（刻み 0.125 で 2 目盛）**しかない。**境界 1.369 の近傍で取りうる値は 1.250 と 1.375** | **2 目盛の差で「どちらの錨に近いか」を決めることになる**ので、**P4 単独で結論を出さない**。**P1・P2 の補助として読む** |

### 3-5. 全域被覆（**4 つの判定量の組み合わせがすべて意味を持つか**）

**🔴 表中の「(A) 情報の説」は、すべて「(A) 本質か (C) 跨ぎによる汚染かは本実験では残る」**
**という限定つきで読む**（§3-7）。**以下では「(A/C)」と書く。**

| P1（衝突） | P2（裾） | 読み | 次の一手 |
|---|---|---|---|
| **(A/C)** | **(A/C)** | **次元は無害。exp_021 の悪化は「短期文脈の情報が効かない」か「渡した文脈が壊れていた」かのどちらかによる** | **(B) を消せた。第 2 弾 B（LSTM）は次元の手当てなしで設計してよい。ただし (C) の決着は第 2 弾 B の設計判断に持ち越す**（§7-bis） |
| **(B)** | **(B)** | **次元が有害。exp_021 の帰無は次元の増加で説明される**（**⚠️ ただし §7 限界 10 の限定つき — 本実験が揃えるのは次元とパラメータ数であって入力の共分散の階数ではない**） | **(B) 確定。第 2 弾 B は次元の手当てが必須**（隠れ状態の大きさ・入力の圧縮を設計に入れる） |
| **(A/C)** | **(B)** | **衝突は情報／汚染由来だが、裾の厚さは次元・容量由来** | **「裾の改善は容量が生んだ」＝ 記憶の情報とは別の道がある**。第 2 弾 B の設計に容量の軸を足す |
| **(B)** | **(A/C)** | **衝突は次元由来だが、裾の厚さは情報／汚染由来** | **記憶の情報は裾（深く入れること）に効き、次元は衝突を増やす**という切り分け。**第 2 弾 B に最も有利な読み** |

- **P3 は上の表に入れない**（**分離能が弱いため**。§3-4）。**値は必ず報告する**
- **P4 も表に入れない**（**同**）。**値は必ず報告する**
- **どの組み合わせでも「次に何をするか」が決まる**ことを、**投入前に確かめた**

### 3-6. 🔴 反証可能性の点検（**exp_021 の誤りの型 5・9 を当てる**）

| # | **どの観測が出たら (A) が否定されるか** | **構成上必ず (A) になる経路は無いか** |
|---|---|---|
| **P1** | **にせ履歴群の衝突が 1.031 を超える**（**exp_021 は 2.125 だった**ので、**同じ水準になれば否定される**） | **無い**。**にせ履歴群は情報がゼロなので「情報が効いて衝突が減る」経路が存在しない**。**それでも対照群と同じ値になるとは限らない**（**次元が効けば悪化する**） |
| **P2** | **にせ履歴群の 5 区画以上到達が 9 件以上**（**exp_021 は 26 件**） | **無い**。**同上** |
| **P3** | **にせ履歴群でゴールが 1 件以上出る** | **無い** |
| **P4** | **にせ履歴群の前進が 1.369 を下回る** | **無い** |

**🔴 追加検査の発火条件にも全域被覆を当てた（誤りの型 9）**:
**本実験には追加検査（条件つきで発火する群）を置かない。**
**置かない理由**: **本実験自体が exp_021 の追加検査であり、
ここでさらに条件つきの群を積むと、判定の依存関係が 3 段になって読めなくなる。**
**本実験の結果で足りない場合は、新しいカードを立てる。**

### 3-7. 🔴 本実験で確定しないこと（**exp_021 §5-2 と同じ型の限定・投入前に書く**）

> **にせ履歴群が対照群と同等（(A/C) 側）になっても、確定するのは
> 「【exp_021 が実際に渡した形の】短期文脈は走行制御を改善しない」までである。**
> **「短期文脈そのものが効かない」((A)) と「渡した文脈が跨ぎで壊れていた」((C)) は分離できない。**

**理由**: **にせ履歴群は情報がゼロなので、跨ぎの有無という区別自体が存在しない。**
**(C) を否定するには「跨がない履歴」を作って比べる必要があり、それは本実験の介入ではない。**

- **これは条文の設計の限界であり、結果を見てから読み替えない**
- **(A) と (C) の正面対決は第 2 弾 B へ申し送る**（§7-bis）

### 3-8. 跨いだ歩を除いた層別集計（**探索的・事前登録・追加の走行は要らない**）

> **完走後に、【履歴 128 歩がリスポーンを跨いでいない歩】だけに限った層別集計を出す。**
> **対象は 3 群すべて（対照 exp_019・参照 exp_021・にせ履歴 exp_022）。**

- **判定には使わない**（**判定は §3-2 の 4 件のみ**）。**(C) の当たりをつけるための材料**である
- **既存の `raw`（毎歩の `d_hist`・`resp_hist`）から計算できる**ので、**追加の走行は要らない**
  （**exp_019・exp_021 の分は既に計算して 10.7%・29.0% を得ている**）
- **🔴 交絡の限界（投入前に明記）**: **跨ぐ歩は衝突の直後に偏る。**
  **したがって「跨がない歩」に限った集計は、同時に「衝突から遠い歩」に限った集計でもある。**
  **層別で差が消えても、それが跨ぎのせいか衝突からの距離のせいかは分けられない。**
  **この層別は (C) を否定も肯定もしない — 当たりをつけるだけである**

---

## 4. 打ち切り条文と報告トリガー

### 4-1. 打ち切り条文（**exp_021 と同じ形・成立しても走行は止めない**）

> **10 万〜100 万歩の定期評価 10 点すべてでゴール率 < 0.05 なら、その seed の打ち切り条文が成立する。**
> **🔴 境界: ゴール率が厳密に 0.05 の点は「< 0.05」を満たさない**（**不成立側**）。
> **exp_021 で実際にこの境界に判定が乗った**（seed 6 の 70 万歩点が厳密に 0.05）ので、**投入前に固定する。**

- **成立しても走行は止めない。**本実験の判定量はゴール率ではない
- **成立の状況は記録するが、判定には使わない**（**exp_021 の Q5 に相当する予測は置かない** —
  **本実験の問いは (A)/(B) の分離であって、ゴール率の予測ではない**）

### 4-2. 報告トリガー（**判定には使わない**）

> **80 万歩の退避重みを `measure_driving.py` で測り、`net_progress_per_1000` の 6 seed 中央値が
> 【exp_019 の 80 万歩の退避重みで測った中央値 1.25】の 0.5 倍（＝ 0.625）以下なら、教授へ即報告する。**

- **exp_021 と同じ閾値・同じ対照**である（**exp_019 の 80 万歩の 6 本は測定済み・版管理下**）
- **健全な走行では作動しないことの実証は済んでいる**（**exp_019 の 6 seed は 1.00〜1.50 で、
  発火線 0.625 の外側**。exp_021 カード §4-5）。**exp_021 でも作動しなかった**（実測 1.125）
- **中断の降格条項**: **トリガーによる中断を行った場合、本実験の検証的判定は行わず探索的な報告へ降格する**

---

## 5. 実装

### 5-1. `mouse/obs_history.py` に `sham` を足す（**既定 False ＝ exp_021 と完全に同一**）

```python
class ObsHistoryWrapper(gym.Wrapper):
    def __init__(self, env, lags=DEFAULT_LAGS, sham=False):
        ...
        self.sham = bool(sham)

    def _stack(self):
        if self.sham:
            # にせ履歴: 遅れの位置に**現在の観測を複製**する（次元は同じ・情報はゼロ）
            return np.concatenate([self._buf[0]] * (1 + len(self.lags))).astype(np.float32)
        return np.concatenate([self._buf[0]] + [self._buf[l] for l in self.lags]).astype(np.float32)
```

- **`sham=False` のとき経路は exp_021 と 1 行も変わらない**（**bit 一致が構成上成立する**）
- **履歴の入れ物（`_buf`）は `sham=True` でも同じように更新する** —
  **更新をやめると `reset`／`step` の計算量が変わり、乱数以外の差が入りうる**。
  **計算量の差は結果に影響しないが、差の数を 1 つでも減らす**

### 5-2. 学習側 `train.py`

- **CLI 引数 `--obs-history-sham` を新設**（**渡さなければ既定 off**）。
  **`--obs-history` と併用する**（遅れの組は exp_021 と同一の `"1,2,4,8,16,32,64,128"`）
- **設定の出力を 1 行足す**: `[train] にせ履歴 = 有効（遅れの位置に現在の観測を複製・情報ゼロ）`
- **評価にも同じラッパを掛ける**（`env_wrapper`。exp_021 と同じ経路）

### 5-3. 測定 `measure_driving.py`

- **`--history-sham` を足す**（**測定側でも同じラッパ設定にする**）
- **P2・P3 の判定量を出力に足す**: **`n_reach_ge5`（5 区画以上到達した走行の数）**と
  **`n_goal_rollout`（ゴールした走行の数）**を、**120 走行の件数として `summary` に入れる**
  - **🔴 判定量が記録に残るかの検査（作法 9）**: **どちらも既存の `raw`（毎歩の `d_hist`）と
    `metrics` の `outcome` から計算できる**ことを確認済み
    （**exp_019・exp_021 の既存の出力からも同じ値が再計算できる** — **実際に計算して
    対照 3 件／exp_021 26 件・対照 0 件／exp_021 4 件を得た**）

### 5-4. 単体テスト `tests/test_sham_history.py`（**投入前に全項目 PASS が必要**）

| # | 検査 | **空振りしないための条件** |
|---|---|---|
| S1 | **`sham=True` の出力が、9 ブロックすべて現在の観測と bit 一致する** | **現在の観測が定数でないこと**を先に確認する |
| S2 | **`sham=True` と `sham=False` で観測の次元が同じ** | — |
| S3 | **`sham=True` でも `sham=False` でも、素の env の状態遷移が bit 一致する**（乱数を消費しない） | **リスポーンを 1 回以上跨ぐこと**（exp_021 の T7 で踏んだ空振り） |
| S4 | **`sham=True` の出力に、過去の観測が 1 要素も混ざっていない** | **k 歩進めた後、過去の観測と一致するブロックが 0 個であることを確認**（**過去と現在がたまたま一致しないよう、観測が毎歩変化する方策で回す**） |
| S5 | **`sham` の既定が False で、exp_021 の経路と bit 一致する** | — |
| S6 | **`n_reach_ge5` と `n_goal_rollout` の計算が、合成データで手計算と一致する** | **境界（ちょうど 5 区画）を含む合成例を入れる** |

### 5-5. 判定 `judge_sham.py`（**新規・`judge.py` と同じ作法**）

- **§3-1 の相対位置 r を計算し、(A)/(B)/どちらの錨よりも外 の 3 値を返す**
- **錨の値（exp_019・exp_021 の実測）はスクリプトに定数として書き、出力に記録する**
  （**後から差し替えられない形にする**）
- **安全弁**: seed 数が 6 でない／学習量が 3 群で揃っていない／
  **にせ履歴の設定が測定の記録に無い**／**参照群と対照群の値が事前登録と違う**
- **単体テストは合成データだけで書く**（**誤りの型 8**。`tests/test_judge_sham.py`）

---

## 6. 手順

1. 起票（本書）→ **教授検収** → **准教授の投入前監査** → **GO** → 投入
2. **投入前に 3 件**:
   - **不活性の bit 一致検証**（`--obs-history` も `--obs-history-sham` も渡さず 10 万歩 →
     exp_019 seed 1 と照合）
   - **にせ履歴の等価性検査**（§2-3 の固有検査。**9 ブロックすべてが現在の観測と bit 一致**）
   - **単体テスト S1〜S6 と `tests/test_judge_sham.py`**
3. 投入時に**投入版のコミットハッシュを記録**（**プロセスが記録した値を正とする**）
4. **投入後**: 80 万歩の報告トリガー
5. 完走後: **P1〜P4 の機械的判定** → 判定文書 → 教授検収 → 准教授の独立再計算
   - **🔴 判定の前に、3 群すべてを同一版の `measure_driving.py` で測り直す**。
     **理由**: **P2・P3 の判定量（`n_reach_ge5`・`n_goal_rollout`）は exp_022 の起票時に
     新設したもので、exp_019・exp_021 の既存の測定出力には入っていない**。
     **3 群を同じ版で測れば、判定量の定義が群ごとに違う事故が構成上起こらない**
   - **対照群・参照群の値が事前登録の錨と一致することは `judge_sham.py` が機械的に検査する**
     （**食い違えば落ちる ＝ 錨を後から差し替えられない**）
6. **完走後に重み 12 本を保全**（研究計画書 §9-21 (c)。`experiments/preserved_weights.md` へ追記）
7. **完走後に測定出力を保全**（**研究計画書 §9-21 (a)**・准教授 AUDIT_045 要記載 2）:
   **生データを除いた軽量版（`*.slim.json`）を版管理下へ入れ、
   完全版の SHA-256 をカードへ記録する**（**生成は `slim_output.py`**）。
   **exp_021 では事後に整えたが、手順に書いておけば取りこぼさない**

---

## 7. 限界（**投入前に明記**）

1. **🔴 本実験は「次元が有害か」を測るのであって、「どの次元数なら無害か」は測らない**。
   **153 次元という 1 点だけである**
2. **P3（ゴール件数）は分離能が弱い**（**exp_019 と exp_021 の差自体が両側 Fisher で p = 0.122**）。
   **単独では判別できない**
3. **P4（正味の前進）は隔たりが 2 目盛しかない**。**単独では判別できない**
4. **「5 区画以上」の閾値は exp_019 の最大到達（5 区画）に由来する**。
   **exp_019 の実測に依存した定義**であり、**別の対照群を使うなら定義し直す必要がある**
5. **6 seed は「迷路の引き」については独立していない**（exp_021 と同じ）。
   **3 群とも同じ迷路の列を走る**ので、**seed 間のばらつきが表すのは方策の初期化と PPO の乱数だけ**
6. **判定は 6 seed の中央値（P1・P4）と 120 走行の件数（P2・P3）である**。
   **n = 6 で判別できるのはほぼ完全に分離する差までである**
7. **🔴 (A) が支持されても「短期文脈は原理的に効かない」ことにはならない**。
   **遅れの組・窓の長さ・与え方はこの 1 通りしか試していない**（exp_021 の限界 2 を引き継ぐ）
8. **符号検定などで棄却できなかったことは「効果が無い」の証拠ではない**
   （**准教授の軌跡追跡の限界の記録を引き継ぐ**。**n が小さいときの棄却失敗は、
   帰無仮説が正しいことを意味しない**）
9. **6×6・検証用の 20 迷路・迷路モード `loop`** の範囲での判定である
10. **🔴 にせ履歴は (B) の分離として不完全である**（**准教授 AUDIT_045 要記載 1・採択**）:

    | | exp_021 | にせ履歴（本実験） |
    |---|---|---|
    | 入力の次元 | 153 | **153（同じ）** |
    | 第 1 層のパラメータ数 | 153 × 128 | **同じ** |
    | **独立に変動する値の数（入力の共分散の階数）** | **最大 153** | **🔴 17**（**9 ブロックが完全に同一**） |

    **(B) は「入力次元 9 倍の最適化難」の説だが、最適化の難しさは次元の数ではなく
    入力の共分散の構造（条件数）に効く。**
    **完全に相関した 9 個の複製は、独立に変動する 136 次元とは別の最適化問題である**
    （**複製では 9 ブロックの勾配が同一になり、その方向の実効的な学習率が 9 倍になる**。
    **「難しさが同じ」でも「小さい」でもなく、別種の難しさ**である）。

    > **したがって、にせ履歴群が対照群と同等だったとき確定するのは
    > 「完全に相関した 9 個の複製を足すことは無害だった」までであり、
    > 「変動する 136 次元を足すことは無害だった」ではない。**
    > **後者への外挿は仮定である。**

    - **雑音を入れる代替が優れているとは言わない**: **雑音は次元も変動も揃うが、
      方策が当てにいける新しい変動源を持ち込む**という別の難点がある。
      **どちらが良いかは自明ではない**
    - **本実験は「完全に相関した複製」を選ぶ。**理由は
      **「情報がゼロである」ことが構成上明らかで、単体テスト S1・S4 で直接検査できる**からである
      （**雑音では「方策がその雑音を当てにしていないか」を別に検査する必要が出る**）
    - **これは §3-7 の (A)/(C) の限定と同じ型である**（**准教授の指摘**）。
      **同じ規律を (B) にも当てた**

---

## 7-bis. 第 2 弾 B（LSTM）への申し送り

1. **🔴 (C) 汚染の説と同型の設計判断が、LSTM では必ず出る**:
   **「リスポーンのときに隠れ状態をリセットするか」**である。
   - **リセットしない** ＝ **exp_021 と同じ汚染**（**瞬間移動の前後がひと続きの文脈になる**）
   - **リセットする** ＝ **立て直しのたびに文脈を捨てる**（**「立て直したあとどう走るか」の学習が難しくなる**）
   - **どちらを採るかは実験の問いに関わる設計判断**であり、**既定に任せてはならない**
   - **`sb3-contrib` の `RecurrentPPO` はエピソード境界で隠れ状態をリセットするが、
     リスポーンはエピソード境界ではない**（**環境 v2 のリスポーンは走行を続ける**）ので、
     **既定では汚染される側になる**（**未確認。第 2 弾 B の起票時にコードで確かめること**）
2. **(A) と (C) の正面対決の設計案**（**第 2 弾 B の起票で判断する**）:
   **同じ方策構造で「リスポーンで文脈をリセットする群」と「しない群」を比べる**。
   **これは本実験（にせ履歴）では作れない対比**である
3. **観測の次元の手当て**は、**本実験の (B) の判定に従う**
4. **🔴 対照の取り方（教授裁定 2026-08-15・本実験の判定を受けて。起票時の必須検討）**:
   > **LSTM の対照は「素の 17 次元（exp_019）」ではなく「前向きの 153 次元（exp_021）」に置く。**
   > **そうしないと、容量の効果と再帰構造の効果が交絡する。**

   **根拠は本実験の P2 である**: **履歴の情報がゼロでも、容量を増やすだけで
   浅い裾（5 区画の壁越え）が 3 → 28 件に増えた。**
   **「LSTM で裾が厚くなった」は、容量だけでも起こる。**
5. **🔴 深さの層別の判定量を、閾値の根拠つきで事前登録する**（同・教授裁定）:
   > **本実験の探索的所見「5 区画は容量で越えられるが、7 区画以上へ深く入る（12 件）のは
   > 履歴の情報を持つ参照群だけ」を、第 2 弾 B では判定量として事前登録する。**

   - **閾値は構成上の根拠を持つものを選ぶ**。**5 区画は「対照群が 1 区画 400 歩で
     2000 歩の上限に当たる ＝ 構成上の天井」という根拠がある**
   - **7 区画は本実験では事後に選んだ値**であり、**そのままでは使えない**。
     **第 2 弾 B の起票時に根拠を立て直すこと**

## 7-ter. 🟢 投入前 3 件の結果（2026-08-15・**すべて合格**）

| 項目 | 結果 |
|---|---|
| **① 不活性の bit 一致検証** | **✅ 合格**。**両フラグを渡さず 10 万歩**（`logs/exp_022_bitcheck/`）→ **先頭 51 エピソードの全 8 列が exp_019 seed 1 と一致**（不一致 0 件）。参照側に**ゴール 2 件・リスポーン経験 50 本**を含み空振りではない。出力 `outputs/exp_022_bit_identity.json` |
| **② にせ履歴の等価性検査** | **✅ 合格**。**単体テスト S1**（reset 直後・20 歩後とも全ブロックが現在の素の観測と bit 一致）と **S4**（過去の観測が 1 要素も混ざらない）で確認 |
| **③ 単体テスト** | **✅ S1〜S6 が 6/6 PASS**（`tests/test_sham_history.py`）・**T1〜T9 が 9/9 PASS**（`tests/test_obs_history.py`） |

**あわせて退行を確認した**（**`sham` の追加が既存の経路を動かしていないこと**）:
**新版で対照群 6 本を測り直し、既存の出力と `metrics` + `p5` の 1,708 項目で不一致 0 件・集約値も完全一致。**
**新設の判定量も既知の値を再現した**（`n_runs` = 120／`n_reach_ge5` = 3／`n_goal_rollout` = 0／
到達区画数の分布 `{0:7, 1:37, 2:12, 3:22, 4:39, 5:3}`）。

## 7-quater. 🟢 投入の記録（2026-08-15）

| 項目 | 内容 |
|---|---|
| **投入時刻** | **2026-08-15 00:49:50 JST**（6 本同時） |
| **PID** | **21620 / 21622 / 21624 / 21626 / 21628 / 21630**（seed 1〜6 の順） |
| **投入版（プロセスが記録した値を正とする）** | **`987ad40399e1eac2f7b21b4f00af83f3979ba5ed`**（**6 本一致**。投入直前に読んだ HEAD とも一致） |
| **設定の同一確認** | **6 本とも**「観測空間 = Box(-inf, inf, (153,), float32)」「観測履歴の遅れ = (1,2,4,8,16,32,64,128)」「**にせ履歴 = 有効（遅れの位置に現在の観測を複製・情報ゼロ）**」「環境の版 = v2」「評価環境の版 = v2」「距離カリキュラム = 無効」 |
| **`caffeinate -i -w <PID>`** | **6 本とも付与済み** |
| **記録先** | `logs/exp_022_seed{1..6}/` ／ モデルは `models/exp_022_seed{N}.zip` |
| **完走の見込み** | **約 70 分**（**exp_021 の実測 64〜73 分**が基準）→ **2026-08-15 02:00 頃**。**学生A が CPU 1 本を使用中**（25〜30 分の走行）なので数分伸びうる |
| **CPU の調整** | **学生A へ連絡済み**（**互いに止めない取り決め**。8 コアで 6 ＋ 1） |

## 7-quinquies. 🟢 報告トリガーの実行結果（2026-08-15 01:26・**作動せず**）

**条文（§4-2）どおり、80 万歩の退避重み 6 本を測り、exp_019 の同じ 80 万歩の退避重みと比べた。**

| 項目 | 値 |
|---|---|
| にせ履歴群 6 seed の `net_progress_per_1000`（昇順） | **1.00, 1.25, 1.50, 1.50, 1.50, 3.50** |
| **にせ履歴群の中央値** | **1.50** |
| 対照群の中央値（80 万歩） | 1.25 |
| **発火線（対照の 0.5 倍）** | **0.625** |
| **判定** | **✅ 作動せず**（1.50 > 0.625） |

**出力**: `outputs/exp_022_driving_sham_800k.json`。**6 本とも `num_timesteps` = 800,000・
遅れ (1,2,4,8,16,32,64,128)・`history_sham` = true** を記録から確認した。

**⚠️ 以下は同じ測定の副産物であり、条文の判定量ではない（探索的・80 万歩時点の途中の値）**:

| 量（**80 万歩時点**） | にせ履歴（exp_022） | 対照（exp_019） | 参照（exp_021） |
|---|---|---|---|
| `net_progress_per_1000` | 1.500 | 1.250 | 1.125 |
| `respawn_per_1000` | 0.500 | 0.375 | 1.750 |
| `steps_per_entry` | 192.7 | 281.7 | 138.8 |
| `entries_per_distinct_cell` | 2.333 | 1.587 | 2.875 |
| 5 区画以上到達 | 17 / 120 | **未測定** | **未測定** |
| rollout のゴール件数 | 0 | **未測定** | **未測定** |

- **🔴 これらは判定ではない。**§3 の P1〜P4 は**すべて最終方策（200 万歩）で判定する**。
  **途中の値で予測を読み替えない**（**条文も錨も投入前に固定してある**）
- **記録する理由**: **投入後に測ったものを後から都合よく採らないため、
  測った以上はその場で全部書き出しておく**（exp_021 §10-1 と同じ扱い）
- **⚠️ 対照群・参照群の「5 区画以上到達」「ゴール件数」が未測定なのは、
  この 2 つの判定量が exp_022 の起票時に新設されたものだからである**
  （**§6 手順 5 のとおり、判定の前に 3 群すべてを同一版で測り直す**）
- **⚠️ 解釈は完走後の最終方策の値を見るまで保留する**（**80 万歩は学習の途中**である）

## 8. 投入手順（**GO 後にそのまま使う。この呼び出しに待ちを入れない**）

```bash
cd /Users/kouhei/tmp/github/micromouse_rl
for N in 1 2 3 4 5 6; do
  mkdir -p logs/exp_022_seed$N
  OMP_NUM_THREADS=1 nohup .venv/bin/python \
    experiments/exp_012_continuous_potential/train.py --condition E --env-version v2 \
    --action-highpass-penalty 0 --seed $N \
    --obs-history "1,2,4,8,16,32,64,128" --obs-history-sham \
    --log-dir logs/exp_022_seed$N --model-out models/exp_022_seed$N.zip \
    > logs/exp_022_seed$N/train.log 2>&1 &
done
```

**投入後（別々の呼び出しで）**: `ps` で 6 本の生存と PID → 設定出力の同一確認
（**「にせ履歴 = 有効」の行を含む**）→ `caffeinate -i -w <PID>` を 6 本 →
**准教授へ版通知** → **教授へ投入成立の報告** → **学生A へ CPU 使用の連絡**。
