# 研究計画: センサ→モータ直接学習によるマイクロマウス

制定: 2026-08-10（教授セッション）。本計画は研究プログラム全体の基本方針であり、変更は教授セッションの判断でのみ行う。

## 1. ミッション定義

- **最終目標**: マイクロマウスクラシック競技ルールに準拠した 16x16 未知迷路を全自動で完走し、スタート→ゴールの走行時間を競う。持ち時間内に複数回走行でき、**完走した走行のうち最速の 1 走行のタイム**が成績。
- **方策の入出力契約（不変）**:
  - 入力 = 車載センサのみ（距離センサ、ジャイロ、加速度計、車輪角速度）。真の自己位置・迷路地図・ゴール方向などの特権情報は使用禁止。
  - 出力 = **左右モータ電圧指令（2次元連続値）**。「目標速度」などの中間層や、プログラムされた直進・旋回プリミティブを最終方策に含めない。
- **学習手段の制約（段階的解禁）**: まず報酬のみの純粋な強化学習（カリキュラム学習は可）。§8 の停滞基準を満たした場合に限り、模倣学習・特権教師からの蒸留を解禁する。解禁後も入出力契約は不変。自己教師あり補助タスク（深度予測・ループクロージャ検出など、外部の教師データ・特権情報を使わないもの）は純 RL 段階でも使用可（2026-08-10 裁定、`research_notes/note_002` 参照）。
- **報酬に迷路地図を用いることの扱い（2026-08-10 裁定）**: 報酬は学習時にのみ計算され方策の入力ではないため、**ゴールまでの迷路距離をポテンシャル整形に使うことは入出力契約に触れない**（M1 でも使用済み）。ただし条件: (1) ポテンシャル整形の形で使い最適方策を変えないこと、(2) 実験カードに明記すること、(3) **最終報告で「探索を学習した」と述べる際は「方策は実行時にセンサのみを使うが、学習時の報酬には迷路地図を用いた」と正確に併記すること**、(4) 将来、疎な報酬（ゴール到達のみ）でも学習が成立するかを別実験で確かめる（将来課題）。
- **「規約既知の情報」と「特権情報」の線引き（2026-08-10 裁定）**: **ゴールが迷路中央 2x2 にあることは競技規定に明記された既知情報**であり、方策がこれを前提にしてよい（実機のマウサーも同じ）。一方、**いま自分がどこにいるか**は方策が自力で推定する（真の自己位置を環境から与えない）。環境が真の位置から計算した訪問済み情報を渡すのも特権情報として禁止。自己位置推定にはオドメトリ（車輪角速度・ジャイロの積分）を用いる。
- **計算資源**: Apple M3 / 8コア / 24GB RAM の Mac 1台のみ。実験は直列実行し、1 本の学習は数時間〜一晩を上限に設計する。
- **副次目的（教育・学術、2026-08-10 追加）**:
  - **教育**: 四足・二足歩行ロボットが RL で運動を獲得する時代に、「どこまでが古典制御で、どこからが学習による制御か」を、最小構成（アクチュエータ 2 個）のマイクロマウスで追体験できる教材を作る。§7 の自律度ラダーが教材の背骨。
  - **学術**: 本ミッションは公開研究に達成例がない可能性がある。研究ノート（`research_notes/`、§10）を常時作成し、文献調査で新規性を評価し、最終的に論文体の報告書にまとめる。

## 2. 評価規約（全アプローチ共通・凍結）

評価ハーネスは一度確定したら変更しない。全アプローチを同じ物差しで比較する。

- **評価迷路（2026-08-12 改訂・v4 帯を凍結）**: 評価帯は `competition/mazes/eval/`（v4・2026-08-12 凍結）、検証帯は `competition/mazes/validation/`（v4・評価帯と同一の受理条件・別の候補プールから生成）の各 20 迷路。受理条件（規定 6 項目・最終 $D$ の窓・経路比 $R$ の層別・候補経路数 $K$ の階級別 {4,8,8}）と限界 3 点（時間差の右裾の欠落＝ユーザ許容済み／橋の割合の乖離・因果未確認／$\beta$ の散らばりの狭さ）は各帯の README と `freeze_record.json`（npz の SHA-256・消費 seed・受理率）を凍結記録とする。**壁配列 npz を一次成果物（真実の源）**とし、MuJoCo XML は npz とロボット仕様から再生成可能な派生物とする。
  - **採用 seed は連番ではない**（生成器が受理条件でリジェクトサンプリングするため。eval は候補プール [1000, 20999]、validation は [21000, 40999] から採る）。**学習への使用禁止の対象は採用 20 seed ではなく候補プール全体 [1000, 40999]** — プール内の seed は「選ばれなかった」だけで評価帯と同じ生成過程にあり、帯の再作成でも使われうる。旧記述「評価迷路 = seed 1000〜1019・学習用 = 2000 以降」は 1 面 1 seed を消費していた旧生成器の前提であり**失効**（2026-08-12）。
  - **v3 帯と v4 帯は 20 面中 10 面を共有する**（候補プールの拡大のみで生成手順が同じため。npz ハッシュ照合済み）。**両帯の成績を「独立な 2 つの帯での結果」として扱わないこと**（2026-08-12・学生A の照合）。
- **迷路の規格（2026-08-10 全面是正・出典つき）**: 生成器は以下を全て満たすまでリジェクトサンプリングし、自動検査に合格してから凍結する。旧版（DFS 完全迷路 + 中央開放）は**規定違反**であったため `eval_v1_nonconforming/` へ退避し記録として保存する。
  - 寸法: 18 cm 角の単位区画、16×16 区画、壁の高さ 5 cm・厚さ 1.2 cm（NTF 規定 2-2）
  - ゴール: 中央の 4 区画（同 2-3）。**ゴール 4 区画内には壁も柱も存在しない**（同「注意」9）。すなわち**ゴール中央の格子点 1 本のみ柱がなく**、それ以外の格子点には最低 1 枚の壁が接する（同 2-4「終点の中央を除いたすべての格子点には少なくとも 1 つの壁が接している」）。外周壁は全て存在（同 2-4）
  - **ゴールの入口は 1 箇所のみ**（IEEE R2 SAC 規定 4.3 "The destination square has only one gateway."）。NTF 規定に明文はないが実競技の慣例と一致するため採用
  - **壁づたい走行（左手法・右手法）でゴールに到達できない配置**（IEEE 規定 4.5 "The destination square will be positioned so that a wall-hugging mouse will NOT be able to find it."）。NTF 規定に明文はないが、これを満たさないと探索アルゴリズムの能力を評価できないため必須要件とする
  - **複数経路（ループ）を持つ**（IEEE 規定 4.5 "Multiple paths from the starting square to the destination square are allowed and should be expected."、2026-08-10 ユーザ裁定）
  - スタート区画は四隅のいずれか・3 方向が壁（IEEE 規定 4.3）
  - **経路長の受理窓（2026-08-11 追加・教授裁定）**: 真の最短距離 $D_0$ が **[45, 110] 区画**に入るまでリジェクトサンプリングする。旧生成器は $D_0$ 中央値 20 区画で、大会実迷路（中央値 63）の 1/3 しかなく競技の実態を代表していなかったため（`docs/MAZE_DIFFICULTY_REPORT.md` §5 の案 3 を採用）。実測の消費試行は 20 面あたり 46〜54 回（1 面あたり中央値 2 回）。
    - **この窓は大会実迷路 42 面のうち 33 面（78.6%）しか含まない**。除外されるのは下限 45 未満の 5 面（$D_0$ = 40, 41, 41, 43, 43）と上限 110 超の 4 面（115, 191, 231, 249）で、**合計 9 面 21.4%**。上限超のうち 191 / 231 / 249 の 3 面は $\beta$=1 のほぼ一本道で、持ち時間 420 秒では 5 走行を成立させられない。上限はエピソード長と学習コストからの実務的な頭打ちである。
    - **ただし窓で切っても大会分布の中央値は 63 で変わらない**（四分位は 51 / 84 → 54 / 75 と縮む）。窓は分布の中心を歪めていない。**両裾 21.4% を捨てていることは最終報告で限界として明記する。**
    - 参考（将来の見直し候補）: 窓を [40, 115] にすれば保持率 92.9%・中央値 60 になる。現時点では中央値が既に整合しているため再生成はしない。
- **競技プロトコル**（2026-08-10 詳細確定）:
  - **持ち時間 420 秒（＝7 分、シミュレーション時間）、最大 5 走行**。走行回数は「スタート区画から出発した回数」で数える。
    - 出典: 公益財団法人ニューテクノロジー振興財団「クラシックマウス競技規定」3-6「マイクロマウスは**7分間の持ち時間**を有し、この間**5回まで**の走行をすることができる」、同 3-1「マイクロマウスが始点から終点への走行に要した**最短の時間**をそのマイクロマウスの迷路通過時間記録とする」（https://www.ntf.or.jp/?page_id=118 ）。**2026-08-10 是正**: 当初 300 秒としていたのは規定違反だった（教授セッションの設定誤り）。
  - **1 走行の定義と計時（2026-08-10 ユーザ教示により厳密化）**: 1 走行とはスタートから始まる走行を指す。
    - **開始**: **機体の先端**がスタート区画と隣の区画との境界（＝**スタートセンサ**の光軸位置）を通過した時点。この瞬間から走行タイムの計測が始まる。
    - **終了（計時）**: **機体の先端**がゴール区画の入口（＝**ゴールセンサ**の光軸位置）を通過した時点。ここまでが走行タイム。
    - **走行成立の条件（2026-08-10 ユーザ教示・重要）**: 計時はセンサ通過で終わるが、**その走行が有効と認められるには機体全体がゴール区画内に完全に入る必要がある**。先端がセンサを横切っただけで引き返した・入り切らなかった場合、**その走行は無効（ゴール未達）**として扱う。したがってゴール判定は 2 段階:
      1. 先端がゴールセンサを通過 → **タイムを暫定確定**
      2. 機体全体がゴール区画（中央 2x2）の内側に入る → **走行成立**（このタイムが記録として有効になる）。さらに区画内で停止する
      2 が満たされないまま区画を出た場合は暫定タイムを破棄し、その走行は失敗とする。
    - **判定は機体中心ではなく先端・機体全体**で行う（実競技のセンサは光軸を遮った瞬間に反応するため）。出典: NTF 規定「注意」8「光軸は水平であり、床面より 1cm の高さにある。位置：始点のセンサ 始点の区画と次の区画との境／終点のセンサ 終点の入口部分」
    - 走行タイムは 1 で確定しているため、完全進入・停止までの時間はタイムに含まれない。
  - **走行間の扱い**: ゴール到達後は**自走でスタートへ戻る**（帰路に探索を続けるのも可）。**この帰路の間も持ち時間の時計は止まらず動き続ける**。スタート区画に戻って再出発し、**再びスタートセンサを横切った時点で次の走行が始まる**。
  - 衝突・転倒、またはスタック時は「係員回収」に相当する再配置をスタート区画へ行う。当該走行は失敗。再配置後の再出発は新たな走行として数える。**スタックの定義（2026-08-10 意味論明確化）**: 直近 20 秒間の全時刻で、現在位置を中心とする半径 5 cm 圏内に留まり続けた場合（＝閉じ込め判定）。窓の端点間変位で判定してはならない — 探索で同じ地点を再訪しただけの走行を誤検出するため（実際に旧実装で 21 件の誤検出が発生し是正した。評価器の protocol_version に記録）。
  - **方策の内部状態（記憶）は持ち時間を通じて保持してよい**（再配置でもリセットしない）。評価器は走行境界・再配置を方策にフックで通知する。
- **成績（2026-08-10 指標分離）**: 「ゴールできたか」と「最短走行を成立させたか」は競技上まったく別の達成であり、**1 つの数字に混ぜない**。迷路ごとに以下を独立に記録し、5 指標すべてを併記して報告する。
  - **(a) ゴール到達率**: 持ち時間内にゴール区画へ到達した走行が 1 回以上ある迷路の割合（＝一般に言う「完走率」）。
  - **(b) 最短走行成立率**: 初回ゴール到達**より後に開始**した走行で再びゴールへ到達した迷路の割合（＝探索で得た地図を使って走り直せたか）。帰還が間に合わず持ち時間切れになった面はここで落ちる。
  - **(c) 有効最短走行率**: (b) のうち、最短走行タイムが探索走行タイムより **10% 以上短い**迷路の割合（＝地図・記憶が実際にタイムへ効いたか）。走り直しても速くなっていない面はここで落ちる。
    - **二値化した割合だけを報告しない（2026-08-11 追加・必須）**: (c) は「10% 以上」で二値化しているため、**短縮率 9% と 0% が同じ「不合格」**になり、閾値が現象を潰す。**必ず「短縮率 = 1 − 最短走行タイム / 探索走行タイム」の分布（中央値・四分位・最大・n）を併記**する。
    - **面ごとに対応をとった比の中央値**を出すこと。**中央値どうしの比とは一致しない**（実際に教授セッションが取り違え、L0-a の短縮率を 7.2% と報告した。正しくは **1.9%** で 3.8 倍の乖離があった。2026-08-11）。
    - (b) も同様に、割合だけでなく**「探索後に成立した走行回数」の分布**を併記する。
    - **(c) を 2 つに分解して報告する（2026-08-11 追加・必須）**: 時間短縮は「経路が短くなったこと」と「同じ経路を速く走れたこと」の積である。**両者を分けないと、何が効いたのかが分からない。**
      - **(c1) 経路短縮率** ＝ 1 −（最短走行の実走経路長 / 探索走行の実走経路長）
      - **(c2) 速度向上率** ＝ （最短走行の平均速度 / 探索走行の平均速度）− 1
      - **分解の関係**: タイムは 経路長 ÷ 速度 なので、**(c) の時間短縮は (c1) と (c2) の積に分解される**（恒等式 $1 - t_\text{最短}/t_\text{探索} = 1 - (1-c_1)/(1+c_2)$。独立実装で 217 面すべて最大絶対誤差 2.1e-16 で成立を確認、2026-08-11）。(c2) を「時間短縮率」と定義してはならない — それは (c) そのものであり、分解にならない。
      - **⚠️ 恒等式が成立するのは面単位であって、中央値どうしでは成立しない。**中央値は積について閉じないので、報告された (c1) と (c2) の中央値から (c) の中央値を復元しようとすると 1〜3 ポイントずれる。**検算する者が混乱するので、表には必ずこの注記を添えること。**
      - **⚠️ 記号の衝突についての注意（2026-08-11）**: 同日、**別のセッションから「(c2) = 時間短縮率 1 − t_最短/t_探索、かつ (c1)(c2) の両方を M4 の gate に入れる」という通知が学生A・准教授へ届いた**。**その定義は無効**であり、本節の定義（(c2) = 速度向上率、gate は (d) と併用）が正本である。同じ記号が 2 つの量を指す状態が一時的に生じたので、**過去の報告に現れる (c2) は、どちらの定義かを確認すること。**
      - **経緯**: 独立検証により、評価帯における (c) の方式間差（L0-a 10% / L0-b 30% / L0-c 100%）は**ほぼ全部が (c2) 由来**であり、**(c1) は 3 方式とも 0.4〜1.6% しかない**ことが判明した（2026-08-11）。つまり当時の評価帯では「地図を得ても経路はほとんど短くならない」状態で、(c) は実質的に速度だけを測っていた。**(c) だけを見て「地図が効いた」と読んではならない。**
      - **(c)・(c1)・(c2) のいずれも、単独でマイルストーンの gate に使わない（必須）。3 つとも別々の抜け道を持つ。**
        - **(c) と (c2) の抜け道**: **探索走行をわざとゆっくり走る**と、分母が大きくなって値が上がる。速く走れるようになったわけではない
        - **(c1) の抜け道**: **探索走行でわざと遠回りする**と、分母の経路長が伸びて値が上がる。地図が効いたわけではない
        - **したがって gate に使う場合は必ず (d)（絶対タイム）と併用する。**絶対タイムを条件に入れれば、分母を悪化させる抜け道は自分の首を絞めるので閉じる
        - 旧 gate「(c) ≥ 75%」を撤回した理由がこれである。**分解しただけでは穴は塞がらない**（2026-08-11、旧・教授セッションの指摘により明確化）
  - **(d) 最速タイム**: 完走走行の最速値。中央値と分布で報告。探索走行タイムと最短走行タイムも分けて記録する。
  - **(e) 初回最短走行効率（2026-08-10 追加）** ＝ **初回の最短走行タイム ÷ その迷路での最良タイム**。1.00 が理想（1 回の探索で最良の経路に到達できた）、大きいほど探索が不十分で走り直しを要したことを意味する。初回の最短走行が成立しなかった面は未定義として別途件数を記録する。
    - **追加の理由**: (d) は 5 走行を使い切った後の値なので、**探索の質を測れない**。実際 L0-a では、探索戦略を E0（ゴール到達で打ち切り）から E1（追加探索ループ）に改良しても (a)〜(d) がまったく変わらなかった一方、**初回の最短走行で最良タイムに到達した面は 8/20 → 20/20 に激変**した（最大改善 maze_1011: 66.57 → 26.41 秒）。**5 走行という余裕が E0 の探索不足を隠していた**わけで、これを可視化する指標が必要である。
    - 競技的にも意味がある（持ち時間が厳しい状況や低速な機体では走り直しの回数を確保できない）。M4（走行間記憶）の評価にも直結する。
    - **(e) が測定にならない条件（2026-08-11 追加・必須）**: 探索後に成立した走行が **1 回だけ**で、かつ**最良タイムがその走行**である場合、(e) は定義上必ず 1.00 になり、**測定になっていない**。この場合は **1.00 と書かず未定義**として扱い、**該当面数を必ず併記**する。したがって **(e) は「探索後に成立した走行回数」の分布と必ずセットで報告**する。
      - ただし探索後の走行が 1 回でも、**その走行が探索走行より遅ければ最良タイムは探索走行側になり (e) > 1 となる**ので、そこは情報を持つ。機械的に「走行回数 1」だけで未定義に落とさないこと。
      - **経緯（2026-08-11）**: (e) は「**5 走行という余裕が探索不足を隠していた**」ことへの対策として追加された指標だが、迷路の経路長を大会実迷路の水準へ是正した結果、**持ち時間の余裕が消えて逆向きに壊れた**（L0-a では 20 面中 9 面が該当し、報告値 1.00 はその 9 面を含んでいた）。**走行回数に依存する指標は持ち時間と交絡する**という同じ根から出た、反対向きの故障である。指標を追加して欠陥を可視化したつもりが、動作点が変わると新しい指標の方が先に壊れた。
  - **(e') 初回最短走行の経路効率（2026-08-11 追加・こちらを主として報告する）** ＝ **初回の最短走行が通過した区画数 ÷ $D_0$**。1.00 が理想（1 回の探索で真の最短経路を引けた）。
    - **⚠️ 距離（m）で正規化してはならない（2026-08-11 是正）。**当初この指標を「実走経路長 ÷（区画寸法 × $D_0$）」と定義したが、**実測すると理想的な走行でも 1.00 にならない**（L0-a の最短走行で中央値 **0.948**、範囲 0.927〜0.983）。原因は 2 つあり、いずれも避けられない: (i) 計時が**スタートセンサ通過からゴールセンサ通過まで**なので両端が切り取られる、(ii) 旋回で**内側を回る**ぶん、区画中心を結んだ折れ線より実走距離が短くなる（旋回が多い経路ほど短く出る）。**距離で正規化すると、経路の良し悪しではなく旋回の多さを測ってしまう。**
    - **区画数で正規化すれば、完璧な走行がちょうど 1.00 になる。**整数の数え上げなので、切り取りにも内回りにも影響されない。
    - **(e) との違いと、こちらを主とする理由**: (e) は**タイム**を最良タイムと比べるため、**探索後の走行が 1 回しかないと定義上 1.00 に固定される**（上記の退化）。(e') は**経路長**を理論最短と比べるので、**走行回数に依存せず、常に測定になる**。指標の目的（1 回の探索で最良の経路に到達できたか）にも (e') の方が忠実である。
    - **(e) も併記する**（過去との連続性、および「経路は最短だがタイムが伸びた」場合の検出のため）。**(e') が 1.00 に近いのに (e) が悪い面は、経路ではなく走り方に問題がある**ことを示す。
    - 実測（2026-08-11、独立検証）: 大会実迷路帯では L0-a の (e) が **18 面すべて未定義**になる一方、(e') は全面で定義される。
  - **旋回回数 $n_\text{turns}$ の定義（2026-08-12 裁定 R4・これが正本）**: 方式・方策をまたぐ比較および対外報告に使う旋回回数は、**実走の区画列から進行方向の変化を数える**（90° 転回 = 1、180° 反転 = 2）。回転量そのものが必要な場合は**総回転角（$|\Delta\psi|$ の積算・リセットなし）**を別指標として併記する。
    - **「ヨー角の累積が ±45° を超えるたびに数え、符号反転で累積を捨てる」数え方は方式間比較・対外報告に使用禁止**（方式内の診断に限り可。その場合も $n_\text{turns}$ の名を使わない）。理由: リセット規則が超信地旋回で累積回転角の約 42% を破棄し、**同一経路を走る 2 方式に約 1.8 倍の系統差**を与える（実際の回転量の差は 1.17 倍。2026-08-12 実測、`research_notes/data/nturns_defs_*.json`）。系統差の大きさが走り方に依存するため、**方式間比較の場面でだけ壊れる**。

> ⚠️ **2026-08-12 更新（教授）**: 評価帯・検証帯は **v4 として再生成・凍結済み**（本節冒頭）。
> 以下の数値は**旧帯（是正前。真の最短距離が中央値 20 区画。大会実迷路は 63 区画）**で
> 測ったもので、現行帯では**無効**。**新規の判断・比較の基準に使わないこと。**
> 記録として残すため値そのものは削除しない。E1・E2 × 4 腕の再評価（実施中）の結果で教授が改訂する。

- **L0-a（古典ベースライン・超信地旋回走行・区画ごと停止）の実績値**: 2026-08-10、**規定準拠迷路 × 持ち時間 420 秒**で確定。

  | 指標 | 旧（規定違反迷路・300 秒） | **確定値（規定準拠・420 秒）** |
  |---|---|---|
  | (a) ゴール到達率 | 100% | **100%**（20/20） |
  | (b) 最短走行成立率 | 60% | **100%**（20/20） |
  | (c) 有効最短走行率 | 5% | **60%**（12/20） |
  | (d) 最速タイム 中央値 | 74.62 s | **22.86 s**（最速 15.09 / 最長 35.05） |
  | 探索走行 / 最短走行 中央値 | 74.62 / 56.78 s | **28.31 / 23.34 s** |
  | **(e) 初回最短走行効率** | — | **1.00**（E1 標準搭載時。20/20 面で初回の最短走行が最良に一致） |

  **探索戦略による差（E0 → E1）**: (a)〜(d) は不変（最速タイム中央値 22.87 → 22.86 s）だが、**初回の最短走行で最良に到達した面が 8/20 → 20/20**、初回最短走行タイム中央値が 28.01 → 23.39 s に改善。5 走行の余裕が E0 の探索不足を隠していたため、指標 (e) を追加した（上記）。確定判定の理論も 20/20 面で実証済み（確定時の楽観距離＝真の最短距離、確定までの訪問区画は中央値 54/256＝21%）。

  **(c) が 5% → 60% に変わったことが本質**。旧迷路は壁づたい走行で解け、足立法が「未知の壁は通れる」と仮定して 1 走目からほぼ最短を引けたため、走り直しても短縮しなかった（＝探索に意味がなかった）。規定準拠迷路では 1 走目は遠回りせざるを得ず、地図を得てからの最短走行が実際に速くなる（短縮率の実例: maze_1004 が 64.1→28.9 s で 55% 短縮）。**「探索して地図を作り、それを使って最短を走る」という競技の本質が、ようやく評価に現れる土俵になった。**
- **中間指標**（マイルストーン判定用）: 単走完走率、平均到達セル数、衝突率、経路効率（実走行距離 / 最短経路距離）。

## 3. 現状の課題（2026-08-10 調査結果）

1. 既存の階層構造（Phase1 凍結 + Phase3 が目標速度を出力）は「モータ指令をゼロから学習」という目標に対して妥協しており、さらに Phase1 へ渡すセンサ値を偽装するハックに依存している。
2. 報告されている Phase3 成功率 85% はモデル・ログが残っておらず再現不能。
3. ドキュメントとコードの数値乖離が複数箇所（報酬定数、観測次元、モデルパス）。
4. ロボット物理モデルに非現実的な質量（車輪・モータ各 50g、総重量 233g 相当）。モータは電気ダイナミクスのない簡略トルクモデル。前方正面センサがない。
5. 報酬設計がアドホックな試行錯誤に陥っていた（直近コミットで自己診断済み）。
6. 16x16 迷路での学習・評価は未着手（最大 7x7）。

## 4. ロボット仕様（M0 で確定・以後凍結）

- **寸法**: 現行踏襲（車幅 72mm トレッド、車輪半径 13.5mm）。
- **質量**: 実機クラシックマウス相当に修正。総重量 90〜120g 目標（車輪 ~5g、モータ ~15g、電池相当の質量を車体に付与）。慣性も整合させる。
- **モータモデル**: 電圧指令を正直にする。DC モータの電気モデルを環境側で計算しトルクとして印加する:
  $$\tau = \frac{K_t}{R}\left(V - K_e\,\omega\right)$$
  （$V$: 印加電圧 [V]、$\omega$: 車輪角速度 [rad/s]、$R$: 巻線抵抗 [Ω]、$K_t$: トルク定数 [N·m/A]、$K_e$: 逆起電力定数 [V·s/rad]）。小型 DC モータの現実的な定数を選定し `docs/ROBOT_SPEC.md` に根拠とともに記録。
- **センサ**: 距離センサ **4 本（LF, RF, LS, RS）**。cutoff 0.3 m（16x16 では長い直線での前方視程が重要なため、旧 0.15 m から延長）。ジャイロ・加速度計・車輪角速度は継続。ノイズは当面ゼロだが、ノイズ注入フックを実装しておく（後の頑健化用）。
  - **2026-08-10 ユーザ裁定（r6）**: 監査所見 U2 を受けて一時 6 本（前方正面ペア FL/FR を追加）としたが、**実機マイクロマウスの大多数が 4 灯で走行できている**ことを根拠に 4 本へ戻す。センサ本数は学習難易度に直結するため、**実機で実現可能な構成を超える情報を方策に与えない**ことを優先する。6 本版の学習結果は「センサ本数のアブレーション」として研究ノートに保存する。
- **制御周期**: 高レベル刻み 100Hz を初期値とする（変更する場合は実験カードに記録）。
- **検証（2026-08-10 追加）**: 本節の数値は暫定であり、物理モデル監査に基づく `docs/MODEL_VERIFICATION_PLAN.md` が確定値と検証プロトコルの正本。同計画書の検証テストスイートに全合格して初めて仕様凍結とする。

## 5. マイルストーンと合格基準

| # | 内容 | 合格基準（gate） |
|---|------|------------------|
| M0 | 評価ハーネス + ロボット仕様確定 + 古典ベースライン + 速度計測 | 物理モデル検証スイート（`docs/MODEL_VERIFICATION_PLAN.md`）全合格、かつベースラインが評価 20 迷路中 18 以上完走しタイムが記録される |
| M1 | 低レベル運動の直接学習（廊下追従・旋回をモータ電圧で） | **分岐のない試験コース 20 本 ×5 試行（計 100 試行）を壁接触なしで完走する率 ≥90%**。コースは直線・90°ターン・S 字の組合せで、分岐がないため方策は「壁に当たらず前進する」だけで完走できる ＝ 目標方向等の特権情報を一切与えない。平均前進速度も併記。**→ 2026-08-10 達成（exp_003b: 完走率 0.92、1 区画 0.173 s ＝ L0-a の 7.2 倍速）。留保 3 件は下記** |
| M2 | 小迷路の単走 | 未知 **6x6** 迷路 単走完走率 ≥90%（2026-08-10 改訂: 5x5 では中央 1 区画がゴールとなり、実競技の「中央 2x2 の広場」と構造が違ってゴール認識の課題が M3 と別物になるため、偶数サイズへ変更）。中間段階として **M2-0（分岐はあるが行き止まりのない迷路）**を挟み、失敗時の原因をゴール認識に絞り込めるようにする |
| M3 | 16x16 単走完走 | ゴール到達率 (a) ≥50%（時間不問）。**M3 に着手する前に、報酬の割引後総収益を 16x16 のエピソード長で再計算し、ゴール > 探索 > 滞留 > 衝突 の順序が保たれることを確認すること（2026-08-11 追加）。M1・M2 の報酬をそのまま持ち込むと、$\gamma$=0.995 の実効地平 200 歩に対し 16x16 は約 1350 歩なので、ゴール側だけが $\gamma^T$ で消えて滞留との差が +0.017 まで縮む（M1 では +0.797）。** |
| M4 | 走行間記憶 | **有効最短走行率 (c) ≥75%**（20 迷路中 15 以上で、最短走行が探索走行より 10% 以上速い）。参考: L0-a は (c)=60%【再測定待ち 2026-08-11】 |
| M5 | 公式評価 | §2 の 5 指標すべてで古典ベースライン **L0-a / L0-b / L0-c と比較**。**暫定合格条件: (a) ≥90% かつ (d) 最速タイム中央値が L0-a 以下**。基準値は旧迷路での 22.86 s だったが、評価迷路の再生成により**停止中【再測定待ち 2026-08-11】**。新迷路での L0-a/b/c の再評価後に教授が再設定する。L0-c（スラローム走行）の基準値が確定した時点で、主たる比較対象を L0-c に切り替えて条件を再設定する |

### M1 達成時の留保（2026-08-10・M2 進行前に解消すべき課題）

1. **チェックポイント選択が必要だった**: 検証帯で 60 万ステップ時点が最良（0.96）、100 万まで回すと 0.83 に低下。gate も 0.92（60 万）と 0.89（100 万）に分かれる。「100 万回せば 0.92 が出る」という再現性ではない。
2. **報酬の滞留局所解が未解決**: 並列 1 でも 20〜25 万ステップで完走率 0.00 まで落ち、30 万で自力回復した。**滞留解は並列数によらない構造的な罠**であり、学習後半（85 万 0.95 → 100 万 0.85）でも圧力が効き続けている。exp_004（$\Phi$ オフセット）で根治する。
3. **行動振動が実機水準にない**: 左右モータ電圧の符号反転が 34.8 回/s（60 万）〜52.3 回/s（100 万）で、**学習前の乱数方策（53.3 回/s）と同水準**。100 Hz 制御で 2〜3 ステップに 1 回符号が反転する指令は駆動系に有害で、**実機に持っていけない**。学習が進むほど振動が増える傾向があり、速度を上げる過程で「振り回して走る」解に寄っている。M1 の gate 定義には含まれないが、**プログラムの目的（実機で成立する制御の獲得）に照らして M2 進行前に主題として扱う**。

#### 留保 3 件の状態（2026-08-11・独立検証による監査後）

当初この節は 3 件とも「解消」と記録したが、独立検証の反証監査（`verification/AUDIT_001_m1_clearance.md`）により**留保 1 は未解消、留保 2 は適用範囲を限定、留保 3 は表現が過大**と判明したため、以下のとおり改訂する。

1. **留保 1（チェックポイント選択が必要だった）**: **未解消。問題が別の層へ移った。**
   - チェックポイントを選ぶ必要は消えた（最終モデルで判定できる）。
   - **しかし採用条件（案 3・$k=8.7\times10^{-3}$）は 3 seed 中 2 seed しか成功していない**（seed1 完走率 0.970 / **seed2 0.020** / seed3 0.940）。**seed2 は学習曲線 21 点すべてで 0.00〜0.15 で、一度も立ち上がっていない**。
   - 留保 1 の本質は**再現性の欠如**（原文「『100 万回せば 0.92 が出る』という再現性ではない」）。**チェックポイント選択が seed 選択（2/3）に置き換わっただけ**であり、同じ欠如が残っている。
   - **当初の判定文「0.94〜0.97 で安定」は、生き残った 2 本だけの範囲だった**（§9-8 の n 記載義務に違反）。
2. **留保 2（報酬の滞留局所解）**: **廊下および M2 の規模までは解消。M3 では成立しない。**
   - 廊下では**学習曲線 19 本を当たって滞留の再発が 1 件も無く**、失敗はすべて衝突・時間切れ率 0.00。**廊下については崩せなかった**。
   - **しかし同じ報酬を 16x16 へ持っていくと壊れる**。割引後総収益の ゴール − 滞留 の差:

     | 条件 | ゴール | 滞留 | 差 | $\gamma^T$ |
     |---|---|---|---|---|
     | 廊下（M1） | +0.628 | −0.169 | **+0.797** | 0.222 |
     | 6x6（M2 想定） | +0.595 | −0.200 | **+0.795** | 0.165 |
     | **16x16（M3 想定）** | **−0.183** | −0.200 | **+0.017** | **0.00115** |

   - **機構**: $\gamma$=0.995 の実効地平は $1/(1-\gamma)$ = **200 歩**。16x16 の約 1350 歩は地平の 6.75 倍で $\gamma^T$ = 0.00115 になる。**ゴール報酬と整形の総和はどちらも $\gamma^T$ で割り引かれて消えるが、滞留の −0.2 は時間罰の等比和の漸近値なのでエピソード長によらない。ゴール側だけが縮み、滞留側は縮まない**。
   - **exp_003 で実際に滞留解へ落ちたときの差は +0.267 だった。M3 の +0.017 はその 16 分の 1** である。
   - 速度感度: 0.5 m/s なら差はちょうど 0.000。M1 の方策の速度は約 1.0 m/s。
   - **限界**: M2-0 の報酬は exp_005 と同一ではない（訪問報酬 0.02 が加わる設計、学習は n=0）。本計算は「exp_005 の報酬をそのまま持っていった場合」の話である。
3. **留保 3（行動振動が実機水準にない）**: **改善。ただし条件つき。**
   - **以下の限界 (a)〜(f) は判定の一部であり、上の「ただし条件つき」はこれらを指す（見出しだけで「解消した」と読めないようにするための明記）。**
   - **改善の根拠**: 案 3（行動の高周波成分だけを罰する。$\alpha=0.5$・$k=8.7\times10^{-3}$）で**符号反転 11.6〜14.8 回/s ＜ 必達線 15.4 回/s**（車輪ジョイントの遮断周波数 7.72 Hz から導出した基準）を満たした。**ただしこれは 3 seed 中 2 seed（seed1: 14.78、seed3: 11.63）の範囲であり、失敗した 1 本（seed2、完走率 0.020）の符号反転 31.91 回/s は含まれていない**。これにより必要な巻線熱時定数が 32.6 s → 1.7 s（**中央値どうしで 9.4 分の 1、exp_005 単体との比なら 19 分の 1**）に緩和され、銅損は 9.33 W → 1.01 W（**中央値どうしで 5.4 分の 1、exp_005 単体との比なら 9.2 分の 1**）に低下した。当初の数字は最悪の対照 対 最良の処理の比であり、約 2 倍過大だった。速度はむしろ 20% 向上し、gate 完走率も 0.93 → 0.94〜0.97（3 seed 中 2 seed）へ改善した。
   - **限界 (a)**: RMS 電流は依然として**連続定格の 1.17〜1.23 倍**であり、定格内には収まっていない。
   - **限界 (b)**: **巻線の熱時定数 $\tau_{th}$ が仕様書に無い**ため、熱の判定は「実際の $\tau_{th}$ が 1.7 s 以上なら 1 走行では焼けない」という**条件式の形に留まる**。データシート入手時に埋めること。
   - **限界 (c)**: **持ち時間 420 秒を通した熱の累積は未計算**。電流が流れるのは最速走行の間だけではなく、探索走行・追加探索・帰路のすべてで流れる。
   - **限界 (d)【重要】**: **本判定は電気的・熱的な害のみに基づく。**減速機のバックラッシュに起因する機械的な害（打音・歯打ち・摩耗・歯面衝撃）は、モデルにバックラッシュが無いため**評価できていない**（`docs/ROBOT_SPEC.md` §2.2 の注記を参照）。**「電流が定格内に近づいたから実機で安全」とは言えない**。
   - **限界 (e)**: 旧来の目標値「符号反転 10 回/s 未満」は**未達**（最良 11.6 は 1.16 倍）。ただしこの 10 という値は**由来が復元できず gate から外した**ものであり、gate は 15.4 を用いる。
   - **限界 (f)【新設・重要】**: **符号反転数という gate 指標が、判定した動作域で害の順位を取り違え始めている。**
     - 反転 / HF比 の比が $k$ とともに単調に下がる（k=0 で 105〜133、案3 $k$=8.7e-3 で 69〜91、$k$=1.1e-2 で 64〜79）。**$k$ を上げるほど、符号反転は減るが高周波エネルギーは同じだけ減らない**。
     - **実際に順位が逆転している**（中央値）: 案3 $k$=8.7e-3 は 反転 13.5 / HF比 0.170 / RMS電流 定格比 1.198、$k$=1.1e-2 は**反転 12.4（良い）/ HF比 0.189（悪い）/ 電流 1.272（悪い）**。
     - **符号反転数で選ぶと $k$=1.1e-2 が良いことになるが、HF比でも RMS 電流でも $k$=8.7e-3 の方が良い。電流は害の実体（$I^2R$ の発熱）なので、gate 指標のほうが間違っている**。
     - 判定の結論は覆らない（**選ばれた条件は電流でも最良だったので、たまたま正解している**）。**しかし選択の根拠に使った指標は、この領域でもう信頼できない**。
     - これは `experiments/exp_006_action_smoothness/card.md` §3-7 が「微小振幅で振動する方策が現れたとき 2 指標は分離する」と**予言していた事象が実際に起きた**ものである。

## 6. アプローチのポートフォリオ

- **Track A — 再帰型エンドツーエンド PPO**: GRU 付き方策でセンサ→モータ電圧を直接学習。カリキュラム（廊下 → 曲がり角 → 3x3 → 5x5 → 7x7 → 16x16）。報酬整形はポテンシャルベースを第一選択とする:
  $$F(s, s') = \gamma\,\Phi(s') - \Phi(s)$$
  （$\Phi$: 状態ポテンシャル。例: ゴールまでの迷路距離の負値。この形の整形は最適方策を変えないことが理論保証される）。それ以外のボーナス項を足す場合は実験カードに根拠を明記。
- **Track B — 構造化地図メモリ**: 方策に外付けの格子状メモリ（自己位置周りに書込/読出）を持たせ、書込内容自体も学習させる。M4（走行間記憶)は素の RNN では困難と予想されるため、その主力候補。
- **Track C — 世界モデル系（DreamerV3 小型）**: 本 Mac の資源では優先度低。Track A/B が停滞した場合に検討。
- **Track D — 特権教師からの蒸留（封印中）**: 全迷路情報を持つ古典プランナ + 軌道最適化を教師とし、センサのみの学生方策へ蒸留（DAgger）後に RL 微調整。**§8 の解禁基準を満たすまで着手禁止。**

## 7. 自律度ラダー（教育の背骨・比較の物差し）

「どこまでが古典制御で、どこからが学習か」を体系的に示すため、同一課題・同一評価規約（§2）の下で自律度を 5 段階定義する。各レベルの実装・成績・「そのレベルで新たに学習が担う機能」を教材として記録する。

| Level | 運動制御（モータ指令） | 経路計画・探索 | 地図・記憶 | 実装 |
|---|---|---|---|---|
| L0-a | 古典（**超信地旋回走行・区画ごと停止**＋PID） | 古典（足立法） | 古典（明示的な壁地図） | `competition/baseline_classical.py`（M0 実装済み） |
| L0-b | 古典（**超信地旋回走行・直進連続**＝直進区間は止まらず、曲がるときのみ停止旋回） | 古典（足立法） | 古典（明示的な壁地図） | **未実装・M2 までに用意**（探索走行の実機標準） |
| L0-c | 古典（**スラローム走行**＝曲がるときも止まらない。軌道生成＋追従制御）。**探索走行・最短走行ともスラローム**（2026-08-10 ユーザ教示） | 古典（足立法） | 古典（明示的な壁地図） | 実装済み（要改良） |
| L1 | 学習（速度追従 NN） | 古典 | 古典 | 旧 Phase1〜3 相当（優先度低・必要時に再実装） |
| L2 | 学習（センサ→モータ電圧） | 古典（特権情報使用） | 古典 | 中間ベースライン（カリキュラム過程で自然に得られる） |
| L3 | 学習 | 学習（単走・走行間記憶なし） | なし | M2〜M3 の主対象 |
| L4 | 学習 | 学習 | 学習（方策の内部記憶） | M4〜M5 の主対象・最終目標 |

- L0〜L2 は最終回答ではないが、(a) 教材として古典と学習の境界を可視化し、(b) 科学的にはアブレーション（ablation、要素抜き比較実験: どの機能を学習に置き換えると性能がどう変わるかの対照実験）として機能する。
- **探索戦略の軸（2026-08-10 ユーザ指摘・別軸として追加）**: 自律度ラダーの L0 系は「**走行方式**」（a/b/c）で分けているが、**探索戦略**はそれとは独立した軸である。現行実装は「素朴な足立法でゴールに到達したら探索を打ち切る」もので、**未探索区画が大きく残ったまま最短走行に移っており、真の最短経路を見つけられていない可能性が高い**（動画の既知地図で確認）。
  - **探索戦略の段階（2026-08-10 調査に基づき確定。`research_notes/note_006` 参照）**:
    - **E0**: ゴール到達で打ち切り（**現行実装**）。一度でも既知区画だけの経路が見つかると、以後その経路を往復するだけで未知区画へ迂回しない。**見つけた経路が真の最短である保証がない**
    - **E1**: **追加探索ループ**（＝マウサーの標準手法・**L0 系の標準として採用**）。往路でゴール到達後、「現在の最短経路上に残る未知壁」を目的地に切り替えて調べに行き、それが無くなるまで繰り返す。帰路のみ悲観的（未知壁＝壁）で安全に戻る
    - **E2**: k 最短経路版（Yen's algorithm で上位 k 本の候補経路を保持し、そのいずれかに乗る未知壁を調べる）。同じ歩数の経路が複数あるとき、走りやすい方（ターンが少ない・直線が長い）を取り逃さない
    - **E3**: 全面探索（拡張足立法）。上位機が僅差を争う際に使う。探索時間が長い
  - **最短経路の確定判定（理論的根拠）**: 未知壁を「通れる」とみなした楽観地図の辺集合は真の迷路の上位集合なので、楽観地図での距離 $D_{opt}$ は真の最短距離 $D_{true}$ の下界（$D_{opt} \le D_{true}$）。一方、楽観地図上の最短経路が**確定済みの辺だけ**で構成されていれば、その経路は真の迷路にも実在するので $D_{true} \le D_{opt}$。ゆえに $D_{opt} = D_{true}$ が成立する。**つまり「現在の最短経路上に未知壁が 1 つも残っていない」瞬間に、それが真の最短経路であると確定できる**（迷路の他の場所がどれだけ未探索でも影響しない）。この性質により全面探索は理論上不要になる。
  - **注意（当初定義の誤り）**: 当初 E1 を「帰路も探索」としたが、**これは実務の標準ではない**。標準は「帰路は悲観的・安全側で戻る」であり、能動的な探索は往路後の追加探索フェーズで行う（訪問区画での受動的な壁観測は帰路でも当然行われる）。
  - **重要**: 探索戦略を改良すると L0 の基準タイムが変わる（探索走行は長くなるが、最短走行は速くなる可能性が高い）。改良前後の 5 指標を両方記録し、比較できるようにすること。現行の E0 版は「記念すべき最初のベースライン」として記録・動画とともに保存する。
  - **重要**: 探索戦略を改良すると L0 の基準タイムが変わる（探索走行は長くなるが、最短走行は速くなる可能性が高い）。改良前後の 5 指標を両方記録し、比較できるようにすること。

- **走行方式の 3 段階と比較の公平性（2026-08-10 ユーザ教示）**: マウサーの走行方式は少なくとも 3 段階あり、現行 L0-a は**最も基本的・低速な部類**にあたる。
  1. **超信地旋回走行・区画ごと停止**（L0-a、実装済み）: 1 区画進むたびに停止。最も遅い
  2. **超信地旋回走行・直進連続**（L0-b）: 直進できる区間は止まらず走り続け、曲がるところでのみ停止旋回。**実機の探索走行で広く使われる標準方式**
  3. **スラローム走行**（L0-c）: 曲がるときも停止しない。**実機の最短走行の標準方式**（上位機はさらに斜め走行を用いる）
  - **L0-a のみを比較対象にすると「学習が古典制御に勝った」という主張が不当に有利になる**。L0-b・L0-c を M2 までに実装し、**L0-c を主たる比較対象**とする。L0-a は「古典の最も素朴な実装」として下限基準・教材用に残す。
  - 学習方策の成績を報告する際は、**3 方式すべてと比較**し、どれに対する優劣かを明示すること。
  - 実装順は L0-b（L0-a の軽微な拡張で安価）→ L0-c（軌道生成が必要で重い）。
  - **L0-c は探索走行もスラロームで行う（2026-08-10 ユーザ教示）**: 実機のマウサーも探索走行からスラロームを使う。ただし未知の壁があるため**最短走行より低い速度**で走るのが定石であり、「未知の区画へ進入する際は、壁が現れても止まれる速度まで落とす」という制約を軌道生成・速度計画に組み込む必要がある（制動距離 $v^2/(2a)$ が壁までの距離を下回ること）。探索走行を超信地旋回に落とす実装は L0-c として不完全であり、逆に速度制限なしでスラロームすると衝突する（実際に第1走行で衝突が発生した）。
- 各レベルの §2 評価成績を並べた一覧表が、本プロジェクトの教育・学術両面の中心成果物となる。

## 8. Track D の解禁基準

純 RL（Track A/B）が以下のいずれかに該当したとき、教授セッションの判断で解禁する:
- (a) あるマイルストーンで、本学習 5 本（実験カード 5 枚）を費やしても gate 未達
- (b) 学習曲線が 3 実験連続で有意な改善なし

## 9. 実験規律（アドホック報酬いじりの再発防止）

1. 全実験は `experiments/exp_NNN_<短い名前>/` に実験カード（`card.md`: 仮説、変更点、gate、結果、判定）を残す。**1 実験 1 変更**。
   - **成果物の分離（2026-08-10 追加・必須）**: ログ・モデル・評価結果も**実験ごとに別のパスへ保存**する（`logs/exp_NNN_*/`、`models/exp_NNN_*.zip`、`outputs/exp_NNN_*/`）。実験名をハードコードせず引数化すること。**実際に exp_002 が exp_001 の保存先を上書きし、exp_001 の学習ログが失われる事故が発生した**（学生B が発見）。
   - **報告する数値は必ず出所（どの実行・どのログ）を明記**する。異なる実行の数値を混ぜないこと（exp_001 と exp_002 の数値が混在した指示書を教授セッションが発行する事故が発生した）。
2. 乱数 seed 固定、TensorBoard ログ、チェックポイント保存を必須とする。
   - **迷路・コースの再現性（2026-08-10 ユーザ指摘・必須）**: 生成は **seed のみから決定的**でなければならない。同一 seed からは常に同一の迷路が得られること（**受け入れ条件のリジェクトサンプリング（棄却再試行）を含めて決定的**であること — 試行回数も seed から一意に定まる実装にする）を単体テストで保証し、生成器を変更するたびに再検査する。
   - **学習ログに各エピソードの迷路 seed を記録**し、**後から任意のエピソードの迷路を復元して分析できる**ようにする（例: 「この形状で必ず衝突する」を事後に再現して原因を調べられること）。§9-6 の毎エピソード新規生成は、この決定的規則（`base + 通算エピソード番号` 等）と両立させる。並列環境では worker ごとに衝突しない seed 規則を定め、規則自体を実験カードに明記する。
   - **学習全体の再現性**: 同一 seed・同一コードで学習を再実行したとき結果が一致することを確認する。並列実行やハードウェアに起因して bit-exact な再現が得られない場合は、**その事実と再現の程度（例: 評価成績が誤差 ±x% で一致）を実験カードに正直に記録する**。「再現できるはず」で済ませない。
3. 評価は必ず §2 の凍結ハーネス経由。学習中の自己申告値でマイルストーン判定をしない。
   - **走行ごとの指標を保存する（2026-08-11 追加・必須）**: 評価の成果物に**条件ごとの集約値（平均・中央値）だけを保存してはならない。**最低限「**走行ごと**（コース × 試行）の指標」を、可能なら**行動列（左右モータ電圧の時系列）**も保存すること。
     - **理由**: 集約値しか無いと、**後から集計母集団も定義も検算できない**。実際 exp_006 では、**M1 の完了判定の根拠となる数値（符号反転・高周波成分比・RMS 電流）を、学生B 本人を含め誰も再実行なしに検算できない**状態になった（2026-08-11、准教授の独立検証で発覚）。しかもこの箇所は**集計母集団の誤り（失敗走行の混入）が実際に一度起きている**。
     - **「再実行すれば確かめられる」は検算可能とは言わない。**再実行にはコードと環境が当時と同一である保証が要るが、それこそが疑いの対象になる。
     - 保存量が問題になる場合は、**行動列は代表走行のみ・走行ごとの指標は全件**とする。集約値だけにはしない。
   - **`ep_rew_mean` を性能の判断に使わない（2026-08-10 追加・必須）**: ポテンシャル整形（$F=\gamma\Phi(s')-\Phi(s)$）を使う限り、Monitor が出す割引なしの報酬総和はエピソード長に強く依存し、**性能と逆行することさえある**。実際 exp_003 では ep_rew_mean が 4.75 → **31.2** に上昇しながら**完走率は 0.00**（方策が「動かずに時間切れまで粘る」局所解へ収束）だった。判断は必ず**検証帯でのタスク指標（完走率・タイム等）を一定ステップごとに実測**して行うこと。この計測がなければ exp_003 は「報酬 6 倍の大成功」として誤って記録されていた。
   - 関連する既知の落とし穴: $\Phi=-D$（常に負）だと滞留時の整形報酬が $(1-\gamma)D>0$ となり**止まるだけで正の報酬**が入る。$\Phi=D_0-D$（初期距離を加えたオフセット版。Ng の定理の保証は保たれる）にすると滞留が明確に損になり、ゴールと滞留の総収益差が 0.27 → 0.98（3.6 倍）に広がる。
     - **重要な訂正（2026-08-10、学生B の検算による）**: $\Phi$ に定数 $c$ を加えることは、$F'=\gamma(\Phi+c)-(\Phi+c)=F-(1-\gamma)c$ より**毎ステップ一律に $(1-\gamma)c$ の罰を加えることと数学的に等価**である。したがって「報酬定数を触らずに済む」「遅い走行が損になる副作用を避けられる」という理由づけは**誤り**（教授セッションの当初の説明）。
     - **【2026-08-10 追加の重大な知見】オフセット版は単独では機能しない**。Ng の定理はエピソード的タスクで**終端状態の $\Phi=0$** を要求する。$\Phi'=D_0-D$ はゴール時 $\Phi'=D_0>0$、衝突時 $\Phi'=$ 走行距離 $>0$ となり条件を満たさないため、整形の総和 $\gamma^T\Phi'(s_T)-\Phi'(s_0)$ が**「いつ終わるか」に依存**し、**早く終わるほど得**になる。結果、**衝突が最適解**になる（実測: exp_004 で完走率 0.05・速度 1.6 m/s の「速く走って壁に当たる」方策へ収束。総収益はゴール 0.627 < 衝突 0.890）。時間罰を上げる案も数値的に等価で同じ破綻をする（**時間罰を上げること自体が「早く終わること」に価値を与えるため**）。
     - **是正**: 滞留と衝突は**片方だけ塞ぐと他方に落ちる**関係にあり、**衝突罰を元報酬側に加えて同時に塞ぐ**必要がある（$\Phi'$ ＋ 衝突罰 −1.0 で ゴール 0.627 > 衝突 0.285 > 滞留 −0.200 と正しい順序になる）。**現行の報酬に衝突罰が無かったこと自体が設計の欠落**だった。
     - **実装上の注意**: 「終端で $\Phi=0$ と強制する」は**実装してはならない**。$\Phi=-D$ の場合、最後の遷移で $F=\gamma\cdot 0-\Phi(s_{T-1})=D_{T-1}>0$ となり**衝突した瞬間に残り距離ぶんの正の報酬**が入る。ゴール時は $D=0$ で自然に $\Phi=0$ が成立する。
     - オフセット版の（滞留に対する）優位は「罰の大きさがコース長 $D_0$ に自動追従すること」にある。定数の時間罰 $t_p$ では滞留時の報酬が $(1-\gamma)D-t_p$ となり**長いコースほど滞留が得**になるため、全コースで滞留を潰すには短いコースに過剰な罰を課す値が必要になる（実測: 検証・評価帯 40 本のうち $t_p=0.011$ では **28 本**で滞留がまだ得、全滅させるには 0.020 以上が必要）。オフセット版なら短いコースで実効罰 0.0046、長いコースで 0.0208 と必要な分だけ効く。**コース長の分布が変わる M2 以降（5x5 → 16x16）でこの性質が効く。**
6. **同一迷路での反復学習の禁止（2026-08-10 ユーザ指摘・必須）**: 学習時の迷路・コースは**毎エピソード新規生成**する（seed は `base + 通算エピソード番号` のような決定的規則で再現可能にする）。固定の有限集合を繰り返し使ってはならない。競技の本質は**未知**迷路の走破であり、同一迷路の反復は迷路形状の暗記（過学習）を招き、評価成績が「汎化能力」を測らなくなるため。
   - **区別すべき例外**: 1 エピソード（＝持ち時間 420 秒・最大 5 走行）の**内部**で同じ迷路を複数回走ることは競技仕様そのものであり必須。禁止するのは**エピソードをまたぐ**同一迷路の再使用。
   - カリキュラムで迷路サイズを段階的に上げる場合も、各段階で毎回新規生成する。
7. **データ分割の三分割（2026-08-10 追加・2026-08-12 seed 帯を改訂）**: 迷路の seed 帯を用途で分離し、混用を禁じる。
   - **16x16 評価系（competition/）**: 評価帯（test）= `competition/mazes/eval/` の 20 面。**マイルストーン判定時のみ使用**し、これを見てパラメータを調整しない（test の成績で設定を選ぶ行為は test への間接的な過学習）。検証帯（validation）= `competition/mazes/validation/` の 20 面。**日常の実験判断（ハイパーパラメータ選択・実験の合否見込み・学習の打ち切り判断）はすべてこちらで行う**。両帯の実体・受理条件・凍結記録は §2 を参照
   - **⚠️ 予約帯は「採用 20 seed」ではなく候補プール全体 [1000, 40999]**（§2。2026-08-12 改訂）。**M3 で 16x16 の学習環境を作るとき、安全弁を旧記述の「1000-1019 / 4000-4019」で書くと評価迷路が学習へ漏れる**（採用 seed は eval 1018〜14037・validation 21003〜25842 に散らばっている）。**16x16 の学習用迷路 seed は 41000 以降**を使うこと
   - **6x6 学習系（mouse/・M2）**: 学習 8000 以降／検証 7000-7019／評価 6000-6019（生成器が別系統のため 16x16 の候補プールとは独立の名前空間）
   - M1 の廊下コースも同様に扱う（学習: 毎エピソード生成、**検証: seed 5000-5019**、gate 判定: seed 3000-3019 固定）。
   - **予約 seed の保護（2026-08-10 追加・必須）**: 学習用の seed 列が予約帯（上記の候補プール・6x6 の検証/評価帯・廊下 3000-3019 / 5000-5019）に入る場合は**その seed を飛ばす**実装上の安全弁を設けること。**実際に exp_001（300 万ステップ）で学習 seed が gate 用コース帯 3000-3019 を通過していた**（学生B が発見。ワーカ 0 が約 1700 エピソードに達したため）。規律を文書で守るのではなく実装で保証する。
4. ドキュメントとコードの数値乖離は発見次第その場で修正。
5. 既存の `phase1〜4` コードは参照用に凍結し、新規実装は `competition/`（評価・ベースライン）、`mouse/`（環境・方策）、`experiments/` に構築する。
8. **単一 seed の結果で結論を確定しない（2026-08-11 追加・必須。教授裁定）**: 学習を伴う実験は、**学習 seed（PPO 本体の乱数）を変えるだけで結果が質的に変わりうる**。1 本の学習結果は「その seed でそうなった」以上のことを意味しない。
   - **探索段階のスクリーニングは 1 seed でよい。ただしその結果を「確定」と書かない**（実験カードに**「暫定（n=1）」**と明記する）。
   - **結論を確定するときは最低 2 seed**（3 seed が望ましい）。「確定」に当たるのは、①実験カードに結論として記載する ②マイルストーン判定に使う ③設計変更・方針転換の根拠にする、のいずれか。
   - **seed 間でばらついた場合は、ばらつきそのものを結果として報告する**（代表値だけを書かない。最良・最悪・全値を出す）。
   - **同一 seed で同じ結果が再現したことは「決定性の確認」であって「頑健性の確認」ではない**。両者を報告文で区別すること。
   - **この規律を追加した経緯（2026-08-11）**: exp_006 は「行動差分への罰 $-k\lVert\Delta a\rVert^2$ を入れると完走率が 0.93 → 0.05 に崩れる」ことから「**滑らかさと曲がる能力は根本的にトレードオフ**」と結論していた。しかし各条件 n=1 だった。教授指示で $k$=0 と $k$=1e-4 について学習 seed を 0/1/2 の 3 本に増やしたところ、**$k$=1e-4 は seed 1 で完走率 0.90 に達した**（seed 0 は 100 万ステップ・21 評価点を通じて 0.15 を超えなかった）。**「罰のせいで壊れた」のではなく、seed 0 の 1 本が外れを引いていた。**結論とそこから導かれた設計方針が、単一 seed のために誤っていた実例である。
   - 併せて確立した手順: **新しい報酬項を入れる前に、代表条件での割引後総収益の順序（ゴール > 探索 > 滞留 > 衝突）と、その間の学習勾配の符号を机上で確認する**。exp_006 の $k$=1e-2・1e-3 の失敗は、この計算だけで学習を回さずに予測できた（$k$=1e-2 は「早く衝突するほど得」に勾配が反転していた）。上限は計算で出る量であり、実験で探る量ではない。
9. **複数セッション並行時のコミット手順（2026-08-11 追加・必須）**: 複数のセッションが同一の作業ツリーを共有するため、**`git add -A` / `git add .` の禁止だけでは他セッションの変更の巻き込みを防げない**。`git commit` は「実行時点で staged なもの全部」を対象にするので、自分が `git add` してから `git commit` するまでの隙間に他セッションが add したファイルが入る。
   - **必ず `git commit <パス...> -F -` の形でパスを明示する**。この形はインデックスの他のパスを無視し、**指定パスだけをコミットする**ので、確認と commit の間に他セッションが add しても構造的に巻き込まない（学生B の提案）。
   - 併せて**コミット直前に `git status --short` を実行し、staged 欄に担当外のファイルが無いことを確認**する。あれば `git restore --staged <path>` で外す（学生A の提案）。目視確認だけでは上記の隙間を塞げないため、両方を行う。
   - 注意: `git commit <パス>` は**作業ツリーの内容**をコミットする（`git add -p` 等で作った部分ステージは反映されない）。部分コミットをしたい場合はこの形を使わず、`git status --short` の確認を厳格に行うこと。
   - **実際に事故が 2 回発生している**。1 回目は前任の学生セッションが `git add -A` で他セッションのステージを巻き込んだ件。2 回目は 2026-08-11、学生B の `dc29a31` が学生A の成果物 9 本を巻き込んだ件（内容の欠損はなく、コミットメッセージと変更内容の対応のみが壊れた。**履歴は書き換えず**、本来のメッセージを空コミット `d35d162` に記録して追跡可能性を保全した — 他セッションが上に積んでいる状態での rebase の方が危険なため）。
10. **学習規模の選定理由を実験カードに書く（2026-08-11 追加）**: 総ステップ数・
    並列環境数・チェックポイント間隔を**なぜその値にしたか**を実験カードに 1 行残す。
    値そのものは設定ファイルとログから復元できるが、**選んだ理由は復元できない。**
    - **実際に復元不能になっている**: exp_001 の 300 万ステップと exp_002 の
      100 万ステップは、**なぜその数かがどこにも残っていない**
      （`experiments/exp_002_sensors4/card.md` §7 が「復元できなかった」と明記）
    - 書くべき最小限は次の 2 つ。**「打ち切った理由」と「打ち切らなかった場合の判断基準」**
      - 例: 「16 万ステップで頭打ちしたので 100 万で打ち切った。改善が続いていれば 300 万まで回した」
    - **学習曲線が頭打ちで打ち切った場合は、頭打ちと判断したステップ数と根拠（検証帯の
      完走率が N 点連続で改善なし等）を書く。**「頭打ちだった」だけでは、後から
      「本当に頭打ちだったのか」を確かめられない
13. **条件つきの限界は、発火する時点の作業へ紐づける（2026-08-11 追加・必須）**: 「**将来こういう条件になったら効く**」形の限界を、**発見した文書の末尾に書くだけにしてはならない。**その条件が成立する時点の作業（マイルストーンの着手前提・実験カードの前提条件）に**紐づけて書く**こと。
    - **理由**: **決定は次の作業の前提として毎回参照されるが、限界は参照されない。**しかも条件つきの限界は**まだ起きていない問題**なので、**落ちたことに気づく契機がない。**
    - **実例（2026-08-11、独立検証が発見）**: `docs/MAZE_DIFFICULTY_REPORT.md` §7 の限界 8 は「**経路が 3 倍長くなるとエピソード長・報酬スケールが変わる影響は全く検証していない**」と、**是正案を出した当人が自分の案の未検証点として正しく記録していた**。にもかかわらず迷路は是正され、M1 の「留保 2 解消」判定が下され、**誰もこの 2 つを結びつけなかった。**翌日、別の担当が別の目的（判定の監査）で同じ問題に到達して初めて再発見された。
    - **これは note_009（決定は引き継がれるが根拠は引き継がれない）の変種**である。note_009 は**根拠**が落ちる話、本項は**限界**が落ちる話。
    - **既知の条件つき限界（発火時点へ紐づけ済み。増えたらここに追記する）**:
      - **M3 の着手前** → 報酬の割引後総収益を 16x16 のエピソード長で再計算する（§5 の M3 行に記載）
      - **RMS 電流の定格比が 1.05 級に入ったとき** → 電流の測定精度（現在は制御周期の代表値であり物理サブステップの積分ではない＝過小評価側）を先に詰める。必要熱時定数の誤差増幅が定格比 1 近傍で 4 倍級になるため（`verification/AUDIT_001_m1_clearance.md` §8-3）
      - **並列環境数を変えるとき** → M1 で測った実時間比（並列 1 が 1.9 倍速い）は課題によって変わる。M2 では 1.07 倍まで縮んだ。**条件が変わったら測り直す**
12. **複数チャンネルを持つ量は、集約の順序を定義文に明記する（2026-08-11 追加・必須）**: 左右のモータのように**チャンネルを持つ量**について「左右平均」とだけ書いてはならない。**チャンネルごとに指標を計算してから平均するのか、チャンネルを先に平均してから指標を計算するのかで、値が変わる。**
    - **とくに変動を測る指標（実効値・分散・反転率）では致命的である。**旋回のように**左右が逆位相に振れる**区間があると、**先に平均を取った時点でその変動が打ち消し合って消える。**測りたい振動そのものが消える。
    - **実測（2026-08-11、盲検の独立実装が発見）**: モータの交流電流について、チャンネルごとに計算してから平均する解釈と、先に平均してから計算する解釈で、**exp_005 相当の方策では 3〜4 倍の差**が出た（2.08 A 対 0.55〜0.77 A）。
    - **原則として「チャンネルごとに計算してから集約」**とする（変動が保存されるため）。定義文には**どちらかを必ず書く**こと。
    - **併せて**: $I_{rms}^2 = I_{dc}^2 + I_{ac}^2$ の恒等式は**単一チャンネルでのみ厳密に成立する**。チャンネルごとに計算して平均した量の間では、左右の相関項が残るため厳密には成立しない。**検算が合わないこと自体は異常ではない**ので、そう注記すること。
11. **報酬をスケールの違う課題へ転用するときは、割引後総収益を再計算する（2026-08-11 追加・必須）**: §9-8 の手順「新しい報酬項を入れる前に総収益の順序を確かめる」は**報酬を変えるとき**の規定である。**報酬を変えずに課題のスケール（エピソード長・経路長）を変えるときも、同じ再計算が要る。**
    - 理由: 割引率 $\gamma$ が定める実効地平 $1/(1-\gamma)$ をエピソード長が大きく超えると、**ゴール報酬と整形の総和は $\gamma^T$ で消えるが、毎ステップの時間罰の等比和は漸近値に達して縮まない。**片側だけが縮むので順序が壊れる。
    - **実例**: exp_005 の報酬は廊下（約 300 歩）で ゴール − 滞留 = +0.797 だが、16x16（約 1350 歩）では **+0.017** になる（2026-08-11、独立検証が発見）。
    - 確認すべきは順序だけでなく**差の大きさ**である。exp_003 で実際に滞留解へ落ちたときの差は +0.267 だった。

## 10. 研究記録と教材化

- **研究ノート `research_notes/`**: 通し番号 `note_NNN_<題名>.md`（テンプレート: `research_notes/template.md`）。書く契機: (a) 方針決定・仕様変更の当日、(b) 各実験カードの完了時（結果と解釈の要約）、(c) 各マイルストーン到達時（総括）。
- 実験カード（§9）は生データ寄り、研究ノートは解釈・考察寄りの記録と位置づけ、ノートから実験カード・コミットへ必ずリンクする。
- **文献調査**: 関連研究（end-to-end ナビゲーション学習、特権蒸留、記憶付き RL、マイクロマウス×RL の先行例）を調査し新規性を評価する。
- **最終成果物**: (a) 自律度ラダー比較表（§7）を核とした学生向け教育資料、(b) 論文体の研究報告書（背景・手法・実験・考察）。
- 記録は日本語。図表は再生成可能なスクリプトとともに保存する。

## 11. 体制

- **教授セッション（本計画の author）**: 方針決定、指示書発行、成果の検収、マイルストーン判定、Track D 解禁判断。
- **学生セッション（micromouse-rl-8d ほか）**: 実装、学習ジョブの実行（長時間はバックグラウンド）、結果の定量報告。
- 指示・報告は SendMessage、成果物の共有は git コミットで行う。
