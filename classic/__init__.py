"""
classic
================
古典的なマイクロマウス走行アルゴリズム（地図・歩数マップ・経路計画・走行制御）を
まとめたパッケージ。

設計方針は `research_notes/note_030_classical_rebuild_plan.md` を正とする
（実機が実際にやっていることを PC 上で再現する。速さは目的ではなく結果）。

本パッケージは **numpy 以外に依存しない**。MuJoCo や `mouse/` のシミュレータを
import してはならない層（S0: 地図と経路、純計算）がここに含まれる。
"""
