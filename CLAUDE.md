# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 言語設定

- 応答は日本語で行うこと
- コードのコメントも日本語で記述すること
- **用語は `docs/JA_ENGINEERING_TERMS.md`（用語辞書）に従うこと。チャット報告・セッション間メッセージ・コミットメッセージ・文書は、送信・コミットの前に同辞書 §6 のチェックリストを毎回適用する**（2026-08-13 制定。辞書はあったのに参照が任意だったため、違反語がそのまま報告に流通した — 例:「正本」「腕」）

## ⚠️ 最初に読むこと（2026-08-10 プログラム再編）

本リポジトリは 2026-08-10 に研究プログラムとして白紙から再編された。**旧 Phase1〜4 系のコード・ドキュメントは凍結レガシーであり、新規作業の参照先ではない**（詳細は下記「レガシー」節）。

必読の正本（この順で読む）:

1. `docs/RESEARCH_PLAN.md` — プログラム研究計画書（ミッション、評価規約、マイルストーン、実験規律、体制）
2. `docs/MODEL_VERIFICATION_PLAN.md` — 物理モデルの確定仕様と検証プロトコル（ロボットパラメータの正本。改訂履歴は同文書の冒頭を見ること）
3. `research_notes/` — 研究ノート（最新の番号のノートに直近の意思決定と経緯がある）

### ⚠️ 本ファイルに数値を書かないこと（2026-08-11 制定）

**評価規約・物理パラメータ・判定基準などの具体的な数値を、本ファイルに転記してはならない。**値は正本にのみ置き、ここには**参照だけ**を書く。

**理由（実際に起きた事故）**: 本ファイルはすべてのセッションが起動時に自動で読み込み、かつ「これに従え」と優先を宣言している。そのため**正本を是正しても、本ファイルの古い値が生き残っていると、次に起動したセッションは古い値を読み、しかもそれが正本より強く効く**。2026-08-11、持ち時間（正本で 2026-08-10 に是正済み）とセンサ本数（同じく是正済み）の 2 つが本ファイルに残っており、学生セッションが是正済みの誤りを再導入した。**是正が届かない経路が存在していた。**

**数値が食い違った場合は、常に正本が勝つ。**本ファイルの「OVERRIDE」は作業の進め方についての指示に適用され、**事実・数値には適用されない**。食い違いを見つけたら勝手に直さず、教授セッションへ報告すること。

## ミッション（要約）

センサ入力（距離センサ・ジャイロ・加速度計・車輪角速度）→ **左右 2 モータ電圧を直接出力する**方策を強化学習で獲得し、マイクロマウスクラシック競技の公式ルールで評価する。目標速度の中間層やプログラム済み動作プリミティブは最終方策に含めない。学習手段は**報酬と環境のみ**（教師信号の注入 — 蒸留・模倣・古典軌跡の教師あり学習 — は不使用。2026-08-14 ユーザ裁定・`docs/RESEARCH_PLAN.md` §8）。

**具体的な仕様はすべて正本を見ること**:
- 入出力契約・学習手段の解禁条件 → `docs/RESEARCH_PLAN.md` §1、§8
- **競技プロトコル（迷路の規格・持ち時間・走行回数・計時・成績指標）** → 同 §2
- マイルストーンと合格基準 → 同 §5
- **センサの構成・本数・取り付け・モータ定数** → `docs/ROBOT_SPEC.md`、`docs/MODEL_VERIFICATION_PLAN.md`

## 体制（マルチセッション運用）

- **教授セッション**: 方針決定・指示書発行・検収・マイルストーン判定・コミット判断
- **学生セッション**: 実装・学習実行・結果の定量報告
- 役割は増減する。**現時点で稼働している役割の一覧は `handover/` 配下のファイル名を見ること**（ここに列挙しない）
- セッション間連絡は SendMessage、成果共有は git コミット。仕様と矛盾する事実を見つけたら勝手に仕様を変えず教授セッションへ相談すること
- **複数セッションが同一の作業ツリーを共有している。**コミットは必ず `git commit <パス...> -F -` の形でパスを明示すること（`git add -A` 禁止。理由と手順は `docs/RESEARCH_PLAN.md` §9-9）

## アクティブなディレクトリ（新系統）

- `competition/` — 凍結評価ハーネス（評価迷路 npz・evaluator・古典ベースライン・model_verification）
- `mouse/` — ロボット v2（params・ビルダー・環境）
- `assets/mouse_v2.xml` — 新ロボットモデル（パラメータの正本は MODEL_VERIFICATION_PLAN §4）
- `experiments/` — 実験カード（1 実験 1 変更、RESEARCH_PLAN §9）
- `research_notes/` — 研究ノート（意思決定・考察の記録、テンプレートあり)
- `tests/` — 新系統のテスト

**鉄則**: **評価用に予約された seed 帯は学習・調整に使用禁止**（帯の定義は `docs/RESEARCH_PLAN.md` §2・§9-7 が正本。実装上の安全弁も §9-7 に規定）。ロボット物理パラメータの変更は MODEL_VERIFICATION_PLAN の改訂＋検証スイート再実行が必須。**ドキュメントとコードの数値を乖離させない**（乖離を見つけたら、正本を正とし、教授セッションへ報告する）。

## レガシー（凍結・変更禁止）

旧 Phase1〜4 系一式は `legacy/`（`legacy/phase1_open/ legacy/phase2_slalom/ legacy/phase3_maze/ legacy/phase4_speed/`、旧 `assets/micromouse_*.xml`、`models/*.zip`、旧 README.md の記述）へ移設済み（2026-08-10）。**過去実験の再現性保全のため凍結**。既知の重大欠陥（質量が非現実的・キャスタ実効摩擦 μ=1・壁すり抜け等、MODEL_VERIFICATION_PLAN §2、要約は `legacy/README.md`）を含むため、**新規作業で流用・参照しないこと**。

## セッション再開手順

1. **自分の引き継ぎメモ `handover/<役割>.md` を読む**（規約は `docs/SESSION_HANDOVER.md`）
2. `git log -5 --oneline` と直近コミットの Next steps を確認
3. `research_notes/` の最新ノートを読む
4. `git status` で未コミット変更を確認（他セッションの作業中ファイルの可能性あり — 勝手に消さない・コミットしない）
5. 役割が不明なら、まず教授セッション宛て（ListAgents で確認）に SendMessage で確認

## 引き継ぎメモの維持（必須）

長時間稼働すると動作が重くなり作業途中で停止することがあるため、**定期的にまとめてセッションを入れ替える**運用とする。各セッションは `handover/<役割>.md` に現況（進行中タスク・未コミット変更・直近の裁定・詰まっている点・次にやること）を**常時維持**すること。更新の契機はコミット時・作業の区切り・**中断するとき**。詳細は `docs/SESSION_HANDOVER.md`。

## 環境

- Python は `.venv`（mujoco / gymnasium / stable-baselines3 / torch 導入済み）。バージョンは requirements.txt を参照
- Gymnasium API・OutputManager（`common/output_manager.py`、これは新系統でも継続使用）・TensorBoard ログの慣例は踏襲
