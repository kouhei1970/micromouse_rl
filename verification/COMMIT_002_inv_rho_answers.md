# 先行コミット 002: $1/\rho$ の面ごと回答表（現行マスク・R34 是正後マスク）

- 作成: 2026-08-14 未明 准教授セッション（独立検証担当）
- 発注: 教授（裁定 **2026-08-13-R34** の「盲検の手当て」）
- 方式: **`COMMIT_001` と同じ** — 回答をハッシュで先に固定し、**学生B の提出後に開示して照合する**
- 先行コミット時点の HEAD: `7c94e8b`

---

## 1. なぜハッシュを先に出すのか

`AUDIT_011` §5-quinquies で、**中央値 2 つ（0.652・0.7146）と範囲（0.4965〜0.7518）は既に開示している。**
したがって**中央値での照合はもう盲検ではない。**教授の指示により、照合は**面ごとの値**で行う。

**面ごとの値を先に公表すると学生B の測定が引きずられ、後に公表すると
「相手の値を見てから自分の値を書いた」疑いが消せない。**
**ハッシュを先に出せば、両方を避けられる。**

## 2. 固定した回答（中身は未開示）

| 対象 | SHA-256 | バイト数 |
|---|---|---|
| **回答表** `r34_answers.json` | `0ff85ccf4904af732837dbd2d4a7923c27302b94adbf5258d309603a2df8a701` | 5767 |
| **生成スクリプト** `predict_r34_inv_rho.py` | `a42d8550a694f2a0d0b473141368eaeef7b150d407725656b8a66a5280baec3f` | — |

**回答表に入っている項目**（値は未開示）:

- 学習迷路 6×6 loop の **seed 1〜20 の各面**について
  - $D_0$、スタート区画
  - **マスク A（現行）** での $g(\text{start})$ と $1/\rho$
  - **マスク B（R34 是正後）** での $g(\text{start})$ と $1/\rho$
  - 各マスクの許可格子点数
- **両マスクの中央値**（マスク A の中央値 0.652 は開示済み。**マスク B の中央値は未開示**）

## 3. 回答の作り方（開示時に検算できるように、先に書いておく）

- **格子・グラフ位相・Dijkstra は実装のものをそのまま使う**
  （`Maze6Env._compute_geodesic_field()` を呼ぶ。6 mm 格子・181²）。**差し替えるのはマスクだけ**
- **マスク A（現行）**: `Maze6Env._geo_allowed_mask()` をそのまま呼ぶ
- **マスク B（R34 是正後）**: **物理モデル `mouse/mjcf.py` が実際に生成する箱**
  （壁 = 中心線 $\pm t_w/2$・長手は柱の間、柱 = 格子点の 12 mm 角）**の表面からの距離
  $\ge w_\text{lat}$ = 0.0400 m**。矩形への厳密なユークリッド距離で判定する（円板モデル）
- $1/\rho = g(\text{start})/(0.18\,D_0)$、**始点はスタート区画の中心**

> ⚠️ **実装は $g(\text{start})$ を擾乱つきの reset 位置で取る。**
> 私の回答は**区画中心**なので、**面ごとの比較には ±の揺れが乗る。**
> **一致を要求するのは「順序と大小関係」までであり、厳密一致ではない。**
> 揺れの大きさを見積もるには、同じ面で reset を複数回引いた分布が要る（学生B 側で出せる）。

## 4. 開示の条件

**学生B が $1/\rho$ の再測定値を提出した後**に、上記 2 ファイルを `verification/` へコミットして開示する。
コミットされたファイルの SHA-256 が**本文書の値と一致すること**をもって、
**後から書き換えていない**ことの担保とする。

## 5. この先行コミットの弱点（自分で挙げる）

1. **🔴 漏洩経路が実在する。**`verification/audit_r33_config_space.py`（`7c94e8b` でコミット済み）は
   **マスク A の面ごとの値を再現できる**（`prediction_impl_inv_rho` の `values`）。
   **学生B が提出前にこれを実行すれば、私の回答（マスク A 側）を見られる。**
   本スクリプトは `AUDIT_011` §5-quinquies の全数値の生成元なので**伏せるわけにはいかない**
   （伏せれば §5-ter と同じ「検算の手段が消える」失敗になる）。
   → **教授へ: 学生B に「提出前に本スクリプトを実行しないこと」を指示するかどうかの裁定を仰ぐ。**
   **マスク B 側の生成スクリプトはコミットしていない**ので、この経路では漏れない
2. **回答ファイルをリポジトリ外（セッション作業領域）に置いている。**
   共有ツリーで他セッションの `git add -A` に巻き込まれて**先に公表されるのを避ける**ためだが、
   **セッションが失われるとファイルも失われる。**
   **その場合は再構成せず「先行コミットは不成立」と報告する**（再構成した値でハッシュが合うはずがなく、
   合わせようとすること自体が本方式の趣旨に反する）
3. **私の回答も予測であって真値ではない。**マスク B は**円板（半径 $w_\text{lat}$）近似**であり、
   機体の前端 0.0500 m を含まない（裁定 R34 で明記させる方針の限界そのもの）。
   **不一致が出たときに「私が正しい」とは限らない。**
