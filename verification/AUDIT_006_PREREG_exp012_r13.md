# 監査 006 事前登録: exp_012 改訂版（裁定 R13 による Φ 定義の是正）の確認項目

- 登録: **2026-08-13 18:16** 准教授セッション（独立検証担当）
- 登録時点の HEAD: **`893202da127b250615aa06db7287247b12f664a9`**
- 発注: 教授セッション `uds:/tmp/cc-socks/56989.sock`（発注 1・**監査完了が投入の前提**。R8-3 の「報告と同時投入」は不適用と裁定済み）

---

## 0. なぜ事前に登録するのか

**改訂版 `design.md` を読む前に、確認項目と合否条件を確定させておくため。**
監査 003 §8 で同じ形を取り、監査 005 で実行した。後から項目を選べば、
「見つかったものが確認項目だった」ことにできてしまう（後知恵バイアス）。

**本文書を登録した時点で私が読んでいないもの**:

- 改訂版 `experiments/exp_012_continuous_potential/design.md`（§4 の新 Φ 定義を含む）
- 学生B の単体テスト `tests/test_maze6_potential.py` の内容（コミット `893202d` は未読）
- 学生B の実装（`mouse/` 配下の Φ 連続化コードの改訂版）

**私が知っていること**（＝この登録の材料）:

- 改訂前の `design.md`（`2e4ec33`）— 監査 005 で全文読解済み
- 裁定 R13 の 2 案の**要旨のみ**（教授メッセージ）: 案 1 ＝ 線分ごと残り弧長の min ／
  案 2 ＝ 降下隣接すべての min・tie-break 廃止
- 学生B のテストが検出した欠陥 2 件の**要旨のみ**（同）:
  - **D1**: 折れ区画内側の角二等分線上に真の不連続（最大 0.124 m・刻み幅無依存・$\sqrt{2}\rho$ の解析解と 1.4e-9 一致）
  - **D2**: tie-break が選ばなかった降下開口部を**前進**通過すると跳ぶ（最大 0.18 m。前進で起きるので裁定 R12 の除外 2 種に当たらない）
- 未確認①（`step()` 経路の `_prev_cell`）は学生B の (b-3) で閉じた（2346 ステップ・最大差 0.000e+00）

---

## 1. 事前に立てる予測（改訂版を見る前の私の見立て）

**外れたら外れたと書く。**当たったかどうかも監査の材料にする。

### 予測 P-A: 「降下開口部すべての min」を取れば、D1 と D2 は**同時に**消えるはずである

理由は 1 行で言える — **1-Lipschitz 関数の min は 1-Lipschitz** である。
各降下開口部 $i$ について「その開口部を通る残り弧長」は
$d_i(\mathbf{x}) = |\mathbf{x} - \mathbf{w}_i| + \text{const}_i$（$\mathbf{w}_i$ は経由点）の形で、
三角不等式よりリプシッツ定数 1。したがって $d(\mathbf{x}) = \min_i d_i(\mathbf{x})$ も
リプシッツ定数 1 で、**不連続は原理的に生じない**。

**D1・D2 はどちらも「min を取らず tie-break で 1 本選んだ」ことの帰結である**と予測する:

- **D1**（角二等分線上の跳び $\sqrt{2}\rho$）は、二等分線が「どちらの経由点が近いか」の
  切り替え面だから、min なら値が一致して連続、tie-break なら選ばれた側の値へ跳ぶ
- **D2**（選ばれなかった降下開口部の前進通過での跳び 0.18 m）も同じ切り替え面の話で、
  跳び幅が 1 区画ぶんなのは経由点が 1 区画ずれるため

→ **つまり案 2 は D1・D2 の両方に効く単一の是正である。**案 1（線分ごとの min）が
どちらに効くかは定義を見るまで判断を保留する。

### 予測 P-B: Φ は「位置のみの関数」になり、報酬ポンプの検査は 1 行に縮む

$d(\mathbf{x})$ が上式の形なら $\Phi$ は $(x, y)$ と迷路だけの関数で、
`_prev_cell` などの履歴に依存しない。**監査 005 で確立した「正しい検査は
『$\Phi$ が状態の一価関数か』の一点」**（自分の誤った最初の検査を棄却して得た結論）が、
そのまま自明に満たされる。**ポンプ検査は閉路の列挙ではなく、
「実装が位置以外の状態を読んでいないこと」の確認に置き換わるはずである。**

### 予測 P-C: 錨（区画中心で階段版と一致）は保たれるが、**自明ではない**

区画中心での残り弧長は、中心を結ぶ折れ線で測れば「曲がる区画でも 0.09 + 0.09 = 0.18 m」
なので $d = 0.18 D$ となり階段版と一致する。**ただし min を全開口部に広げると、
経由点を直線で結ぶ取り方によっては角を切って $d < 0.18D$ になりうる。**
錨が壊れるとすれば**ここ**なので、区画中心の一致は全区画で機械的に確認する。

---

## 2. 確認項目（合格条件と反証観測）

**各項目について「主張が偽ならどの観測がどう見えるか」を先に書く**（グローバル規約の反証形式）。

| # | 確認項目 | 合格条件 | **反証観測（偽ならこう見える）** | 実施方法 |
|---|---|---|---|---|
| **N-1** | **新定義の連続性（1-Lipschitz 性）** | 迷路の走行可能領域の全域で $|\Phi(\mathbf{x}_1)-\Phi(\mathbf{x}_2)| \le k\,|\mathbf{x}_1-\mathbf{x}_2|$ | 細かい格子で差分商 $|\Delta\Phi|/|\Delta\mathbf{x}|$ を取ると **1 を超える点が残る**。刻みを半分にしても跳び幅が縮まなければ真の不連続 | 独立実装した $\Phi$ を格子（区画内 $\ge 40\times40$）で評価し、**刻み幅 2 段階**で差分商の最大値を比較。**刻み幅無依存なら真の不連続**（学生B の D1 の判定法と同じ土俵に乗せる） |
| **N-2** | **錨: 区画中心で階段版と一致** | 全 256 区画（到達可能なもの全部）で $\Phi_\text{連続}(\text{中心}) = \Phi_\text{階段}$、許容 1e-12 | 曲がる区画・複数降下開口部を持つ区画で**中心の値が階段版より小さい**（角を切っている）。ずれが 0.18 m の端数で出る | 全区画で機械的に照合。ずれた区画は「開口部の数」「曲がるか」で層別して原因を切り分ける |
| **N-3** | **境界一致を全降下開口部で** | 隣接 2 区画の共有辺上の全点で、両区画側から計算した $\Phi$ が一致（許容 1e-12） | 特定の辺で系統的に $\rho$ や $0.18$ の端数だけずれる。**tie-break が残っていれば「選ばれなかった開口部」の辺で出る** | 監査 005 の R-5（2032 通り・最大差 4.4e-16）を**全降下開口部×辺上の複数点**へ拡張。**tie-break 廃止の実効性はここで決まる** |
| **N-4** | **報酬ポンプ R-6'** | $\Phi$ が位置のみの一価関数であること。同一位置に**異なる履歴**で到達したとき値が一致 | 同じ $(x,y)$ でも直前区画が違うと $\Phi$ が違う。＝ $\Phi$ が多価 | ①実装が位置以外の状態を読んでいないかを読解 ②同一位置へ複数経路で到達させて値を照合。**予測 P-B が当たれば閉路列挙は不要**（不要になったこと自体を報告する） |
| **N-5** | **D1 の解消** | 角二等分線を横切る経路で $\Phi$ が連続。学生B の実測 0.124 m が **0 になる** | 二等分線上に跳びが残る。**縮んだが 0 でない**場合は「別の是正が要る」ので合格にしない | 学生B が D1 を出した条件（折れ区画内側）を**独立に再現**してから、新定義で測り直す。$\sqrt{2}\rho$ の解析解も独立に導出して照合する |
| **N-6** | **D2 の解消** | tie-break が選ばなかった降下開口部を前進通過しても跳ばない。実測 0.18 m が **0 になる** | 特定の開口部でだけ 0.18 m の跳びが残る（＝ min が一部の開口部に及んでいない） | 全区画の全降下開口部を列挙し、各開口部を前進通過する経路で $\Delta\Phi$ の最大値を取る。**「選ばれた／選ばれなかった」の区別なく全数**で見る |
| **N-7** | **採用案の明示と、不採用案との差の記述** | 案 1・案 2 のどちらを採ったかが書かれ、**もう一方との差**（値が違う点があるか）が示されている | 「連続化した」とだけ書かれ、どちらの定義か文面から復元できない。または両案が同一だと**根拠なく**書かれている | 両案を独立に実装し、格子上で値を照合。**同一なら同一であることを証明つきで、違うなら違いを定量で** |
| **N-8** | **整形の telescoping** | 割引総和が $\gamma^T\Phi_T-\Phi_0$ に telescoping する（ポテンシャル整形の定義どおり） | 整形報酬の定義が $\gamma\Phi(s')-\Phi(s)$ の形から外れている（例: $\gamma$ が落ちている・$\Phi$ の符号が経路依存） | 条文の式を代数で確認。監査 005 の**自分の誤り**（割引なしの閉路和で判定しかけた）を再発させないため、**割引つきで見る** |
| **N-9** | **§1 の数値表の再計算** | 改訂後の $\Phi$ で §1 の表が再計算され、値が更新されている | 表が**改訂前のまま残っている**（tie-break 前提の値）。監査 003 で全 6 行一致（最大差 9e-4）を確認した表なので、**定義が変われば必ず動く** | 新定義で独立に再計算して照合。**動いていなければそれ自体が指摘** |
| **N-10** | **§7-6 の「14.4%」など tie-break 前提の記述の掃除** | tie-break 廃止で無効になった記述が更新または撤回されている | 「後戻り 1448 通りのうち 208 件（14.4%）で跳ぶ」がそのまま残る。**tie-break が無いのだから、この文の前提が消えている** | 全文を読み、tie-break・「選ばれた開口部」に依存する記述を列挙して突き合わせる |
| **N-11** | **⚠️ 事前登録済みの判定基準が動かされていないか** | **支持＝ 3 seed の中央値 ≥0.30 ／ 棄却＝ 3 seed すべて <0.10 ／ 打ち切りは棄却側に算入**、副指標も §5-2 のまま | 閾値・集約統計（中央値→最大値 等）・打ち切りの扱いのいずれかが変わっている | §5-2 を逐語で突き合わせる。**動いていれば、それ自体が最優先の指摘**（前任からの申し送り: 「動かす提案が出たら、それ自体が指摘対象」） |
| **N-12** | **H（機構仮説）の記述が新しい介入と整合しているか** | 監査 003 の C2（階段でも連続でも 1 区画あたり整形総和は $\gamma\cdot0.18$ で同一、という代数的恒等式）と、監査 005 で確認した「§7-7 の免責が主張の本体へ昇格した」構図が保たれている | 「連続化により報酬の平均が改善する」型の主張が**復活**している（C2 で棄却済み） | §3・§7-7 を読み、C2 の恒等式が新定義でも成立するかを代数で再確認 |

---

## 3. 実施の方針

1. **$\Phi$ は改訂版の条文から私が独立に実装する。**学生B の実装は**照合にのみ**使う
   （exp_013 の再計算と同じ作り）。条文だけで実装できなければ、それ自体が指摘（条文の穴）
2. **N-1・N-5・N-6 は「刻み幅を 2 段階で変える」ことを必ず行う。**
   刻み幅に比例して縮む跳びは離散化の artefact、**縮まない跳びが真の不連続**
3. **N-2・N-3・N-6 は全数で行う**（サンプリングしない）。到達可能な全区画・全降下開口部
4. **合否は「崩せたか」で書く。**「崩せなかった」と「支持された」を書き分ける
5. 計算量が問題になる場合は教授へ相談する（学生 2 名が学習・評価を回している）

## 4. 私から提供を申し出るもの

- **N-3（全降下開口部の境界一致）と N-6（全開口部の前進通過）の全数検査**。
  監査 005 の R-5・R-6 で使った枠組みをそのまま拡張できる
- **$\sqrt{2}\rho$ の解析解の独立導出**（学生B の D1 の根拠の裏取り）

## 4-bis. 【追記 2026-08-13 18:4x】予測 P-A の前提が崩れた — 改訂版を読む前に記録する

**裁定 R18 の通知（教授セッション）により、私の P-A の前提が誤っていたことが分かった。
改訂版 `design.md` はまだ読んでいない。**この時点で記録しておく。

**誤っていたのは「リプシッツ定数が 1」の部分である。**私は各降下開口部の残り弧長を
$d_i(\mathbf{x}) = |\mathbf{x}-\mathbf{w}_i| + \text{const}_i$ と書いたが、**これは直線距離の式であって、
$\Phi$ が実際に測っている量（折れ線に沿った弧長）ではない。**曲がる区画では、
入口の経由点 $\mathbf{w}_\text{in}$ と出口の経由点 $\mathbf{w}_\text{out}$ の**直線距離は $0.09\sqrt{2}$** しかないのに、
**弧長は区画中心を経由するので $0.18$ 必要**である。したがって

$$\frac{|\Delta\Phi|}{|\Delta\mathbf{x}|} \ \ge\ \frac{0.18}{0.09\sqrt{2}} \ =\ \sqrt{2}$$

**が下限として立ち、$|\nabla\Phi|\le 1$ は境界一致と数学的に両立しない。**
学生B が実装前報告で示し、教授も検算済みとのこと。

**この誤りにより P-A のどこが崩れ、どこが残るか**:

| P-A の主張 | 状態 |
|---|---|
| 「1-Lipschitz 関数の min は 1-Lipschitz」 | **定理としては正しいが、$d_i$ が 1-Lipschitz でないので適用できない。前提が偽** |
| リプシッツ定数は **1** | **棄却。正しくは $\sqrt{2}$ が下限** |
| **min を取れば連続**（D1・D2 が同時に消える） | **残る。**リプシッツ定数が何であれ、**有限個のリプシッツ連続関数の min はリプシッツ連続**なので、不連続は生じない |
| D1・D2 が**同じ切り替え面**に由来する | **未判定。**改訂版で検証する |

**確認項目 N-1 の合格条件を改訂する（緩和ではなく訂正）**:
- 旧: 「全域で $|\Phi(\mathbf{x}_1)-\Phi(\mathbf{x}_2)| \le k|\mathbf{x}_1-\mathbf{x}_2|$」（$k$ を 1 と想定していた）
- **新: 「全域でリプシッツ連続であること。定数は $\sqrt{2}$ 以下に収まるか、
  収まらない場合はその区画の幾何から説明がつくこと」**
- **追加（N-1b）: $\sqrt{2}$ という下限そのものを独立に再導出する。**
  $0.09\sqrt{2}$ と $0.18$ の由来（区画寸法 0.18 m・経由点が辺の中点・中心経由の折れ線）を
  自分で確認し、学生B の解析と一致するかを見る。**下限の導出が正しければ、
  「$|\nabla\Phi|\le1$ を課すこと自体が誤り」という結論も同時に確定する**

**この訂正は、改訂版 `design.md`・実装・テストのいずれも読まずに、
教授通知の要旨だけから導いたものである。**判定の独立性は保たれている。

> **教訓（自分用）**: 「$|\mathbf{x}-\mathbf{w}|+\text{const}$ の形だから 1-Lipschitz」と書いた時点で、
> **測る量（弧長）と、その引数の距離（ユークリッド）を取り違えていた。**
> 経路に沿った量は、経路が曲がる分だけ位置に対して速く変わる。
> **「min は連続性を保つ」という定性的な部分だけが正しく、定数の主張は幾何を見ていなかった。**

## 4-ter. 【先行実施 2026-08-13 19:0x】N-1b を改訂版を読む前に実施した

**改訂版 `design.md`・実装・テストはいずれも未読のまま実施した。**
N-1b は迷路の幾何と壁配列だけで決まるので、改訂版を待つ必要が無い。

### (1) $\sqrt{2}$ の下限を独立に導出した — 学生B・教授の解析と一致

区画中心を原点、区画寸法 $s$、経由点を各辺の中点に置く。境界一致の要求は
「距離 $D$ の区画と $D-1$ の区画の共有辺で $\Phi$ が $D-0.5$ 区画ぶんの値をとる」ことなので、
**1 つの区画の入口の辺中点と出口の辺中点で、$\Phi$ はちょうど 1 区画ぶん（$k\cdot s$）違う。**

| 入口と出口の関係 | 経由点間の直線距離 | 必要な $|\Delta\Phi|/k$ | $L$ の下限 |
|---|---|---|---|
| **直交（曲がる区画）** | $(s/2)\sqrt{2}$ = 0.127279 m | $s$ = 0.180000 m | $\dfrac{s}{(s/2)\sqrt{2}} = \sqrt{2}$ = **1.414214** |
| 対向（直進する区画） | $s$ = 0.180000 m | $s$ = 0.180000 m | 1.000000 |

**4 辺の 12 通りの入口・出口の組をすべて数え上げ、直交 8 通りが $\sqrt{2}$、対向 4 通りが 1** となった。
**最も厳しいのは曲がる区画で、下限は $\sqrt{2}$。**

**$s$ が約分される**ので、この定数は**区画寸法によらない純粋な幾何の帰結**である。
18 cm を変えても $\sqrt{2}$ は動かない。

> **判定: 学生B の解析（$0.09\sqrt{2}$ 対 $0.18$ → 下限 $\sqrt{2}$）と一致する。**
> **したがって「$|\nabla\Phi|\le 1$ を課すこと自体が誤り」という結論も、独立に確定した。**
> 私の P-A（リプシッツ定数 1）の棄却は正しい。

### (2) 副産物: 案 1 型（経由点へのユークリッド距離）が境界一致を壊す量を出した

$d(\mathbf{x}) = |\mathbf{x}-\mathbf{w}_\text{out}| + R_\text{out}$ とすると、曲がる区画の入口中点での値は
$0.127279 + R_\text{out}$ になるが、境界一致が要求するのは $0.180000 + R_\text{out}$ である。

**差 0.052721 m（29.29% ＝ $1 - 1/\sqrt{2}$）。**曲がる区画では**必ず**壊れる。
**裁定 R18 で案 1 が撤回された理由を、独立に再現した。**

### (3) N-3 の適用範囲を確認した — **降下開口部の検査で漏れは無い**（ゴール区画内部を除く）

$\Phi$ の連続性は**開いている全ての辺**で要る。降下開口部だけを検査すると、
「距離が等しい隣接区画どうしの辺」が漏れるのではないかを確認した。

評価帯 v4・20 面の開いた辺 5481 本を数えたところ:

- **降下（距離差 1）: 5401 本（98.5%）**
- **等距離: 80 本（1.5%）— そのすべてがゴール 2×2 区画の内部辺**（外部は **0 本**）

**理由は幾何ではなくグラフの性質である**: 16×16 の格子グラフは**二部グラフ**なので任意の閉路長が
偶数になり、**連結な隣接区画の BFS 距離は必ずパリティが異なる ⇒ 差はちょうど 1**。
等距離が生じうるのは「距離 0 の始点集合が複数区画にまたがる」ゴール区画の内部だけである。
§2 が要求するループ（複数経路）があっても**この結論は変わらない**。

> **N-3 の実施方法を訂正する（範囲の確定であって、緩和ではない）**:
> 「全降下開口部 × 辺上の複数点」で、**ゴール区画の内部辺を除く全ての開口部が尽くされる**。
> **追加項目 N-3b**: **ゴール 2×2 区画の内部（4 区画すべて距離 0）で $\Phi$ が
> どう定義されているかを別途確認する。**降下隣接が存在しないので
> 「全降下隣接の min」が定義されず、**別扱いが要るはずの唯一の領域**である。
> ここが未定義・不連続だと、ゴール到達直前の整形報酬が壊れる。

**（削除した登録項目は無い。N-1 の合格条件の訂正と N-3b の追加のみ。）**

## 4-quater. 【追加登録 2026-08-13 19:3x】裁定 R21 — 2 条件（E・C）の確認項目

**改訂版 `design.md`・実装・テストはいずれも未読のまま登録する。**
材料は教授メッセージの要旨のみ:

- **条件 E**: 明示式＋**中点一致**（(b) の定数を **3.2223** へ改訂）。
  学生B の主張「**$\sqrt{2}$ は代表点による過小評価**」の検証を含む
- **条件 C**: **測地距離場**（1-Lipschitz 定数 1・全辺連続・**錨なし**）
- 各条件 3 seed。ユーザ方針「考えられることは全てやって比較する。結果と理由を求める」

### N-1c: 定数 3.2223（辺全体一致の下限）の独立再導出

**先に、材料を見ずに言える構造的事実を 1 つ登録する。**

> **中点だけの一致条件は、辺全体の一致条件の部分集合である。**
> リプシッツ定数の下限は「制約を満たす点対の上限」なので、**制約を増やせば下限は
> 増えるか変わらないかのどちらかで、減ることはない。**
> $$L_\text{辺全体} \ \ge\ L_\text{中点} = \sqrt{2}$$
> **したがって「$\sqrt{2}$ は過小評価である」という学生B の主張は、値を見る前に
> 構造として正しい。**争点は向きではなく**値**だけである。

| # | 確認項目 | 合格条件 | **反証観測** |
|---|---|---|---|
| **N-1c-1** | 3.2223 が $\sqrt{2}$ 以上 | $3.2223 \ge 1.41421$ ✓（**登録時点で確認済み**） | — |
| **N-1c-2** | **導出が書かれているか**（数値だけでないか） | 「どの 2 点対で上限が取られるか」が明示され、私が同じ最適化を組んで再現できる | 定数だけが書かれ、出所が追えない。**私は 18 通りの単純な閉形式を当たったが 3.2223 に一致するものは無かった**（最近傍は $s/((s-t)/3)=3.2143$ 差 0.008、$1+\sqrt5=3.2361$ 差 0.014）。**特定の最適化の値と見られるので、導出が無ければ検算できない** |
| **N-1c-3** | **次元依存性** | $s$（区画寸法）が約分されるか、しない場合は**壁厚 $t$ との比 $t/s$ に依存する形**で書かれている | 0.18 や 0.012 が式に生のまま残る。その場合、**壁の規格を変えると定数が黙って変わる**（M5 基準値の凍結と同じ型の問題） |
| **N-1c-4** | 中点一致を選んだ理由が書かれているか | 「辺全体一致だと定数が 3.2223 に跳ね上がるので中点一致に留める」という**交換条件として**書かれている | 「中点で一致する」とだけ書かれ、**辺全体では一致しないことが読み取れない**。これは条文の穴になる |

### 条件 C（測地距離場）の確認項目

| # | 確認項目 | 合格条件 | **反証観測** |
|---|---|---|---|
| **C-1** | 1-Lipschitz（定数ちょうど 1） | 走行可能領域の全域で差分商 ≤ 1（離散化誤差の範囲で） | 1 を超える点が残る。測地距離は三角不等式から必ず 1-Lipschitz なので、**超えるなら距離場になっていない** |
| **C-2** | 全ての開いた辺で連続 | 区画境界で跳びが無い | 区画境界で跳ぶ。**それは区画ごとに別々に計算していて、大域的な距離場になっていない兆候** |
| **C-3** | **錨が無いことの明記と、外れ量の定量** | 「区画中心で階段版と一致しない」ことが明記され、**外れ量が数値で出ている** | 「ほぼ一致する」と書かれている。**曲がる区画では最大 $1-1/\sqrt2 = 29.29\%$ 外れる**（登録時点の私の見積り） |
| **C-4** | **⚠️ 監査 003 の C2 恒等式が条件 C では成立しないことの明記** | 下記の交絡が設計書に書かれている | 条件 C が「同じ報酬の連続化」として書かれている。**それは誤り**（下記） |
| **C-5** | $|\nabla\Phi|$ の検査方法 | **差分商で検査**している | 解析的な勾配で検査している。測地距離場は**カットローカス（等距離線の合流線）で微分不可能**なので、解析勾配は定義されない点がある |
| **C-6** | **離散化の感度** | 格子解像度を 2 段階で変えて値と 1-Lipschitz 性を確認している | 単一解像度でしか確認していない。**8 近傍 Dijkstra は測地距離を最大 8% 程度過大評価する**ので、解像度依存は条件 C 固有の新しい故障様式である |
| **C-7** | 報酬ポンプ | $\Phi$ が位置のみの関数 → **自明に無し**（予測 P-B のとおり） | 位置以外の状態を読んでいる |

### C-4 の中身 —【最重要】E と C の比較は「連続性」と「報酬の大きさ」を交絡させる

監査 003 の C2 は「**階段版でも連続版でも、1 区画あたりの整形の総和は $\gamma\cdot0.18$ で同一**」という
代数的恒等式だった（教授評「本監査の白眉」）。**これは条件 E では保たれるが、条件 C では崩れる。**

測地距離場は**曲がる区画で角を切る**ので、1 区画あたりの整形量が

| | 直進する区画 | 曲がる区画 |
|---|---|---|
| 階段版・条件 E | $k\cdot0.18$ | $k\cdot0.18$ |
| **条件 C** | $k\cdot0.18$ | **$k\cdot0.12728$ 以下**（$-29.29\%$） |

となる。**総整形量 $=k\cdot d(\text{スタート})$ は $k\cdot0.18\,D_0$ より小さくなり、
その差は経路の曲がり方に依存する。**

**評価帯 v4 の幾何からの概算**（$D_0$ と真の最短経路上の旋回数を使った見積り）:

> **条件 C の総整形量 / 条件 E の総整形量 = 中央値 0.837（範囲 0.812〜0.863）**
> ＝ **条件 C は報酬の大きさが約 16% 小さい。**

**したがって条件 C が条件 E より悪い結果を出しても、原因が「連続性・リプシッツ定数」なのか
「整形報酬の大きさが 16% 小さいこと」なのかを、この設計では分離できない。**

> **登録する要求**: **面ごと（学習迷路ごと）に $d(\text{スタート})/(0.18\,D_0)$ を報告する**こと。
> これを出せば交絡の大きさが定量され、結果の解釈が可能になる。
> **さらに望ましいのは、条件 C の $k$ を $0.18D_0/d(\text{スタート})$ で規格化した第 3 条件**だが、
> 条件を増やすことの是非は教授の判断（下記 N-11b の多重性の問題がある）。

### N-11b: 条件が 2 つになったことによる多重性

**事前登録した判定基準は 1 条件を前提に書かれている**（支持＝ 3 seed の中央値 ≥0.30 ／
棄却＝ 3 seed すべて <0.10 ／打ち切りは棄却側）。

**2 条件を試して「どちらかが支持されれば H を支持」とすると、偽陽性の確率は約 2 倍になる。**
これは「考えられることは全てやって比較する」という方針の**統計的な代価**であって、
方針が悪いのではなく、**代価を先に勘定に入れておく必要がある**という話である。

| # | 確認項目 | 合格条件 |
|---|---|---|
| **N-11b** | **2 条件の合成規則が結果を見る前に決まっているか** | 「E と C を独立に判定し、**それぞれについて支持/棄却を述べる**」のか、「どちらかが支持なら H を支持」なのかが**明記**されている。後者を採るなら**閾値を上げるか、多重性を明記する** |

**⚠️ 結果が出た後にこの規則を決めるのは、事前登録の意味を失わせる。**
**私は結果を見る前に本項を登録した。**

**（削除した登録項目は無い。N-1c・C-1〜C-7・N-11b の追加のみ。）**

## 4-quinquies. 【N-1c を一部先行実施 2026-08-13 19:5x】定数のパラメトリック形

**改訂版 `design.md` は未読。**材料は教授メッセージ（裁定 R24）の
「$0.05586 = 2(s/2 - w_\text{robot})$、機体半幅 $w_\text{robot}=0.0395$」という記述のみ。

### (1) 伝達された式は計算が合わない。**成立する形は別**

$$2\left(\frac{s}{2}-w_\text{robot}\right) = 2(0.09-0.0395) = \mathbf{0.101}\ \text{m} \ \ne\ 0.05586$$

$s/0.101 = 1.782$ で、目標の 3.2223 とは **1.8 倍**ずれる。**一方**

$$\sqrt{2}\,w_\text{robot} = \sqrt{2}\times0.0395 = \mathbf{0.055861}\ \text{m},\qquad
\frac{s}{\sqrt{2}\,w_\text{robot}} = \mathbf{3.22226}$$

で **3.2223 に一致する**。

**幾何としては両方が同じ導出の中に現れる**ので、伝達時の取り違えと見られる:

- 機体中心が辺の上で到達できる範囲は、両端が機体半幅ぶん内側に入るので
  **長さ $2(s/2-w_\text{robot}) = 0.101$ m**（＝伝達された式の値）
- **その到達可能区間どうしの最接近距離**が、直交する 2 辺では
  端点から隅までの隙間 $w_\text{robot}$ を 2 辺ぶん見て $\sqrt2\,w_\text{robot} = 0.055861$ m
- 必要な $|\Delta\Phi|$ は 1 区画ぶん $= s$ なので $L \ge s/(\sqrt2\,w_\text{robot})$

> **求めるべき無次元形**: $$\boxed{\;L \ \ge\ \frac{s}{\sqrt{2}\,w_\text{robot}} \;=\; \frac{1}{\sqrt2}\cdot\frac{s}{w_\text{robot}}\;}$$
> **区画寸法と機体半幅の比だけで決まる。**$s$ も $w_\text{robot}$ も生では残らない。
> N-1c-3（次元依存性）の合格条件はこの形である。

### (2)【要確認】$w_\text{robot}=0.0395$ は、評価器が使う footprint の値ではない

**計時とゴール進入判定が使っている footprint の規約**（geom の AABB 8 隅を機体座標系へ写す。
監査 007 §4 で教授が読解）を、**同じ規約のまま $\pm y$ 方向に適用して測った**:

| 量 | 値 | 出所となる geom |
|---|---|---|
| **$+x$ 最大（＝ front_offset）** | **0.05051 m** | `mein_body2` / `mein_body3`（前側コーナー片） |
| **$\pm y$ 最大（＝ 機体半幅）** | **0.04141 m** | 同上 |
| 車輪の $y$ 張り出し | 0.0395 m | `left_wheel` / `right_wheel` |
| シャーシ box の半幅 | 0.0300 m | `mein_body1` |

**$+x$ = 0.05051 は、監査 007 で軌跡から挟み込んだ $[0.0505, 0.0506)$ と一致する。**
（計時・`visited_cells` に続く **3 つ目の独立確認**。0.0500 との 0.5 mm 差の出所も
コーナー片 `mein_body2/3` と特定できた。）

**同じ規約で測った機体半幅は 0.04141 m であって 0.0395 m ではない。**
0.0395 は**車輪の張り出しだけ**で、コーナー片の方が外側にある。

$$w_\text{robot}=0.04141 \ \Rightarrow\ L \ge \frac{0.18}{\sqrt2\times0.04141} = \mathbf{3.0736}$$

**3.2223 との差は 4.8%。**

**どちらが正しいかは、$w_\text{robot}$ が何を表すかで決まる**（機体が壁にどこまで寄れるか）。
**寄れる限界を決めるのは最も外側の点なので 0.04141 のはず**だが、
**設計書の意図を読んでから判定する。**

> **N-1c-3 に追加する合格条件**: 定数は**モデルから実行時に導出**され、
> **評価器の footprint 規約（AABB 8 隅）と同じ値を使っている**こと。
> 数値のハードコードは `competition/` 側が既に禁じている原則（`front_offset()` の docstring）。
> **整形報酬の幾何と評価の幾何が別の機体寸法を使っている状態は、それ自体が欠陥である。**

**（本節は伝達された要旨からの推定を含む。改訂版 `design.md` を読んだ時点で判定を確定する。）**

### 【追記 2026-08-13 20:1x】照合先の値が 3.2223 → **2.7972** へ訂正された

学生B の自己申告（`49badea`）により、**3.2223 は壁厚の半分 $t_w/2 = 6$ mm を落とした誤り**と判明。
**本節 (1) は「誤った定数を逆算した記録」として残す**（削除しない）。**下記が確定版である。**

#### 導出を幾何から独立に再構成した — **一致した**

| 段 | 式 | 値 |
|---|---|---|
| 開口部の半幅 | $(s-t_w)/2$ | 0.084000 m |
| 中心が中点から外れられる量 | $\mu_\text{max} = (s-t_w)/2 - w_r$ | **0.044500 m** |
| 隅までの隙間 | $h-\mu_\text{max} = t_w/2 + w_r$ | 0.045500 m |
| 直交 2 辺の通行可能部の最短距離 | $d_\text{min}=\sqrt2\,(t_w/2+w_r)$ | 0.064347 m |
| **下限** | $L_E = s/d_\text{min}$ | **2.79735**（学生B の 2.7972 と一致） |

**上限を取る点対は「両辺とも内側の隅へ最大限寄った点」（$\mu=\lambda=+\mu_\text{max}$）**で、
私の再構成でも同じ点対になった。

**$\sqrt2$ の由来**: 2 点は $x$ 方向にも $y$ 方向にも同じ量 $(h-\mu_\text{max})$ だけ離れるので、
距離が $\sqrt2$ 倍になる。**$h = s/2$ が約分されて $t_w/2 + w_r$ になる**のがこの導出の要点で、
**最短距離は区画寸法に依存しない**（依存するのは分子の $s$ だけ）。

> **無次元形（確定）**: $$\boxed{\;L_E \ \ge\ \frac{s}{\sqrt2\,\left(\tfrac{t_w}{2}+w_r\right)}\;}$$
> **区画寸法・壁厚・機体半幅の 3 つの比だけで決まる。**生の 0.18 / 0.012 / 0.0395 は残らない。

#### 整合性の確認

- **中点のみの場合**（$\mu=\lambda=0$）: $d=\sqrt2\,h$ → $L=\sqrt2$。**§4-bis の私の導出と一致。**
- **単調性**: $L_\text{辺全体}=2.7973 \ \ge\ L_\text{中点}=1.4142$。
  **§4-quater で「値を見る前に」登録した構造的判定と整合する。**
- **旧値 3.2223 の構造**: $d_\text{min}$ から $t_w/2$ を落とすと $\sqrt2\,w_r = 0.055861$ →
  $L=3.22226$。**§4-quinquies (1) で私が逆算した $L=s/(\sqrt2\,w_r)$ は、
  壁厚が抜けた実装の幾何を正しく写していた**（＝誤りの構造を言い当てていた）。

#### §4-quinquies (2) の指摘は**そのまま生きている**

$w_r$ の取り方の問題は訂正後も残る:

| $w_r$ の取り方 | $L_E$ |
|---|---|
| 車輪の張り出し 0.0395 m（学生B 採用） | **2.7973** |
| **評価器の footprint 規約 0.04141 m**（コーナー片） | **2.6846** |

**差 4.0%。**正しい $w_r$（外側）を使うと定数は**小さくなる＝検査は厳しくなる**方向。
**N-1c-3 の合格条件（評価器と同じ footprint 規約を使うこと）は変更しない。**

#### 【実施 2026-08-13 20:3x】裁定 R25 — **真の最外側をメッシュ頂点から導出した**

教授裁定 R25 のとおり、**AABB（膨張する代理量）ではなく真の衝突幾何**で測り直した。
方法: 各 geom の**厳密な支持関数**（box・cylinder・sphere は解析形、**mesh は頂点の最大値**）を
機体座標系で評価する。

| 方向 | **真の最外側** | 決めている geom |
|---|---|---|
| $+x$（前端） | **0.05000 m** | `mein_body1`（シャーシ box）と `mein_body2`（コーナー片）が同値 |
| $\pm y$（半幅 $w_\text{lat}$） | **0.04000 m** | **`mein_body2`〜`mein_body5`（コーナー片）** |

**$+x$ = 0.05000 は、監査 007 で「モデルの最前端」として読んだ値と一致する。**
評価器の AABB 値 0.05051 との差 0.51 mm が**膨張ぶん**であることも、これで確定した
（同じ機構で $\pm y$ の AABB 値 0.04141 も 1.41 mm 膨らんでいる）。

#### 3 つの候補値と定数

| $w_\text{lat}$ の取り方 | 値 | $\mu_\text{max}$ | $L_E$ | 天井としての性格 |
|---|---|---|---|---|
| 車輪の外面（学生B 採用） | 0.0395 | 44.5 mm | **2.7973** | 真値より **+1.1% 甘い** |
| **真の最外側（コーナー片）** | **0.0400** | **44.0 mm** | **2.7669** | **これが正しい値** |
| AABB（膨張値） | 0.04141 | 42.6 mm | 2.6846 | 真値より **−3.0% 厳しい**（正しい実装を落としうる） |

**教授が R25 で警告した「AABB を使うと到達集合を過小評価して正しい実装を落とす」は、
定量的にも確認された**（−3.0%）。ただし**現在の判別には影響しない** — 実測 2.392 は
3 候補すべての下、階段版 ≥8.08 は 3 候補すべての上にあるので、**合否は動かない。**
争点は教授の言うとおり「**根拠のある値を使い、正しく記述すること**」に純化される。

#### 【M1 への波及・要是正】接触水準 44.5 mm は **44.0 mm** が正しい

$w_\text{lat}$ の真値が車輪の 39.5 mm を**超えている**（40.0 mm）ので、教授指示のとおり波及を判定した。

`research_notes/note_009_decisions_outlive_their_grounds.md` の表は

| 部位 | 半幅 |
|---|---|
| シャーシ箱 | 30.0 mm |
| 車輪の外面 | 39.5 mm |

の 2 行で「最外部は車輪」と結論しているが、**コーナー片 `mein_body2`〜`5` の 40.0 mm が
抜けている**。同じ値は `research_notes/scripts/check_seed_robustness.py` の docstring にもある。

$$\text{真の接触水準} = 84.0 - 40.0 = \mathbf{44.0\ mm}\quad(\text{44.5 mm ではない})$$

> **結論の向き**: note_009 の主張「文書の 54 mm は誤りで、横偏差 54.6 mm は
> **とっくに接触水準を超えていた**」は、**弱まるのではなく強まる**（甘さが 9.5 mm → 10.0 mm）。
> 同ノートのより深い論点（横偏差の最大は壁の無い場所で記録されるので安全性の指標にならない）も
> **無傷である。**したがって**是正は数値 2 箇所の差し替えで足り、結論の再検討は要らない。**

> **構造の指摘**: note_009 は「**決定はその根拠より長く生き残る**」という題のノートであり、
> **部位を 2 つ数え上げて 3 つ目を落とした表**を含んでいた。
> **列挙を完全と見なす**という、同じ形の失敗である。
> **是正は `research_notes/` の担当（私の編集範囲外）**なので、教授へ報告する。

#### 追加登録 N-1c-5: **この定数が何の役割かを条文が正しく述べているか**

条件 E は**中点一致**しか課さないので、**辺全体一致の下限 $L_E$ に縛られない**
（実測 2.392 < 2.7973 はそれで説明がつく）。したがって $L_E$ は
「**E が満たすべき必要条件**」ではなく「**階段版（≥8.08）と E（2.392）を分離する天井**」である。

- **合格条件**: 条文が $L_E$ を**判別のための天井**として位置づけている
- **反証観測**: 「$\Phi$ は $L_E$ 以下でなければならない」と**必要条件として**書かれている。
  それは**辺全体一致を課していない条件 E には当てはまらない**ので、条文の誤りになる

## 4-sexies. 【追加登録 2026-08-13 21:4x】D3（条件 C の壁越し参照）の確認項目

**D3 改訂版の `design.md`・実装・テストはいずれも未読。**材料は教授メッセージ（裁定 R27 の予告）のみ:
「下側包絡がユークリッド直線距離で壁の向こうの低値を拾い、測地距離を**過小評価**していた。
可視性判定で対処中」「定理と近似の分離」「誤差上界は方法から導出（実測への当てはめ＝循環の禁止）」
「(L) の主判定は刻み不変性」。

### 先に言える構造的事実 — **測地距離場は「ユークリッド距離に対して」1-Lipschitz ではない**

$$\lvert g(A)-g(B)\rvert \;\le\; d_\text{geo}(A,B),\qquad d_\text{geo}(A,B)\;\ge\;\lvert A-B\rvert$$

**上界は測地距離であって直線距離ではない。**壁を挟んだ 2 点は**直線では近いのに測地では遠い**ので、
$\lvert\Delta g\rvert/\lvert\Delta P\rvert$ はいくらでも大きくなりうる。

> **これは欠陥ではなく、距離場の性質である。**実機は壁を通り抜けないので、
> **連続する 2 サンプルは自由空間内の短い経路で結ばれ、$d_\text{geo}\approx\lvert\Delta P\rvert$ になる**。
> **したがって (L) の走査は「自由空間内の到達可能な変位」に限定されていなければならない。**
> **格子走査が壁をまたぐ点対を含むと、真の不連続でないものを不連続と誤判定する。**

| # | 確認項目 | 合格条件 | **反証観測（偽ならこう見える）** |
|---|---|---|---|
| **D3-1** | **定理と近似の分離**（R27） | 「真の測地距離は連続・1-Lipschitz」（定理）と「実装は格子近似」（近似）が**書き分けられ**、跳びが出たら近似の欠陥と読む枠組みになっている | 「測地距離場だから連続」とだけ書き、**実装の近似誤差を定理の性質で正当化**している。D3 はまさにその読み方で見逃されうる欠陥だった |
| **D3-2** | **(L) の走査範囲** | 走査が**自由空間内の到達可能な変位**に限定されている（壁をまたぐ点対を含まない）か、含む場合は**別枠で報告**している | 格子走査に壁をまたぐ点対が混ざり、大きな比が出ている。**それを「不連続」と読むのも、逆に「閾値を緩める口実」にするのも誤り** |
| **D3-3** | **可視性判定の解像度依存** | 壁の角をかすめる線分の可視判定が解像度にどう依存するかが評価され、**壁の角の近傍で可視と判定される最小の隙間**が報告されている | 単一解像度でしか確認しておらず、**細かくすると値が動き続ける** |
| **D3-4** | **誤差上界が方法から導出されているか**（R27） | 近傍数と刻みから**解析的に**上界が出ている（例: 8 近傍 Dijkstra の方位量子化による過大評価の既知上界） | **実測値に当てはめて上界を決めている**（＝循環。「上界を超えなかった」が自明になる） |
| **D3-5** | **格子刻みと壁厚の比** | 刻みが壁厚 $t_w$ = 0.012 m に対して十分細かく、**壁が格子の隙間から漏れない**ことが示されている | 刻みが $t_w$ と同オーダー以上。**壁が「通れる」と判定される経路が生じうる**（D3 と同じ機構の別経路） |
| **D3-6** | **D3 発見前に測った条件 C の数値の扱い** | D3 前に測った Lipschitz 実測値・区画中心のずれ分布などが**撤回または再測定**されている | 古い数値がそのまま残っている。**「現行と書いた瞬間から文書は腐る」と同型** |
| **D3-7** | **$1/\rho$ の分布が D3 修正で動いたか** | 下記の予測どおり**増える方向**へ動き、修正前後の値が併記されている | 修正前後で $1/\rho$ が変わっていない。**修正が効いていないか、記録が更新されていない** |

### 事前予測 P-D（**改訂版を見る前に登録する**）

**D3 は測地距離を過小評価する欠陥**なので、修正すると $g(\text{start})$ は**増える**。よって

$$1/\rho \;=\; \frac{g(\text{start})}{0.18\,D_0}\qquad\text{は\;\textbf{増える方向}へ動く。}$$

さらに**上限が言える**: 私の概算 **0.837**（§4-quater）は
「中心を結ぶ折れ線から、辺中点どうしを結ぶ直線へ角を切る」ぶんだけを見込んだ値である。
**真の測地線は壁の角をさらに回り込めるので、真の $1/\rho$ は 0.837 以下**になる。まとめると

$$(\text{D3 修正前の }1/\rho) \;<\; (\text{修正後の真値}) \;\le\; \mathbf{0.837}$$

**この 3 者の順序が崩れたら、修正か私の概算のどちらかが誤っている。**
（外れたら外れたと書く。**私の概算が上限として誤っている可能性も含めて判定する。**）

### 【追加登録 2026-08-13 22:1x】D3-8: **条件 C の合格線は、条件 E の残存欠陥と対称でなければならない**

教授経由で D3 の中間所見を受領した（**改訂版・実装・テストはいずれも未読**。材料は要旨のみ）:
壁またぎ対の偽不連続は回避済み、**壁除外後も加法的な超過が残る**
（$\lvert\Delta g\rvert \le \lvert\Delta p\rvert + \delta$、$\delta$ は格子間隔 0.006 m に収束。
窓 3×3 → 7×7 で **5.6 → 1.7 mm**）。機構は「候補窓が $P$ と動き、min を与える候補が窓から抜ける瞬間の跳び」。

**⚠️ ここで合格線を「跳び 0」に置くと、実験そのものを壊す。**

| 条件 | **受け入れ済みの残存跳び** | 出所 |
|---|---|---|
| **条件 E** | **最大 44.0 mm**（$\mu_\max$。中点以外の境界不一致） | §7-0 に限界として登録・**投入を承認済み** |
| **条件 C（7×7 窓）** | **1.7 mm** | 本中間所見 |

**条件 C の残存跳びは、既に承認した条件 E の残存跳びの 1/26 である。**

> **D3-8 の合格条件**: 条件 C の (L)/(b) の合格線が、**条件 E に課した基準と同じ種類**であること
> （どちらも「有界な残存超過を数値で登録し、刻み不変性で真の不連続と区別する」形）。
> **反証観測**: 条件 C にだけ「跳び 0」や「1-Lipschitz を厳密に満たす」を要求し、
> 条件 E は 44 mm の跳びを持ったまま通す、という**非対称な基準**になっている。
>
> **なぜ重大か**: 本実験の目的は **E と C の比較**である。
> **合格線が条件ごとに違うと、E 対 C の差が「Φ の性質の差」なのか
> 「通した基準の差」なのか分離できなくなる。**これは C-4（報酬量の交絡）を
> 条件 C' で潰したのと**同じ型の交絡**であり、同じ厳しさで扱う必要がある。

### 【追加登録】D3-9: 窓が $P$ 依存であることの**治し方**が構造的か

中間所見③「docstring の『min だから 1-Lipschitz』は候補集合が $P$ 依存なので誤り」は、
**私の予測 P-A が壊れたのと同型**である（min の定理は正しいが、適用の前提が偽）。

**この機構は、窓を広げても消えない**（5.6 → 1.7 mm と縮むだけ）。
**消すには候補集合を $P$ に依存させないこと**が要る。

| # | 確認項目 | 合格条件 | 反証観測 |
|---|---|---|---|
| **D3-9** | 対処が**機構に対応しているか** | 「候補集合を $P$ 非依存にする」か、「$P$ 依存を残すなら**超過の上界を窓の幾何から解析的に導く**」のどちらかになっている | **窓を広げて実測値が小さくなったことをもって合格**としている。それは**上界を実測に当てはめる**ことであり、R27 が禁じた循環そのもの。**窓をさらに広げれば値は変わり続ける** |

### 【先行実施 2026-08-13 22:3x】双線形補間の上界 $\sqrt2$ を独立に検証 — **導出値であることを確認**

教授経由で、D3 の対処が**双線形補間への置き換え**（候補集合を持たない ＝ D3-9 の「機構への対応」）と
決着し、**上界 $\sqrt2$ は双線形の理論上界なので導出値**である、と伝達された。
**改訂版・実装は未読のまま**、この主張だけを独立に検証した。

**設定**: 格子間隔を 1 に正規化。標本 $f_{00},f_{10},f_{01},f_{11}$ が 1-Lipschitz
（軸隣接 $\lvert\Delta f\rvert\le1$、対角隣接 $\lvert\Delta f\rvert\le\sqrt2$）のとき、
双線形補間 $B$ の勾配はどこまで大きくなれるか。

$$\frac{\partial B}{\partial x}=(f_{10}-f_{00})(1-y)+(f_{11}-f_{01})\,y,\qquad
\frac{\partial B}{\partial y}=(f_{01}-f_{00})(1-x)+(f_{11}-f_{10})\,x$$

**どちらも $\lvert\cdot\rvert\le1$ の 2 値の凸結合**なので $\lvert\partial_x B\rvert\le1$、
$\lvert\partial_y B\rvert\le1$、したがって $\lvert\nabla B\rvert\le\sqrt2$。

**等号は達成される**: $f_{00}=0,\ f_{10}=f_{01}=1,\ f_{11}=\sqrt2$（全制約を満たす）の
角 $(x,y)=(0,0)$ で $\nabla B=(1,1)$。**数値探索でも 1.414213562 で一致**（差 0.0e+00）。

> **判定: $\sqrt2$ は双線形補間の tight な理論上界であり、実測への当てはめではない。**
> **R27 の「誤差上界は方法から導出（実測への当てはめ＝循環の禁止）」を満たしている。**
>
> **前提の確認**: 8 近傍 Dijkstra の出力は、軸辺 $h$・対角辺 $h\sqrt2$ の重みなので、
> **隣接標本間で $\lvert\Delta f\rvert\le$ ユークリッド距離**を満たす（＝上の制約と一致）。
> **したがって上界の前提は成立している。**

### 【追加登録】D3-10: $\kappa$ の根拠が条件 C にも当てはまるか

条件 E の $\kappa=1.10$ は「**弧長／弦長 1.0553 に 4% の余裕**」という**条件 E 固有の導出**である。
条件 C にも同じ $\kappa$ を使うなら、**その根拠が条件 C でも成り立つことを示す必要がある。**

| # | 確認項目 | 合格条件 | 反証観測 |
|---|---|---|---|
| **D3-10** | $\kappa$ の持ち越しの正当化 | 弧長／弦長の議論が**条件 C でも同じ形で成り立つ**ことが書かれている（離散時刻でサンプルする以上、曲線を弦で測る効果は方式によらない）か、条件 C 用に別途導出されている | 「E と同じ $\kappa$」とだけ書かれ、**根拠が E 固有の導出のまま流用**されている。$L$ を方式ごとに導出したのに $\kappa$ だけ共通、という非対称は D3-8 の裏返しになる |

**（削除した登録項目は無い。D3-1〜D3-10 と予測 P-D の追加のみ。）**

## 4-septies. 【是正追記 2026-08-13 深夜】§4-quinquies の定数表に旧 $w_r$=0.0395 由来の値が残っている

**行の書き換え・削除はしない**（本文書は時系列の事前登録記録である）。**下記を現行値とする。**

学生B の grep 掃討（`0.0455` / `45.5`）で、**本文書 368 行目**の
「隅までの隙間 $h-\mu_\max = t_w/2 + w_r$ = **0.045500 m**」が
**旧 $w_r$=0.0395（車輪外面）由来の値のまま残存**していると指摘を受けた（教授経由）。

| 量 | 本文書 §4-quinquies の記載（**旧値**） | **現行値** | 出所 |
|---|---|---|---|
| 機体の最外側半幅 | $w_r$ = 0.0395（車輪外面） | $w_\text{lat}$ = **0.0400**（メッシュ頂点由来のコーナー片） | **裁定 R26** |
| 中心が中点から外れられる量 | $\mu_\max$ = 0.044500 | $\mu_\max$ = **0.0440** | $(s-t_w)/2-w_\text{lat}$ |
| 隅までの隙間 | $h-\mu_\max$ = 0.045500 | $h-\mu_\max$ = **0.0460**（＝ $t_w/2+w_\text{lat}$） | 同上 |
| 直交 2 辺の通行可能部の最短距離 | $d_\min$ = 0.064347 | $d_\min$ = **0.065054** | $\sqrt2\,(t_w/2+w_\text{lat})$ |
| 下限 | $L_E$ = 2.79735 | $L_E$ = **2.7669** | $s/d_\min$ |

**基準点を必ず併記する**（裁定 **R33**）: **0.0460 は壁の中心線からの離隔**であり、
**壁面からは $w_\text{lat}$ = 0.0400 m** である。**同一の帯の 2 通りの書き方であって、二重計上ではない。**

**この残存自体が、本文書 §4-quinquies および `AUDIT_011` §5-quater で私が指摘した機構の実例である** —
**合成値（$t_w/2+w$ のような足し算の結果）は、元の値が動いても自動では追随しない。**
**是正を起案した本人の文書にも旧値が生き残った。**

**なお $L_E$ は裁定 R31 で判定から記録へ降格している**（辺全体一致を課したときの下限であり、
本設計はそれを課さない）。**上表の是正は判定基準を動かすものではない。**

## 4-octies. 【追加登録 2026-08-14 未明】裁定 R34（マスクを真の配置空間へ是正）の確認項目と、C-6 の判定形の精緻化

**追加であって削除ではない。**教授の裁定 R34・R35 を受けて登録する。

### (a) R34 の実装の確認項目 R34-1〜R34-5

| # | 確認項目 | 合格条件 | 反証観測 |
|---|---|---|---|
| **R34-1** | 障害物集合の**導出元** | `mouse/mjcf.py` が**実際に生成する形状**から導かれている（壁の長手が柱の分だけ短いこと・柱が格子点にあること・16×16 の中央柱の除外条件が座標で書かれていること、が形状定義の**一箇所**から来ている） | 幾何が**独立に再記述**されている（マスク用に寸法を手で書き直している）。この場合、`mjcf.py` を直せばマスクが追随する保証が消える（R11 の柱の是正が届かない） |
| **R34-2** | 円板近似の**限界の条文化** | design.md に「円板半径 = 横半幅 $w_\text{lat}$ の**向き非依存近似**であり、**前端 0.0500 m を含まない**。厳密には $(x,y,\theta)$ の 3 次元配置空間が要るが、整形ポテンシャル用途には過剰と判断した」旨が**判断ごと**書かれている | 「配置空間を使う」とだけ書かれ、近似であることが書かれていない。**R32 と同じ失敗の再演**（代理量を真値として扱う） |
| **R34-3** | 距離の**厳密さ** | 障害物（矩形）への**厳密なユークリッド距離**で判定している | 画像処理的な膨張（構造要素による近似）・チェビシェフ／マンハッタン距離・「隅を四角く切る」実装。**隅の扱いが本件の核心なので、隅で近似する実装は指摘の対象そのものを残す** |
| **R34-4** | 是正後の**連結性** | `_compute_geodesic_field()` の到達不能検査が発火しない（許可点が孤立しない）。**発火した面があれば面ごとに報告**されている | 例外を握り潰す・孤立点を許可側から外して黙って通す |
| **R34-5** | **$\rho$ の再導出と C' への連動**（教授が追加） | マスク是正後の $\rho$ が**再測定**され、**条件 C' の報酬スケールがその値で作り直されている**ことが design.md C' 節の確定条件に入っている | 旧 $\rho$（マスク是正前の値）のまま C' を投入する。**C' は「総整形量を E に揃える」ための条件なので、$\rho$ が古いと条件の定義そのものが崩れる** |

**予測（R34-P）**: 提出帯 seed 7000〜7019 で $1/\rho$ 中央値は **0.6923 → 0.7344（+6.1%）**、
面ごとの値は `verification/out/r34_band7000.json`。**外れたらどちらかの幾何が違う。**
⚠️ **この予測は学生B の集計値を受け取った後に作った**ので、**水準については盲検ではない**
（`COMMIT_002` §11 に明記）。

### (b) C-6（解像度感度）の判定形の精緻化

**登録済みの C-6 を置き換えるのではなく、判定形を細かくする。**

1. **方向の反証条件（`AUDIT_011` §5 で登録済み・維持）**: 8 近傍 Dijkstra は測地距離を**上に外す**
   （最大 **+8.24%** ＝ $\sqrt{4-2\sqrt2}$、方位 22.5° で最大）。**格子を細かくすれば $1/\rho$ は下がる。
   上がるなら、格子の張り方か壁の描画に問題がある。**
2. **🔴 新規: 解像度の掃引は「実効離隔」と交絡する。**許可集合は「障害物までの真の距離 $\ge$ 閾値」を
   **格子点で標本化**したものなので、**格子上で実現される最小の離隔（実効離隔）は閾値より大きくなる**:

   | 格子 | 閾値 0.0400 に対する**実効離隔**（面への垂直方向） | 過大 |
   |---|---|---|
   | 7.5 mm | 0.0465 | **+16.3%** |
   | 6.0 mm | 0.0420 | +5.0% |
   | 3.0 mm | 0.0420 | +5.0% |

   **格子を細かくすると実効離隔も下がる**（自由空間が広がる → $1/\rho$ が下がる）ので、
   **1 の計量誤差と同じ向きに効く。**したがって**「細かくしたら下がった」だけでは、
   どちらの機構がどれだけ効いたかを言えない。**
   → **合格条件**: 解像度ごとに**実効離隔**（許可格子点の障害物までの最小距離）を**併記**し、
   2 つの機構を分離して報告していること。
3. **🔴 新規: 閾値の感度は格子より大きく動かす。**7.5 mm 格子では 0.0455 と 0.0460 が
   **同一の許可集合**を与える（`AUDIT_011` §5-quinquies §3）。**刻みより小さい変化で
   「感度が無い」と結論しないこと。**
4. **🔴 新規: 量子化の議論は成分ごとに行う。**面までの距離は格子に乗るが、**隅（柱の角・壁の端）
   までの距離は 2 つの量子化量の $\sqrt{\;}$ 和なので乗らない**（同 §3 の訂正）。
   **実測では、侵食半径 0.0400 と 0.0460 で判定が変わる格子点は 100% が隅で決まっていた**
   （seed 1〜5 で 1225 点・面への垂直で決まる点は 0 点）。

## 4-nonies. 【追加登録 2026-08-14 未明】裁定 R37 — 計量の条文化の確認項目と、FMM 対 8 近傍の測定の事前登録

**追加であって削除ではない。**裁定 R37（条件 C の計量は 8 近傍 Dijkstra を維持）を受けて登録する。

### (a) 条文化の確認項目 R37-1

| # | 確認項目 | 合格条件 | 反証観測 |
|---|---|---|---|
| **R37-1** | 場の定義の宣言 | design.md に**「条件 C の場の定義そのものが『配置空間上の 8 近傍格子測地』である」**と書かれている。**真の測地の近似として誤差を詫びるのではなく、定義として宣言**し、対ユークリッド誤差の上界（**最大 +8.24% ＝ $\sqrt{4-2\sqrt2}$**・方位 22.5° で最大・角度一様平均 ≈5.5%）を注記している | 「測地距離場」とだけ書かれ計量が特定できない／「近似的に測地距離」と書いて誤差の大きさを書かない／$\rho$ の定義と場の定義で計量が食い違う |

**なぜ「定義として宣言」が要るか**: $\Phi_C$ は**報酬**であって物理量の推定値ではない。
**方策が最大化するのはこの場そのもの**なので、**近似誤差ではなく設計の一部**である。
「真値からずれている」と書くと、**将来「精度を上げる」是正が正当化されてしまい、
場が動けば $\rho$ も C' の報酬スケールも動く**（R34-5 と同じ経路）。

### (b) 測定 F（教授からの受注）の事前登録 — **測る前に書く**

**内容**: 同一迷路・同一マスク・同一格子で、**8 近傍 Dijkstra と Fast Marching 法（FMM）の差**を測る。
**FMM は私が独立に実装する**（`.venv` に scipy が無いのでいずれにせよ自前になる）。
用途は **R37 の誤差条文の実測点**。

**測定の作り（先に固定する）**:

1. **既知解での校正を先に行う**（順序を固定する。迷路で測る前に）。
   **障害物の無い自由空間**に点源を置き、**厳密なユークリッド距離との比を方位の関数**として出す。
   **これは答えが解析的に分かっているので、両解法の異方性を非循環に測れる。**
2. そのうえで迷路（**seed 7000**・現行マスク・6 mm 格子）で $1/\rho$ を両解法で計算する

**予測（外れたら外れたと書く）**:

| # | 予測 | 反証観測 |
|---|---|---|
| **F-1** | 校正試験で **8 近傍の対ユークリッド比は 0°・45° で 1.000、22.5° で最大 1.0824** | ずれる → 私の 8 近傍実装か理論値のどちらかが違う |
| **F-2** | 校正試験で **FMM の最大偏差は 2% 未満**（8 近傍より小さい） | 2% を超える → 私の FMM 実装の欠陥（1 次精度 FMM の既知の異方性はこれより小さい） |
| **F-3** | 迷路で **FMM の $1/\rho$ は 8 近傍より小さく、差は 1〜5%**（角度一様平均 5.5% より**小さい**と予測する） | 差が **5% を超える**、または **FMM の方が大きい** → 下の理由づけか実装のどちらかが違う |

**F-3 の理由（先に書く）**: **迷路の測地線は、直線区間が軸方向（0°）に、角切りが概ね 45° に寄る。
どちらも 8 近傍の誤差が 0 になる方位である。**したがって**方位が一様分布する場合の平均 5.5% より
小さくなるはずである。**もし 5.5% に近ければ、**測地線の方位分布が一様に近い**ことを意味し、
それ自体が条件 C の場の性質についての知見になる。

> ⚠️ **本測定は「どちらが正しいか」を決めるものではない。**R37 で**場の定義は 8 近傍に確定**した。
> **測るのは、その定義が真のユークリッド測地からどれだけ離れているか**であり、
> **条文に載せる実測点**としてのみ使う。

## 5. 本登録の効力

**本文書をコミットした後に改訂版 `design.md` を読む。**
読んだ後に確認項目を**追加**する場合は、追加であることと理由を明記する（削除はしない）。
