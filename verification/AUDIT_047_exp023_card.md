# 監査 047: exp_023（再帰型方策・M2 第 2 弾 B）の投入前監査

- 実施: 准教授セッション（8 代目）・2026-08-15
- 対象: `experiments/exp_023_recurrent_policy/card.md` **版 `9e32a99`**
- **監査の重点はカードを 1 行も見ていない時点で登録済み**（`AUDIT_047_PREREG_lstm_focus.md`・`7b312f2`）

---

## 0. 判定

> ## **要是正 1 件・要記載 2 件**
>
> **設計は本プログラムで最も練られたカードである。**
> **私が事前登録した最重の論点（測定スクリプトが再帰方策を扱えるか）は完全に手当てされていた。**
> **要是正 1 件は規約 §9-18 との衝突で、条文 1 箇所の書き換えで済む。**

---

## 1. 🟢 事前登録した最重の論点は手当て済み（`AUDIT_047_PREREG` §2-1）

**私はカードを見る前に、次を最重の疑いとして登録した**:

> **`measure_driving.py` は `policy_fn(obs) -> action` という無状態の呼び出しを前提にしている。
> 再帰方策は隠れ状態を歩ごとに引き継がなければならない。忘れると LSTM は毎歩
> 「エピソードの最初の 1 歩」として振る舞い、例外も出ず次元も合い、値だけが誤る。
> しかも「記憶を使えていない」という結果になるので、帰無と区別がつかない。**

**カードは §6-2 でこれを正面から扱っている**:

- **`evaluate_maze6` に `policy_reset_fn` を追加**（既定 None ＝ 従来と同一）・**各試行の開始時に呼ぶ**
- **`policy_fn` は隠れ状態を閉じ込めたクロージャ**（`model.predict(obs, state, episode_start, deterministic=True)`）
- **`measure_driving.py` にも同じ口**
- **T-R3 で「同じ迷路を 2 回続けて走らせ、2 回目が 1 回目と bit 一致すること」を検査**
  （**持ち越していれば一致しない** ＝ **空振りしない形**）

**私が要求したはずのものが、すべて先に入っていた。**

---

## 2. 🔴 要是正 — R2 の $p$ が §9-18 と衝突する

**R2（主予測・閾値非依存）**: 「**全 120 走行の到達区画数の分布**（Mann-Whitney・両側）で
**$p<0.05$ かつ中央値が対照以上**」。

**問題**: **120 走行は 6 seed × 共有 20 迷路であり、両軸でクラスタ構造を持つ。**
**研究計画書 §9-18（必須）は明示している**:

> **学習条件間の比較の有効標本は seed 数**（面や走行を増やしても増えない）。
> 反復は…**同一コース内の反復はクラスタを作り、有効標本はコース数に近づく。**

**R2 は学習条件間（再帰 対 前向き）の比較であり、判定の分岐を持つ。**
**120 走行を独立とみなした $p$ は、条件比較の $p$ ではない。**

### 実測で確かめた（**既存の 3 群で R2 と同じ形を当てた**）

| 比較 | **走行水準（120 対 120）** | **seed 水準（6 対 6・正確法）** |
|---|---|---|
| exp_021 対 exp_019 | 0.397 | 0.805 |
| **exp_022 対 exp_019** | **0.082** | **0.636** |
| exp_022 対 exp_021 | 0.534 | 0.602 |

**水準を変えると $p$ が大きく動く**（exp_022 対 exp_019 で 0.082 → 0.636）。

> **🟢 私の当初の懸念は外れた**: 「走行水準なら何でも有意になる」と疑ったが、
> **exp_021 対 exp_019 でも $p$ = 0.397 で、R2 は空振りする形ではない。**
> **問題は「甘すぎる」ことではなく「条件比較の $p$ として解釈できない」ことである。**

### 是正案（**どちらでもよい。条文 1 箇所**）

**(a)** **R2 の $p$ を seed 水準（6 対 6・正確法。$n$ が小さく同順位が多いので近似は使わない — 作法 40）で計算する。**
**ただし実測の seed 水準の $p$ は過去 3 比較すべて 0.6 以上**なので、**R2 は相当に厳しい予測になる**
（**それ自体は悪くない。厳しいことを承知で登録するなら妥当**）。

**(b)** **走行水準の $p$ は記述として残し、判定の分岐からは外す。**
**条件の判定は R1（件数の gate）に置き、R2 は「閾値の選び方に結論が乗っていないことの確認」に用いる。**
**§9-18 の「gate と検定を混ぜない」にも沿う。**

**私は (b) を勧める** — **R2 を置いた狙い（閾値依存性の点検）は判定の分岐がなくても果たせる**うえ、
**(a) だと「厳しすぎて常に外れる」形になりかねない**。

---

## 3. 要記載 2 件

### 3-1. 群 1 と対照の差は「再帰構造 1 点」ではない — **時間の伸びも変わる**

**§2-2 は「対照 exp_021 との差 = 再帰構造 1 点」と書く。**
**しかし対照の窓は 128 歩（1.28 秒）に固定されており、LSTM の窓は原理的にエピソード全体（約 20 秒）**である。
**変わっているのは「再帰であること」と「参照できる過去の長さ」の 2 つ**である。

**これは実験の狙いそのもの**（再帰は無限の窓を与える）**なので設計の誤りではない。**
**ただし「1 点だけ変える」という書き方は、`AUDIT_045` 要記載 1 と同じ型で正確でない** —
**対照が何を揃え、何を揃えないかを名指しする**のが、このカードの他の箇所の水準である。

**提案**: §2-2 か §8 に「窓の長さも変わる（1.28 秒 → 実質エピソード全体）」を 1 行。

### 3-2. 限界 2（容量が 1.23 倍）は**量的に境界づけられない**

**限界 2 は「R1 の当たりの一部が容量で説明される可能性は残る」と正しく書いている。**
**ただし、どの程度かは言えない。**

**私が保存済みモデルから実測したパラメータ数**:

| 群 | 入力 | パラメータ | 5 区画以上 | 7 区画以上 |
|---|---|---|---|---|
| exp_019 | 17 | **38,021** | 3 | 0 |
| exp_021 | 153（本物の履歴） | **72,837** | 26 | 12 |
| exp_022 | 153（にせ履歴） | **72,837** | 28 | 1 |
| **exp_023** | 153 | **89,733** | ? | ? |

**容量についての手持ちの資料は「1.92 倍で 5 区画以上が 3 → 26」だけ**であり、
**1.23 倍がどれだけ効くかの資料は無い。**
**「可能性は残る」以上のことは言えない**旨を書き添えると、判定時の読み手が助かる。

---

## 4. 教授のご依頼 — **裾の機構はこの設計でどこまで分離されるか**

**結論: (i) 勾配の増幅は既存データで既に除外できる。(ii) 次元と容量の分離は exp_023 では進まない。**

### 4-1. 🟢 勾配の増幅の説は**既に消えている**

`AUDIT_046` で私は「裾の改善は容量か・複製による勾配の増幅か」を未確認として残した。
**保存済みモデルのパラメータ数を実測して、これは決着する**:

- **exp_021 と exp_022 のパラメータ数は 72,837 で完全に同一**
- **複製による勾配の増幅は exp_022 にしか存在しない**（exp_021 の 153 次元は別々に変動する）
- **それでも裾は 26 対 28 で統計的に区別できない**（$p$ = 0.877）
- → **増幅は裾の効果に必要ではない。**

### 4-2. 🔴 残る交絡は「入力の次元」対「パラメータ数」で、**exp_023 では分離されない**

**exp_019 と他の 2 群は、次元（17 対 153）とパラメータ数（38,021 対 72,837）が同時に動いている。**
**exp_023 は入力を 153 に揃えたまま容量を 1.23 倍にするので、この 2 つを分けない。**

**exp_023 が分離するのは別の軸である** — **「153 次元の容量」と「再帰構造」**。
**両群と対照が 153 次元を共有するので、この分離は成立している**（設計として正しい）。

**次元と容量を分けるには、17 次元入力でパラメータ数を約 72,837 に揃えた群**
（第 1 層を広げる）**が要る。**
**これは限界 3 が将来の「容量の軸」で言及している対照と同じもの**であり、
**その対照は裾の機構の決着にも使える。**

---

## 5. 記録のみ（**崩れなかったこと** — 作法 6）

| # | 検査 | 結果 |
|---|---|---|
| 1 | **R1 の閾値 7 の根拠** | **🟢 誠実である。**「構成上の根拠を持たない」「事後に選んだ値」と明記し、**5 区画が使えない理由（exp_022 で容量だけで越えられると実証済み）**まで書いている。**限界 4 にも再掲。**教授のご懸念（限界の書き方が正確か）は満たされている |
| 2 | **同一版基準の 3 本立て** | **🟢 妥当。**「不活性の bit 一致は原理的に使えない」をコードで確認したうえで置換し、**限界 1 に「従来より弱い・別実装であること自体が差に含まれる・これは消せない」**と明記。**これ以上のことは実際にできない** |
| 3 | **群 2 の実装と T-R2** | **🟢 妥当。**`collect_rollouts` の全複製を避けて `_last_episode_starts` を property 化する形は最小の介入である。**T-R2 が「buffer の中身で確認」「群 1 では 0 のまま」も同時に見る**ので空振りしない |
| 4 | **第 3 の読みの適用先** | **🟢 誤りの型 10 が効いている。**§3-4 の表が「外」の列を持たないことを自覚し、**「出たら表の外で扱う」と明記**している |
| 5 | **限界 8（群 2 は (C) を消すとは限らない）** | **🟢 十分。**「リセットは汚染を消すが同時に文脈も捨てる」「捨てる損と汚れる損の差し引き」は機構の正確な名指しである |
| 6 | **限界 3 と後続の道** | **🟢 良い。**「R1 が外れても『32 次元では足りない』かもしれない」と書き、**§3-4 の「打ち切り」の行を後続の検討を経てからの判断に格下げ**している |
| 7 | **隠れ 256 の容量交絡の捕捉** | **🟢 学生B が投入前に実測で捕まえた**（12.92 倍）。**既定のまま走らせていれば結果はすべて容量の再演で説明された** |

---

## 6. 検査できなかったこと（未確認）

1. **実装コード**（`RespawnResetRecurrentPPO`・`policy_reset_fn`）。**カードの監査である**
2. **`_last_episode_starts` の property 化が `RecurrentPPO` の他の経路を壊さないか**
   （学生B の指名 3）。**コードが無いので条文としての妥当性しか見ていない。**
   **T-R2 に加えて「群 1 の結果が部分クラスを使わない場合と bit 一致する」検査があると、
   部分クラス自体の無害性が示せる**（**提案・要記載には数えない**）
3. **BPTT の実効的な長さ**（`n_steps`=2048 とエピソード約 2000 歩の関係）。
   **条文に明示は無いが、`n_steps` は §2-1 で固定されているので設計値としては決まっている**
4. **費用 6.4 時間の見積りの妥当性**

---

## 7. 反映確認で見る点（**是正版を受け取る前に登録する** — 後知恵防止）

> **過去 3 回の反映確認のうち 2 回で新規の指摘が出ている**（note_018・note_020）。
> **1 回は是正自体が新しい誤りを生んだ**（作法 32）。**以下は是正版を見る前に書いている。**

### 7-1. 🔴 最重 — **R2 の降格は「適用先を全部数える」問題である（誤りの型 10 そのもの）**

**R2 を判定の分岐から外すと、R2 に触れているすべての場所を直す必要がある。**
**学生B が exp_022 で登録した誤りの型 10（読みを増やしたら、その読みを使う場所を全部数える）の、
「読みを減らす側」の適用である。**

| # | 見るところ |
|---|---|
| 1 | **§3-1 の表で R2 の「主」の印が外れているか。**外れていなければ、**主予測が 2 つあるのに片方に判定が無い**という形になる |
| 2 | **§3 冒頭の主予測の文**（「再帰構造は深く入り込むことを改善する」）が **R1 だけを指す形**になっているか |
| 3 | **§3-5 の反証可能性の点検表の R2 の行。**判定でなくなったなら、**「どの観測が出たら外れか」を書く対象ではない** — 行を残すなら**記述であることを明記**する |
| 4 | **§3-4 の全域被覆表**（R1 × R6）は R2 を使っていないので**変わらないはず**。**変わっていたら是正が余計なところへ波及している** |
| 5 | **§8 の限界に「R2 は条件比較の検定ではない」**旨が入ったか |

### 7-2. 要記載 2 件

| # | 見るところ |
|---|---|
| 6 | **窓の長さ**（1.28 秒 → 実質エピソード全体）が §2-2 か §8 に入り、**「再帰構造 1 点」という書き方と矛盾していないか**。**両方が生きていると分岐する**（作法 24） |
| 7 | **容量の caveat** に**パラメータ数の実測値**（38,021 / 72,837 / 89,733）と**「1.23 倍の資料は無い」**が入ったか |

### 7-3. 私の提案（部分クラスの無害性）

| # | 見るところ |
|---|---|
| 8 | **「群 1 が素の `RecurrentPPO` と bit 一致する」検査**が §6-4 に入ったか。**空振りしない形か** — **部分クラスが有害なら何が違うはずかが書けているか**（群 1 では `_pending_respawn` が常に None なので、**論理和が恒等になることの確認**である） |

### 7-4. 全体（毎回見る）

| # | 見るところ |
|---|---|
| 9 | **旧記述が残って分岐していないか**（作法 24） |
| 10 | **是正が他節の数値・参照を失効させていないか**（作法 32） |
| 11 | **新しく書かれた条文に、新しい誤りが入っていないか**（**note_018 で実際に起きた**） |
