# 引き継ぎメモ: 学生A（評価・古典ベースライン担当）

**停止 2026-08-15 夕。未コミット変更なし・プロセス 0 本・走行なし。50 行以内・現況のみ**（§12-7）。

## 🚀 次の担当の最初の仕事 — **第三容疑の診断**

**016-H1d 第 2 段で「上乗せの 62 % は円弧に入る前にできている」と判明**（V1 的中・`18b0e81`）。
**ただし飽和ではない**（W3。進入区間の電圧の飽和率は中央値も最大も **0.00 %**）。

**⇒ 次に測るのは「進入側の横偏差がなぜ速度とともに成長するか」。**
**第三容疑（私の見立て・未検証）**: Stanley 形の横項 $\mathrm{atan}(k_y e_y/(\lvert v\rvert+v_\epsilon))$ は
**速度が上がると実効ゲインが下がる**ので、直線・斜め区間での是正が遅くなる。
**測り方の案**: 進入直線で $e_y$ の減衰の時定数を速度水準ごとに出し、$1/v$ に比例するかを見る。

**⚠️ 起票の前に**: 予測は**主判定量を 1 つの実数に落とし、実数直線を切れ目なく分割する**。
**連言（「単調に増え、かつ …」）を入れると場合分けが掛け算で増え、覆いに穴が空く**
（016-H1d 第 1 段の S1〜S4 で実際に「どれにも当たらない」が出た）。

## 現況

**主戦線は古典の完成**（計画 = `research_notes/note_025`）。
第 0 章 物理限界の理想タイム（**完了**）／**1 遷移域の是正（現在地）**／2 自己位置推定／
3 探索のセンサ化／4 経路単位選択。**准教授の第 2 章は私の第 1 章の確定待ち。**

| 事項 | 状態 |
|---|---|
| **016-H1**（角加速度の前置補償） | **実装しない**（判定 K4）。**円弧の中で効く仕掛けなので、上乗せの増分の高々 38 % にしか届かない** |
| **016-H1d**（上乗せ機構の同定） | 第 1 段 = 摩擦滑りは**不支持**（実効半径比 0.884〜0.924 ＝ 1 未満・内側へ食い込む）。第 2 段 = **進入側で 62 %・飽和ではない** → **第三容疑へ** |
| 016-H2（遷移域の速度・曲率半径） | 登録のみ。**$R$=60 mm の円弧の理論上限 0.610 m/s に対し実測 0.590 ＝ 97 %。上げるには半径を上げるしかない** |
| 実戦帯 19 面の配置 | ✅ 完了（`28ff0e1`） |
| ③ 理想タイムと真値制御の突き合わせ | **GO 済み・未着手**（走行 1 本） |

## 効いている裁定・制約

- **帯の 3 段**: 開発・カード判定 = 設計帯（seed 41000〜）／章の完了確認 = 検証帯／
  最終の完成判定 = 評価帯 20 迷路 ＋ **実戦帯（クラシック規格 19 面）**
- **完成基準の分母に実機は使えない**（実機は本モデルの理想の 3.0 倍速い）。
  理想タイムは正本パラメータの関数（`note_025` §2-0）
- **理想タイムには横加速度 6.20**（下界性のため）／**016 系の成立条件は 5.87 のまま**（連続性）
- **走行の投入前に本数と見込み時間を学生B へ連絡する**
- **§12**: 沈黙 = 受理／報告 10 行以内／**教訓は登録せずテストか自動検査へ**／裁定は 3 類型のみ

## 手持ちの道具（`research_notes/scripts/check_*.py`・`experiments/exp_016_diagonal/run_016h1_diag.py`）

- 物理限界と理想ラップ／実戦帯 42 面の理想タイム／クラス別点検／実機記録との差の分解
- 診断ハーネス（**制御に触れず記録を包む**。無害性は `tests/test_h1d_probe_harmless.py` で bit 一致を検査）
- 🔴 **記録は必ず `step_control` の【前】で読む**。後で読むと 1 ティック先の値になり、
  相関が +0.782 → +0.077 まで動いた（判定が反転する幅）
