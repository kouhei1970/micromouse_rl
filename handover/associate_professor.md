# 引き継ぎメモ — 准教授セッション（独立検証担当・副査）

最終更新: 2026-08-15（9 代目・停止時点）。**運用は `docs/RESEARCH_PLAN.md` §12**（旧慣行を再現しない）。
**過去の経緯は git 履歴が保存する。本メモは現況のみ・50 行以内。**

## 状態: **exp_023 は完了。私の担当分に未了なし・未コミット変更なし。**

**判定は 6 件すべて学生B と一致・不一致 0 件**（`AUDIT_048_exp023_verdict.md`・`12cfd04`）。
**R1 外（7 区画以上が 0/120・対照 12）／R3 当／R4 外（衝突 7.25 = 対照の 3.4 倍）／R5 当／
R6 当（群 2 の 9 > 群 1 の 0）／R7 外。**
**🔴 第 3 の読み（どちらの群よりも外）が成立し、§3-4 の表は引かない**
（深さ・正味の前進・衝突の 3 量すべてで両群が対照より悪く、表が前提する「挟む」構図が無い）。

**残る唯一の未確認**: 裾の機構（入力の次元 対 パラメータ数）。**本実験では分離されない**
（カード限界 4）。**17 次元入力でパラメータ数を 72,837 に揃えた群**が要る。

## 次の担当の最初の仕事

**古典完成計画（自己位置推定の導入）の、設計が固まる前の否定対照の登録。**
**教授から回付される取り決め**（ユーザ裁定 2026-08-15・教授が採択済み）。

> **登録する内容**: **推定器の検証を「推定誤差を真値と比較できる構成」にするだけでは足りない。**
> **比較が通ること自体が安心材料になってしまう。**
> **要るのは「真値を意図的に壊した対照」** — **壊しても走行が変わらなければ、
> その経路は真値を使っていない**ことの実証。**exp_023 の L6-b とまったく同じ形である。**

## 効いた検査（次の実験でもそのまま使える形）

| 検査 | 中身 | 実績 |
|---|---|---|
| **L6**（`verification/audit_exp023_l6_hidden_state.py`） | 提示順を変えて判定量が変わらないか ＋ **否定対照**（リセットを外すと変わるか） | 80 万歩・最終方策とも合格。**空振りしない形で書いてある** |
| **錨の独立再計算**（`audit_exp023_anchors.py`） | 対照の値を自分で出して事前登録と照合 | 4 件一致 |
| **判定形の事前登録**（`AUDIT_048_PREREG`・`8ac0994`） | 同値の扱い・許容差・層 L1〜L6・私の予測 | 条文を 1 字も変えずに判定できた |
| **安定性**（`audit_exp023_stability.py`） | **閾値を対照で校正**して判別力を作る | 群 2 の 4/6 が対照の範囲外（固定閾値では 18/18 で判別力ゼロだった） |

## 実測で確かめた事実（次の実験の設計に効く）

- **走行水準の p は条件比較に使えない**: 群 2 対 対照で**走行水準 0.000・seed 水準 0.242**。
  **旧 R2 を判定に残していれば判定が動いていた**（研究計画書 §9-18 の実証）
- **件数は少数の seed に支配される**: 対照の 12 件は `[0,0,0,2,5,5]`・群 2 の 9 件は `[0,0,0,0,3,6]`。
  **合計だけで「増えた」と書かない**
- **`run_summary.json` に群の識別が無い**（R11 で是正予定）。**判定時に群を同定できるのは
  `verification/evidence/exp_023{a,b}_launch_argv.txt`（走行中に保全した argv）と `train.log` の 1 行**

## 効いている原則（`RESEARCH_PLAN` §12-6 の 7 つ）

判定条件は結果を見る前に確定／錨は独立に再計算／**提案した旗は提案と同時に全場合を叩く**／
**検査には否定対照を付ける**／一次記録を自分で見る／予約 seed 帯は学習に使わない／1 実験 1 変更。
**教訓は作法として登録しない。テストか自動検査を書く。書けないものは書かない。**
