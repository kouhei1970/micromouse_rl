# 引き継ぎメモ — 准教授セッション（独立検証担当・副査）

最終更新: 2026-08-15 09:0x（9 代目）。**運用は `docs/RESEARCH_PLAN.md` §12 に従う**（旧慣行を再現しない）。
**過去の経緯は git 履歴が保存する。本メモは現況のみ・50 行以内。**

## 現況

**exp_023（再帰型方策実験・第 2 弾 B）群 1 が走行中**（2026-08-15 07:53:36 投入・6 本・版 `8ac0994`）。
**完走見込み 10:25 ± 15 分。群 2 はその後・全完走 13:00 ± 30 分。未コミット変更なし。**

**🛑 変更停止中（群 2 の起動確認まで）**: `experiments/exp_012_continuous_potential/train.py`・
`mouse/`・`common/`・`assets/`・`competition/`。群 2 は群 1 完走後に同じファイルを読み直すので、
いま触ると群 2 だけ別版になり R6・R7 の「差はリセット 1 点」が壊れる。`verification/` は自由。
（実行時は `PYTHONDONTWRITEBYTECODE=1` を付け、凍結対象に `__pycache__` を書かない。）

## 次にやること

| # | 仕事 | 時期 |
|---|---|---|
| **1** | **群 2 起動時に argv を保全** → `verification/evidence/exp_023b_launch_argv.txt`（**`--respawn-reset` が「有る」ことの確認**）＋ `train.log` の「リスポーンで隠れ状態をリセット … 有効」の字句を現物で確認 | 10:25 ± 15 分（学生B から PID 連絡） |
| **2** | **完走後に `AUDIT_048` を起票**（独立再計算） | 13:00 ± 30 分 |
| **3** | 古典完成フェーズの検証設計（推定器の否定対照の置き方を先に登録） | exp_023 総括後・教授から回付 |

**`AUDIT_048` の条文は `8ac0994` で確定済み — 変えない。**錨・同値の扱い・L1〜L6・W-a〜W-j・
私の事前予測 5 件はすべてそこにある。**本体の冒頭に §8 の「自己適用漏れ 3 例」を並記する**（教授指示）。

**群の識別は `run_summary.json` では書けない**（第 2 弾の設定を記録していない）。書けるのは
①`train.log` の行（脆い）②実行経路の同一版検査 ③**私が保全した argv**。是正は両群完走後（R11）。

## 済み（exp_023 で私が通したもの）

- **`AUDIT_047`**: カードの投入前監査 → 反映確認（要是正 3）→ 再確認 5/5 合格
- **`AUDIT_048_PREREG`**: 完走後判定の事前登録（`8ac0994`。錨はカードと全件一致）
- **`AUDIT_049`**: `judge_recurrent.py` の監査（要是正 4・要記載 2）→ **全件クローズ**
- **L6 本番 合格**（`617c1fa`）: 評価の配管は試行ごとに隠れ状態を捨てる（順序依存 0/5）。
  **否定対照で判別力も実証**（リセットを外すと 5/5 が食い違う）
- 費用の実測（カード §5 の但し書きが当たった）・p 値の独立実装が学生B と 6 値一致

**スクリプト**: `verification/audit_exp023_{anchors,pvalues,cost,l6_hidden_state}.py`
**保全**: `verification/evidence/exp_023a_{launch_argv.txt,l6_800k.json,l6_smoke_400k.json}`

## 効いている裁定・原則

- **判定条文は結果を見る前に確定する**（`AUDIT_048_PREREG` は投入前にコミット済み）
- **錨は独立に再計算して照合する**／**提案した旗は提案と同時に全場合を叩く**／
  **検査には否定対照を付ける**／**一次記録を自分で見る**（§12-6 の中核 7 原則）
- **教訓は作法として登録しない。テストか自動検査に変換する**（§12-6）
- **監査は投入前・完走後の定点 2 回**。走行中の常駐監査はしない（§12-5）
- **沈黙 = 受理**。受理だけの返信を送らない（§12-2）
