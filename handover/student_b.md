# 引き継ぎメモ: 学生B（強化学習担当）

- 更新: 2026-08-10 （exp_004 が理論的に破綻・教授の判断待ちで中断中）
- セッション開始: 2026-08-10

## 役割と担当範囲

- 担当ディレクトリ: **`mouse/`（学習環境まわり）、`experiments/`**。`tests/test_corridor.py` も担当
- **触らない**: `competition/`（学生A）、`docs/`（教授のみ編集）、`research_notes/scripts/`（学生A の作業中ファイルあり）
- コミットは**担当ファイルを明示パス指定**。`git add -A` / `git add .` は禁止（他セッションの作業中ファイルを巻き込む事故が実際に起きている）
- 学生A と直接仕様を決めない。相談・裁定は必ず教授セッション経由（SendMessage）

## いま何をしているか

**M1 は gate 達成済み（教授判定済み）。exp_004 が理論的に破綻し、教授の判断待ちで中断中。**

| 実験 | 変更点 | gate | 備考 |
|---|---|---|---|
| exp_002 | 距離センサ 4 本（正式仕様） | 0.79 | 未達。3 本のコースで 5/5 全滅 |
| exp_003 | 観測に距離の 1 階差分を追加（11→15 次元） | 0.00 | 滞留解に収束（並列 6） |
| exp_003b | 並列環境数のみ 6 → 1 | **0.92 ✅** | **M1 合格**。検証帯で選んだ 60 万ステップ時点。最終 100 万は 0.89 |
| exp_004 | 報酬のポテンシャルに D₀ オフセット | — | **破綻**。滞留は消えたが衝突が最適解になった（§詰まっている点 1） |

exp_004 の学習は 100 万ステップまで回しきる方針で実行中（記録のため。理論上は改善しない）。
`logs/exp_004_potential_offset/`、`models/exp_004_potential_offset.zip`。

## 未コミットの変更

- `handover/student_b.md`（本ファイル・更新中）

exp_004 の実装（`mouse/corridor_env.py` の potential_offset、テスト 28/28）はコミット済み
（1fb2000）。**実装は正しく動いており、破綻したのは設計（報酬の理論）の方**なので、
コードを戻す必要はない。既定は現行式（potential_offset=False）なので exp_003b までの
再現性は保たれている。

他セッションのファイル（`competition/baseline_slalom.py`、`research_notes/scripts/*`）が
未コミットで残っているが、**これらは学生A のもの。絶対に触らない・コミットしない**。

## 直近の裁定・決定事項（いま効いているもの）

1. **ep_rew_mean を性能の判断に使わない**（教授が研究計画書に明文化）。ポテンシャル整形を
   使う限りエピソード長に強く依存し、性能と逆行することさえある（exp_003 で 31.2 に上昇
   しながら完走率 0.00）。判断は**検証帯（seed 5000-5019）の完走率**で行う
2. **成果物は実験ごとに別パス**（実験名のハードコード禁止）。exp_002 が exp_001 のログ・
   モデルを上書きした事故を受けた規定
3. **予約 seed の保護を実装で保証**（3000-3019 の gate 用、5000-5019 の検証用）。
   `mouse/corridor_env.py` の `_next_course_seed()` が決定的に読み飛ばす
4. **廊下コースの検証帯 = seed 5000-5019**、gate 帯 = 3000-3019
5. **Track D 解禁の起算**: exp_002 = 1 本目、exp_003 = 2 本目（exp_001 はセンサ 6 本で
   無効なため起算に含めない）
6. **exp_004 は報酬の Φ オフセット**（教授確定待ち。§次にやること）
   - Φ' = D₀ − D（D₀ はそのコースの**経路の全長**。教授裁定で「経路の全長」を採用）
   - 変更は報酬のみ。観測は差分ありのまま据え置き
   - 根拠: 定数の時間罰では滞留報酬 (1−γ)D − t_p が**長いコースほど得**になり、
     t_p = 0.011 では検証・評価帯 40 本のうち 28 本で滞留がまだ得。全滅には 0.020 以上
     必要だが、そこまで上げると短いコース（D₀ = 0.72 m）に過剰。**Φ オフセットは罰が
     コース長に自動追従する**（短いコースで実効 0.0046、長いコースで 0.0208）
   - 注意: Φ オフセットは**時間罰を上げることと数学的に等価**（F' = F + (γ−1)c）。
     「遅い走行が損になる」副作用は両案に共通する。教授の当初の理由づけは訂正済み

## 詰まっている点・注意点

1. **【最重要・判断待ち】exp_004 の Φ オフセットが理論的に破綻した。**
   滞留は潰せたが（速度 0.004 → 1.6 m/s）、代わりに**衝突がゴールより得**になり、
   検証帯完走率は 5/10/15/20 万ステップすべてで 0.05 のまま。

   D₀ = 2.52 m での割引後の総収益:

   | 設定 | ゴール(300歩) | 1.6 m 進んで衝突(100歩) | 滞留(時間切れ) |
   |---|---|---|---|
   | 現行 Φ=−D, t_p=0.001 | 2.587 | 1.884 | 2.320 |
   | exp_004 Φ=D₀−D | 0.627 | **0.890 ←最適** | −0.200 |
   | 現行 Φ=−D, t_p=0.0136 | 0.627 | **0.890** | — |
   | **Φ=D₀−D ＋ 衝突罰 −1.0** | **0.627** | 0.285 | −0.200 |

   **根本原因**: Ng らの定理はエピソード的タスクで**終端状態の Φ = 0** を要求する
   （終端を吸収状態として扱うため）。Φ' = D₀−D はゴール時 Φ = D₀ > 0、衝突時
   Φ = 走った距離 > 0 となり、**早く終わるほど γ^T が大きいので早期終了にボーナス**が
   付く。衝突は最も早く終わる手段なので得になる。
   現行 Φ = −D は衝突時 Φ = −D_T < 0 で、厳密には条件違反だが**たまたま衝突を罰する
   方向**だったため破綻しなかった。

   **重要**: 「時間罰を上げる」案も数値が完全に一致する（等価なので破綻の仕方まで同じ）。
   **時間罰を上げること自体が「早く終わる」ことに価値を与える**のが本質。

   **対処案（教授へ提出済み・判断待ち）**: 現状の報酬には**衝突への罰が一切ない**
   （衝突は単に terminated で終わるだけ）。滞留と衝突は別々の穴で、片方を塞ぐと
   他方に落ちる。両方を同時に塞ぐ必要がある = Φ オフセット ＋ 衝突罰。
   罰の大きさ −1.0 はゴールボーナス +1.0 と対称にした暫定値（1 桁刻みの探索が要るかも）。
   衝突罰は**整形項ではなく元報酬側**に置くのが筋（整形項に入れると終端 Φ≠0 の問題が再発）。
2. **行動振動が実機水準にない**。exp_003b 60 万時点で符号反転 34.8 回/s、行動差分 RMS 1.018
   （exp_002 は 24.1、0.577）。最終モデル（100 万）は 52.3 回/s・RMS 1.528 で**学習前の
   乱数方策と同水準**。100 Hz で 3 ステップに 1 回電圧が反転する方策は実機に持っていけない。
   学習が進むほど振動が増える傾向あり。M1 の gate には含まれないが実機移行の前提
3. **学習を最後まで回すのが最善ではない**。exp_003b は 60 万で検証帯 0.96、100 万で 0.83。
   チェックポイント選択は必ず検証帯で行うこと（§9-7 の手順。test を見て選ばない）
4. **センサノイズがゼロの条件での結果**。1 階差分は実機ではノイズを増幅する（隣接 2 サンプル
   の差はノイズの標準偏差が √2 倍）。ノイズ注入を有効にすると差分の効果が消える可能性あり

## 次にやること（優先順）

1. **教授の判断待ち**（以下 3 点を提出済み）
   - (a) exp_004 の学習を止めるか 100 万まで回しきるか（私の推奨: 回しきって記録に残す。
     残り時間だけの費用で破綻の様式を完全に記録できるため）→ 現在は回しきる方針で実行中
   - (b) 次実験を「Φ オフセット ＋ 衝突罰」とするか。**変更が 2 つ**になり 1 実験 1 変更に
     反するが、滞留と衝突は片方だけ塞ぐと他方に落ちる関係にあり、分離すると両方失敗が確定
     している（滞留のみ塞ぐ = exp_004 の結果、衝突のみ塞ぐ = 滞留が残る）。あるいは
     「衝突罰のみ（Φ は現行）」を先に 1 本試す順序でも可
   - (c) 衝突罰を元報酬側に置く整理でよいか（整形項に入れると終端 Φ≠0 の問題が再発する）
2. exp_004 の学習完了後、`experiments/exp_004_potential_offset/card.md` を作成
   （破綻の記録として価値がある。総収益の表・終端 Φ≠0 の分析を含める）
3. 教授の裁定後、次実験を実施
4. **exp_005 候補: 行動振動の抑制**（教授裁定済み: 報酬項として行動差分に罰を与えるのは
   入出力契約に触れない＝第一候補。LQR の入力コスト u^T R u と同じ発想。低域通過フィルタを
   方策の外に置くのは原則不可、増分指令も契約違反で不可）。判定基準は
   **符号反転 10 回/s 未満・完走率 90% 以上・速度低下 20% 以内**
5. 上記が済んでから **M2 = 未知 5x5 迷路 単走完走率 ≥90%** へ

## 実行方法（コマンド）

```bash
# 学習（並列 1 が現状の推奨。並列 6 は滞留解に落ちた）
.venv/bin/python experiments/exp_003_sensor_history/train.py --n-envs 1 \
    --log-dir logs/<実験名> --model-out models/<実験名>.zip

# gate 判定（seed 3000-3019 × 5 試行）
.venv/bin/python experiments/exp_003_sensor_history/evaluate.py --model models/<実験名>.zip

# 検証帯での日常判断（seed 5000-5019）
.venv/bin/python experiments/exp_003_sensor_history/evaluate.py --model <...> --split validation

# 単体テスト（24 項目）
.venv/bin/python tests/test_corridor.py
```

学習時間の目安: 100 万ステップで並列 1 が約 34 分、並列 6 が約 26 分（いずれも検証帯の
定期評価 20 回ぶん 90 秒を含む）。**バックグラウンド実行**すること（10 分の前景タイムアウトを
超えるため）。

## 参照すべき文書

- `CLAUDE.md` — 体制と鉄則
- `docs/RESEARCH_PLAN.md` — §1 入出力契約、§5 マイルストーン、§9 実験規律（特に §9-1 成果物の
  分離、§9-7 データ三分割）
- `docs/JA_ENGINEERING_TERMS.md` — 日本語用語辞書。**書く文章すべてに §6 チェックリストを適用**
- `experiments/exp_002_sensors4/card.md` — 事後復元したカード（復元の限界も記載）
- `experiments/exp_003_sensor_history/{design.md,card.md}` — 滞留解の分析はこちら
- `experiments/exp_003b_single_env/card.md` — M1 gate 達成の記録
- `docs/SESSION_HANDOVER.md` — 本メモの規約
