# 引き継ぎメモ: 学生B（強化学習担当）

**⚠️ 本メモは 50 行以内・「現況」のみ**（体制運用規約 `docs/RESEARCH_PLAN.md` §12-7）。
**過去の経緯は git 履歴と研究ノート・判定文書が保存する。ここに積み上げない。**

## 現況（2026-08-15 15 時台・9 代目セッション）

### 実行中のプロセス: **無し**（学習 0 本・測定 0 本）。**未コミット変更: 無し**

**exp_023（再帰型方策実験・第 2 弾 B）は完走・判定確定・総括まで完了。第 2 弾は閉じた。**

| 成果物 | 場所 |
|---|---|
| 判定文書 | `experiments/exp_023_recurrent_policy/judgment.md`（教授検収合格） |
| **第 2 弾の総括** | **`research_notes/note_026_memory_stage_close.md`**（教授検収合格・是正 2 点反映済み） |
| 判定スクリプト | `experiments/exp_023_recurrent_policy/judge_recurrent.py`＋`tests/test_judge_recurrent.py`（66/66 PASS） |
| 重みの保全 | 18 本（最終 12・80 万歩 6）＋ `experiments/preserved_weights.md` に SHA-256 |
| 測定出力 | `outputs/exp_02{1_driving_final_samever, 3a_driving_final, 3b_driving_final}.slim.json` |

### 判定の結果（詳細は判定文書）

**R1（主）外れ・R3 当たり・R4 外れ・R5 当たり・R6 当たり・R7 外れ。**
**§3-3 (iii) が成立したため §3-4 の全域被覆表は引かない**（群 1・群 2 とも 3 判定量すべてで対照より悪い側）。
**准教授の独立再計算で 6 件すべて一致・不一致 0 件。**

**確定**: 「短期文脈では届かない」＋**「隠れ 32 次元の再帰構造では届かない」**。
**未確定**: **「記憶では届かない」**（隠れ状態の次元を変えた実験をしていない）。

## 次の担当がやること

1. **RL の新実験の起票はしない** — **ユーザ裁定（2026-08-15）で古典の完成（自己位置推定つき）の後まで保留**。
   **完遂後の役回り（古典側の支援か待機か）は教授が指示する**
2. **R11（`run_summary.json` に方策構造の設定〔`obs_history`・`recurrent`・`lstm_hidden`・
   `respawn_reset`〕を記録する）は登録済み・着手時期は教授が判断**（exp_021〜023 も同じ欠落）
3. **再開時は `docs/RESEARCH_PLAN.md` §12（体制運用規約）を先に読む** —
   実作業 80% 以上／沈黙 = 受理（受理だけの返信を送らない）／教授の裁定が要るのは 3 類型のみ／
   **教訓は作法登録せずテストか自動検査に変換する**／報告は 10 行以内

## 効いている裁定・設計（**次に記憶を試すときに要る**）

- **判定量 `n_reach_ge7` は対照 exp_021 の実測に依存した定義**。**別の対照群では定義し直す**
- **完走後の測定で `--recurrent` を付けるのは再帰型の群だけ**（対照 exp_021 には付けない）
- **走行中は実行コードを凍結する** — **群 2 は同じ `train.py` を読み直して起動する**ので、
  変更すると群 2 だけ別版になり「差は 1 変更」が壊れる（今回は変更停止で守られた）
- **監視はファイルの存在で判定し、発火と空振りの両方を実測してから使う**
  （`pgrep` は監視スクリプト自身のコマンド行に一致する）
