# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-19。段階 S3（最短走行）を実装・測定・判定した（未完了）。実行中プロセス 0 本・未コミット変更なし**
- 🔴 **主戦線 = 古典手法のゼロからの再構築**（ユーザ裁定 2026-08-19）。**旧実装は全破棄済み**（`dc01c51`）。総括 = `research_notes/note_029`、計画 = `research_notes/note_030`（**§8 に実測による段階表の是正あり**）。北極星は**「実機が実際にやっていることを PC 上で再現する」**（速さは結果であって目的ではない）
- 運用規約 = `docs/RESEARCH_PLAN.md` §11・**§12-9（単独運用）**。実作業 80%・報告 10 行以内・裁定 3 類型・本メモ 80 行以内。**旧慣行を再現しないこと**
- **教授セッション 1 本。**実装・測定は subagent へ委譲し必ず検収する。**准教授の独立検証は自動検査へ全面変換** — 実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照。**3 つが書けない実験は起票しない**
- **復旧（複数運用へ戻す）**: 移行は `bfaed2e` に閉じている。`git checkout ac3e9f9 -- handover/{student_a,student_b,associate_professor}.md`（🔴 `handover/` 丸ごとは本メモを壊す）。射程は §12-9 (e)

## 次の最初の仕事（優先順・`note_030` §8 が根拠）

1. 🔴 **スラローム（止まらずに曲がる）を段階 S4 として起票する。**実測で**旋回が最短走行の 41.3%** を占めており、
   直線をいくら伸ばしても 4 割は縮まない。`note_030` §1 の表は「ターン種別ごとの動作（スラローム）」と
   書いているのに実装は超信地旋回のまま。**速度上限を上げるより先にこちら**
2. **探索完了前の帰還（`Phase.RETURN`）が持ち時間の 34〜49% を食っている。**足立法で戻るのは正しいが
   1 区画ごとに止まる必要は無い（区画中心での読み直しは走りながらできる。最短走行では既にそうしている）
3. 探索がゴールへ届かない迷路が 2 つ（立ちすくみ・衝突）。S1 の既知の限界の切り分け

## 🔴 exp_024（段階 S3）の判定 — **未完了**（`006c9f3`・詳細は `experiments/exp_024_s3_fast_run/judgment.md`）

主判定量 `R = 0.9358`（合格側）／成立率 `C = 1/6`（宣言条件 `C >= 0.5` を満たさず）／`E = 0.9358`。
🔴 **事前登録した予測 2 つを外した。**理由は一次記録から切り分け済み:
- **出口条件「探索より速い」が判定として機能していなかった。**探索走行と最短走行がどちらも同じ 52 区画の
  経路を走り、直線を伸ばさない対照のタイムが探索走行と**完全に一致**（160.650 秒）。その結果 `R` と `E` が
  同じ数になった。**次に出口条件を書くときは対照を探索走行に取らない**
- 6 迷路中 5 迷路で最短走行が始まらなかった（2 迷路は到達せず、3 迷路は帰還で持ち時間切れ）

直線延伸そのものは設計どおり効いた（直進部分 -10.5%・全体 -6.3%）。

## 新実装の現況

`classic/` にゼロから構築。**`requires_privileged = False`**（真値も真の壁も使わない）。
構成: `maze_map`・`flood`・`route`・`sensing`・`motion`・`localization`・`explorer`・`policy`・**`checks`**。
段階は EXPLORE → RETURN → **FAST（最短走行）** → **RETURN2（最短経路で帰る）** → FAST …
`extend_straights=False` で「直線を伸ばさない」対照が作れる。最短走行中は**地図を書き換えない**。
ゴール到達後 **2.0 秒静止**する（評価器の完全進入確認に余裕を持たせるため。タイムには入らない）。
🔴 効いた是正（S1 期）: `DEFAULT_OMEGA_SETTLE` 0.05 → 0.20 rad/s で 90 度旋回が 476 → 200 ティック。戻すと検査が落ちる。
片付けた負債: テスト収集の是正（`4211d03`）・巻き添え破棄の復元（`16ffdcf`）・専用迷路 10 迷路（`24c0e67`）。

## 🔴 再発防止の 4 検査（`classic/checks.py`）— 旧実装の 4 つの事故に 1 対 1

`plan_adherence` / `assert_same_callable` / `negative_control`（**3 値で判定保留を分ける**）/ `find_retracted_numbers`。
**すべて作動側と空振り側を対で検査済み。**
🔴 **検収で分かった教訓**: 「地図が書き換わらなかった」を根拠にする検査は**単純な迷路では素通りする**。
禁止したい呼び出しは**配線（呼び出し回数）で検査する**こと。

## 成果物と環境

`outputs/video_rebuild/story.mp4`（78 秒・発表用・**未公開**。X / YouTube へ出すかはユーザの判断）。
**`.venv` は版管理外。**無ければ `requirements-lock.txt` から再構築する（無いと走行が 1 本も回せない）。
**進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8` は 2026-08-14 で停止・要更新。
🔴 **この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**

## 段階（`note_030` §3 の段階表 ＋ §8 の是正が現況）

S0 骨組み ✅ / S1 探索 ⚠️ 6 迷路中 4 迷路でゴール到達・帰還が遅い / S2 推定 ✅ /
**S3 最短走行 ⚠️ 実装済み・判定は未完了** / **S4 ← 次（スラローム。速度上限より先）** /
S5 斜め（**最後に置く** — 旧実装は早期に入れて対照比 +18.6% 遅かった）。
理想タイム（物理限界からの下界）は実装非依存なので健在（`note_025` §2-0）。

## 強化学習（温存・ユーザ指示）

**確定** =「短期文脈でも隠れ 32 次元の再帰構造でも届かない」／**未確定** =「記憶では届かない」。
総括 `research_notes/note_026`。**新実験は古典の完成まで起票しない。**

## 効いているユーザ裁定

1. **単独運用**（08-18）／**古典はゼロから再構築・RL は温存**（08-19）
2. **完成基準の分母に実機は使えない**（実機は理想の 3.0 倍速い）。基準は物理限界からの理想タイム
3. カード判定 = 調整用迷路（seed 41000〜）／章の完了確認 = 検証用迷路／最終判定 = 評価用＋実戦用。
   **予約された seed 1000〜40999 は学習・調整に使わない**
4. **突き合わせはサーキットを実装せず実戦帯の迷路ごとに**（note_025 §4-2）

## 注意点

- **走行水準の p 値は条件比較に使えない**（有効標本は迷路の数）。**合計だけで語らない**
- **閾値は対照で校正する**／**検査は作動と空振りの両方を実測してから使う**
- 🔴 **subagent は測定をバックグラウンドに投げた直後にターンを終えるとプロセスが落ちる。**前景で完走させること（4 回）
- 🔴 `maze_info["xml_path"]` の MJCF には**壁の真値が含まれる**。評価器は塞いでいないので規約として自分で守る

## 手持ちの道具

`classic/checks.py`（**新しい実験はここから書き始める**）／`experiments/exp_024_s3_fast_run/`（事前登録・測定・錨の独立再計算の雛形）／
`competition/maze_gen_turnmix.py`（目的別の専用迷路）／`research_notes/scripts/check_physical_limits_and_ideal_lap.py`（理想タイム）／`make_story_video.py`
