# 引き継ぎメモ: 教授セッション

- 更新: 2026-08-15 09:15 頃（8 代目）。**運用規約 = `docs/RESEARCH_PLAN.md` §12（50 行上限・本メモは現況のみ・過去分は git 履歴）**

## 効いているユーザ裁定（2026-08-15・3 件）

1. **斜め走行「採らない」裁定の差し戻し**: v_斜め が低いのは制御の未熟（→ まず遷移域の是正 = 016-H1 起票済み・設計承認済み）。経路生成時に斜めあり/なしをシミュレーション比較し**経路単位で採否**（note_024 登録済み・一律二択は廃止）
2. **exp_023 完遂 → 古典の完成に一度集中 → RL 再開**。古典には**自己位置推定とそれに基づく走行**を導入（計画骨子: 第 1 章 遷移域是正／第 2 章 推定器〔観測モード並走→切り替え→再校正〕／第 3 章 経路単位選択。正式起案は exp_023 完了後・完成基準はユーザ合意を取る。准教授の「真値を壊した対照」を検証に採択済み）
3. **体制 80/20**: 実作業 80% 以上・その他 20% 以内。新体制は RESEARCH_PLAN §12 に恒久化済み（沈黙 = 受理・裁定 3 類型・是正の直接主義・監査定点 2 回・作法登録の廃止→検査への変換・文書上限）

## 進行中: exp_023（再帰型方策実験・第 2 弾 B）

- **07:53:36 投入・群 1 走行中**。見込み: 群 1 完走（= 群 2 起動）10:25 ± 15 分・全完走 13:00 ± 30 分（最遅 seed 基準）
- カード確定版 `e06758b`・投入版 `8ac0994`（実行経路の差分ゼロ確認済み）・記録先 `logs/exp_023a_seed{1..6}` → `exp_023b_seed{1..6}`
- **判定体制は完成**: judge_recurrent.py 確定（`b0f5cac`・AUDIT_049 全件クローズ）・事前登録 AUDIT_048_PREREG（`8ac0994`）・**L6 合格**（隠れ状態の走行またぎなし・否定対照で判別力実証）・argv 保全済み（群 2 起動時も再保全）
- **実行コードの変更停止中**（`train.py`・`mouse/`・`common/`・`assets/`・`competition/`）: 群 2 起動確認まで全面停止 → 以後 3 群測定完了まで教授の個別承認制。理由 = 群 2 は同じファイルを読み直して起動する
- 残る手順: 群 2 起動対応 → 全完走 → 3 群測定（**対照に --recurrent を付けない**）→ 機械的判定 + 准教授の独立再計算（AUDIT_048 起票）→ 教授判定（R1・R6 は seed 内訳を見る）→ 総括ノート → ユーザ報告
- 判定の確定事項: R2 は記述に降格・R6 同数は外れ/R7 同値は当たり・R3 は字句 1.563 優先・「外」の旗 = 対照より悪い側のみ・表の抑止は n_reach_ge7 の旗に限る

## 古典側（次の主戦線・学生A 待機中）

- 016-H1（遷移域の横偏差 = 角加速度の前置補償・係数 1.0 固定・実装前診断 D1〜D3 つき）起票済み `b5f6028`・設計承認済み。016-H2（遷移域の速度）は登録のみ。**走行・実装は 3 群測定完了後に個別 GO**
- 学生A の調査確定: 古典は距離センサ不使用・真値走行の理想化構成。柱検出は幾何的に成立（感度 −1.03 mm/mm・間欠 13.4% → 推定器要）= 第 2 章の一次材料

## 次にやること（優先順）

1. 学生B の群 2 起動報告 → 変更停止を個別承認制へ移行
2. 全完走 → 判定 → ユーザ報告（「本物の記憶は走りを変えるか・汚染を断てば効くか」への答え）
3. 3 群測定完了後: 学生A へ 016-H1 の診断 D1〜D3 の個別 GO・古典完成計画の正式起案（ユーザへ完成基準を提示）
4. 下位 3 名の handover 50 行化の確認（新規約 12-7・各自の次の更新時）
