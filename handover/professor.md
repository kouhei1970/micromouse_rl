# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-20 🔴 判定基準を全面是正した（ユーザ裁定）。`research_notes/note_031` が現況の正本。**\
  未コミット変更なし・`classic/profile.py` を subagent が実装中
- 🔴 **判定量は `η = T_ideal / T_measured`（機械限界からの肉薄度）。相対比は廃止。**分解 = `η_map × η_track`。
  分母は**予測経路**の理想時間（真の壁＋自分の判断ロジックの空回しで事前に確定）。
  **センサ見間違いは η の低下として自動的に現れる**ので経路一致の別建て検査は置かない
- 🔴 **マウスの中身＝「最適な速度プロファイルを作り、そのとおりに走らせる」。**現行の「1 コマンド発行 → 完了待ち → 次」は劣化版。
  **同じ計画器が、入力を真の地図に替えるだけで判定の分母になる。経路の表現は曲率 `κ(s)`**（ターン種別の列にしない）
- 🔴 **`note_030` §3 の段階表は失効**（方式の一択を段階に置く立て方を廃止）。段階は「使える動作の種類を増やす」で、
  **どれを使うかは経路ごとに最適化器が決める**（`note_024`）。**`exp_025` の判定条文（主判定量 R）も失効**
  （測定 0 本なので実測は失われていない。**幾何の錨 A3・ハーネス・10 迷路の選定は有効**）
- 🔴 **「これはやらない・やる」を先に決めない**（08-20）。できることは試し、**できなかった理由**（計算資源／時間／物理／手法）
  **まで台帳に残す** → `research_notes/attempts_register.md`
- 🔴 **古い裁定は破棄された**（08-20）。**教師信号の禁止・報酬契約の凍結・RL 起票の門は破棄**（§8 改訂済み）。
  **残したのは**予約 seed 帯／分母は物理限界／最終形はセンサのみ→電圧／**事前登録・錨・否定対照の 3 点**。
  🔴 **破棄されたのは「何を試してよいか」の制限だけ。検証の作法は破棄されていない**
- 運用規約 = `RESEARCH_PLAN` §11・**§12-9（単独運用）**。実作業 80%・報告 10 行以内・裁定 3 類型・本メモ 80 行以内。
  **教授 1 本。実装・測定は subagent へ委譲し必ず検収。**実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照
- **復旧（複数運用へ）**: `git checkout ac3e9f9 -- handover/{student_a,student_b,associate_professor}.md`（🔴 丸ごとは本メモを壊す）

## 次の最初の仕事（優先順・`note_031` 「未解決の問い」が根拠）

1. `classic/profile.py` を完成・検収する（車両限界・最小時間スイープ・加減速/定常の内訳・`v(s), κ(s)` の引き出し）
2. 経路 → 幾何プリミティブ列。壁・柱との干渉判定。半径の掃引
3. **ターン種別・半径・斜めの採否を経路上で最適化**。候補経路を生成して厳密にスイープ。
   ⚠️ 探索が不完全なぶん `T_ideal` は真の最小より**大きい側**に出る（η を甘く見る）。**曲率制約を無視した下界も出して挟むこと**
4. `classic/motion.py` をプロファイル追従へ作り替える
5. `note_030` §3 の段階表の差し替え本文を書く／S1〜S3 を同じ物差しで再採点する

## 確定した数値（`note_031` §結果。再計算は下記スクリプト）

- **その場旋回は機械限界の 8.4%**: 90° 限界 0.167 s に対し実測 2.00 s（180° は 0.236 / 3.32 s）。
  `I_eff = 9.975473e-05 kg·m²`（MuJoCo から合成）・最大ヨートルク 0.02246 N·m（**摩擦律速**）
- **四分円スラローム**（`e607c01`）: 半径 90 mm・**実測 1.180 s / 設計 1.178 s**・終端速度は巡航の 100.7%
- **斜め**: 経路長 70.7% だが横の余地は **15.15 mm（直線の 34%）**・最大半径 103.5 mm・0.801 m/s。
  🔴 理想では**斜めの直線区間は狭くても遅くならない**（代償はターンだけ）
- **巡航速度 0.12 m/s は最高速 3.84 m/s の 3%**（最大の損失要因の候補）

## 新実装の現況

`classic/` にゼロから構築。**`requires_privileged = False`**（真値も真の壁も使わない）。構成: `maze_map`・`flood`・
`route`・`sensing`・`motion`・`localization`・`explorer`・`policy`・`checks`・**`profile`(実装中)**。
🔴 **`classic/` は実行時に迷路 XML を読んではならない**（MJCF に壁の真値が含まれる）。モデル由来の定数は
**定数として持ち、テストが実モデルから再計算して照合する**。`motion.py` には円弧スラローム（既定では未使用）と `_rolling` が入っている。

## 🔴 再発防止の 4 検査（`classic/checks.py`）— 旧実装の 4 つの事故に 1 対 1

`plan_adherence` / `assert_same_callable` / `negative_control`（**3 値で判定保留を分ける**）/ `find_retracted_numbers`。
🔴 禁止したい呼び出しは**配線（呼び出し回数）で検査**（「地図が書き換わらなかった」型は単純な迷路で素通りする）。

## 成果物と環境

`outputs/video_rebuild/story.mp4`（78 秒・発表用・**未公開**）。**`.venv` は版管理外**（無ければ `requirements-lock.txt` から再構築）。
**進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8` は 2026-08-14 で停止・要更新。
🔴 **この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**
既知の無関係な失敗: `tests/test_mouse_v2.py::test4_sensors`（本作業前から落ちている）。

## 強化学習（温存・ユーザ指示）

**確定** =「短期文脈でも隠れ 32 次元の再帰構造でも届かない」／**未確定** =「記憶では届かない」（`note_026`）。
**新実験は古典の完成まで起票しない。**到達点は**コースどりを生物のように学び取ること**（裁定 08-20）。

## 効いているユーザ裁定

1. **単独運用**（08-18）／**古典はゼロから再構築・RL は温存**（08-19）／**判定は機械限界からの肉薄度**（08-20）
2. **完成基準の分母に実機は使えない**（実機は理想の 3.0 倍速い）。基準は物理限界からの理想タイム
3. カード判定 = 調整用迷路（seed 41000〜）／最終判定 = 評価用＋実戦用。**予約 seed 1000〜40999 は使わない**
4. **斜めあり／なしの一律二択は廃止・経路単位で選ぶ**（`note_024`・08-15）

## 注意点

- **走行水準の p 値は条件比較に使えない**（有効標本は迷路の数）／**閾値は対照で校正**／**検査は作動と空振りの両方を実測してから使う**
- 🔴 **subagent は測定をバックグラウンドに投げた直後にターンを終えるとプロセスが落ちる。**前景で完走させること
- 🔴 コミットは `git commit <パス...> -F -` の形でパスを明示する（`git add -A` 禁止・CLAUDE.md）

## 手持ちの道具

`classic/checks.py`（**新しい実験はここから書き始める**）／`experiments/exp_025_s4_slalom/`（幾何の錨・10 迷路のハーネス）／
`research_notes/scripts/check_physical_limits_and_ideal_lap.py`（**`classic/profile.py` の祖先**。理想ラップの再現が錨）／
`competition/maze_gen_turnmix.py`／`make_story_video.py`
