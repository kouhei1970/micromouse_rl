# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-19 ⏸ 停止（ユーザ指示・セッション終了）。実行中プロセス 0 本・未コミット変更なし・未 push なし**
- 🔴 **主戦線 = 古典手法のゼロからの再構築**（ユーザ裁定 2026-08-19）。**旧実装は全破棄済み**（`dc01c51`・117 ファイル・61,086 行）。総括 = `research_notes/note_029`、計画 = `research_notes/note_030`。北極星は**「実機が実際にやっていることを PC 上で再現する」**（速さは結果であって目的ではない）
- 運用規約 = `docs/RESEARCH_PLAN.md` §11・**§12-9（単独運用）**。実作業 80%・報告 10 行以内・裁定 3 類型・本メモ 80 行以内。**旧慣行を再現しないこと**
- **教授セッション 1 本。**実装・測定は subagent へ委譲し必ず検収する。**准教授の独立検証は自動検査へ全面変換** — 実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照。**3 つが書けない実験は起票しない**
- **復旧（複数運用へ戻す）**: 移行は `bfaed2e` に閉じている。`git checkout ac3e9f9 -- handover/{student_a,student_b,associate_professor}.md`（🔴 `handover/` 丸ごとは本メモを壊す）。射程は §12-9 (e)

## 次の最初の仕事（優先順）

1. **S3 最短走行**（`note_030` の段階表）。探索で作った地図から最短経路を引き、**ターン種別の列**として走る。
   `classic/route.py` の `plan_route` が既にターン列を返すので、それを実行する層を作る
2. **到達率を上げる**（現状 3 面中 2 面。未到達は推測航法の誤差蓄積が主因）／**専用迷路の作成**（`note_030` §4・未着手）

## 🔴 新実装の現況（2026-08-19）

`classic/` にゼロから構築。**`requires_privileged = False`** — 真値位置も真の壁情報も使わない。
**到達 2/3**（設計帯 3 面・既定構成）／地図一致率 **95.8〜98.9%**／**テスト 87 件通過**（約 10 分）／否定対照 N1/N2 合格。
構成: `maze_map`・`flood`・`route`・`sensing`・`motion`・`localization`・`explorer`・`policy`・**`checks`**。

🔴 **効いた是正**: 旋回 1 回が遅く（角度は合っているのに角速度がゼロになるのを待つ空費）走行時間の 6 割を旋回に使っていた。
`DEFAULT_OMEGA_SETTLE` 0.05 → 0.20 rad/s で 90 度旋回が 476 → 200 ティック。
**これで初めて壁補正の価値が見えた**（到達 0/3 → 2/3）。戻すと検査が落ちることを実測済み。

## 🔴 再発防止の 4 検査（`classic/checks.py`）— 旧実装の 4 つの事故に 1 対 1

`plan_adherence`（計画に乗っていた割合）/ `assert_same_callable`（実行経路の同一性）/
`negative_control`（真値を壊して変わらないこと・**3 値で判定保留を分ける**）/
`find_retracted_numbers`（失効数値の伝播）。**すべて発火側と空振り側を対で検査済み。**
実際に `verification/REVIEW_008`・`REVIEW_010` の未訂正引用を見つけて是正した。

## 成果物: 発表用動画（完成・未公開）

`outputs/video_rebuild/story.mp4`（78 秒・9.3 MB・1920x1080・音声なし）。
再生成は `research_notes/scripts/make_story_video.py`。素材は `maze_42134_classic.mp4`。
**⚠️ まだ公開していない。**X / YouTube へ出すかはユーザの判断。

## 破棄の経緯（詳細は `note_029`）

旧古典実装は**斜め経路が実走で 0.23% しか使われていなかった**（29,422 ティック中 69）。
それは 5 日前に発見・記録され、**修正も別ファイルに実装済みだったのに本体は未修正**のまま忘れられていた。
失効した数値が総括ノートと設計提案へ訂正なしで流れ込んでもいた。
**個別の不注意ではなく積み上げ方の構造から出ている**と判断し、積み上げ方ごと捨てた。
🔴 **復元は `dc01c51` の親から**（`git show dc01c51^:<パス>`）。

## 環境と保留

**`.venv` は版管理外。**無ければ `requirements-lock.txt` から再構築する（無いと走行が 1 本も回せない）。
**進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8` は 2026-08-14 で停止・要更新。
🔴 **この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**

## 段階（`note_030` の段階表が正本）

S0 骨組み ✅ / **S1 探索 ⚠️ 動くが 3 面中 2 面** / S2 推定 ✅ 実装済み・既定で有効 /
**S3 最短走行 ← 次** / S4 速度 / S5 斜め（**最後に置く** — 旧実装は早期に入れて全部直しても対照比 +18.6% 遅かった）。
第 0 章の理想タイム（物理限界からの下界）は実装非依存なので健在（`note_025` §2-0 が正本）。

## 強化学習（温存・ユーザ指示）

**確定** =「短期文脈でも隠れ 32 次元の再帰構造でも届かない」／**未確定** =「記憶では届かない」。
総括 `research_notes/note_026`。**新実験は古典の完成まで起票しない。**残る未確認は裾の機構（入力次元 対 パラメータ数）。

## 効いているユーザ裁定

1. **単独運用**（08-18）／**古典はゼロから再構築・RL は温存**（08-19）
2. **完成基準の分母に実機は使えない**（実機は理想の 3.0 倍速い）。基準は物理限界からの理想タイム
3. **帯の 3 段**: カード判定 = 設計帯（seed 41000〜）／章の完了確認 = 検証帯／最終判定 = 評価帯 + 実戦帯。**予約帯は学習・調整に使わない**
4. **突き合わせはサーキットを実装せず実戦帯の迷路ごとに**（note_025 §4-2）

## 注意点

- **走行水準の p 値は条件比較に使えない**（有効標本は seed 数）。**件数は少数の面に支配される** — 合計だけで語らない
- **閾値は対照で校正する**／**検査は発火と空振りの両方を実測してから使う**
- 🔴 **subagent は測定をバックグラウンドに投げた直後にターンを終えるとプロセスが落ちる。**前景で完走させること（3 回起きた）

## 手持ちの道具

`classic/checks.py`（再発防止の 4 検査。**新しい実験はここから書き始める**）／
`research_notes/scripts/check_physical_limits_and_ideal_lap.py`（理想タイム・実装非依存）／
`research_notes/scripts/make_story_video.py` と `_video_l0_common.py`（走行の描画と動画の組み立て）
