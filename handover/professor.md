# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-21 ⏸ 停止（ユーザ指示）。センサモデルの作り直しを次セッションへ持ち越す。**\ 未コミット変更なし・実行中プロセス 0 本。正本は `note_031`（判定）・`note_033`（工程）・`note_034`（センサ）
- 🔴 **前提が覆った。**距離センサは MuJoCo の `rangefinder`（幾何距離をそのまま返す）だったが、 実機は IR LED ＋ フォトトランジスタで**反射強度**を読む。**別物**。`note_034` が正本。
  ユーザから「**確定するまでこれまでの決定に縛られなくてよい**」と許可を得ている（08-21）
- 🔴 **判定量は `η = T_ideal / T_measured` ただ 1 つ。行き先は η → 1**（段階 0.60 → 0.70 → 0.80）。 分解 = `η_backoff × η_map × η_track`（**08-21 に定義を是正**。同じ計画条件どうしで比べる）。
  **古典も強化学習も同じ基準**（`RESEARCH_PLAN` §5-0）。同じマウスで制御系の作り方が違うだけ
- 🔴 **完走率は合格の門にしない**（究極の速度を競うもの）。**整定時間は工夫で消す対象**
- 🔴 **「これはやらない・やる」を先に決めない**（08-20）。できなかった理由まで `research_notes/attempts_register.md` に残す。**古い決定は破棄済み**（教師信号の禁止・報酬の凍結・RL 起票の門）
- 運用規約 = `RESEARCH_PLAN` §11・§12-9。実作業 80%・報告 10 行以内・本メモ 80 行以内。教授 1 本。 実装は subagent へ委譲し必ず検収。実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照
- 🔴 **用語辞書 `docs/JA_ENGINEERING_TERMS.md` §6 を報告・コミット・文書の前に毎回適用する** （守らず 3 回指摘された: 機械限界→物理限界／律速・現金化／裁定）

## 次の最初の仕事（`note_033` の工程・`note_034` の未解決）

1. **`classic/explorer.py` の `sense_walls()` 呼び出し 10 箇所を `sim.ir_ranges()` へ差し替える**
2. **`classic/sensing.py` の壁判定を新しいモデルの上で作り直す**（現行は幾何距離が正確に返る前提）
3. `docs/MODEL_VERIFICATION_PLAN.md` の改訂と検証スイートの再実行（**物理モデルの仕様変更にあたる**）
4. `note_033` 第 1 工程（探索の信頼度）を再開。**間違いと分かっている地図で最短走行を走らせない**
5. 持ち越し: **表の速さが未達**（`note_034`）。安全弁が 500/500 で発動し利得ゼロ。 決着には「幾何だけで危険な姿勢を判定できる指標」が要る。**強化学習は毎ティック観測を使うので必要になる**

## センサモデルの現況（`note_034`）

確定した物理: 白い射出成形プラスチック・**半値角 5° 以下**・離隔 **6〜7mm**・平行・**縦配置**（ユーザ判断 08-21）・
アライメント誤差 ±1°・**変調光**。実装 = `mouse/ir_sensor.py`（遮蔽つき・同一平面の壁と柱を統合）・
`mouse/sim.py::ir_ranges()`（**既定 `ir_sensor_mode="rangefinder"` で従来と同一**）・表 = `mouse/data/*.npz`。

- 🔴 **応答は距離に対して単調でない。**ピーク 44mm。**26.7mm と 90.0mm が同じ値**。5mm でほぼゼロ
- 🔴 **強度で使うか距離に直すかを決めうちしない**（ユーザ指示）。モデルは生の信号を出す
- 縦配置と横配置は**近距離かつ床ありで 11.4% 違う**。比較は**未決着**（無限平面では回転対称で差が出ない）
- センサの配置（4 本・位置・光軸）は**実機に近いが最適とは分かっていない**。 🔴 **モデルが実機に近いと確かめるまで配置の探索はしない**
- `observation()` に混ぜないこと。毎ティック計算だと 10 迷路×2 条件で 53 時間かかる （`sense_walls()` は毎ティック呼ばれない。必要なときだけ計算すれば 0.76 時間）

## 確定した数値（`note_031`）

- **10 迷路の理想時間表** `experiments/exp_025_s4_slalom/ideal_table.json`（版管理下・走行 0 本で算出）
- **物理限界**: 直線 1 区画 0.3595s／その場 90° 旋回 0.1729s／`V_TOP` 3.84 m/s／`A_TR` 5.57／`A_LAT` 6.20
- **コマンド方式の分解**（maze_41001・実測 154.24s）: `9.17 × 2.17(方式) × 4.23(速度上限) × 1.83(制御)`、η=5.9%
- **追従にすると**その場 90° 旋回が物理限界の **11.6 倍 → 1.04 倍**。単体の 実測/計画 は 0.91〜1.15
- **初の完走**（exp_028・余裕 25mm・摩擦円の使用率 0.30）: 北向き 5/10・**η 中央値 0.533**。 🔴 **ただし真の地図を与えた測定**。全手順（exp_029）では **η 中央値 0.053**（420 秒だと 0.024）
- **地図の正しさ**（exp_030）: 致命的な誤りが 0 の迷路は **3/10**。誤り 14 件は**全て側方センサ**由来で、 真の横ずれ 17.5〜35.7mm が較正前提の **±15mm を超過**していた。**書き込み先の誤りは 0 件**
- 中心線上の 90° 最大半径 190.2mm（祖先の 300.5mm は機体長を見ておらず実現不能）

## 新実装の現況

`classic/` にゼロから構築。**`requires_privileged = False`**。`maze_map` `flood` `route` `sensing` `motion`
`localization` `explorer` `policy` `checks` `profile` `geometry` `ideal` `tracker` `fast_planner`。
🔴 **実行時に迷路 XML を読んではならない**（MJCF に壁の真値が含まれる）。🔴 `fast_planner.py` の
`v_walls`/`h_walls` は**学習した地図**の中身で、`grep` では真値の参照と区別できない（**違反が紛れて見逃されうる**）。

## 成果物と環境

**解説動画 第 1 部が完成**（`outputs/video_kinematics/part1.mp4`・8 節・6 分 50 秒・音声つき）。台本は
`research_notes/scripts/video_kinematics/narration/`・組み立ては `build_part1.py` を 1 回走らせるだけ。
ナレーションは Azure Speech（七海）。鍵は `.secrets/azure_speech.env`（版管理外）。🔴 **鍵が会話の履歴に
残っているので Azure ポータルで再生成すること。**第 2 部は完走が安定してから。`.venv` は版管理外。
🔴 **進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8`（2026-08-14 で停止）。
**この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**
評価基準の図解 `https://claude.ai/code/artifact/14b9d8ae-c403-4095-893e-d64277534663`。
既知の無関係な失敗: `tests/test_mouse_v2.py::test4_sensors`（本作業前から落ちている）。

## 強化学習（温存）

**確定** =「短期文脈でも隠れ 32 次元でも届かない」／未確定 =「記憶では届かない」（`note_026`）。
🔴 **この結論は旧センサ前提である。**起票の禁止は破棄済み。到達点は**コースどりを生物のように学び取ること**。

## 効いているユーザの決定

1. 単独運用（08-18）／古典はゼロから再構築（08-19）／判定は物理限界からの肉薄度・全トラック共通（08-20）
2. **完成基準の分母に実機は使えない**。基準は物理限界からの理想タイム
3. **予約 seed 1000〜40999 は使わない**（41000〜61000 は設計帯として消費済み・学習用は 61001 以降）
4. 斜めあり／なしの一律二択は廃止・経路単位で選ぶ（`note_024`）
5. **探索を信頼できるものにしてから最短走行へ**（08-21・`note_033`）

## 注意点

- p 値は条件比較に使えない（有効標本は迷路の数）／閾値は対照で校正／検査は作動と空振り両方を実測してから使う。コミットは `git commit <パス...> -F -`（`git add -A` 禁止）
- 🔴 **どのパラメータでも隣接値で結果が反転する。**孤立して良く見える 1 点は再現しない（`note_031`）
- 🔴 **単一の標本で判断しない。**乱数種を変えると結論が変わった事例が複数ある
- 🔴 **最適化の前に「それが何回実行されるのか」を数える**（表を作ったが呼び出しは 600 回で不要だった）
- 🔴 **subagent は待ち受けでターンを終えると落ちる。**測定は前景で完走させること（この件で 10 回超）
