# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-18（単独運用へ移行。状態: 稼働中・実行中プロセス 0 本・未コミット変更なし）**
- 運用規約 = `docs/RESEARCH_PLAN.md` §11・**§12-9（単独運用）**。実作業 80%・報告 10 行以内・裁定 3 類型・本メモ 80 行以内。**旧慣行（作法の逐次登録・受理の往復・多重検収・メモの追記蓄積）を再現しないこと**

## ⚠️ 体制（2026-08-18 ユーザ裁定・単独運用）

**教授セッション 1 本だけで進める。**学生A・学生B・准教授の常設セッションは停止。役割別の引き継ぎメモは廃止（**単独運用中は復元しない**）。
**復旧手順（複数運用へ戻すとき）**: 移行は `bfaed2e` 1 コミットに閉じている — `git checkout ac3e9f9 -- handover/{student_a,student_b,associate_professor}.md` で役割別メモ 3 本が SHA-256 一致で戻り（🔴 `handover/` を丸ごと指定すると**本メモが 8 代目の旧版で上書きされる**ので避ける。旧指示書が要るなら同 commit の `instruction_student_{a,b}.md`）、文書側は `git diff ac3e9f9 bfaed2e -- CLAUDE.md docs/RESEARCH_PLAN.md docs/SESSION_HANDOVER.md` を見て **4 か所**を書き戻す（①§11 ②CLAUDE.md 体制節・再開手順 ③SESSION_HANDOVER §3-2〔役割を名乗る前の確認〕・§4-2 ④§12-7 の行数上限）。**§12-9 (c) の 3 点セットだけは失効させない**（射程の詳細 = §12-9 (e)）。
実装・測定・探索は **subagent へ委譲**し、教授セッションが検収する。**subagent は文脈を持ち越さない** — 委譲のたびに正本の場所・判定条文・禁止事項を書き渡す。
**准教授の独立検証は自動検査へ全面変換した**（§12-9 (c)）: 実験ごとに **①判定条文の事前登録 ②錨の独立再計算スクリプト ③否定対照** を結果を見る前に版管理へ入れる。**3 つが書けない実験は起票しない。**

## 次の最初の仕事（優先順）

1. **第 1 章の第三容疑の診断**（下記「古典」参照）。**起票の前に**: 主判定量は 1 つの実数に落とし、**実数直線を切れ目なく分割する**。連言（「単調に増え、かつ …」）を入れると場合分けが掛け算で増えて覆いに穴が空く（016-H1d〔遷移域の上乗せ機構の同定〕第 1 段で実際に「どれにも当たらない」が出た）
2. **理想タイムと真値制御の実測の突き合わせ**（GO 済み・未着手・走行 1 本）→ note_025（古典完成計画）の完成基準の倍率を根拠づける
3. **note_025 を `docs/RESEARCH_PLAN.md` のマイルストーンへ正式化**（§4 の未合意 3 点のうち、範囲と v_斜め〔斜め走行の速度〕はユーザ合意済み。**残るは倍率のみ** = 上の 2 の結果待ち）

## 古典（現在の主戦線）— 計画は `research_notes/note_025_classical_completion_plan.md`

**第 0 章 物理限界の理想タイム = 完了**（最高速 3.840 m/s・最大加減速 5.572 m/s²・横加速度 6.200 m/s²〔理想タイム用〕/5.87〔016 系の成立条件・**併存させる**〕。サーキット〔16×16 外周周回課題〕の理想ラップ 5.276 s／経路も理論限界なら 4.817 s。実戦帯 19 面の理想タイムも算出済み）。

**第 1 章 遷移域の是正 = 現在地。**

| 事項 | 状態 |
|---|---|
| 016-H1（角加速度の前置補償） | **実装しない**（判定 K4 = 設計へ戻る）。円弧の中で効く仕掛けなので、上乗せの増分の高々 38% にしか届かない |
| 016-H1d（上乗せ機構の同定） | 第 1 段: 摩擦滑りは**不支持**（実効半径比 0.884〜0.924 < 1 = 内側へ食い込む）。第 2 段（`18b0e81`）: **上乗せの 62% は円弧に入る前にできている**（進入側で確定）／**電圧の飽和ではない**（進入区間の飽和率は中央値も最大も 0.00%） |
| **第三容疑（未検証・要診断）** | **Stanley 形の横項 atan(k_y·e_y/(\|v\|+v_ε)) は速度が上がると実効ゲインが下がる**ので、直線・斜め区間での是正が遅くなる。**測り方の案**: 進入直線で横偏差 e_y の減衰の時定数を速度水準ごとに出し、1/v に比例するかを見る |
| 016-H2（遷移域の速度・曲率半径） | 登録のみ。R = 60 mm の円弧の理論上限 0.610 m/s に対し実測 0.590 = 97%。**上げるには半径を上げるしかない** |

**第 2 章 自己位置推定 = 未起票・設計未確定。**着手前に **否定対照を先に登録する**（ユーザ裁定 2026-08-15）:

> 推定器の検証を「推定誤差を真値と比較できる構成」にするだけでは足りない。**比較が通ること自体が安心材料になってしまう。**
> 要るのは**真値を意図的に壊した対照** — 壊しても走行が変わらなければ、その経路は真値を使っていない。exp_023 の L6-b と同型。

第 3 章 探索のセンサ化・第 4 章 経路単位の斜め選択 = 未起票。

## 強化学習（第 2 弾「記憶と文脈」）= 正式クローズ済み

**判定**: R1 外れ（深部到達 12→0）・R4 外れ（衝突 3.4 倍）・R6 当たり（リセットで 9 まで戻るが対照 12 に届かず）。准教授の独立再計算で 6 件すべて一致。
**確定** =「短期文脈でも隠れ 32 次元の再帰構造でも届かない」／**未確定** =「記憶では届かない」（隠れ状態の次元を変えていない）。
文書: `experiments/exp_023_recurrent_policy/judgment.md`・総括 `research_notes/note_026_memory_stage_close.md`。**RL の新実験は古典の完成まで起票しない**（ユーザ裁定 2026-08-15）。
**残る唯一の未確認**: 裾の機構（入力の次元 対 パラメータ数）— **17 次元入力でパラメータ数を 72,837 に揃えた群**が要る。

## 効いているユーザ裁定

1. **単独運用**（2026-08-18・上記）
2. **斜め「採らない」差し戻し**: v_斜め は制御の未熟。経路単位の斜めあり/なし選択（note_024）とし、一律二択は廃止。**v_斜め に到達目標は設けない**
3. **古典の完成（自己位置推定つき）に集中 → その後 RL 再開**。探索もセンサ化する。**完成判定 = 評価帯 20 面 + 実戦帯 19 面**（配置済み `28ff0e1`）
4. **完成基準の分母に実機は使えない**（実機は本モデルの理想の 3.0 倍速い）。基準は物理限界から計算した理想タイム
5. **帯の 3 段**: 開発・カード判定 = 設計帯（seed 41000〜）／章の完了確認 = 検証帯／最終判定 = 評価帯 + 実戦帯。**評価用に予約された seed 帯は学習・調整に使わない**
6. **体制 80/20** = §12 に恒久化済み

## 注意点（判定が反転する幅の落とし穴）

- 🔴 **診断ハーネスの記録は必ず `step_control` の【前】で読む。**後で読むと 1 ティック先の値になり、相関が +0.782 → +0.077 まで動いた
- **走行水準の p 値は条件比較に使えない**（有効標本は seed 数）。**件数は少数の seed に支配される** — 合計だけで「増えた」と書かない
- **閾値は対照で校正する**（固定閾値では 18/18 で判別力ゼロだった）
- **走行中は実行コードを凍結する**（同じ `train.py` を読み直して起動する群があるため）
- 監視はファイルの存在で判定し、**発火と空振りの両方を実測してから使う**

## 手持ちの道具

`research_notes/scripts/check_*.py`（物理限界と理想ラップ・実戦帯 42 面の理想タイム・実機記録との差の分解）／`experiments/exp_016_diagonal/run_016h1_diag.py`（診断ハーネス。**制御に触れず記録を包む**。無害性は `tests/test_h1d_probe_harmless.py` で bit 一致を検査）／`verification/audit_exp023_*.py`（L6 隠れ状態・錨の独立再計算・安定性。**否定対照つきで空振りしない形**）
