# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-23 ⏸ 停止。座標系を正本化して方位を工学系へ付け替え、実大会迷路 102 面を取り込んだ**
- 🔴 **判定量は `η = T_ideal / T_measured` ただ 1 つ。行き先は η → 1**（段階 0.60→0.70→0.80。
  分解 = `η_backoff × η_map × η_track`）。**古典も強化学習も同じ基準**（`RESEARCH_PLAN` §5-0）
- 🔴 **完走率は合格の門にしない**／**整定時間は工夫で消す対象**／**「やらない」を先に決めない**
- 運用規約 = `RESEARCH_PLAN` §11・§12-9（実作業 80%・報告 10 行・本メモ 80 行・実装は委譲して検収・
  実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照）
- 🔴 **用語辞書 `docs/JA_ENGINEERING_TERMS.md` §6 を報告・コミット・文書の前に毎回適用する**
- 🔴 **座標系の正本は `docs/COORDINATE_SYSTEM.md`**（2026-08-23 制定。`CLAUDE.md` の必読正本に追加済み）

## 次の最初の仕事
1. `classic/sensing.py` の壁判定を新センサモデルの上で作り直す（`explorer.py` の `sense_walls()` 10 箇所）
2. `note_033` 第 1 工程（探索の信頼度）を再開。**間違いと分かっている地図で最短走行を走らせない**
3. 新: **`competition/mazes/contest_historical/` の実大会迷路 102 面で第 1 工程の判定量を測る。**
   生成迷路で通っても実物で通らないなら、生成の側に見落としがある
4. 保留: `note_036`（迷路データベースの形式と置き場・着手前の設計提案。ユーザ承認待ち）／
   隅の相互反射の高速化／`AUDIT_059`・`060` は**速さが目標未達**（1.57ms、目標 1.0ms）。精度は合格

## センサモデル: 実機に乗り、実時間より速くなった
既定値と ρ=0.8・床は無視の決定は `note_034` 追記16 が正本。
表引き `response_table()` は**精度合格・速さ未達**。**模擬 1 秒 → 実時間 0.57 秒（1.75 倍速）。**

## 2026-08-23 に入れたもの
- **`docs/COORDINATE_SYSTEM.md`**（正本。定義は同書を見ること）。
  `ROBOT_SPEC` §8.1 は本書への参照に置き換えた（値の二重管理をやめた）
- **方位インデックスを工学系へ付け替え**（`Direction` を N=0,E=1,S=2,W=3 → **E=0,N=1,W=2,S=3**）。
  検収済み（下記）
- **実大会迷路 102 面**（1980〜2012・`competition/mazes/contest_historical/`）。
  NTF ヒストリーアーカイブの BMP から読み取り。年・区分が一次資料で確定。`note_035`
- **`tests/test_coordinate_system.py`**（15 件）が座標系と角度の分枝を見張る
- 歴史の調査 `research_notes/survey_micromouse_history.md`（834 行）と読み物 Artifact（下記）

## 🔴 迷路について分かったこと（次に触る人へ）
- **決勝・予選・フレッシュマンで難度が違う**（最短距離の中央値 66／57／46）。**混ぜて比べてはいけない**
- **現行の生成迷路（66・迂回 4.71・行止 21）は大会の決勝迷路（66・4.75・21）とほぼ同じ水準。**
  `MAZE_DIFFICULTY_REPORT` の是正は効いていた。同レポートの限界 1（区分が推測）は解消した
- 残る差は**閉路数 β だけ**（生成は 19 前後に固まり、実物は 5〜29）。**まず測る。作り直しはそのあと**
- 資料が食い違う面が 5 つある。**どちらが正しいかは決められない**（`note_035`）

## 🔴 教授セッションが今回犯した誤り（同じ轍を踏まないこと）
- 🔴 **一致確認が、変えたものが効く場面を通っていなかった。**方位の付け替えで
  「実大会 3 面の sha256 完全一致」を根拠に「振る舞いは保たれた」と報告したが、**3 面はすべて
  北向き発進**で、実際に変わったのは**南向き・西向きの角度の分枝**だった。回帰 2 件をすり抜けた。
  捕まえたのは**わざわざ南向きを試す既存の検査**。**「一致した」は「全部を通した」ではない**
- 🔴 **subagent と同じファイルを同時に編集していることに気づかず、誤った報告をした。**
  「`_turn_delta` は前後で完全に同一だった」と述べたが、**既に担当の修正が入った後の値を見ていた**。
  検収の前に `git status` と担当の作業状況を確かめること
- 🔴 **Artifact を書き換えただけで publish せず「直しました」と報告した**（ユーザ指摘）
- **画面を見ずに CSS を直そうとした。**迷路が左に寄る症状の原因は、`.maze{margin:… 0 0}` が
  `.wrap{margin:0 auto}` を打ち消していたこと。**Chrome で見たら一目で分かった**
- **調べた数値を文書に書き入れ忘れた**（2015 年の記録。ユーザ指摘）

## 成果物と環境
**歴史の読み物** `https://claude.ai/code/artifact/dabe4606-508d-41a1-ad85-0cba08bb0037`（「マイクロマウスの半世紀」）。
**センサ動画** `outputs/video_sensor/sensor_visualization_narrated.mp4`（4分34秒・ナレーション入り）。
🔴 **`outputs/video_sensor/sensor_visualization.mp4`（104秒・無音）は別物。上書きしないこと。**
**解説動画 第 1 部** `outputs/video_kinematics/part1.mp4`。
🔴 **Azure Speech の鍵はユーザが 08-22 に再生成済み**（`.secrets/azure_speech.env`）。
🔴 **進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8`（08-14 で停止）。
**この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**
センサの現況 `.../424e420f-bf74-4364-a248-70bd96e207c2`／高速化 `.../ec29ce62-feb9-48ff-acb0-5bfd9a98f088`／
表引きの検討 `.../c31e726e-7f93-407e-90e7-1456b0df67e1`（いずれも `claude.ai/code/artifact/` 配下）。
表 `mouse/data/ir_cumulative_table.npz`（56.9MB）は**版管理外**。生成 106 秒。
既知の無関係な失敗: `tests/test_mouse_v2.py::test4_sensors`（本作業前から落ちている）。

## 効いているユーザの決定
1. 単独運用（08-18）／古典はゼロから再構築（08-19）／判定は物理限界からの肉薄度／
   **完成基準の分母に実機は使えない**／**予約 seed 1000〜40999 は使わない**（学習用 61001 以降）
2. **探索を信頼できるものにしてから最短走行へ**（08-21・`note_033`）
3. **光線追跡を前向き計算に採用しない**（測定の道具としては使う）
4. 🔴 **ρ=0.8 に確定・床は無視・以後 ρ の値には戻らない**（08-22）
5. 🔴 **壁の有無の判定はセンサモデルの妥当性の基準に使わない**（08-22。モデルと応用は層が違う）
6. 🔴 **座標系は工学系に統一する**（08-23。方位のインデックスも E=0 から反時計回り）
7. 🔴 **迷路は「アーカイブ」ではなく「データベース置き場」として持つ**（08-23。`note_036`）

## 注意点
- p 値は条件比較に使えない／🔴 **閾値は「対照で空振り分布を測ってから」置く**／コミットは
  `git commit <パス...> -F -`（`git add -A` 禁止。新規は `git add <パス>` を先に）
- 🔴 **隣接値で結果が反転する。単一の標本で判断しない**（`note_031`）
- 🔴 **subagent は「通知を待ちます」でターンを終えると落ちる**
- 🔴 **「〜のはず」で検査を設計しない。**単調減衰も `Δ` の符号も、実測してから書く
- 🔴 **同じ向きでも角度の分枝（±π・±2π）が違うと走行が変わる。**`tracker.reset()` が
  巻き戻していない角度を受け取る設計が根にある（`COORDINATE_SYSTEM` §10-3b）
