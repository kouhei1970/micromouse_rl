# 引き継ぎメモ: 教授セッション

- 更新: **2026-08-21 ⏸ 停止（ユーザ指示）。センサモデルの検証が大きく進んだ。**\
  未コミット変更なし・実行中プロセス 0 本。正本は `note_031`（判定）・`note_033`（工程）・`note_034`（センサ）
- 🔴 **判定量は `η = T_ideal / T_measured` ただ 1 つ。行き先は η → 1**（段階 0.60 → 0.70 → 0.80）。
  分解 = `η_backoff × η_map × η_track`。**古典も強化学習も同じ基準**（`RESEARCH_PLAN` §5-0）
- 🔴 **完走率は合格の門にしない**／**整定時間は工夫で消す対象**／**「やらない」を先に決めない**（08-20）
- 運用規約 = `RESEARCH_PLAN` §11・§12-9。実作業 80%・報告 10 行以内・本メモ 80 行以内。
  実装は subagent へ委譲し必ず検収。実験ごとに ①事前登録 ②錨の独立再計算 ③否定対照
- 🔴 **用語辞書 `docs/JA_ENGINEERING_TERMS.md` §6 を報告・コミット・文書の前に毎回適用する**

## 次の最初の仕事

1. **モデル既定値の更新（ユーザ承認済み・08-21）**。`mouse/ir_sensor.py` の
   `IrSensorSpec`: `led_half_angle_deg` 5.0→**3.0**、`pt_half_angle_deg` 5.0→**6.0**、
   `separation_m` 0.0065→**0.0055〜0.0060**。`SurfaceSpec`: `specular` 0.0→**0.10**、
   `shininess` 20.0→**35.0**。**表 `mouse/data/*.npz` の作り直しと
   `docs/MODEL_VERIFICATION_PLAN.md` の改訂・検証スイートの再実行を伴う**
2. **面積分に相互反射を足す**（`AUDIT_050` 工程2 の帰結）。**ただし下の ρ の件を先に読むこと**
3. `classic/sensing.py` の壁判定を新しいモデルの上で作り直す（`explorer.py` の
   `sense_walls()` 呼び出し 10 箇所を `sim.ir_ranges()` へ差し替える）
4. `note_033` 第 1 工程（探索の信頼度）を再開。**間違いと分かっている地図で最短走行を走らせない**

## センサモデルの現況（`note_034` 追記6〜13 が最新。ここが一番動いた）

🔴 **実機の実測が 2 件手に入り、モデルは実機に乗った。**（`assets/` と `mouse/data/` に一次データ）

- **距離**（`mouse/data/ir_measured_front_20260821.csv`・200〜50mm・正対）:
  右センサで残差 **0.77%**・距離のずれ +0.5mm。再現 = `verification/audit_051_measured_vs_model.py`
- **角度**（`assets/angle_vs_sensor.csv`・±25°・4 距離・**センサ 1 本ずつ別々に測ったもの**・
  光軸が壁に垂直な状態が 0 度で**壁の角度**を振った）: 鏡面成分を入れると残差が 26 倍良くなる。
  再現 = `verification/audit_052_angle_vs_model.py`
- **素子が確定**: LED = OSRAM **SFH 4550**（半値角 **±3°**）、PT = KODENSHI **ST-1KL3A**（**±6°**）。
  データシートは `assets/datasheets/`。**モデルの「両方 5°」は両方とも外れていた**
- 🔴 **非単調性は実機で確認済み**（ユーザ「実際に近づきすぎると下がりますよ」）。飽和の説明は棄却。
  山の位置は **36〜44mm**。機構 = 照射円と視野円のずれ。否定対照 = `audit_053_peak_mechanism.py`
- **区画中心での壁面までの距離: 前方 53.0mm・側方 70.1mm。**前方は山のすぐ外側で、
  20mm 前進すると山の内側に入る。**しきい値ひとつで切る現行 `sensing.py` が成り立たない理由**
- 他機体の記事（micromouseonline 等 4 件）の較正式は本機体に当てはまらない（残差 8.6〜26.8%）。
  山の位置は `離隔/(2·tan 半値角)` で決まり、広い半値角の機体では 15mm 付近になるだけ
- ユーザ談: LED と PT を**内輪に傾ける**と近距離も見える（そういう機体もある）。モデルで再現済み
- 🔴 **配置の探索はまだしない**（モデルが実機に近いと確かめるまで、が本ノートの規約）

## AUDIT_050（光線追跡との突き合わせ）— 完了

`verification/AUDIT_050_PREREG_ir_raycast.md`（追記3 まで）と `AUDIT_050_RESULT.md` が正本。

| 段 | 実測 | 判定 |
|---|---|---|
| 工程1 `M1/σ95` | **1.38** | 帯1: **面積分の実装は定式化どおり** |
| 否定対照（半値角 7°） | **477** | 10 超。**検査は差を検出できる** |
| 工程2 `M2` | **0.03015** | 帯2: **単一反射の仮定は成り立たない** |

- 独立に導出した閉じた形 `P = ρ/((m+2)d²)` に、面積分は 5 桁一致・光線追跡は 1.00018±0.084%
- 🔴 **反射回数ごとの増分は単調減衰しない。これはバグではない**（面の偶奇）。
  LED が照らす点と PT が見る点は同じ平面上にあり、平面を出た光は戻れないので、
  間接照明の主経路は「壁→床→壁→PT」の**反射 3 回**になる。検算 = `audit_050_bounce_parity.py`。
  **教授セッションは一度これをバグと誤判定して取り下げ、検算して撤回した**
- `audit_050_raycast_selftest.py` の**検査2（単調減衰）は無効**（教授の誤った指示による）

## 🔴 相互反射の実装に着手する前に読むこと（`AUDIT_050_RESULT` 追記2）

**壁の拡散反射率 ρ を実測していない。**いま 0.8 と仮定している。実迷路 80 姿勢での M2 は
**ρ=0.5 で 0.0074（帯1＝相互反射は不要）／ρ=0.6 で 0.0128（帯2＝要る）**で、
**境目は ρ≈0.55**。既存データでは ρ を決められない（測定の場面では多重反射が 2〜5% しか
占めないため。走査 = `verification/audit_054_reflectance_sweep.py`）。
**ρ を確かめずに相互反射を実装すると、不要な工事になりうる。**既知の反射率の面と
並べて同じセンサで読み比べれば比として取れる。ユーザは 08-21 時点で実験できない。

## 確定した数値（`note_031`）

- **10 迷路の理想時間表** `experiments/exp_025_s4_slalom/ideal_table.json`（版管理下）
- **物理限界**: 直線 1 区画 0.3595s／その場 90° 旋回 0.1729s／`V_TOP` 3.84 m/s／`A_TR` 5.57／`A_LAT` 6.20
- **初の完走**（exp_028・真の地図を与えた測定）: η 中央値 **0.533**。全手順（exp_029）では **0.053**
- **地図の正しさ**（exp_030）: 致命的な誤りが 0 の迷路は **3/10**。誤り 14 件は**全て側方センサ**由来で、
  真の横ずれ 17.5〜35.7mm が較正前提の ±15mm を超過していた

## 新実装の現況

`classic/` にゼロから構築。**`requires_privileged = False`**。🔴 **実行時に迷路 XML を読んではならない**。
🔴 `fast_planner.py` の `v_walls`/`h_walls` は**学習した地図**の中身で、`grep` では真値の参照と
区別できない（**違反が紛れて見逃されうる**）。

## 成果物と環境

**解説動画 第 1 部が完成**（`outputs/video_kinematics/part1.mp4`）。鍵は `.secrets/azure_speech.env`。
🔴 **鍵が会話の履歴に残っているので Azure ポータルで再生成すること。**
🔴 **進捗盤 Artifact** `https://claude.ai/code/artifact/33987bdb-da0e-44f7-85d1-741721dd43c8`（08-14 で停止）。
**この URL は過去 3 回メモの圧縮で脱落している。この行を消さないこと。**
評価基準の図解 `https://claude.ai/code/artifact/14b9d8ae-c403-4095-893e-d64277534663`。
既知の無関係な失敗: `tests/test_mouse_v2.py::test4_sensors`（本作業前から落ちている）。

## 強化学習（温存）

🔴 **`note_026` の結論は旧センサ前提である。**新モデルでは観測の意味が変わる。起票の禁止は破棄済み。
毎ティック計算が要るので**表が必要になる**（`note_034` 追記4 の持ち越し）。

## 効いているユーザの決定

1. 単独運用（08-18）／古典はゼロから再構築（08-19）／判定は物理限界からの肉薄度
2. **完成基準の分母に実機は使えない**／**予約 seed 1000〜40999 は使わない**（学習用は 61001 以降）
3. **探索を信頼できるものにしてから最短走行へ**（08-21・`note_033`）
4. **光線追跡を前向き計算に採用しない**（08-21・計算時間。1 姿勢 2.3 秒＝面積分の 40 倍）
5. **モデル既定値の更新を承認**（08-21）／**光線追跡を正しいと仮定することを承認**（08-21）

## 注意点

- p 値は条件比較に使えない／閾値は対照で校正／**検査は作動と空振り両方を実測してから使う**
- コミットは `git commit <パス...> -F -`（`git add -A` 禁止。新規は `git add <パス>` を先に）
- 🔴 **どのパラメータでも隣接値で結果が反転する。単一の標本で判断しない**（`note_031`）
- 🔴 **subagent は「通知を待ちます」と書いてターンを終えると落ちる。**本セッションで 2 回発生。
  測定は**前景**で、1 回 10 分以内に分割して走らせること。Bash も `run_in_background` は落ちるが、
  **前景で走らせて 120 秒で自動的に背景へ移ったものは完走する**
- 🔴 **「〜のはず」で検査を設計しない。**教授セッションは「反射は単調減衰するはず」という
  誤った予想で subagent に無効な検査を書かせ、正しい実装をバグと誤判定した
