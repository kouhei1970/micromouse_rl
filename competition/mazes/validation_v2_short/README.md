# 【退避・使用禁止】是正前の検証迷路（seed 4000-4019）

2026-08-11 に検証迷路を再生成したため退避した**旧セット**である。**新規の実験には
使わないこと。**現行の検証帯は `competition/mazes/validation/`。

退避の理由・是正の内容・無効になる数値については、対になる
`competition/mazes/eval_v2_short/README.md` を参照（同じ生成器の同じ欠陥による）。

本セット（是正前・20 面）と現行 validation（是正後・20 面）の比較:

| 指標 | 大会実迷路 42 面 | 本セット（是正前） | 現行 validation（是正後） |
|---|---|---|---|
| 真の最短距離 D_true [区画] | 中央値 63（40〜249） | 中央値 20 前後 | 中央値 62（46〜95） |
| 独立閉路数 β | 20（1〜40） | 34 前後 | 19（18〜21） |

※ 是正前の本セットの詳細な指標は `../eval_v2_short/ours_stats_pre_fix.csv`
（`source` 列が `validation/maze_4xxx.npz` の 20 行）にある。

本セットで測定した L0-a/b/c の失敗率・ゲイン掃引・帰属分離の数値は**すべて再測定が
必要**。過去結果の再現性保全のためセットそのものは削除せず保存する。
