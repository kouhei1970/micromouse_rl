#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""L0-c + E1T + TR + F0 + F0-b + **校正済みの安全率 0.75** — 古典トラックの**新しい既定**。

2026-08-14 新設（裁定 = 016-cal〔旋回安全率の校正実験〕の (A) 採用）。

--------------------------------------------------------------------------
なぜ新しいファイルを作るのか
--------------------------------------------------------------------------
**`SlalomPolicy.__init__` の既定値 `safety_factor=0.7` は変更しない。**
変えると **`baseline_slalom.py` が凍結の基準でなくなり、過去の全記録を
ビット単位で再現できなくなる**（`maze_gen_v2.py`・`K_TARGETS` を凍結したのと同じ理由）。

**`SlalomE1TTRF0bPolicy`（安全率 0.7）も変更しない。**
**そちらは切り替え前の基準スナップショットを再現するために残す**
（`card_016f0b.md` §8 の (γ) はその方策で測ってある）。

--------------------------------------------------------------------------
校正の根拠（`card_016cal.md` §8）
--------------------------------------------------------------------------
**校正用の 60 迷路（seed 51000〜）・安全率 8 水準（0.60〜0.95・0.05 刻み）**:

| 安全率 | 完走 | 片側 95% 下限 | 判定規則（≥ 0.95） |
|---|---|---|---|
| 0.70（旧既定） | 60/60 | 0.957 | 満たす |
| **0.75（新既定）** | **60/60** | **0.957** | **満たす。これが最大** |
| 0.80 | 54/60 | 0.818 | **満たさない** |

**同じ迷路どうしの対応差（0.70 → 0.75）: 中央値 −0.325 s（−2.51 %）・
60 迷路すべてが改善・悪化 0 迷路。**
**横偏差の最悪値は 32.12 mm**（中心から壁まで公称 50 mm）で余裕の内側。

**⚠️ 安全率は 0.75 が「安全の上限」ではない。**
**判定規則（完走率の片側 95% 下限 ≥ 0.95）を満たす最大値**であり、
**0.80 を超えると崖がある**（完走 54 → 15 迷路）。
"""
from competition.baseline_slalom_e1t_tr_f0b import SlalomE1TTRF0bPolicy

# 016-cal（旋回安全率の校正実験）の結論。**探索の結果であって決め打ちではない**
SAFETY_FACTOR_CALIBRATED = 0.75


class SlalomE1TTRF0bCalPolicy(SlalomE1TTRF0bPolicy):
    """古典トラックの新しい既定（**安全率だけが親と違う**）。"""

    name = "L0-c+E1T+TR+F0+F0b (calibrated safety factor 0.75)"

    def __init__(self, *a, **kw):
        kw.setdefault("safety_factor", SAFETY_FACTOR_CALIBRATED)
        super().__init__(*a, **kw)
